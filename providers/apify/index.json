{
 "_": "GENERATED \u2014 what an authoring tool needs to FIND an operation. The calling table is the .lua beside it.",
 "provider": "apify",
 "source": "https://docs.apify.com/api/openapi.json",
 "operations": [
  {
   "slug": "apify.actor_build_abort_post",
   "name": "Abort build",
   "description": "Aborts an Actor build and returns an object that contains all the details about the build. Only builds that are starting or running are aborted. For builds with status `FINISHED`, `FAILED`, `ABORTING` and `TIMED-OUT` this call does nothing.",
   "input": {
    "type": "object",
    "properties": {
     "buildId": {
      "type": "string",
      "description": "ID of the build, found in the build's Info tab."
     }
    },
    "required": [
     "buildId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-builds/{buildId}/abort",
    "path": [
     "buildId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_build_default_get",
   "name": "Get default build",
   "description": "Get the default build for an Actor. Use the optional `waitForFinish` parameter to synchronously wait for the build to finish. This avoids the need for periodic polling when waiting for the build to complete. This endpoint does not require an authentication token. Instead, calls are authenticated using the Actor's unique ID. However, if you access the endpoint without a token, certain attributes (e",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "waitForFinish": {
      "type": "number",
      "description": "The maximum number of seconds the server waits for the build to finish. By default it is `0`, the maximum value is `60`. If the build finishes in time then the returned build object will have a terminal status (e.g. `SUCCEEDED`), otherwise it will have a transitional status (e.g. `RUNNING`)."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/builds/default",
    "path": [
     "actorId"
    ],
    "query": [
     "waitForFinish"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_build_delete",
   "name": "Delete build",
   "description": "Delete the build. The build that is the current default build for the Actor cannot be deleted. Only users with build permissions for the Actor can delete builds.",
   "input": {
    "type": "object",
    "properties": {
     "buildId": {
      "type": "string",
      "description": "ID of the build, found in the build's Info tab."
     }
    },
    "required": [
     "buildId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-builds/{buildId}",
    "path": [
     "buildId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_build_get",
   "name": "Get build",
   "description": "Gets an object that contains all the details about a specific build of an Actor. By passing the optional `waitForFinish` parameter the API endpoint will synchronously wait for the build to finish. This is useful to avoid periodic polling when waiting for an Actor build to finish. This endpoint does not require the authentication token. Instead, calls are authenticated using a hard-to-guess ID of t",
   "input": {
    "type": "object",
    "properties": {
     "buildId": {
      "type": "string",
      "description": "ID of the build, found in the build's Info tab."
     },
     "waitForFinish": {
      "type": "number",
      "description": "The maximum number of seconds the server waits for the build to finish. By default it is `0`, the maximum value is `60`. If the build finishes in time then the returned build object will have a terminal status (e.g. `SUCCEEDED`), otherwise it will have a transitional status (e.g. `RUNNING`)."
     }
    },
    "required": [
     "buildId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-builds/{buildId}",
    "path": [
     "buildId"
    ],
    "query": [
     "waitForFinish"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_build_log_get",
   "name": "Get build's log",
   "description": "Retrieves Actor build's logs. This endpoint is a shortcut for getting the build's log. Same as [Get log](/api/v2/log-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "buildId": {
      "type": "string",
      "description": "ID of the build, found in the build's Info tab."
     },
     "stream": {
      "type": "boolean",
      "description": "If `true` or `1` then the logs will be streamed as long as the run or build is running."
     },
     "download": {
      "type": "boolean",
      "description": "If `true` or `1` then the web browser will download the log file rather than open it in a tab."
     }
    },
    "required": [
     "buildId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-builds/{buildId}/log",
    "path": [
     "buildId"
    ],
    "query": [
     "stream",
     "download"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_build_openapi_json_get",
   "name": "Get open api definition",
   "description": "Get the OpenAPI definition for Actor builds. Two similar endpoints are available: - [First endpoint](/api/v2/actor-openapi-json-get): Requires both `actorId` and `buildId`. Use `default` as the `buildId` to get the OpenAPI schema for the default Actor build. - [Second endpoint](/api/v2/actor-build-openapi-json-get): Requires only `buildId`. Get the OpenAPI definition for a specific Actor build. Au",
   "input": {
    "type": "object",
    "properties": {
     "buildId": {
      "type": "string",
      "description": "ID of the build, found in the build's Info tab. Use the special value `default` to get the OpenAPI schema for the Actor's default build."
     }
    },
    "required": [
     "buildId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-builds/{buildId}/openapi.json",
    "path": [
     "buildId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_builds_get",
   "name": "Get user builds list",
   "description": "Gets a list of all builds for a user. The response is a JSON array of objects, where each object contains basic information about a single build. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `startedAt` field in ascending order. Therefore, you can use pagination to incrementally f",
   "input": {
    "type": "object",
    "properties": {
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `startedAt` field in descending order. By default, they are sorted in ascending order."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-builds",
    "path": [],
    "query": [
     "offset",
     "limit",
     "desc"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_delete",
   "name": "Delete actor",
   "description": "Deletes an Actor with the specified ID.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actors/{actorId}",
    "path": [
     "actorId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_get",
   "name": "Get actor",
   "description": "Gets an object that contains all the details about the Actor with the specified ID.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}",
    "path": [
     "actorId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_openapi_json_get",
   "name": "Get open api definition",
   "description": "Get the OpenAPI definition for Actor builds. Two similar endpoints are available: - [First endpoint](/api/v2/actor-openapi-json-get): Requires both `actorId` and `buildId`. Use `default` as the `buildId` to get the OpenAPI schema for the default Actor build. - [Second endpoint](/api/v2/actor-build-openapi-json-get): Requires only `buildId`. Get the OpenAPI definition for a specific Actor build. To",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "buildId": {
      "type": "string",
      "description": "ID of the build, found in the build's Info tab. Use the special value `default` to get the OpenAPI schema for the Actor's default build."
     }
    },
    "required": [
     "actorId",
     "buildId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/builds/{buildId}/openapi.json",
    "path": [
     "actorId",
     "buildId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_put",
   "name": "Update actor",
   "description": "Updates an Actor's settings with the values specified in an `Actor` object passed as JSON in the POST payload. Only the fields specified in the request body are updated. Returns the full `Actor` object, the same as the [Get Actor](/api/v2/actor-get) endpoint. In the HTTP request, set the `Content-Type` header to `application/json`. ### Authentication To provide the authentication token, we recomme",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "name": {
      "type": "string",
      "description": "The identifier of the Actor. Use lowercase letters, numbers, and hyphens. Spaces or special characters aren't allowed. Must be unique across your account."
     },
     "description": {
      "type": [
       "string",
       "null"
      ],
      "description": "Short description of the Actor, displayed in Apify Store and Console."
     },
     "isPublic": {
      "type": "boolean",
      "description": "Whether the Actor is available to users in Apify Store. If `false`, the Actor is private and only visible to you."
     },
     "actorPermissionLevel": {
      "type": "string",
      "description": ""
     },
     "seoTitle": {
      "type": [
       "string",
       "null"
      ],
      "description": "Name of the Actor to display by search engines such as Google. Can be different from the Actor's name displayed in Apify Store and Console. Recommended length is 40-50 characters."
     },
     "seoDescription": {
      "type": [
       "string",
       "null"
      ],
      "description": "Description of the Actor to display by search engines such as Google. Recommended length is 140-156 characters."
     },
     "title": {
      "type": [
       "string",
       "null"
      ],
      "description": "Human-readable name of the Actor, displayed in Apify Store and Console. Can contain spaces and capital letters. Recommended length is 40-50 characters. You can change this title without affecting the Actor's URL or SEO."
     },
     "restartOnError": {
      "type": "boolean",
      "description": ""
     },
     "versions": {
      "type": "array",
      "description": "An array of `Version` objects. Each object represents a specific version of the Actor's source code: its location, builds, and environment configuration."
     },
     "pricingInfos": {
      "type": "array",
      "description": ""
     },
     "categories": {
      "type": [
       "array",
       "null"
      ],
      "description": "A list of categories that best define the Actor. Reflected in Apify Store's search and filtering options."
     },
     "defaultRunOptions": {
      "type": "string",
      "description": "The default settings applied to an Actor run. Can be overridden by the user."
     },
     "taggedBuilds": {
      "type": [
       "object",
       "null"
      ],
      "description": "A dictionary that maps tag names to specific builds. For details, see [Update build tags](#update-build-tags)."
     },
     "actorStandby": {
      "type": "string",
      "description": "The configuration of the Actor's standby mode. For details, see [Standby mode](https://docs.apify.com/platform/actors/development/programming-interface/standby)."
     },
     "exampleRunInput": {
      "type": "string",
      "description": "Sample input payload that demonstrates what a typical run input for an Actor looks like. Used when no explicit input for a run is provided."
     },
     "isDeprecated": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Whether the Actor is deprecated."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actors/{actorId}",
    "path": [
     "actorId"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "description",
     "isPublic",
     "actorPermissionLevel",
     "seoTitle",
     "seoDescription",
     "title",
     "restartOnError",
     "versions",
     "pricingInfos",
     "categories",
     "defaultRunOptions",
     "taggedBuilds",
     "actorStandby",
     "exampleRunInput",
     "isDeprecated"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_abort_post",
   "name": "Abort run",
   "description": "Aborts an Actor run and returns an object that contains all the details about the run. Only runs that are starting or running are aborted. For runs with status `FINISHED`, `FAILED`, `ABORTING` and `TIMED-OUT` this call does nothing.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "gracefully": {
      "type": "boolean",
      "description": "If true passed, the Actor run will abort gracefully. It will send `aborting` and `persistState` event into run and force-stop the run after 30 seconds. It is helpful in cases where you plan to resurrect the run later."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/abort",
    "path": [
     "runId"
    ],
    "query": [
     "gracefully"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_dataset_delete",
   "name": "Delete default dataset",
   "description": "Deletes default dataset associated with an Actor run. This endpoint is a shortcut for getting the last run's `defaultDatasetId` and then using the [ Delete dataset ](/api/v2/dataset-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/dataset",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_dataset_get",
   "name": "Get default dataset",
   "description": "Returns the default dataset associated with an Actor run. This endpoint is a shortcut for getting the run's `defaultDatasetId` and then using the [Get dataset](/api/v2/dataset-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/dataset",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_dataset_items_get",
   "name": "Get default dataset items",
   "description": "Returns data stored in the default dataset of the Actor run in the desired format. This endpoint is a shortcut that resolves the run's `defaultDatasetId` and proxies to the [Get dataset items](/api/v2/dataset-items-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "format": {
      "type": "string",
      "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml` and `rss`. The default value is `json`."
     },
     "clean": {
      "type": "boolean",
      "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i.e. fields starting with the # character). The `clean` parameter is just a shortcut for `skipHidden=true` and `skipEmpty=true` parameters. Note that since some objects might be skipped from the output, that the result might contain less items than the `limit` value."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. By default there is no limit."
     },
     "fields": {
      "type": "string",
      "description": "A comma-separated list of fields which should be picked from the items, only these fields will remain in the resulting record objects. Note that the fields in the outputted items are sorted the same way as they are specified in the `fields` query parameter. You can use this feature to effectively fix the output format."
     },
     "outputFields": {
      "type": "string",
      "description": "A comma-separated list of output field names that positionally rename the fields specified in the `fields` parameter. For example, `?fields=headline,url&outputFields=title,link` renames `headline` to `title` and `url` to `link` in the output. The number of names in `outputFields` must match the number of names in `fields`. Requires the `fields` parameter to be specified as well."
     },
     "omit": {
      "type": "string",
      "description": "A comma-separated list of fields which should be omitted from the items."
     },
     "unwind": {
      "type": "string",
      "description": "A comma-separated list of fields which should be unwound, in order which they should be processed. Each field should be either an array or an object. If the field is an array then every element of the array will become a separate record and merged with parent object. If the unwound field is an object then it is merged with the parent object. If the unwound field is missing or its value is neither "
     },
     "flatten": {
      "type": "string",
      "description": "A comma-separated list of fields which should transform nested objects into flat structures. For example, with `flatten=\"foo\"` the object `{\"foo\":{\"bar\": \"hello\"}}` is turned into `{\"foo.bar\": \"hello\"}`. The original object with properties is replaced with the flattened object."
     },
     "desc": {
      "type": "boolean",
      "description": "By default, results are returned in the same order as they were stored. To reverse the order, set this parameter to `true` or `1`."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header, forcing a web browser to download the file rather than to display it. By default this header is not present."
     },
     "delimiter": {
      "type": "string",
      "description": "A delimiter character for CSV files, only used if `format=csv`. You might need to URL-encode the character (e.g. use `%09` for tab or `%3B` for semicolon). The default delimiter is a simple comma (`,`)."
     },
     "bom": {
      "type": "boolean",
      "description": "All text responses are encoded in UTF-8 encoding. By default, the `format=csv` files are prefixed with the UTF-8 Byte Order Mark (BOM), while `json`, `jsonl`, `xml`, `html` and `rss` files are not. If you want to override this default behavior, specify `bom=1` query parameter to include the BOM or `bom=0` to skip it."
     },
     "xmlRoot": {
      "type": "string",
      "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
     },
     "xmlRow": {
      "type": "string",
      "description": "Overrides default element name that wraps each page or page function result object in `xml` output. By default the element name is `item`."
     },
     "skipHeaderRow": {
      "type": "boolean",
      "description": "If `true` or `1` then header row in the `csv` format is skipped."
     },
     "skipHidden": {
      "type": "boolean",
      "description": "If `true` or `1` then hidden fields are skipped from the output, i.e. fields starting with the `#` character."
     },
     "skipEmpty": {
      "type": "boolean",
      "description": "If `true` or `1` then empty items are skipped from the output. Note that if used, the results might contain less items than the limit value."
     },
     "simplified": {
      "type": "boolean",
      "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo` and `unwind=pageFunctionResult` query parameters. This feature is used to emulate simplified results provided by the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "view": {
      "type": "string",
      "description": "Defines the view configuration for dataset items based on the schema definition. This parameter determines how the data will be filtered and presented. For complete specification details, see the [dataset schema documentation](/storage/dataset-schema)."
     },
     "skipFailedPages": {
      "type": "boolean",
      "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the output. This feature is here to emulate functionality of API version 1 used for the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "feedTitle": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the title defaults to `Dataset `."
     },
     "feedDescription": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the description defaults to `Items in dataset with id \"\".`"
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/dataset/items",
    "path": [
     "runId"
    ],
    "query": [
     "format",
     "clean",
     "offset",
     "limit",
     "fields",
     "outputFields",
     "omit",
     "unwind",
     "flatten",
     "desc",
     "attachment",
     "delimiter",
     "bom",
     "xmlRoot",
     "xmlRow",
     "skipHeaderRow",
     "skipHidden",
     "skipEmpty",
     "simplified",
     "view",
     "skipFailedPages",
     "feedTitle",
     "feedDescription",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_dataset_items_post",
   "name": "Store items",
   "description": "Appends an item or an array of items to the end of the Actor run's default dataset. This endpoint is a shortcut that resolves the run's `defaultDatasetId` and proxies to the [Store items](/api/v2/dataset-items-post) endpoint. To save bandwidth and speed up your upload, you can send the request payload compressed and set the `Content-Encoding` header accordingly. Below is a list of supported `Conte",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/dataset/items",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_dataset_put",
   "name": "Update default dataset",
   "description": "Updates the default dataset associated with an Actor run. This endpoint is a shortcut for getting the run's `defaultDatasetId` and then using the [Put dataset](/api/v2/dataset-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/dataset",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_dataset_statistics_get",
   "name": "Get default dataset statistics",
   "description": "Returns statistics for the Actor run's default dataset. This endpoint is a shortcut that resolves the run's `defaultDatasetId` and proxies to the [Get dataset statistics](/api/v2/dataset-statistics-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/dataset/statistics",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_delete",
   "name": "Delete run",
   "description": "Delete the run. Only finished runs can be deleted. Only the person or organization that initiated the run can delete it.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-runs/{runId}",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_get",
   "name": "Get run",
   "description": "This is not a single endpoint, but an entire group of endpoints that lets you retrieve the run or any of its default storages. ##### Convenience endpoints for Actor run default storages * [Dataset](/api/v2/default-dataset) * [Key-value store](/api/v2/default-key-value-store) * [Request queue](/api/v2/default-request-queue) Gets an object that contains all the details about a specific run of an Act",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "waitForFinish": {
      "type": "number",
      "description": "The maximum number of seconds the server waits for the run to finish. By default it is `0`, the maximum value is `60`. If the run finishes in time then the returned run object will have a terminal status (e.g. `SUCCEEDED`), otherwise it will have a transitional status (e.g. `RUNNING`)."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}",
    "path": [
     "runId"
    ],
    "query": [
     "waitForFinish"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_key_value_store_delete",
   "name": "Delete default store",
   "description": "Delete the default key-value store. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Delete store](/api/v2/key-value-store-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/key-value-store",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_key_value_store_get",
   "name": "Get default store",
   "description": "Gets an object that contains all the details about the default key-value store. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Get store](/api/v2/key-value-store-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/key-value-store",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_key_value_store_keys_get",
   "name": "Get default store's list of keys",
   "description": "Returns a list of keys for the default key-value store of the Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Get list of keys](/api/v2/key-value-store-keys-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "exclusiveStartKey": {
      "type": "string",
      "description": "All keys up to this one (including) are skipped from the result."
     },
     "limit": {
      "type": "number",
      "description": "Number of keys to be returned."
     },
     "collection": {
      "type": "string",
      "description": "Limit the results to keys that belong to a specific collection from the key-value store schema. The key-value store need to have a schema defined for this parameter to work."
     },
     "prefix": {
      "type": "string",
      "description": "Limit the results to keys that start with a specific prefix."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/key-value-store/keys",
    "path": [
     "runId"
    ],
    "query": [
     "exclusiveStartKey",
     "limit",
     "collection",
     "prefix",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_key_value_store_put",
   "name": "Update default store",
   "description": "Updates the default key-value store's name and general resource access level using a value specified by a JSON object passed in the PUT payload. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Update store](/api/v2/key-value-store-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/key-value-store",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_key_value_store_record_delete",
   "name": "Delete default store's record",
   "description": "Removes a record specified by a key from the default key-value store of the Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Delete record](/api/v2/key-value-store-record-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     }
    },
    "required": [
     "recordKey",
     "runId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/key-value-store/records/{recordKey}",
    "path": [
     "runId",
     "recordKey"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_key_value_store_record_get",
   "name": "Get default store's record",
   "description": "Gets a value stored under a specific key in the default key-value store of the Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Get record](/api/v2/key-value-store-record-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1`, the response will be served with `Content-Disposition: attachment` header, causing web browsers to offer downloading HTML records instead of displaying them."
     }
    },
    "required": [
     "recordKey",
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/key-value-store/records/{recordKey}",
    "path": [
     "runId",
     "recordKey"
    ],
    "query": [
     "signature",
     "attachment"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_key_value_store_record_post",
   "name": "Store record in default store (post)",
   "description": "Stores a value under a specific key in the default key-value store of the Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "recordKey",
     "runId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/key-value-store/records/{recordKey}",
    "path": [
     "runId",
     "recordKey"
    ],
    "query": [],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_key_value_store_record_put",
   "name": "Store record in default store",
   "description": "Stores a value under a specific key in the default key-value store of the Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "recordKey",
     "runId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/key-value-store/records/{recordKey}",
    "path": [
     "runId",
     "recordKey"
    ],
    "query": [],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_key_value_store_records_get",
   "name": "Download default store's records",
   "description": "Downloads all records from the default key-value store of the Actor run as a ZIP archive. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Download records](/api/v2/key-value-store-records-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "collection": {
      "type": "string",
      "description": "If specified, only records belonging to a specific collection from the key-value store schema. The key-value store need to have a schema defined for this parameter to work."
     },
     "prefix": {
      "type": "string",
      "description": "If specified, only records whose key starts with the given prefix are included in the archive."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/key-value-store/records",
    "path": [
     "runId"
    ],
    "query": [
     "collection",
     "prefix",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_log_get",
   "name": "Get run's log",
   "description": "Retrieves Actor run's logs. This endpoint is a shortcut for getting the run's log. Same as [Get log](/api/v2/log-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "stream": {
      "type": "boolean",
      "description": "If `true` or `1` then the logs will be streamed as long as the run or build is running."
     },
     "download": {
      "type": "boolean",
      "description": "If `true` or `1` then the web browser will download the log file rather than open it in a tab."
     },
     "raw": {
      "type": "boolean",
      "description": "If `true` or `1`, the logs will be kept verbatim. By default, the API removes ANSI escape codes from the logs, keeping only printable characters."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/log",
    "path": [
     "runId"
    ],
    "query": [
     "stream",
     "download",
     "raw"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_metamorph_post",
   "name": "Metamorph run",
   "description": "Transforms an Actor run into a run of another Actor with a new input. This is useful if you want to use another Actor to finish the work of your current Actor run, without the need to create a completely new run and waiting for its finish. For the users of your Actors, the metamorph operation is transparent, they will just see your Actor got the work done. Internally, the system stops the Docker c",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "targetActorId": {
      "type": "string",
      "description": "ID of a target Actor that the run should be transformed into."
     },
     "build": {
      "type": "string",
      "description": "Optional build of the target Actor. It can be either a build tag or build number. By default, the run uses the build specified in the default run configuration for the target Actor (typically `latest`)."
     }
    },
    "required": [
     "runId",
     "targetActorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/metamorph",
    "path": [
     "runId"
    ],
    "query": [
     "targetActorId",
     "build"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_put",
   "name": "Update run",
   "description": "This endpoint can be used to update both the run's status message and to configure its general resource access level. **Status message:** You can set a single status message on your run that will be displayed in the Apify Console UI. During an Actor run, you will typically do this in order to inform users of your Actor about the Actor's progress. The request body must contain `runId` and `statusMe",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": ""
     },
     "statusMessage": {
      "type": "string",
      "description": ""
     },
     "isStatusMessageTerminal": {
      "type": "boolean",
      "description": ""
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-runs/{runId}",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [
     "runId",
     "statusMessage",
     "isStatusMessageTerminal",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_reboot_post",
   "name": "Reboot run",
   "description": "Reboots an Actor run and returns an object that contains all the details about the rebooted run. Only runs that are running, i.e. runs with status `RUNNING` can be rebooted. The run's container will be restarted, so any data not persisted in the key-value store, dataset, or request queue will be lost.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/reboot",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_delete",
   "name": "Delete default request queue",
   "description": "Deletes the default request queue associated with an Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Delete request queue](/api/v2/request-queue-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_get",
   "name": "Get default request queue",
   "description": "Returns the default request queue associated with an Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Get request queue](/api/v2/request-queue-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_head_get",
   "name": "Get default request queue head",
   "description": "Returns the given number of first requests from the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Get head](/api/v2/request-queue-head-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "limit": {
      "type": "number",
      "description": "How many items from queue should be returned."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/head",
    "path": [
     "runId"
    ],
    "query": [
     "limit",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_head_lock_post",
   "name": "Get and lock default request queue head",
   "description": "Returns the given number of first requests from the default request queue of the Actor run and locks them for the given time. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Get head and lock](/api/v2/request-queue-head-lock-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "lockSecs": {
      "type": "number",
      "description": "How long the requests will be locked for (in seconds)."
     },
     "limit": {
      "type": "number",
      "description": "How many items from the queue should be returned."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "lockSecs",
     "runId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/head/lock",
    "path": [
     "runId"
    ],
    "query": [
     "lockSecs",
     "limit",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_put",
   "name": "Update default request queue",
   "description": "Updates the default request queue associated with an Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Update request queue](/api/v2/request-queue-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": "The new name for the request queue."
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_request_delete",
   "name": "Delete request from default request queue",
   "description": "Deletes a request from the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Delete request](/api/v2/request-queue-request-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "requestId",
     "runId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/requests/{requestId}",
    "path": [
     "runId",
     "requestId"
    ],
    "query": [
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_request_get",
   "name": "Get request from default request queue",
   "description": "Returns a request from the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Get request](/api/v2/request-queue-request-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     }
    },
    "required": [
     "requestId",
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/requests/{requestId}",
    "path": [
     "runId",
     "requestId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_request_lock_delete",
   "name": "Delete lock on request in default request queue",
   "description": "Deletes a request lock in the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Delete request lock](/api/v2/request-queue-request-lock-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end after lock was removed."
     }
    },
    "required": [
     "requestId",
     "runId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/requests/{requestId}/lock",
    "path": [
     "runId",
     "requestId"
    ],
    "query": [
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_request_lock_put",
   "name": "Prolong lock on request in default request queue",
   "description": "Prolongs a request lock in the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Prolong request lock](/api/v2/request-queue-request-lock-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "lockSecs": {
      "type": "number",
      "description": "How long the requests will be locked for (in seconds)."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end after lock expires."
     }
    },
    "required": [
     "lockSecs",
     "requestId",
     "runId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/requests/{requestId}/lock",
    "path": [
     "runId",
     "requestId"
    ],
    "query": [
     "lockSecs",
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_request_put",
   "name": "Update request in default request queue",
   "description": "Updates a request in the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Update request](/api/v2/request-queue-request-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "uniqueKey": {
      "type": "string",
      "description": "A unique key used for request de-duplication. Requests with the same unique key are considered identical."
     },
     "url": {
      "type": "string",
      "description": "The URL of the request."
     },
     "method": {
      "type": "string",
      "description": "",
      "enum": [
       "GET",
       "HEAD",
       "POST",
       "PUT",
       "DELETE",
       "CONNECT",
       "OPTIONS",
       "TRACE",
       "PATCH"
      ]
     },
     "retryCount": {
      "type": "integer",
      "description": "The number of times this request has been retried."
     },
     "loadedUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "The final URL that was loaded, after redirects (if any)."
     },
     "payload": {
      "type": [
       "string",
       "object",
       "null"
      ],
      "description": "The request payload, typically used with POST or PUT requests."
     },
     "headers": {
      "type": [
       "object",
       "null"
      ],
      "description": "HTTP headers sent with the request."
     },
     "userData": {
      "type": "object",
      "description": "Custom user data attached to the request. Can contain arbitrary fields."
     },
     "noRetry": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Indicates whether the request should not be retried if processing fails."
     },
     "errorMessages": {
      "type": [
       "array",
       "null"
      ],
      "description": "Error messages recorded from failed processing attempts."
     },
     "handledAt": {
      "type": [
       "string",
       "null"
      ],
      "description": "The timestamp when the request was marked as handled, if applicable."
     }
    },
    "required": [
     "requestId",
     "runId",
     "uniqueKey",
     "url"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/requests/{requestId}",
    "path": [
     "runId",
     "requestId"
    ],
    "query": [
     "forefront",
     "clientKey"
    ],
    "header": [],
    "body": [
     "uniqueKey",
     "url",
     "method",
     "retryCount",
     "loadedUrl",
     "payload",
     "headers",
     "userData",
     "noRetry",
     "errorMessages",
     "handledAt"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_requests_batch_delete",
   "name": "Batch delete requests from default request queue",
   "description": "Batch-deletes requests from the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Delete requests](/api/v2/request-queue-requests-batch-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "Content-Type": {
      "type": "string",
      "description": ""
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "Content-Type",
     "runId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/requests/batch",
    "path": [
     "runId"
    ],
    "query": [
     "clientKey"
    ],
    "header": [
     "Content-Type"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_requests_batch_post",
   "name": "Batch add requests to default request queue",
   "description": "Adds requests to the default request queue of the Actor run in batch. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Add requests](/api/v2/request-queue-requests-batch-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/requests/batch",
    "path": [
     "runId"
    ],
    "query": [
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_requests_get",
   "name": "List default request queue's requests",
   "description": "Returns a list of requests from the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [List requests](/api/v2/request-queue-requests-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "exclusiveStartId": {
      "type": "string",
      "description": "All requests up to this one (including) are skipped from the result. (Deprecated, use `cursor` instead.)"
     },
     "limit": {
      "type": "number",
      "description": "Number of keys to be returned. Maximum value is `10000`."
     },
     "cursor": {
      "type": "string",
      "description": "A cursor string for pagination, returned in the previous response as `nextCursor`. Use this to retrieve the next page of requests."
     },
     "filter": {
      "type": "array",
      "description": "Filter requests by their state. Possible values are `locked` and `pending`. You can combine multiple values separated by commas, which will mean the union of these filters \u2013 requests matching any of the specified states will be returned. (Not compatible with deprecated `exclusiveStartId` parameter.)"
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/requests",
    "path": [
     "runId"
    ],
    "query": [
     "clientKey",
     "exclusiveStartId",
     "limit",
     "cursor",
     "filter"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_requests_post",
   "name": "Add request to default request queue",
   "description": "Adds a request to the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Add request](/api/v2/request-queue-requests-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     },
     "uniqueKey": {
      "type": "string",
      "description": "A unique key used for request de-duplication. Requests with the same unique key are considered identical."
     },
     "url": {
      "type": "string",
      "description": "The URL of the request."
     },
     "method": {
      "type": "string",
      "description": "",
      "enum": [
       "GET",
       "HEAD",
       "POST",
       "PUT",
       "DELETE",
       "CONNECT",
       "OPTIONS",
       "TRACE",
       "PATCH"
      ]
     },
     "retryCount": {
      "type": "integer",
      "description": "The number of times this request has been retried."
     },
     "loadedUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "The final URL that was loaded, after redirects (if any)."
     },
     "payload": {
      "type": [
       "string",
       "object",
       "null"
      ],
      "description": "The request payload, typically used with POST or PUT requests."
     },
     "headers": {
      "type": [
       "object",
       "null"
      ],
      "description": "HTTP headers sent with the request."
     },
     "userData": {
      "type": "object",
      "description": "Custom user data attached to the request. Can contain arbitrary fields."
     },
     "noRetry": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Indicates whether the request should not be retried if processing fails."
     },
     "errorMessages": {
      "type": [
       "array",
       "null"
      ],
      "description": "Error messages recorded from failed processing attempts."
     },
     "handledAt": {
      "type": [
       "string",
       "null"
      ],
      "description": "The timestamp when the request was marked as handled, if applicable."
     }
    },
    "required": [
     "runId",
     "uniqueKey",
     "url"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/requests",
    "path": [
     "runId"
    ],
    "query": [
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [
     "uniqueKey",
     "url",
     "method",
     "retryCount",
     "loadedUrl",
     "payload",
     "headers",
     "userData",
     "noRetry",
     "errorMessages",
     "handledAt"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_request_queue_requests_unlock_post",
   "name": "Unlock requests in default request queue",
   "description": "Unlocks requests in the default request queue of the Actor run that are currently locked by the client. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Unlock requests](/api/v2/request-queue-requests-unlock-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/request-queue/requests/unlock",
    "path": [
     "runId"
    ],
    "query": [
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_resurrect_post",
   "name": "Resurrect run",
   "description": "**[DEPRECATED]** API endpoints related to run of the Actor were moved under new namespace [`actor-runs`](#/reference/actor-runs).Resurrects a finished Actor run and returns an object that contains all the details about the resurrected run. Only finished runs, i.e. runs with status `FINISHED`, `FAILED`, `ABORTED` and `TIMED-OUT` can be resurrected. Run status will be updated to RUNNING and its cont",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run is resurrected with the same build it originally used. Specifically, if a run was first started with the `latest` tag, which resolves to version `0.0.3` at the time, a run resurrected without this parameter will continue running with `0.0.3`, even if `latest` already points to a newer build."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout specified in the run that is being resurrected."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. The amount of memory can be set to a power of 2 with a minimum of 128. By default, the run uses the memory limit specified in the run that is being resurrected."
     },
     "restartOnError": {
      "type": "boolean",
      "description": "Determines whether the resurrected run will be restarted if it fails. By default, the resurrected run uses the same setting as before."
     }
    },
    "required": [
     "actorId",
     "runId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/{runId}/resurrect",
    "path": [
     "actorId",
     "runId"
    ],
    "query": [
     "build",
     "timeout",
     "memory",
     "restartOnError"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_sync_get",
   "name": "Run actor synchronously without input",
   "description": "Runs a specific Actor and returns a key-value store record. The response contains the record stored under the `OUTPUT` key in the run's default key-value store. This is a legacy approach that has been replaced by the Actor [output object](https://docs.apify.com/platform/actors/development/actor-definition/output-schema#output-object-definition); the record may not exist, in which case the response",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "outputRecordKey": {
      "type": "string",
      "description": "Key of the record from the run's default key-value store to return in the response. Defaults to `OUTPUT`. Actors aren't required to store a record under this key, so if it doesn't exist the response contains no data."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout from its configuration."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. You can set the amount of memory to a power of 2 with a minimum of 128. By default, the run uses the memory limit from its configuration. Don't change this value unless the Actor's documentation recommends it or you're aware of the consequences."
     },
     "maxItems": {
      "type": "number",
      "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors. This does NOT guarantee that the Actor will return only this many items. It only ensures you won't be charged for more than this number of items. Only works for pay-per-result Actors. Value can be accessed in the actor run using `ACTOR_MAX_PAID_DATASET_ITEMS` environment variable."
     },
     "maxTotalChargeUsd": {
      "type": "number",
      "description": "Specifies the maximum total cost of the run. Use it to cap the total amount charged for all pricing models. You can access the maximum cost in your Actor by using the `ACTOR_MAX_TOTAL_CHARGE_USD` environment variable."
     },
     "restartOnError": {
      "type": "boolean",
      "description": "Determines whether the run will be restarted if it fails."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run uses the build from its configuration (typically `latest`)."
     },
     "webhooks": {
      "type": "string",
      "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a notification e.g. when the Actor finished or failed. The value is a Base64-encoded JSON array whose items follow the WebhookRepresentation schema. For more information, see [Webhooks documentation](https://docs.apify.com/platform/integrations/webhooks)."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/run-sync",
    "path": [
     "actorId"
    ],
    "query": [
     "outputRecordKey",
     "timeout",
     "memory",
     "maxItems",
     "maxTotalChargeUsd",
     "restartOnError",
     "build",
     "webhooks"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_sync_get_dataset_items_get",
   "name": "Run actor synchronously without input and get dataset items",
   "description": "Runs a specific Actor and returns its dataset items. The run must finish in 300 seconds otherwise the API endpoint returns a timeout error. The Actor is not passed any input. It allows to send all possible options in parameters from [Get Dataset Items](#/reference/datasets/item-collection/get-items) API endpoint. Beware that it might be impossible to maintain an idle HTTP connection for a long per",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout from its configuration."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. You can set the amount of memory to a power of 2 with a minimum of 128. By default, the run uses the memory limit from its configuration. Don't change this value unless the Actor's documentation recommends it or you're aware of the consequences."
     },
     "maxItems": {
      "type": "number",
      "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors. This does NOT guarantee that the Actor will return only this many items. It only ensures you won't be charged for more than this number of items. Only works for pay-per-result Actors. Value can be accessed in the actor run using `ACTOR_MAX_PAID_DATASET_ITEMS` environment variable."
     },
     "maxTotalChargeUsd": {
      "type": "number",
      "description": "Specifies the maximum total cost of the run. Use it to cap the total amount charged for all pricing models. You can access the maximum cost in your Actor by using the `ACTOR_MAX_TOTAL_CHARGE_USD` environment variable."
     },
     "restartOnError": {
      "type": "boolean",
      "description": "Determines whether the run will be restarted if it fails."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run uses the build from its configuration (typically `latest`)."
     },
     "webhooks": {
      "type": "string",
      "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a notification e.g. when the Actor finished or failed. The value is a Base64-encoded JSON array whose items follow the WebhookRepresentation schema. For more information, see [Webhooks documentation](https://docs.apify.com/platform/integrations/webhooks)."
     },
     "format": {
      "type": "string",
      "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml` and `rss`. The default value is `json`."
     },
     "clean": {
      "type": "boolean",
      "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i.e. fields starting with the # character). The `clean` parameter is just a shortcut for `skipHidden=true` and `skipEmpty=true` parameters. Note that since some objects might be skipped from the output, that the result might contain less items than the `limit` value."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. By default there is no limit."
     },
     "fields": {
      "type": "string",
      "description": "A comma-separated list of fields which should be picked from the items, only these fields will remain in the resulting record objects. Note that the fields in the outputted items are sorted the same way as they are specified in the `fields` query parameter. You can use this feature to effectively fix the output format."
     },
     "outputFields": {
      "type": "string",
      "description": "A comma-separated list of output field names that positionally rename the fields specified in the `fields` parameter. For example, `?fields=headline,url&outputFields=title,link` renames `headline` to `title` and `url` to `link` in the output. The number of names in `outputFields` must match the number of names in `fields`. Requires the `fields` parameter to be specified as well."
     },
     "omit": {
      "type": "string",
      "description": "A comma-separated list of fields which should be omitted from the items."
     },
     "unwind": {
      "type": "string",
      "description": "A comma-separated list of fields which should be unwound, in order which they should be processed. Each field should be either an array or an object. If the field is an array then every element of the array will become a separate record and merged with parent object. If the unwound field is an object then it is merged with the parent object. If the unwound field is missing or its value is neither "
     },
     "flatten": {
      "type": "string",
      "description": "A comma-separated list of fields which should transform nested objects into flat structures. For example, with `flatten=\"foo\"` the object `{\"foo\":{\"bar\": \"hello\"}}` is turned into `{\"foo.bar\": \"hello\"}`. The original object with properties is replaced with the flattened object."
     },
     "desc": {
      "type": "boolean",
      "description": "By default, results are returned in the same order as they were stored. To reverse the order, set this parameter to `true` or `1`."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header, forcing a web browser to download the file rather than to display it. By default this header is not present."
     },
     "delimiter": {
      "type": "string",
      "description": "A delimiter character for CSV files, only used if `format=csv`. You might need to URL-encode the character (e.g. use `%09` for tab or `%3B` for semicolon). The default delimiter is a simple comma (`,`)."
     },
     "bom": {
      "type": "boolean",
      "description": "All text responses are encoded in UTF-8 encoding. By default, the `format=csv` files are prefixed with the UTF-8 Byte Order Mark (BOM), while `json`, `jsonl`, `xml`, `html` and `rss` files are not. If you want to override this default behavior, specify `bom=1` query parameter to include the BOM or `bom=0` to skip it."
     },
     "xmlRoot": {
      "type": "string",
      "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
     },
     "xmlRow": {
      "type": "string",
      "description": "Overrides default element name that wraps each page or page function result object in `xml` output. By default the element name is `item`."
     },
     "skipHeaderRow": {
      "type": "boolean",
      "description": "If `true` or `1` then header row in the `csv` format is skipped."
     },
     "skipHidden": {
      "type": "boolean",
      "description": "If `true` or `1` then hidden fields are skipped from the output, i.e. fields starting with the `#` character."
     },
     "skipEmpty": {
      "type": "boolean",
      "description": "If `true` or `1` then empty items are skipped from the output. Note that if used, the results might contain less items than the limit value."
     },
     "simplified": {
      "type": "boolean",
      "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo` and `unwind=pageFunctionResult` query parameters. This feature is used to emulate simplified results provided by the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "view": {
      "type": "string",
      "description": "Defines the view configuration for dataset items based on the schema definition. This parameter determines how the data will be filtered and presented. For complete specification details, see the [dataset schema documentation](/storage/dataset-schema)."
     },
     "skipFailedPages": {
      "type": "boolean",
      "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the output. This feature is here to emulate functionality of API version 1 used for the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "feedTitle": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the title defaults to `Dataset `."
     },
     "feedDescription": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the description defaults to `Items in dataset with id \"\".`"
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/run-sync-get-dataset-items",
    "path": [
     "actorId"
    ],
    "query": [
     "timeout",
     "memory",
     "maxItems",
     "maxTotalChargeUsd",
     "restartOnError",
     "build",
     "webhooks",
     "format",
     "clean",
     "offset",
     "limit",
     "fields",
     "outputFields",
     "omit",
     "unwind",
     "flatten",
     "desc",
     "attachment",
     "delimiter",
     "bom",
     "xmlRoot",
     "xmlRow",
     "skipHeaderRow",
     "skipHidden",
     "skipEmpty",
     "simplified",
     "view",
     "skipFailedPages",
     "feedTitle",
     "feedDescription"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_sync_get_dataset_items_post",
   "name": "Run actor synchronously and get dataset items",
   "description": "Runs a specific Actor and returns its dataset items. The POST payload including its `Content-Type` header is passed as `INPUT` to the Actor (usually `application/json`). The HTTP response contains the Actors dataset items, while the format of items depends on specifying dataset items' `format` parameter. You can send all the same options in parameters as the [Get Dataset Items](#/reference/dataset",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout from its configuration."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. You can set the amount of memory to a power of 2 with a minimum of 128. By default, the run uses the memory limit from its configuration. Don't change this value unless the Actor's documentation recommends it or you're aware of the consequences."
     },
     "maxItems": {
      "type": "number",
      "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors. This does NOT guarantee that the Actor will return only this many items. It only ensures you won't be charged for more than this number of items. Only works for pay-per-result Actors. Value can be accessed in the actor run using `ACTOR_MAX_PAID_DATASET_ITEMS` environment variable."
     },
     "maxTotalChargeUsd": {
      "type": "number",
      "description": "Specifies the maximum total cost of the run. Use it to cap the total amount charged for all pricing models. You can access the maximum cost in your Actor by using the `ACTOR_MAX_TOTAL_CHARGE_USD` environment variable."
     },
     "restartOnError": {
      "type": "boolean",
      "description": "Determines whether the run will be restarted if it fails."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run uses the build from its configuration (typically `latest`)."
     },
     "webhooks": {
      "type": "string",
      "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a notification e.g. when the Actor finished or failed. The value is a Base64-encoded JSON array whose items follow the WebhookRepresentation schema. For more information, see [Webhooks documentation](https://docs.apify.com/platform/integrations/webhooks)."
     },
     "format": {
      "type": "string",
      "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml` and `rss`. The default value is `json`."
     },
     "clean": {
      "type": "boolean",
      "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i.e. fields starting with the # character). The `clean` parameter is just a shortcut for `skipHidden=true` and `skipEmpty=true` parameters. Note that since some objects might be skipped from the output, that the result might contain less items than the `limit` value."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. By default there is no limit."
     },
     "fields": {
      "type": "string",
      "description": "A comma-separated list of fields which should be picked from the items, only these fields will remain in the resulting record objects. Note that the fields in the outputted items are sorted the same way as they are specified in the `fields` query parameter. You can use this feature to effectively fix the output format."
     },
     "outputFields": {
      "type": "string",
      "description": "A comma-separated list of output field names that positionally rename the fields specified in the `fields` parameter. For example, `?fields=headline,url&outputFields=title,link` renames `headline` to `title` and `url` to `link` in the output. The number of names in `outputFields` must match the number of names in `fields`. Requires the `fields` parameter to be specified as well."
     },
     "omit": {
      "type": "string",
      "description": "A comma-separated list of fields which should be omitted from the items."
     },
     "unwind": {
      "type": "string",
      "description": "A comma-separated list of fields which should be unwound, in order which they should be processed. Each field should be either an array or an object. If the field is an array then every element of the array will become a separate record and merged with parent object. If the unwound field is an object then it is merged with the parent object. If the unwound field is missing or its value is neither "
     },
     "flatten": {
      "type": "string",
      "description": "A comma-separated list of fields which should transform nested objects into flat structures. For example, with `flatten=\"foo\"` the object `{\"foo\":{\"bar\": \"hello\"}}` is turned into `{\"foo.bar\": \"hello\"}`. The original object with properties is replaced with the flattened object."
     },
     "desc": {
      "type": "boolean",
      "description": "By default, results are returned in the same order as they were stored. To reverse the order, set this parameter to `true` or `1`."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header, forcing a web browser to download the file rather than to display it. By default this header is not present."
     },
     "delimiter": {
      "type": "string",
      "description": "A delimiter character for CSV files, only used if `format=csv`. You might need to URL-encode the character (e.g. use `%09` for tab or `%3B` for semicolon). The default delimiter is a simple comma (`,`)."
     },
     "bom": {
      "type": "boolean",
      "description": "All text responses are encoded in UTF-8 encoding. By default, the `format=csv` files are prefixed with the UTF-8 Byte Order Mark (BOM), while `json`, `jsonl`, `xml`, `html` and `rss` files are not. If you want to override this default behavior, specify `bom=1` query parameter to include the BOM or `bom=0` to skip it."
     },
     "xmlRoot": {
      "type": "string",
      "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
     },
     "xmlRow": {
      "type": "string",
      "description": "Overrides default element name that wraps each page or page function result object in `xml` output. By default the element name is `item`."
     },
     "skipHeaderRow": {
      "type": "boolean",
      "description": "If `true` or `1` then header row in the `csv` format is skipped."
     },
     "skipHidden": {
      "type": "boolean",
      "description": "If `true` or `1` then hidden fields are skipped from the output, i.e. fields starting with the `#` character."
     },
     "skipEmpty": {
      "type": "boolean",
      "description": "If `true` or `1` then empty items are skipped from the output. Note that if used, the results might contain less items than the limit value."
     },
     "simplified": {
      "type": "boolean",
      "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo` and `unwind=pageFunctionResult` query parameters. This feature is used to emulate simplified results provided by the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "view": {
      "type": "string",
      "description": "Defines the view configuration for dataset items based on the schema definition. This parameter determines how the data will be filtered and presented. For complete specification details, see the [dataset schema documentation](/storage/dataset-schema)."
     },
     "skipFailedPages": {
      "type": "boolean",
      "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the output. This feature is here to emulate functionality of API version 1 used for the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "feedTitle": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the title defaults to `Dataset `."
     },
     "feedDescription": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the description defaults to `Items in dataset with id \"\".`"
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/run-sync-get-dataset-items",
    "path": [
     "actorId"
    ],
    "query": [
     "timeout",
     "memory",
     "maxItems",
     "maxTotalChargeUsd",
     "restartOnError",
     "build",
     "webhooks",
     "format",
     "clean",
     "offset",
     "limit",
     "fields",
     "outputFields",
     "omit",
     "unwind",
     "flatten",
     "desc",
     "attachment",
     "delimiter",
     "bom",
     "xmlRoot",
     "xmlRow",
     "skipHeaderRow",
     "skipHidden",
     "skipEmpty",
     "simplified",
     "view",
     "skipFailedPages",
     "feedTitle",
     "feedDescription"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_run_sync_post",
   "name": "Run actor synchronously and return key value store record",
   "description": "Runs a specific Actor and returns a key-value store record. The POST payload including its `Content-Type` header is passed as `INPUT` to the Actor (usually application/json). The response contains the record stored under the `OUTPUT` key in the run's default key-value store. This is a legacy approach that has been replaced by the Actor [output object](https://docs.apify.com/platform/actors/develop",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "outputRecordKey": {
      "type": "string",
      "description": "Key of the record from the run's default key-value store to return in the response. Defaults to `OUTPUT`. Actors aren't required to store a record under this key, so if it doesn't exist the response contains no data."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout from its configuration."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. You can set the amount of memory to a power of 2 with a minimum of 128. By default, the run uses the memory limit from its configuration. Don't change this value unless the Actor's documentation recommends it or you're aware of the consequences."
     },
     "maxItems": {
      "type": "number",
      "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors. This does NOT guarantee that the Actor will return only this many items. It only ensures you won't be charged for more than this number of items. Only works for pay-per-result Actors. Value can be accessed in the actor run using `ACTOR_MAX_PAID_DATASET_ITEMS` environment variable."
     },
     "maxTotalChargeUsd": {
      "type": "number",
      "description": "Specifies the maximum total cost of the run. Use it to cap the total amount charged for all pricing models. You can access the maximum cost in your Actor by using the `ACTOR_MAX_TOTAL_CHARGE_USD` environment variable."
     },
     "restartOnError": {
      "type": "boolean",
      "description": "Determines whether the run will be restarted if it fails."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run uses the build from its configuration (typically `latest`)."
     },
     "webhooks": {
      "type": "string",
      "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a notification e.g. when the Actor finished or failed. The value is a Base64-encoded JSON array whose items follow the WebhookRepresentation schema. For more information, see [Webhooks documentation](https://docs.apify.com/platform/integrations/webhooks)."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/run-sync",
    "path": [
     "actorId"
    ],
    "query": [
     "outputRecordKey",
     "timeout",
     "memory",
     "maxItems",
     "maxTotalChargeUsd",
     "restartOnError",
     "build",
     "webhooks"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_get",
   "name": "Get user runs list",
   "description": "Gets a list of all runs for a user. The response is a list of objects, where each object contains basic information about a single Actor run. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 array elements. By default, the records are sorted by the `startedAt` field in ascending order. Therefore, you can use pagination to incrementall",
   "input": {
    "type": "object",
    "properties": {
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `startedAt` field in descending order. By default, they are sorted in ascending order."
     },
     "status": {
      "type": "array",
      "description": "Single status or comma-separated list of statuses, see ([available statuses](https://docs.apify.com/platform/actors/running/runs-and-builds#lifecycle)). Used to filter runs by the specified statuses only."
     },
     "startedAfter": {
      "type": "string",
      "description": "Filter runs that started after the specified date and time (inclusive). The value must be a valid ISO 8601 datetime string (UTC)."
     },
     "startedBefore": {
      "type": "string",
      "description": "Filter runs that started before the specified date and time (inclusive). The value must be a valid ISO 8601 datetime string (UTC)."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-runs",
    "path": [],
    "query": [
     "offset",
     "limit",
     "desc",
     "status",
     "startedAfter",
     "startedBefore"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_abort_post",
   "name": "Abort actor's last run",
   "description": "Aborts the last run of the specified Actor and returns an object that contains all the details about the run. This endpoint is a shortcut for [Abort run](#/reference/actor-runs/abort-run/abort-run) on the Actor's last run. Only runs that are starting or running are aborted. For runs with status `FINISHED`, `FAILED`, `ABORTING` and `TIMED-OUT` this call does nothing.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "gracefully": {
      "type": "boolean",
      "description": "If true passed, the Actor run will abort gracefully. It will send `aborting` and `persistState` event into run and force-stop the run after 30 seconds. It is helpful in cases where you plan to resurrect the run later."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/abort",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "gracefully"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_dataset_delete",
   "name": "Delete last run's default dataset",
   "description": "Deletes the default dataset associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultDatasetId` and then using the [Delete dataset](/api/v2/dataset-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/dataset",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_dataset_get",
   "name": "Get last run's default dataset",
   "description": "Returns the default dataset associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultDatasetId` and then using the [Get dataset](/api/v2/dataset-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/dataset",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_dataset_items_get",
   "name": "Get last run's dataset items",
   "description": "Returns data stored in the default dataset of the last Actor run in the desired format. This endpoint is a shortcut that resolves the last run's `defaultDatasetId` and proxies to the [Get dataset items](/api/v2/dataset-items-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "format": {
      "type": "string",
      "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml` and `rss`. The default value is `json`."
     },
     "clean": {
      "type": "boolean",
      "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i.e. fields starting with the # character). The `clean` parameter is just a shortcut for `skipHidden=true` and `skipEmpty=true` parameters. Note that since some objects might be skipped from the output, that the result might contain less items than the `limit` value."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. By default there is no limit."
     },
     "fields": {
      "type": "string",
      "description": "A comma-separated list of fields which should be picked from the items, only these fields will remain in the resulting record objects. Note that the fields in the outputted items are sorted the same way as they are specified in the `fields` query parameter. You can use this feature to effectively fix the output format."
     },
     "outputFields": {
      "type": "string",
      "description": "A comma-separated list of output field names that positionally rename the fields specified in the `fields` parameter. For example, `?fields=headline,url&outputFields=title,link` renames `headline` to `title` and `url` to `link` in the output. The number of names in `outputFields` must match the number of names in `fields`. Requires the `fields` parameter to be specified as well."
     },
     "omit": {
      "type": "string",
      "description": "A comma-separated list of fields which should be omitted from the items."
     },
     "unwind": {
      "type": "string",
      "description": "A comma-separated list of fields which should be unwound, in order which they should be processed. Each field should be either an array or an object. If the field is an array then every element of the array will become a separate record and merged with parent object. If the unwound field is an object then it is merged with the parent object. If the unwound field is missing or its value is neither "
     },
     "flatten": {
      "type": "string",
      "description": "A comma-separated list of fields which should transform nested objects into flat structures. For example, with `flatten=\"foo\"` the object `{\"foo\":{\"bar\": \"hello\"}}` is turned into `{\"foo.bar\": \"hello\"}`. The original object with properties is replaced with the flattened object."
     },
     "desc": {
      "type": "boolean",
      "description": "By default, results are returned in the same order as they were stored. To reverse the order, set this parameter to `true` or `1`."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header, forcing a web browser to download the file rather than to display it. By default this header is not present."
     },
     "delimiter": {
      "type": "string",
      "description": "A delimiter character for CSV files, only used if `format=csv`. You might need to URL-encode the character (e.g. use `%09` for tab or `%3B` for semicolon). The default delimiter is a simple comma (`,`)."
     },
     "bom": {
      "type": "boolean",
      "description": "All text responses are encoded in UTF-8 encoding. By default, the `format=csv` files are prefixed with the UTF-8 Byte Order Mark (BOM), while `json`, `jsonl`, `xml`, `html` and `rss` files are not. If you want to override this default behavior, specify `bom=1` query parameter to include the BOM or `bom=0` to skip it."
     },
     "xmlRoot": {
      "type": "string",
      "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
     },
     "xmlRow": {
      "type": "string",
      "description": "Overrides default element name that wraps each page or page function result object in `xml` output. By default the element name is `item`."
     },
     "skipHeaderRow": {
      "type": "boolean",
      "description": "If `true` or `1` then header row in the `csv` format is skipped."
     },
     "skipHidden": {
      "type": "boolean",
      "description": "If `true` or `1` then hidden fields are skipped from the output, i.e. fields starting with the `#` character."
     },
     "skipEmpty": {
      "type": "boolean",
      "description": "If `true` or `1` then empty items are skipped from the output. Note that if used, the results might contain less items than the limit value."
     },
     "simplified": {
      "type": "boolean",
      "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo` and `unwind=pageFunctionResult` query parameters. This feature is used to emulate simplified results provided by the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "view": {
      "type": "string",
      "description": "Defines the view configuration for dataset items based on the schema definition. This parameter determines how the data will be filtered and presented. For complete specification details, see the [dataset schema documentation](/storage/dataset-schema)."
     },
     "skipFailedPages": {
      "type": "boolean",
      "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the output. This feature is here to emulate functionality of API version 1 used for the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "feedTitle": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the title defaults to `Dataset `."
     },
     "feedDescription": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the description defaults to `Items in dataset with id \"\".`"
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/dataset/items",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "format",
     "clean",
     "offset",
     "limit",
     "fields",
     "outputFields",
     "omit",
     "unwind",
     "flatten",
     "desc",
     "attachment",
     "delimiter",
     "bom",
     "xmlRoot",
     "xmlRow",
     "skipHeaderRow",
     "skipHidden",
     "skipEmpty",
     "simplified",
     "view",
     "skipFailedPages",
     "feedTitle",
     "feedDescription",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_dataset_items_post",
   "name": "Store items in last run's dataset",
   "description": "Appends an item or an array of items to the end of the last Actor run's default dataset. This endpoint is a shortcut that resolves the last run's `defaultDatasetId` and proxies to the [Store items](/api/v2/dataset-items-post) endpoint. To save bandwidth and speed up your upload, you can send the request payload compressed and set the `Content-Encoding` header accordingly. Below is a list of suppor",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/dataset/items",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_dataset_put",
   "name": "Update last run's default dataset",
   "description": "Updates the default dataset associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultDatasetId` and then using the [Update dataset](/api/v2/dataset-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/dataset",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_dataset_statistics_get",
   "name": "Get last run's dataset statistics",
   "description": "Returns statistics for the last Actor run's default dataset. This endpoint is a shortcut that resolves the last run's `defaultDatasetId` and proxies to the [Get dataset statistics](/api/v2/dataset-statistics-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/dataset/statistics",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_get",
   "name": "Get last run",
   "description": "This is not a single endpoint, but an entire group of endpoints that lets you to retrieve and manage the last run of given Actor or any of its default storages. All the endpoints require an authentication token. The base path represents the last Actor run object is: `/v2/actors/{actorId}/runs/last{?token,status,origin}` Using the `status` query parameter you can ensure to only get a run with a cer",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "waitForFinish": {
      "type": "number",
      "description": "The maximum number of seconds the server waits for the run to finish. By default it is `0`, the maximum value is `60`. If the run finishes in time then the returned run object will have a terminal status (e.g. `SUCCEEDED`), otherwise it will have a transitional status (e.g. `RUNNING`)."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "waitForFinish"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_key_value_store_delete",
   "name": "Delete last run's default store",
   "description": "Deletes the last Actor run key-value store. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Delete store](/api/v2/key-value-store-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/key-value-store",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_key_value_store_get",
   "name": "Get last run's default store",
   "description": "Gets an object that contains all the details about the default key-value store associated with the last Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Get store](/api/v2/key-value-store-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/key-value-store",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_key_value_store_keys_get",
   "name": "Get last run's default store's list of keys",
   "description": "Returns a list of keys for the default key-value store of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Get list of keys](/api/v2/key-value-store-keys-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "exclusiveStartKey": {
      "type": "string",
      "description": "All keys up to this one (including) are skipped from the result."
     },
     "limit": {
      "type": "number",
      "description": "Number of keys to be returned."
     },
     "collection": {
      "type": "string",
      "description": "Limit the results to keys that belong to a specific collection from the key-value store schema. The key-value store need to have a schema defined for this parameter to work."
     },
     "prefix": {
      "type": "string",
      "description": "Limit the results to keys that start with a specific prefix."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/key-value-store/keys",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "exclusiveStartKey",
     "limit",
     "collection",
     "prefix",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_key_value_store_put",
   "name": "Update last run's default store",
   "description": "Updates the last Actor run key-value store's name and general resource access level using a value specified by a JSON object passed in the PUT payload. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Update store](/api/v2/key-value-store-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/key-value-store",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_key_value_store_record_delete",
   "name": "Delete last run's default store's record",
   "description": "Removes a record specified by a key from the default key-value store of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Delete record](/api/v2/key-value-store-record-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     }
    },
    "required": [
     "actorId",
     "recordKey"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/key-value-store/records/{recordKey}",
    "path": [
     "actorId",
     "recordKey"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_key_value_store_record_get",
   "name": "Get last run's default store's record",
   "description": "Gets a value stored under a specific key in the default key-value store of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Get record](/api/v2/key-value-store-record-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1`, the response will be served with `Content-Disposition: attachment` header, causing web browsers to offer downloading HTML records instead of displaying them."
     }
    },
    "required": [
     "actorId",
     "recordKey"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/key-value-store/records/{recordKey}",
    "path": [
     "actorId",
     "recordKey"
    ],
    "query": [
     "status",
     "origin",
     "signature",
     "attachment"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_key_value_store_record_post",
   "name": "Store record in last run's default store (post)",
   "description": "Stores a value under a specific key in the default key-value store of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "actorId",
     "recordKey"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/key-value-store/records/{recordKey}",
    "path": [
     "actorId",
     "recordKey"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_key_value_store_record_put",
   "name": "Store record in last run's default store",
   "description": "Stores a value under a specific key in the default key-value store of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "actorId",
     "recordKey"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/key-value-store/records/{recordKey}",
    "path": [
     "actorId",
     "recordKey"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_key_value_store_records_get",
   "name": "Download last run's default store's records",
   "description": "Downloads all records from the default key-value store of the last Actor run as a ZIP archive. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Download records](/api/v2/key-value-store-records-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "collection": {
      "type": "string",
      "description": "If specified, only records belonging to a specific collection from the key-value store schema. The key-value store need to have a schema defined for this parameter to work."
     },
     "prefix": {
      "type": "string",
      "description": "If specified, only records whose key starts with the given prefix are included in the archive."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/key-value-store/records",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "collection",
     "prefix",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_log_get",
   "name": "Get last actor run's log",
   "description": "Retrieves last Actor run's logs. This endpoint is a shortcut for getting last Actor run's log. Same as [Get log](/api/v2/log-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "stream": {
      "type": "boolean",
      "description": "If `true` or `1` then the logs will be streamed as long as the run or build is running."
     },
     "download": {
      "type": "boolean",
      "description": "If `true` or `1` then the web browser will download the log file rather than open it in a tab."
     },
     "raw": {
      "type": "boolean",
      "description": "If `true` or `1`, the logs will be kept verbatim. By default, the API removes ANSI escape codes from the logs, keeping only printable characters."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/log",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "stream",
     "download",
     "raw"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_metamorph_post",
   "name": "Metamorph actor's last run",
   "description": "Transforms the last run of the specified Actor into a run of another Actor with a new input. This endpoint is a shortcut for [Metamorph run](#/reference/actor-runs/metamorph-run/metamorph-run) on the Actor's last run.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "targetActorId": {
      "type": "string",
      "description": "ID of a target Actor that the run should be transformed into."
     },
     "build": {
      "type": "string",
      "description": "Optional build of the target Actor. It can be either a build tag or build number. By default, the run uses the build specified in the default run configuration for the target Actor (typically `latest`)."
     }
    },
    "required": [
     "actorId",
     "targetActorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/metamorph",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "targetActorId",
     "build"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_reboot_post",
   "name": "Reboot actor's last run",
   "description": "Reboots the last run of the specified Actor and returns an object that contains all the details about the rebooted run. This endpoint is a shortcut for [Reboot run](#/reference/actor-runs/reboot-run/reboot-run) on the Actor's last run. Only runs with status `RUNNING` can be rebooted. The run's container will be restarted, so any data not persisted in the key-value store, dataset, or request queue ",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/reboot",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_delete",
   "name": "Delete last run's default request queue",
   "description": "Deletes the default request queue associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Delete request queue](/api/v2/request-queue-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_get",
   "name": "Get last run's default request queue",
   "description": "Returns the default request queue associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Get request queue](/api/v2/request-queue-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_head_get",
   "name": "Get last run's default request queue head",
   "description": "Returns the given number of first requests from the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Get head](/api/v2/request-queue-head-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "limit": {
      "type": "number",
      "description": "How many items from queue should be returned."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/head",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "limit",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_head_lock_post",
   "name": "Get and lock last run's default request queue head",
   "description": "Returns the given number of first requests from the default request queue of the last Actor run and locks them for the given time. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Get head and lock](/api/v2/request-queue-head-lock-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "lockSecs": {
      "type": "number",
      "description": "How long the requests will be locked for (in seconds)."
     },
     "limit": {
      "type": "number",
      "description": "How many items from the queue should be returned."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "actorId",
     "lockSecs"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/head/lock",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "lockSecs",
     "limit",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_put",
   "name": "Update last run's default request queue",
   "description": "Updates the default request queue associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Update request queue](/api/v2/request-queue-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": "The new name for the request queue."
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_request_delete",
   "name": "Delete request from last run's default request queue",
   "description": "Deletes a request from the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Delete request](/api/v2/request-queue-request-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "actorId",
     "requestId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/requests/{requestId}",
    "path": [
     "actorId",
     "requestId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_request_get",
   "name": "Get request from last run's default request queue",
   "description": "Returns a request from the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Get request](/api/v2/request-queue-request-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     }
    },
    "required": [
     "actorId",
     "requestId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/requests/{requestId}",
    "path": [
     "actorId",
     "requestId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_request_lock_delete",
   "name": "Delete lock on request in last run's default request queue",
   "description": "Deletes a request lock in the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Delete request lock](/api/v2/request-queue-request-lock-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end after lock was removed."
     }
    },
    "required": [
     "actorId",
     "requestId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/requests/{requestId}/lock",
    "path": [
     "actorId",
     "requestId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_request_lock_put",
   "name": "Prolong lock on request in last run's default request queue",
   "description": "Prolongs a request lock in the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Prolong request lock](/api/v2/request-queue-request-lock-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "lockSecs": {
      "type": "number",
      "description": "How long the requests will be locked for (in seconds)."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end after lock expires."
     }
    },
    "required": [
     "actorId",
     "lockSecs",
     "requestId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/requests/{requestId}/lock",
    "path": [
     "actorId",
     "requestId"
    ],
    "query": [
     "status",
     "origin",
     "lockSecs",
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_request_put",
   "name": "Update request in last run's default request queue",
   "description": "Updates a request in the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Update request](/api/v2/request-queue-request-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "uniqueKey": {
      "type": "string",
      "description": "A unique key used for request de-duplication. Requests with the same unique key are considered identical."
     },
     "url": {
      "type": "string",
      "description": "The URL of the request."
     },
     "method": {
      "type": "string",
      "description": "",
      "enum": [
       "GET",
       "HEAD",
       "POST",
       "PUT",
       "DELETE",
       "CONNECT",
       "OPTIONS",
       "TRACE",
       "PATCH"
      ]
     },
     "retryCount": {
      "type": "integer",
      "description": "The number of times this request has been retried."
     },
     "loadedUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "The final URL that was loaded, after redirects (if any)."
     },
     "payload": {
      "type": [
       "string",
       "object",
       "null"
      ],
      "description": "The request payload, typically used with POST or PUT requests."
     },
     "headers": {
      "type": [
       "object",
       "null"
      ],
      "description": "HTTP headers sent with the request."
     },
     "userData": {
      "type": "object",
      "description": "Custom user data attached to the request. Can contain arbitrary fields."
     },
     "noRetry": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Indicates whether the request should not be retried if processing fails."
     },
     "errorMessages": {
      "type": [
       "array",
       "null"
      ],
      "description": "Error messages recorded from failed processing attempts."
     },
     "handledAt": {
      "type": [
       "string",
       "null"
      ],
      "description": "The timestamp when the request was marked as handled, if applicable."
     }
    },
    "required": [
     "actorId",
     "requestId",
     "uniqueKey",
     "url"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/requests/{requestId}",
    "path": [
     "actorId",
     "requestId"
    ],
    "query": [
     "status",
     "origin",
     "forefront",
     "clientKey"
    ],
    "header": [],
    "body": [
     "uniqueKey",
     "url",
     "method",
     "retryCount",
     "loadedUrl",
     "payload",
     "headers",
     "userData",
     "noRetry",
     "errorMessages",
     "handledAt"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_requests_batch_delete",
   "name": "Batch delete requests from last run's default request queue",
   "description": "Batch-deletes requests from the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Delete requests](/api/v2/request-queue-requests-batch-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "Content-Type": {
      "type": "string",
      "description": ""
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "Content-Type",
     "actorId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/requests/batch",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey"
    ],
    "header": [
     "Content-Type"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_requests_batch_post",
   "name": "Batch add requests to last run's default request queue",
   "description": "Adds requests to the default request queue of the last Actor run in batch. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Add requests](/api/v2/request-queue-requests-batch-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/requests/batch",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_requests_get",
   "name": "List last run's default request queue's requests",
   "description": "Returns a list of requests from the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [List requests](/api/v2/request-queue-requests-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "exclusiveStartId": {
      "type": "string",
      "description": "All requests up to this one (including) are skipped from the result. (Deprecated, use `cursor` instead.)"
     },
     "limit": {
      "type": "number",
      "description": "Number of keys to be returned. Maximum value is `10000`."
     },
     "cursor": {
      "type": "string",
      "description": "A cursor string for pagination, returned in the previous response as `nextCursor`. Use this to retrieve the next page of requests."
     },
     "filter": {
      "type": "array",
      "description": "Filter requests by their state. Possible values are `locked` and `pending`. You can combine multiple values separated by commas, which will mean the union of these filters \u2013 requests matching any of the specified states will be returned. (Not compatible with deprecated `exclusiveStartId` parameter.)"
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/requests",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey",
     "exclusiveStartId",
     "limit",
     "cursor",
     "filter"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_requests_post",
   "name": "Add request to last run's default request queue",
   "description": "Adds a request to the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Add request](/api/v2/request-queue-requests-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     },
     "uniqueKey": {
      "type": "string",
      "description": "A unique key used for request de-duplication. Requests with the same unique key are considered identical."
     },
     "url": {
      "type": "string",
      "description": "The URL of the request."
     },
     "method": {
      "type": "string",
      "description": "",
      "enum": [
       "GET",
       "HEAD",
       "POST",
       "PUT",
       "DELETE",
       "CONNECT",
       "OPTIONS",
       "TRACE",
       "PATCH"
      ]
     },
     "retryCount": {
      "type": "integer",
      "description": "The number of times this request has been retried."
     },
     "loadedUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "The final URL that was loaded, after redirects (if any)."
     },
     "payload": {
      "type": [
       "string",
       "object",
       "null"
      ],
      "description": "The request payload, typically used with POST or PUT requests."
     },
     "headers": {
      "type": [
       "object",
       "null"
      ],
      "description": "HTTP headers sent with the request."
     },
     "userData": {
      "type": "object",
      "description": "Custom user data attached to the request. Can contain arbitrary fields."
     },
     "noRetry": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Indicates whether the request should not be retried if processing fails."
     },
     "errorMessages": {
      "type": [
       "array",
       "null"
      ],
      "description": "Error messages recorded from failed processing attempts."
     },
     "handledAt": {
      "type": [
       "string",
       "null"
      ],
      "description": "The timestamp when the request was marked as handled, if applicable."
     }
    },
    "required": [
     "actorId",
     "uniqueKey",
     "url"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/requests",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [
     "uniqueKey",
     "url",
     "method",
     "retryCount",
     "loadedUrl",
     "payload",
     "headers",
     "userData",
     "noRetry",
     "errorMessages",
     "handledAt"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_runs_last_request_queue_requests_unlock_post",
   "name": "Unlock requests in last run's default request queue",
   "description": "Unlocks requests in the default request queue of the last Actor run that are currently locked by the client. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Unlock requests](/api/v2/request-queue-requests-unlock-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs/last/request-queue/requests/unlock",
    "path": [
     "actorId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_delete",
   "name": "Delete task",
   "description": "Delete the task specified through the `actorTaskId` parameter.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}",
    "path": [
     "actorTaskId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_get",
   "name": "Get task",
   "description": "Get an object that contains all the details about a task.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}",
    "path": [
     "actorTaskId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_input_get",
   "name": "Get task input",
   "description": "Returns the input of a given task.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/input",
    "path": [
     "actorTaskId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_input_put",
   "name": "Update task input",
   "description": "Updates the input of a task using values specified by an object passed as JSON in the PUT payload. If the object does not define a specific property, its value is not updated. The response is the full task input as returned by the [Get task input](#/reference/tasks/task-input-object/get-task-input) endpoint. The request needs to specify the `Content-Type: application/json` HTTP header! When provid",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/input",
    "path": [
     "actorTaskId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_last_log_get",
   "name": "Get last actor task run's log",
   "description": "Retrieves last Actor task run's logs. This endpoint is a shortcut for getting last Actor task run's log. Same as [Get log](/api/v2/log-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "stream": {
      "type": "boolean",
      "description": "If `true` or `1` then the logs will be streamed as long as the run or build is running."
     },
     "download": {
      "type": "boolean",
      "description": "If `true` or `1` then the web browser will download the log file rather than open it in a tab."
     },
     "raw": {
      "type": "boolean",
      "description": "If `true` or `1`, the logs will be kept verbatim. By default, the API removes ANSI escape codes from the logs, keeping only printable characters."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/log",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "stream",
     "download",
     "raw"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_put",
   "name": "Update task",
   "description": "Update settings of a task using values specified by an object passed as JSON in the POST payload. If the object does not define a specific property, its value is not updated. The `publicConfig` field carries the display configuration of the task's public landing page, and `isPublic` publishes or unpublishes the task itself. Both require write permission to the task's Actor. To publish a task, its ",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "name": {
      "type": "string",
      "description": ""
     },
     "options": {
      "type": "string",
      "description": ""
     },
     "input": {
      "type": "string",
      "description": ""
     },
     "title": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "description": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "actorStandby": {
      "type": "string",
      "description": ""
     },
     "publicConfig": {
      "type": "object",
      "description": "Public-facing configuration of a published task, used by the task's public landing page. The task's publication state is determined by `publishedAt` - a task is published when `publishedAt` is set and unpublished when it is `null`."
     },
     "isPublic": {
      "type": "boolean",
      "description": "Set to `true` to publish the task on its public landing page, or `false` to unpublish it. Sending the value the task already has does nothing."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}",
    "path": [
     "actorTaskId"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "options",
     "input",
     "title",
     "description",
     "actorStandby",
     "publicConfig",
     "isPublic"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_run_sync_get",
   "name": "Run task synchronously",
   "description": "Runs an Actor task and synchronously returns a key-value store record. The response contains the record stored under the `OUTPUT` key in the run's default key-value store. This is a legacy approach that has been replaced by the Actor [output object](https://docs.apify.com/platform/actors/development/actor-definition/output-schema#output-object-definition); Actors aren't required to store a record ",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout from its configuration."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. You can set the amount of memory to a power of 2 with a minimum of 128. By default, the run uses the memory limit from its configuration. Don't change this value unless the Actor's documentation recommends it or you're aware of the consequences."
     },
     "maxItems": {
      "type": "number",
      "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors. This does NOT guarantee that the Actor will return only this many items. It only ensures you won't be charged for more than this number of items. Only works for pay-per-result Actors. Value can be accessed in the actor run using `ACTOR_MAX_PAID_DATASET_ITEMS` environment variable."
     },
     "maxTotalChargeUsd": {
      "type": "number",
      "description": "Specifies the maximum total cost of the run. Use it to cap the total amount charged for all pricing models. You can access the maximum cost in your Actor by using the `ACTOR_MAX_TOTAL_CHARGE_USD` environment variable."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run uses the build from its configuration (typically `latest`)."
     },
     "outputRecordKey": {
      "type": "string",
      "description": "Key of the record from the run's default key-value store to return in the response. Defaults to `OUTPUT`. Actors aren't required to store a record under this key, so if it doesn't exist the response contains no data."
     },
     "webhooks": {
      "type": "string",
      "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a notification e.g. when the Actor finished or failed. The value is a Base64-encoded JSON array whose items follow the WebhookRepresentation schema. For more information, see [Webhooks documentation](https://docs.apify.com/platform/integrations/webhooks)."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/run-sync",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "timeout",
     "memory",
     "maxItems",
     "maxTotalChargeUsd",
     "build",
     "outputRecordKey",
     "webhooks"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_run_sync_get_dataset_items_get",
   "name": "Run task synchronously and get dataset items",
   "description": "Run a specific task and return its dataset items. The run must finish in 300 seconds otherwise the HTTP request fails with a timeout error (this won't abort the run itself). You can send all the same options in parameters as the [Get Dataset Items](#/reference/datasets/item-collection/get-items) API endpoint. Beware that it might be impossible to maintain an idle HTTP connection for an extended pe",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout from its configuration."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. You can set the amount of memory to a power of 2 with a minimum of 128. By default, the run uses the memory limit from its configuration. Don't change this value unless the Actor's documentation recommends it or you're aware of the consequences."
     },
     "maxItems": {
      "type": "number",
      "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors. This does NOT guarantee that the Actor will return only this many items. It only ensures you won't be charged for more than this number of items. Only works for pay-per-result Actors. Value can be accessed in the actor run using `ACTOR_MAX_PAID_DATASET_ITEMS` environment variable."
     },
     "maxTotalChargeUsd": {
      "type": "number",
      "description": "Specifies the maximum total cost of the run. Use it to cap the total amount charged for all pricing models. You can access the maximum cost in your Actor by using the `ACTOR_MAX_TOTAL_CHARGE_USD` environment variable."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run uses the build from its configuration (typically `latest`)."
     },
     "webhooks": {
      "type": "string",
      "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a notification e.g. when the Actor finished or failed. The value is a Base64-encoded JSON array whose items follow the WebhookRepresentation schema. For more information, see [Webhooks documentation](https://docs.apify.com/platform/integrations/webhooks)."
     },
     "format": {
      "type": "string",
      "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml` and `rss`. The default value is `json`."
     },
     "clean": {
      "type": "boolean",
      "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i.e. fields starting with the # character). The `clean` parameter is just a shortcut for `skipHidden=true` and `skipEmpty=true` parameters. Note that since some objects might be skipped from the output, that the result might contain less items than the `limit` value."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. By default there is no limit."
     },
     "fields": {
      "type": "string",
      "description": "A comma-separated list of fields which should be picked from the items, only these fields will remain in the resulting record objects. Note that the fields in the outputted items are sorted the same way as they are specified in the `fields` query parameter. You can use this feature to effectively fix the output format."
     },
     "outputFields": {
      "type": "string",
      "description": "A comma-separated list of output field names that positionally rename the fields specified in the `fields` parameter. For example, `?fields=headline,url&outputFields=title,link` renames `headline` to `title` and `url` to `link` in the output. The number of names in `outputFields` must match the number of names in `fields`. Requires the `fields` parameter to be specified as well."
     },
     "omit": {
      "type": "string",
      "description": "A comma-separated list of fields which should be omitted from the items."
     },
     "unwind": {
      "type": "string",
      "description": "A comma-separated list of fields which should be unwound, in order which they should be processed. Each field should be either an array or an object. If the field is an array then every element of the array will become a separate record and merged with parent object. If the unwound field is an object then it is merged with the parent object. If the unwound field is missing or its value is neither "
     },
     "flatten": {
      "type": "string",
      "description": "A comma-separated list of fields which should transform nested objects into flat structures. For example, with `flatten=\"foo\"` the object `{\"foo\":{\"bar\": \"hello\"}}` is turned into `{\"foo.bar\": \"hello\"}`. The original object with properties is replaced with the flattened object."
     },
     "desc": {
      "type": "boolean",
      "description": "By default, results are returned in the same order as they were stored. To reverse the order, set this parameter to `true` or `1`."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header, forcing a web browser to download the file rather than to display it. By default this header is not present."
     },
     "delimiter": {
      "type": "string",
      "description": "A delimiter character for CSV files, only used if `format=csv`. You might need to URL-encode the character (e.g. use `%09` for tab or `%3B` for semicolon). The default delimiter is a simple comma (`,`)."
     },
     "bom": {
      "type": "boolean",
      "description": "All text responses are encoded in UTF-8 encoding. By default, the `format=csv` files are prefixed with the UTF-8 Byte Order Mark (BOM), while `json`, `jsonl`, `xml`, `html` and `rss` files are not. If you want to override this default behavior, specify `bom=1` query parameter to include the BOM or `bom=0` to skip it."
     },
     "xmlRoot": {
      "type": "string",
      "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
     },
     "xmlRow": {
      "type": "string",
      "description": "Overrides default element name that wraps each page or page function result object in `xml` output. By default the element name is `item`."
     },
     "skipHeaderRow": {
      "type": "boolean",
      "description": "If `true` or `1` then header row in the `csv` format is skipped."
     },
     "skipHidden": {
      "type": "boolean",
      "description": "If `true` or `1` then hidden fields are skipped from the output, i.e. fields starting with the `#` character."
     },
     "skipEmpty": {
      "type": "boolean",
      "description": "If `true` or `1` then empty items are skipped from the output. Note that if used, the results might contain less items than the limit value."
     },
     "simplified": {
      "type": "boolean",
      "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo` and `unwind=pageFunctionResult` query parameters. This feature is used to emulate simplified results provided by the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "view": {
      "type": "string",
      "description": "Defines the view configuration for dataset items based on the schema definition. This parameter determines how the data will be filtered and presented. For complete specification details, see the [dataset schema documentation](/storage/dataset-schema)."
     },
     "skipFailedPages": {
      "type": "boolean",
      "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the output. This feature is here to emulate functionality of API version 1 used for the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "feedTitle": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the title defaults to `Dataset `."
     },
     "feedDescription": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the description defaults to `Items in dataset with id \"\".`"
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/run-sync-get-dataset-items",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "timeout",
     "memory",
     "maxItems",
     "maxTotalChargeUsd",
     "build",
     "webhooks",
     "format",
     "clean",
     "offset",
     "limit",
     "fields",
     "outputFields",
     "omit",
     "unwind",
     "flatten",
     "desc",
     "attachment",
     "delimiter",
     "bom",
     "xmlRoot",
     "xmlRow",
     "skipHeaderRow",
     "skipHidden",
     "skipEmpty",
     "simplified",
     "view",
     "skipFailedPages",
     "feedTitle",
     "feedDescription"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_run_sync_get_dataset_items_post",
   "name": "Run task synchronously and get dataset items",
   "description": "Runs an Actor task and synchronously returns its dataset items. The run must finish in 300 seconds otherwise the HTTP request fails with a timeout error (this won't abort the run itself). Optionally, you can override the Actor input configuration by passing a JSON object as the POST payload and setting the `Content-Type: application/json` HTTP header. Note that if the object in the POST payload do",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout from its configuration."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. You can set the amount of memory to a power of 2 with a minimum of 128. By default, the run uses the memory limit from its configuration. Don't change this value unless the Actor's documentation recommends it or you're aware of the consequences."
     },
     "maxItems": {
      "type": "number",
      "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors. This does NOT guarantee that the Actor will return only this many items. It only ensures you won't be charged for more than this number of items. Only works for pay-per-result Actors. Value can be accessed in the actor run using `ACTOR_MAX_PAID_DATASET_ITEMS` environment variable."
     },
     "maxTotalChargeUsd": {
      "type": "number",
      "description": "Specifies the maximum total cost of the run. Use it to cap the total amount charged for all pricing models. You can access the maximum cost in your Actor by using the `ACTOR_MAX_TOTAL_CHARGE_USD` environment variable."
     },
     "restartOnError": {
      "type": "boolean",
      "description": "Determines whether the run will be restarted if it fails."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run uses the build from its configuration (typically `latest`)."
     },
     "webhooks": {
      "type": "string",
      "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a notification e.g. when the Actor finished or failed. The value is a Base64-encoded JSON array whose items follow the WebhookRepresentation schema. For more information, see [Webhooks documentation](https://docs.apify.com/platform/integrations/webhooks)."
     },
     "format": {
      "type": "string",
      "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml` and `rss`. The default value is `json`."
     },
     "clean": {
      "type": "boolean",
      "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i.e. fields starting with the # character). The `clean` parameter is just a shortcut for `skipHidden=true` and `skipEmpty=true` parameters. Note that since some objects might be skipped from the output, that the result might contain less items than the `limit` value."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. By default there is no limit."
     },
     "fields": {
      "type": "string",
      "description": "A comma-separated list of fields which should be picked from the items, only these fields will remain in the resulting record objects. Note that the fields in the outputted items are sorted the same way as they are specified in the `fields` query parameter. You can use this feature to effectively fix the output format."
     },
     "outputFields": {
      "type": "string",
      "description": "A comma-separated list of output field names that positionally rename the fields specified in the `fields` parameter. For example, `?fields=headline,url&outputFields=title,link` renames `headline` to `title` and `url` to `link` in the output. The number of names in `outputFields` must match the number of names in `fields`. Requires the `fields` parameter to be specified as well."
     },
     "omit": {
      "type": "string",
      "description": "A comma-separated list of fields which should be omitted from the items."
     },
     "unwind": {
      "type": "string",
      "description": "A comma-separated list of fields which should be unwound, in order which they should be processed. Each field should be either an array or an object. If the field is an array then every element of the array will become a separate record and merged with parent object. If the unwound field is an object then it is merged with the parent object. If the unwound field is missing or its value is neither "
     },
     "flatten": {
      "type": "string",
      "description": "A comma-separated list of fields which should transform nested objects into flat structures. For example, with `flatten=\"foo\"` the object `{\"foo\":{\"bar\": \"hello\"}}` is turned into `{\"foo.bar\": \"hello\"}`. The original object with properties is replaced with the flattened object."
     },
     "desc": {
      "type": "boolean",
      "description": "By default, results are returned in the same order as they were stored. To reverse the order, set this parameter to `true` or `1`."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header, forcing a web browser to download the file rather than to display it. By default this header is not present."
     },
     "delimiter": {
      "type": "string",
      "description": "A delimiter character for CSV files, only used if `format=csv`. You might need to URL-encode the character (e.g. use `%09` for tab or `%3B` for semicolon). The default delimiter is a simple comma (`,`)."
     },
     "bom": {
      "type": "boolean",
      "description": "All text responses are encoded in UTF-8 encoding. By default, the `format=csv` files are prefixed with the UTF-8 Byte Order Mark (BOM), while `json`, `jsonl`, `xml`, `html` and `rss` files are not. If you want to override this default behavior, specify `bom=1` query parameter to include the BOM or `bom=0` to skip it."
     },
     "xmlRoot": {
      "type": "string",
      "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
     },
     "xmlRow": {
      "type": "string",
      "description": "Overrides default element name that wraps each page or page function result object in `xml` output. By default the element name is `item`."
     },
     "skipHeaderRow": {
      "type": "boolean",
      "description": "If `true` or `1` then header row in the `csv` format is skipped."
     },
     "skipHidden": {
      "type": "boolean",
      "description": "If `true` or `1` then hidden fields are skipped from the output, i.e. fields starting with the `#` character."
     },
     "skipEmpty": {
      "type": "boolean",
      "description": "If `true` or `1` then empty items are skipped from the output. Note that if used, the results might contain less items than the limit value."
     },
     "simplified": {
      "type": "boolean",
      "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo` and `unwind=pageFunctionResult` query parameters. This feature is used to emulate simplified results provided by the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "view": {
      "type": "string",
      "description": "Defines the view configuration for dataset items based on the schema definition. This parameter determines how the data will be filtered and presented. For complete specification details, see the [dataset schema documentation](/storage/dataset-schema)."
     },
     "skipFailedPages": {
      "type": "boolean",
      "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the output. This feature is here to emulate functionality of API version 1 used for the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "feedTitle": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the title defaults to `Dataset `."
     },
     "feedDescription": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the description defaults to `Items in dataset with id \"\".`"
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/run-sync-get-dataset-items",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "timeout",
     "memory",
     "maxItems",
     "maxTotalChargeUsd",
     "restartOnError",
     "build",
     "webhooks",
     "format",
     "clean",
     "offset",
     "limit",
     "fields",
     "outputFields",
     "omit",
     "unwind",
     "flatten",
     "desc",
     "attachment",
     "delimiter",
     "bom",
     "xmlRoot",
     "xmlRow",
     "skipHeaderRow",
     "skipHidden",
     "skipEmpty",
     "simplified",
     "view",
     "skipFailedPages",
     "feedTitle",
     "feedDescription"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_run_sync_post",
   "name": "Run task synchronously",
   "description": "Runs an Actor task and synchronously returns a key-value store record. The response contains the record stored under the `OUTPUT` key in the run's default key-value store. This is a legacy approach that has been replaced by the Actor [output object](https://docs.apify.com/platform/actors/development/actor-definition/output-schema#output-object-definition); Actors aren't required to store a record ",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout from its configuration."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. You can set the amount of memory to a power of 2 with a minimum of 128. By default, the run uses the memory limit from its configuration. Don't change this value unless the Actor's documentation recommends it or you're aware of the consequences."
     },
     "maxItems": {
      "type": "number",
      "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors. This does NOT guarantee that the Actor will return only this many items. It only ensures you won't be charged for more than this number of items. Only works for pay-per-result Actors. Value can be accessed in the actor run using `ACTOR_MAX_PAID_DATASET_ITEMS` environment variable."
     },
     "maxTotalChargeUsd": {
      "type": "number",
      "description": "Specifies the maximum total cost of the run. Use it to cap the total amount charged for all pricing models. You can access the maximum cost in your Actor by using the `ACTOR_MAX_TOTAL_CHARGE_USD` environment variable."
     },
     "restartOnError": {
      "type": "boolean",
      "description": "Determines whether the run will be restarted if it fails."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run uses the build from its configuration (typically `latest`)."
     },
     "outputRecordKey": {
      "type": "string",
      "description": "Key of the record from the run's default key-value store to return in the response. Defaults to `OUTPUT`. Actors aren't required to store a record under this key, so if it doesn't exist the response contains no data."
     },
     "webhooks": {
      "type": "string",
      "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a notification e.g. when the Actor finished or failed. The value is a Base64-encoded JSON array whose items follow the WebhookRepresentation schema. For more information, see [Webhooks documentation](https://docs.apify.com/platform/integrations/webhooks)."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/run-sync",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "timeout",
     "memory",
     "maxItems",
     "maxTotalChargeUsd",
     "restartOnError",
     "build",
     "outputRecordKey",
     "webhooks"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_get",
   "name": "Get list of task runs",
   "description": "Get a list of runs of a specific task. The response is a list of objects, where each object contains essential information about a single task run. The endpoint supports pagination using the `limit` and `offset` parameters, and it does not return more than a 1000 array elements. By default, the records are sorted by the `startedAt` field in ascending order; therefore you can use pagination to incr",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `startedAt` field in descending order. By default, they are sorted in ascending order."
     },
     "status": {
      "type": "array",
      "description": "Single status or comma-separated list of statuses, see ([available statuses](https://docs.apify.com/platform/actors/running/runs-and-builds#lifecycle)). Used to filter runs by the specified statuses only."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "offset",
     "limit",
     "desc",
     "status"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_abort_post",
   "name": "Abort actor task's last run",
   "description": "Aborts the last run of the specified Actor task and returns an object that contains all the details about the run. This endpoint is a shortcut for [Abort run](#/reference/actor-runs/abort-run/abort-run) on the Actor task's last run. Only runs that are starting or running are aborted. For runs with status `FINISHED`, `FAILED`, `ABORTING` and `TIMED-OUT` this call does nothing.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "gracefully": {
      "type": "boolean",
      "description": "If true passed, the Actor run will abort gracefully. It will send `aborting` and `persistState` event into run and force-stop the run after 30 seconds. It is helpful in cases where you plan to resurrect the run later."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/abort",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "gracefully"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_dataset_delete",
   "name": "Delete last task run's default dataset",
   "description": "Deletes the default dataset associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultDatasetId` and then using the [Delete dataset](/api/v2/dataset-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/dataset",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_dataset_get",
   "name": "Get last task run's default dataset",
   "description": "Returns the default dataset associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultDatasetId` and then using the [Get dataset](/api/v2/dataset-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/dataset",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_dataset_items_get",
   "name": "Get last task run's dataset items",
   "description": "Returns data stored in the default dataset of the last Actor task run in the desired format. This endpoint is a shortcut that resolves the last task run's `defaultDatasetId` and proxies to the [Get dataset items](/api/v2/dataset-items-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "format": {
      "type": "string",
      "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml` and `rss`. The default value is `json`."
     },
     "clean": {
      "type": "boolean",
      "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i.e. fields starting with the # character). The `clean` parameter is just a shortcut for `skipHidden=true` and `skipEmpty=true` parameters. Note that since some objects might be skipped from the output, that the result might contain less items than the `limit` value."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. By default there is no limit."
     },
     "fields": {
      "type": "string",
      "description": "A comma-separated list of fields which should be picked from the items, only these fields will remain in the resulting record objects. Note that the fields in the outputted items are sorted the same way as they are specified in the `fields` query parameter. You can use this feature to effectively fix the output format."
     },
     "outputFields": {
      "type": "string",
      "description": "A comma-separated list of output field names that positionally rename the fields specified in the `fields` parameter. For example, `?fields=headline,url&outputFields=title,link` renames `headline` to `title` and `url` to `link` in the output. The number of names in `outputFields` must match the number of names in `fields`. Requires the `fields` parameter to be specified as well."
     },
     "omit": {
      "type": "string",
      "description": "A comma-separated list of fields which should be omitted from the items."
     },
     "unwind": {
      "type": "string",
      "description": "A comma-separated list of fields which should be unwound, in order which they should be processed. Each field should be either an array or an object. If the field is an array then every element of the array will become a separate record and merged with parent object. If the unwound field is an object then it is merged with the parent object. If the unwound field is missing or its value is neither "
     },
     "flatten": {
      "type": "string",
      "description": "A comma-separated list of fields which should transform nested objects into flat structures. For example, with `flatten=\"foo\"` the object `{\"foo\":{\"bar\": \"hello\"}}` is turned into `{\"foo.bar\": \"hello\"}`. The original object with properties is replaced with the flattened object."
     },
     "desc": {
      "type": "boolean",
      "description": "By default, results are returned in the same order as they were stored. To reverse the order, set this parameter to `true` or `1`."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header, forcing a web browser to download the file rather than to display it. By default this header is not present."
     },
     "delimiter": {
      "type": "string",
      "description": "A delimiter character for CSV files, only used if `format=csv`. You might need to URL-encode the character (e.g. use `%09` for tab or `%3B` for semicolon). The default delimiter is a simple comma (`,`)."
     },
     "bom": {
      "type": "boolean",
      "description": "All text responses are encoded in UTF-8 encoding. By default, the `format=csv` files are prefixed with the UTF-8 Byte Order Mark (BOM), while `json`, `jsonl`, `xml`, `html` and `rss` files are not. If you want to override this default behavior, specify `bom=1` query parameter to include the BOM or `bom=0` to skip it."
     },
     "xmlRoot": {
      "type": "string",
      "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
     },
     "xmlRow": {
      "type": "string",
      "description": "Overrides default element name that wraps each page or page function result object in `xml` output. By default the element name is `item`."
     },
     "skipHeaderRow": {
      "type": "boolean",
      "description": "If `true` or `1` then header row in the `csv` format is skipped."
     },
     "skipHidden": {
      "type": "boolean",
      "description": "If `true` or `1` then hidden fields are skipped from the output, i.e. fields starting with the `#` character."
     },
     "skipEmpty": {
      "type": "boolean",
      "description": "If `true` or `1` then empty items are skipped from the output. Note that if used, the results might contain less items than the limit value."
     },
     "simplified": {
      "type": "boolean",
      "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo` and `unwind=pageFunctionResult` query parameters. This feature is used to emulate simplified results provided by the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "view": {
      "type": "string",
      "description": "Defines the view configuration for dataset items based on the schema definition. This parameter determines how the data will be filtered and presented. For complete specification details, see the [dataset schema documentation](/storage/dataset-schema)."
     },
     "skipFailedPages": {
      "type": "boolean",
      "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the output. This feature is here to emulate functionality of API version 1 used for the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "feedTitle": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the title defaults to `Dataset `."
     },
     "feedDescription": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the description defaults to `Items in dataset with id \"\".`"
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/dataset/items",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "format",
     "clean",
     "offset",
     "limit",
     "fields",
     "outputFields",
     "omit",
     "unwind",
     "flatten",
     "desc",
     "attachment",
     "delimiter",
     "bom",
     "xmlRoot",
     "xmlRow",
     "skipHeaderRow",
     "skipHidden",
     "skipEmpty",
     "simplified",
     "view",
     "skipFailedPages",
     "feedTitle",
     "feedDescription",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_dataset_items_post",
   "name": "Store items in last task run's dataset",
   "description": "Appends an item or an array of items to the end of the last Actor task run's default dataset. This endpoint is a shortcut that resolves the last task run's `defaultDatasetId` and proxies to the [Store items](/api/v2/dataset-items-post) endpoint. To save bandwidth and speed up your upload, you can send the request payload compressed and set the `Content-Encoding` header accordingly. Below is a list",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/dataset/items",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_dataset_put",
   "name": "Update last task run's default dataset",
   "description": "Updates the default dataset associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultDatasetId` and then using the [Update dataset](/api/v2/dataset-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/dataset",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_dataset_statistics_get",
   "name": "Get last task run's dataset statistics",
   "description": "Returns statistics for the last Actor task run's default dataset. This endpoint is a shortcut that resolves the last task run's `defaultDatasetId` and proxies to the [Get dataset statistics](/api/v2/dataset-statistics-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/dataset/statistics",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_get",
   "name": "Get last run",
   "description": "This is not a single endpoint, but an entire group of endpoints that lets you to retrieve and manage the last run of given actor task or any of its default storages. All the endpoints require an authentication token. The base path represents the last actor task run object is: `/v2/actor-tasks/{actorTaskId}/runs/last{?token,status,origin}` Using the `status` query parameter you can ensure to only g",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "waitForFinish": {
      "type": "number",
      "description": "The maximum number of seconds the server waits for the run to finish. By default it is `0`, the maximum value is `60`. If the run finishes in time then the returned run object will have a terminal status (e.g. `SUCCEEDED`), otherwise it will have a transitional status (e.g. `RUNNING`)."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "waitForFinish"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_key_value_store_delete",
   "name": "Delete last task run's default store",
   "description": "Deletes the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Delete store](/api/v2/key-value-store-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_key_value_store_get",
   "name": "Get last task run's default store",
   "description": "Gets an object that contains all the details about the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Get store](/api/v2/key-value-store-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_key_value_store_keys_get",
   "name": "Get last task run's default store's list of keys",
   "description": "Returns a list of keys for the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Get list of keys](/api/v2/key-value-store-keys-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "exclusiveStartKey": {
      "type": "string",
      "description": "All keys up to this one (including) are skipped from the result."
     },
     "limit": {
      "type": "number",
      "description": "Number of keys to be returned."
     },
     "collection": {
      "type": "string",
      "description": "Limit the results to keys that belong to a specific collection from the key-value store schema. The key-value store need to have a schema defined for this parameter to work."
     },
     "prefix": {
      "type": "string",
      "description": "Limit the results to keys that start with a specific prefix."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/keys",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "exclusiveStartKey",
     "limit",
     "collection",
     "prefix",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_key_value_store_put",
   "name": "Update last task run's default store",
   "description": "Updates the default key-value store associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Update store](/api/v2/key-value-store-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_key_value_store_record_delete",
   "name": "Delete last task run's default store's record",
   "description": "Removes a record specified by a key from the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Delete record](/api/v2/key-value-store-record-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     }
    },
    "required": [
     "actorTaskId",
     "recordKey"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/records/{recordKey}",
    "path": [
     "actorTaskId",
     "recordKey"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_key_value_store_record_get",
   "name": "Get last task run's default store's record",
   "description": "Gets a value stored under a specific key in the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Get record](/api/v2/key-value-store-record-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1`, the response will be served with `Content-Disposition: attachment` header, causing web browsers to offer downloading HTML records instead of displaying them."
     }
    },
    "required": [
     "actorTaskId",
     "recordKey"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/records/{recordKey}",
    "path": [
     "actorTaskId",
     "recordKey"
    ],
    "query": [
     "status",
     "origin",
     "signature",
     "attachment"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_key_value_store_record_post",
   "name": "Store record in last task run's default store (post)",
   "description": "Stores a value under a specific key in the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "actorTaskId",
     "recordKey"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/records/{recordKey}",
    "path": [
     "actorTaskId",
     "recordKey"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_key_value_store_record_put",
   "name": "Store record in last task run's default store",
   "description": "Stores a value under a specific key in the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "actorTaskId",
     "recordKey"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/records/{recordKey}",
    "path": [
     "actorTaskId",
     "recordKey"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_key_value_store_records_get",
   "name": "Download last task run's default store's records",
   "description": "Downloads all records from the default key-value store of the last Actor task run as a ZIP archive. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Download records](/api/v2/key-value-store-records-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "collection": {
      "type": "string",
      "description": "If specified, only records belonging to a specific collection from the key-value store schema. The key-value store need to have a schema defined for this parameter to work."
     },
     "prefix": {
      "type": "string",
      "description": "If specified, only records whose key starts with the given prefix are included in the archive."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/records",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "collection",
     "prefix",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_metamorph_post",
   "name": "Metamorph actor task's last run",
   "description": "Transforms the last run of the specified Actor task into a run of another Actor with a new input. This endpoint is a shortcut for [Metamorph run](#/reference/actor-runs/metamorph-run/metamorph-run) on the Actor task's last run.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "targetActorId": {
      "type": "string",
      "description": "ID of a target Actor that the run should be transformed into."
     },
     "build": {
      "type": "string",
      "description": "Optional build of the target Actor. It can be either a build tag or build number. By default, the run uses the build specified in the default run configuration for the target Actor (typically `latest`)."
     }
    },
    "required": [
     "actorTaskId",
     "targetActorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/metamorph",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "targetActorId",
     "build"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_reboot_post",
   "name": "Reboot actor task's last run",
   "description": "Reboots the last run of the specified Actor task and returns an object that contains all the details about the rebooted run. This endpoint is a shortcut for [Reboot run](#/reference/actor-runs/reboot-run/reboot-run) on the Actor task's last run. Only runs with status `RUNNING` can be rebooted. The run's container will be restarted, so any data not persisted in the key-value store, dataset, or requ",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/reboot",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_delete",
   "name": "Delete last task run's default request queue",
   "description": "Deletes the default request queue associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Delete request queue](/api/v2/request-queue-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_get",
   "name": "Get last task run's default request queue",
   "description": "Returns the default request queue associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Get request queue](/api/v2/request-queue-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_head_get",
   "name": "Get last task run's default request queue head",
   "description": "Returns the given number of first requests from the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Get head](/api/v2/request-queue-head-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "limit": {
      "type": "number",
      "description": "How many items from queue should be returned."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/head",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "limit",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_head_lock_post",
   "name": "Get and lock last task run's default request queue head",
   "description": "Returns the given number of first requests from the default request queue of the last Actor task run and locks them for the given time. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Get head and lock](/api/v2/request-queue-head-lock-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "lockSecs": {
      "type": "number",
      "description": "How long the requests will be locked for (in seconds)."
     },
     "limit": {
      "type": "number",
      "description": "How many items from the queue should be returned."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "actorTaskId",
     "lockSecs"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/head/lock",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "lockSecs",
     "limit",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_put",
   "name": "Update last task run's default request queue",
   "description": "Updates the default request queue associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Update request queue](/api/v2/request-queue-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": "The new name for the request queue."
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_request_delete",
   "name": "Delete request from last task run's default request queue",
   "description": "Deletes a request from the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Delete request](/api/v2/request-queue-request-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "actorTaskId",
     "requestId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/{requestId}",
    "path": [
     "actorTaskId",
     "requestId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_request_get",
   "name": "Get request from last task run's default request queue",
   "description": "Returns a request from the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Get request](/api/v2/request-queue-request-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     }
    },
    "required": [
     "actorTaskId",
     "requestId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/{requestId}",
    "path": [
     "actorTaskId",
     "requestId"
    ],
    "query": [
     "status",
     "origin"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_request_lock_delete",
   "name": "Delete lock on request in last task run's default request queue",
   "description": "Deletes a request lock in the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Delete request lock](/api/v2/request-queue-request-lock-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end after lock was removed."
     }
    },
    "required": [
     "actorTaskId",
     "requestId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/{requestId}/lock",
    "path": [
     "actorTaskId",
     "requestId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_request_lock_put",
   "name": "Prolong lock on request in last task run's default request queue",
   "description": "Prolongs a request lock in the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Prolong request lock](/api/v2/request-queue-request-lock-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "lockSecs": {
      "type": "number",
      "description": "How long the requests will be locked for (in seconds)."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end after lock expires."
     }
    },
    "required": [
     "actorTaskId",
     "lockSecs",
     "requestId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/{requestId}/lock",
    "path": [
     "actorTaskId",
     "requestId"
    ],
    "query": [
     "status",
     "origin",
     "lockSecs",
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_request_put",
   "name": "Update request in last task run's default request queue",
   "description": "Updates a request in the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Update request](/api/v2/request-queue-request-put) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "uniqueKey": {
      "type": "string",
      "description": "A unique key used for request de-duplication. Requests with the same unique key are considered identical."
     },
     "url": {
      "type": "string",
      "description": "The URL of the request."
     },
     "method": {
      "type": "string",
      "description": "",
      "enum": [
       "GET",
       "HEAD",
       "POST",
       "PUT",
       "DELETE",
       "CONNECT",
       "OPTIONS",
       "TRACE",
       "PATCH"
      ]
     },
     "retryCount": {
      "type": "integer",
      "description": "The number of times this request has been retried."
     },
     "loadedUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "The final URL that was loaded, after redirects (if any)."
     },
     "payload": {
      "type": [
       "string",
       "object",
       "null"
      ],
      "description": "The request payload, typically used with POST or PUT requests."
     },
     "headers": {
      "type": [
       "object",
       "null"
      ],
      "description": "HTTP headers sent with the request."
     },
     "userData": {
      "type": "object",
      "description": "Custom user data attached to the request. Can contain arbitrary fields."
     },
     "noRetry": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Indicates whether the request should not be retried if processing fails."
     },
     "errorMessages": {
      "type": [
       "array",
       "null"
      ],
      "description": "Error messages recorded from failed processing attempts."
     },
     "handledAt": {
      "type": [
       "string",
       "null"
      ],
      "description": "The timestamp when the request was marked as handled, if applicable."
     }
    },
    "required": [
     "actorTaskId",
     "requestId",
     "uniqueKey",
     "url"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/{requestId}",
    "path": [
     "actorTaskId",
     "requestId"
    ],
    "query": [
     "status",
     "origin",
     "forefront",
     "clientKey"
    ],
    "header": [],
    "body": [
     "uniqueKey",
     "url",
     "method",
     "retryCount",
     "loadedUrl",
     "payload",
     "headers",
     "userData",
     "noRetry",
     "errorMessages",
     "handledAt"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_requests_batch_delete",
   "name": "Batch delete requests from last task run's default request queue",
   "description": "Batch-deletes requests from the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Delete requests](/api/v2/request-queue-requests-batch-delete) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "Content-Type": {
      "type": "string",
      "description": ""
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "Content-Type",
     "actorTaskId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/batch",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey"
    ],
    "header": [
     "Content-Type"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_requests_batch_post",
   "name": "Batch add requests to last task run's default request queue",
   "description": "Adds requests to the default request queue of the last Actor task run in batch. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Add requests](/api/v2/request-queue-requests-batch-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/batch",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_requests_get",
   "name": "List last task run's default request queue's requests",
   "description": "Returns a list of requests from the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [List requests](/api/v2/request-queue-requests-get) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "exclusiveStartId": {
      "type": "string",
      "description": "All requests up to this one (including) are skipped from the result. (Deprecated, use `cursor` instead.)"
     },
     "limit": {
      "type": "number",
      "description": "Number of keys to be returned. Maximum value is `10000`."
     },
     "cursor": {
      "type": "string",
      "description": "A cursor string for pagination, returned in the previous response as `nextCursor`. Use this to retrieve the next page of requests."
     },
     "filter": {
      "type": "array",
      "description": "Filter requests by their state. Possible values are `locked` and `pending`. You can combine multiple values separated by commas, which will mean the union of these filters \u2013 requests matching any of the specified states will be returned. (Not compatible with deprecated `exclusiveStartId` parameter.)"
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey",
     "exclusiveStartId",
     "limit",
     "cursor",
     "filter"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_requests_post",
   "name": "Add request to last task run's default request queue",
   "description": "Adds a request to the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Add request](/api/v2/request-queue-requests-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     },
     "uniqueKey": {
      "type": "string",
      "description": "A unique key used for request de-duplication. Requests with the same unique key are considered identical."
     },
     "url": {
      "type": "string",
      "description": "The URL of the request."
     },
     "method": {
      "type": "string",
      "description": "",
      "enum": [
       "GET",
       "HEAD",
       "POST",
       "PUT",
       "DELETE",
       "CONNECT",
       "OPTIONS",
       "TRACE",
       "PATCH"
      ]
     },
     "retryCount": {
      "type": "integer",
      "description": "The number of times this request has been retried."
     },
     "loadedUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "The final URL that was loaded, after redirects (if any)."
     },
     "payload": {
      "type": [
       "string",
       "object",
       "null"
      ],
      "description": "The request payload, typically used with POST or PUT requests."
     },
     "headers": {
      "type": [
       "object",
       "null"
      ],
      "description": "HTTP headers sent with the request."
     },
     "userData": {
      "type": "object",
      "description": "Custom user data attached to the request. Can contain arbitrary fields."
     },
     "noRetry": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Indicates whether the request should not be retried if processing fails."
     },
     "errorMessages": {
      "type": [
       "array",
       "null"
      ],
      "description": "Error messages recorded from failed processing attempts."
     },
     "handledAt": {
      "type": [
       "string",
       "null"
      ],
      "description": "The timestamp when the request was marked as handled, if applicable."
     }
    },
    "required": [
     "actorTaskId",
     "uniqueKey",
     "url"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [
     "uniqueKey",
     "url",
     "method",
     "retryCount",
     "loadedUrl",
     "payload",
     "headers",
     "userData",
     "noRetry",
     "errorMessages",
     "handledAt"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_last_request_queue_requests_unlock_post",
   "name": "Unlock requests in last task run's default request queue",
   "description": "Unlocks requests in the default request queue of the last Actor task run that are currently locked by the client. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Unlock requests](/api/v2/request-queue-requests-unlock-post) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "status": {
      "type": "string",
      "description": "Filter for the run status."
     },
     "origin": {
      "type": "string",
      "description": "Filter for the run origin, i.e. the means by which the run was started.",
      "enum": [
       "DEVELOPMENT",
       "WEB",
       "API",
       "SCHEDULER",
       "TEST",
       "WEBHOOK",
       "ACTOR",
       "CLI",
       "CI",
       "STANDBY",
       "MCP"
      ]
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/unlock",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "status",
     "origin",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_runs_post",
   "name": "Run task",
   "description": "Runs an Actor task and immediately returns without waiting for the run to finish. Optionally, you can override the Actor input configuration by passing a JSON object as the POST payload and setting the `Content-Type: application/json` HTTP header. Note that if the object in the POST payload does not define a particular input property, the Actor run uses the default value defined by the task (or Ac",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout from its configuration."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. You can set the amount of memory to a power of 2 with a minimum of 128. By default, the run uses the memory limit from its configuration. Don't change this value unless the Actor's documentation recommends it or you're aware of the consequences."
     },
     "maxItems": {
      "type": "number",
      "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors. This does NOT guarantee that the Actor will return only this many items. It only ensures you won't be charged for more than this number of items. Only works for pay-per-result Actors. Value can be accessed in the actor run using `ACTOR_MAX_PAID_DATASET_ITEMS` environment variable."
     },
     "maxTotalChargeUsd": {
      "type": "number",
      "description": "Specifies the maximum total cost of the run. Use it to cap the total amount charged for all pricing models. You can access the maximum cost in your Actor by using the `ACTOR_MAX_TOTAL_CHARGE_USD` environment variable."
     },
     "restartOnError": {
      "type": "boolean",
      "description": "Determines whether the run will be restarted if it fails."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run uses the build from its configuration (typically `latest`)."
     },
     "waitForFinish": {
      "type": "number",
      "description": "The maximum number of seconds the server waits for the run to finish. By default it is `0`, the maximum value is `60`. If the run finishes in time then the returned run object will have a terminal status (e.g. `SUCCEEDED`), otherwise it will have a transitional status (e.g. `RUNNING`)."
     },
     "webhooks": {
      "type": "string",
      "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a notification e.g. when the Actor finished or failed. The value is a Base64-encoded JSON array whose items follow the WebhookRepresentation schema. For more information, see [Webhooks documentation](https://docs.apify.com/platform/integrations/webhooks)."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/runs",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "timeout",
     "memory",
     "maxItems",
     "maxTotalChargeUsd",
     "restartOnError",
     "build",
     "waitForFinish",
     "webhooks"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_task_webhooks_get",
   "name": "Get list of webhooks",
   "description": "Gets the list of webhooks of a specific Actor task. The response is a JSON with the list of objects, where each object contains basic information about a single webhook. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `createdAt` field in ascending order, to sort the records in desce",
   "input": {
    "type": "object",
    "properties": {
     "actorTaskId": {
      "type": "string",
      "description": "Task ID or a tilde-separated owner's username and task's name."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order. By default, they are sorted in ascending order."
     }
    },
    "required": [
     "actorTaskId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks/{actorTaskId}/webhooks",
    "path": [
     "actorTaskId"
    ],
    "query": [
     "offset",
     "limit",
     "desc"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_tasks_get",
   "name": "Get list of tasks",
   "description": "Gets the complete list of tasks that a user has created or used. The response is a list of objects in which each object contains essential information about a single task. The endpoint supports pagination using the `limit` and `offset` parameters, and it does not return more than a 1000 records. By default, the records are sorted by the `createdAt` field in ascending order; therefore you can use p",
   "input": {
    "type": "object",
    "properties": {
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order. By default, they are sorted in ascending order."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actor-tasks",
    "path": [],
    "query": [
     "offset",
     "limit",
     "desc"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_tasks_post",
   "name": "Create task",
   "description": "Create a new task with settings specified by the object passed as JSON in the POST payload. The response is the full task object as returned by the [Get task](/api/v2/actor-task-get) endpoint. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request's `Authorization` header, rather than the URL.",
   "input": {
    "type": "object",
    "properties": {
     "actId": {
      "type": "string",
      "description": ""
     },
     "name": {
      "type": "string",
      "description": ""
     },
     "options": {
      "type": "string",
      "description": ""
     },
     "input": {
      "type": "string",
      "description": ""
     },
     "title": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "description": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "actorStandby": {
      "type": "string",
      "description": ""
     },
     "publicConfig": {
      "type": "object",
      "description": "Public-facing configuration of a published task, used by the task's public landing page. The task's publication state is determined by `publishedAt` - a task is published when `publishedAt` is set and unpublished when it is `null`."
     }
    },
    "required": [
     "actId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-tasks",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "actId",
     "name",
     "options",
     "input",
     "title",
     "description",
     "actorStandby",
     "publicConfig"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_validate_input_post",
   "name": "Validate actor input",
   "description": "Validates the JSON payload against the Actor's [input schema](https://docs.apify.com/actors/development/actor-definition/input-schema) defined in the specified build. If the specified build has no input schema, any input is considered valid.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "build": {
      "type": "string",
      "description": "Actor build whose input schema is used for validation. Can be a build tag or build number. Defaults to the `latest` build tag."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/validate-input",
    "path": [
     "actorId"
    ],
    "query": [
     "build"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_version_delete",
   "name": "Delete version",
   "description": "Deletes a specific version of Actor's source code.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "versionNumber": {
      "type": "string",
      "description": "Actor version."
     }
    },
    "required": [
     "actorId",
     "versionNumber"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions/{versionNumber}",
    "path": [
     "actorId",
     "versionNumber"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_version_env_var_delete",
   "name": "Delete environment variable",
   "description": "Deletes a specific environment variable.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "versionNumber": {
      "type": "string",
      "description": "Actor version."
     },
     "envVarName": {
      "type": "string",
      "description": "The name of the environment variable"
     }
    },
    "required": [
     "actorId",
     "envVarName",
     "versionNumber"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions/{versionNumber}/env-vars/{envVarName}",
    "path": [
     "actorId",
     "versionNumber",
     "envVarName"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_version_env_var_get",
   "name": "Get environment variable",
   "description": "Gets a [EnvVar object](#/reference/actors/environment-variable-object) that contains all the details about a specific environment variable of an Actor. If `isSecret` is set to `true`, then `value` will never be returned.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "versionNumber": {
      "type": "string",
      "description": "Actor version."
     },
     "envVarName": {
      "type": "string",
      "description": "The name of the environment variable"
     }
    },
    "required": [
     "actorId",
     "envVarName",
     "versionNumber"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions/{versionNumber}/env-vars/{envVarName}",
    "path": [
     "actorId",
     "versionNumber",
     "envVarName"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_version_env_var_post",
   "name": "Update environment variable (post)",
   "description": "Updates Actor environment variable using values specified by a [EnvVar object](#/reference/actors/environment-variable-object) passed as JSON in the POST payload. This endpoint is an alias for the [`PUT` update environment variable](#tag/ActorsEnvironment-variable-object/operation/act_version_envVar_put) method and behaves identically.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "versionNumber": {
      "type": "string",
      "description": "Actor version."
     },
     "envVarName": {
      "type": "string",
      "description": "The name of the environment variable"
     },
     "name": {
      "type": "string",
      "description": "The name of the environment variable."
     },
     "value": {
      "type": "string",
      "description": "The value of the environment variable. If `isSecret` is `true`, this value isn't returned by the API."
     },
     "isSecret": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Whether the environment variable is encrypted. Secret values aren't returned by the API."
     }
    },
    "required": [
     "actorId",
     "envVarName",
     "name",
     "versionNumber"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions/{versionNumber}/env-vars/{envVarName}",
    "path": [
     "actorId",
     "versionNumber",
     "envVarName"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "value",
     "isSecret"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_version_env_var_put",
   "name": "Update environment variable",
   "description": "Updates Actor environment variable using values specified by a [EnvVar object](#/reference/actors/environment-variable-object) passed as JSON in the POST payload. If the object does not define a specific property, its value will not be updated. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "versionNumber": {
      "type": "string",
      "description": "Actor version."
     },
     "envVarName": {
      "type": "string",
      "description": "The name of the environment variable"
     },
     "name": {
      "type": "string",
      "description": "The name of the environment variable."
     },
     "value": {
      "type": "string",
      "description": "The value of the environment variable. If `isSecret` is `true`, this value isn't returned by the API."
     },
     "isSecret": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Whether the environment variable is encrypted. Secret values aren't returned by the API."
     }
    },
    "required": [
     "actorId",
     "envVarName",
     "name",
     "versionNumber"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions/{versionNumber}/env-vars/{envVarName}",
    "path": [
     "actorId",
     "versionNumber",
     "envVarName"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "value",
     "isSecret"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_version_env_vars_get",
   "name": "Get list of environment variables",
   "description": "Gets the list of environment variables for a specific version of an Actor. The response is a JSON object with the list of [EnvVar objects](#/reference/actors/environment-variable-object), where each contains basic information about a single environment variable.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "versionNumber": {
      "type": "string",
      "description": "Actor version."
     }
    },
    "required": [
     "actorId",
     "versionNumber"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions/{versionNumber}/env-vars",
    "path": [
     "actorId",
     "versionNumber"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_version_env_vars_post",
   "name": "Create environment variable",
   "description": "Creates an environment variable of an Actor using values specified in a [EnvVar object](#/reference/actors/environment-variable-object) passed as JSON in the POST payload. The request must specify `name` and `value` parameters (as strings) in the JSON payload and a `Content-Type: application/json` HTTP header. ``` { \"name\": \"ENV_VAR_NAME\", \"value\": \"my-env-var\" } ``` The response is the [EnvVar ob",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "versionNumber": {
      "type": "string",
      "description": "Actor version."
     },
     "name": {
      "type": "string",
      "description": "The name of the environment variable."
     },
     "value": {
      "type": "string",
      "description": "The value of the environment variable. If `isSecret` is `true`, this value isn't returned by the API."
     },
     "isSecret": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Whether the environment variable is encrypted. Secret values aren't returned by the API."
     }
    },
    "required": [
     "actorId",
     "name",
     "versionNumber"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions/{versionNumber}/env-vars",
    "path": [
     "actorId",
     "versionNumber"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "value",
     "isSecret"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_version_get",
   "name": "Get version",
   "description": "Gets a [Version object](#/reference/actors/version-object) that contains all the details about a specific version of an Actor.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "versionNumber": {
      "type": "string",
      "description": "Actor version."
     }
    },
    "required": [
     "actorId",
     "versionNumber"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions/{versionNumber}",
    "path": [
     "actorId",
     "versionNumber"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_version_post",
   "name": "Update version (post)",
   "description": "Updates Actor version using values specified by a [Version object](#/reference/actors/version-object) passed as JSON in the POST payload. This endpoint is an alias for the [`PUT` update version](#tag/ActorsVersion-object/operation/act_version_put) method and behaves identically.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "versionNumber": {
      "type": [
       "string",
       "null"
      ],
      "description": "The version number of the Actor. Two numbers separated by a dot, that represent the `MAJOR.MINOR` part of the semantic versioning."
     },
     "sourceType": {
      "type": "string",
      "description": "Where the source code of the version lives."
     },
     "envVars": {
      "type": "string",
      "description": "Environment variables for the version."
     },
     "applyEnvVarsToBuild": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Whether to inject the environment variables at build time."
     },
     "buildTag": {
      "type": [
       "string",
       "null"
      ],
      "description": "The tag name to apply to a successful build of this version. Can be `null` when the version has no build tag."
     },
     "sourceFiles": {
      "type": "array",
      "description": ""
     },
     "gitRepoUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "URL of the Git repository to clone the source code from. Applies when the `sourceType` is `GIT_REPO`."
     },
     "tarballUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "URL of the tarball to download the source code from. Applies when the `sourceType` is `TARBALL`."
     },
     "gitHubGistUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "URL of the GitHub Gist to clone the source code from. Applies when the `sourceType` is `GITHUB_GIST`."
     }
    },
    "required": [
     "actorId",
     "versionNumber"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions/{versionNumber}",
    "path": [
     "actorId",
     "versionNumber"
    ],
    "query": [],
    "header": [],
    "body": [
     "versionNumber",
     "sourceType",
     "envVars",
     "applyEnvVarsToBuild",
     "buildTag",
     "sourceFiles",
     "gitRepoUrl",
     "tarballUrl",
     "gitHubGistUrl"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_version_put",
   "name": "Update version",
   "description": "Updates Actor version using values specified by a [Version object](#/reference/actors/version-object) passed as JSON in the POST payload. If the object does not define a specific property, its value will not be updated. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request's `Authorization` header",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "versionNumber": {
      "type": [
       "string",
       "null"
      ],
      "description": "The version number of the Actor. Two numbers separated by a dot, that represent the `MAJOR.MINOR` part of the semantic versioning."
     },
     "sourceType": {
      "type": "string",
      "description": "Where the source code of the version lives."
     },
     "envVars": {
      "type": "string",
      "description": "Environment variables for the version."
     },
     "applyEnvVarsToBuild": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Whether to inject the environment variables at build time."
     },
     "buildTag": {
      "type": [
       "string",
       "null"
      ],
      "description": "The tag name to apply to a successful build of this version. Can be `null` when the version has no build tag."
     },
     "sourceFiles": {
      "type": "array",
      "description": ""
     },
     "gitRepoUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "URL of the Git repository to clone the source code from. Applies when the `sourceType` is `GIT_REPO`."
     },
     "tarballUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "URL of the tarball to download the source code from. Applies when the `sourceType` is `TARBALL`."
     },
     "gitHubGistUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "URL of the GitHub Gist to clone the source code from. Applies when the `sourceType` is `GITHUB_GIST`."
     }
    },
    "required": [
     "actorId",
     "versionNumber"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions/{versionNumber}",
    "path": [
     "actorId",
     "versionNumber"
    ],
    "query": [],
    "header": [],
    "body": [
     "versionNumber",
     "sourceType",
     "envVars",
     "applyEnvVarsToBuild",
     "buildTag",
     "sourceFiles",
     "gitRepoUrl",
     "tarballUrl",
     "gitHubGistUrl"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_versions_get",
   "name": "Get list of versions",
   "description": "Gets the list of versions of a specific Actor. The response is a JSON object with the list of [Version objects](#/reference/actors/version-object), where each contains basic information about a single version.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions",
    "path": [
     "actorId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_versions_post",
   "name": "Create version",
   "description": "Creates a version of an Actor using values specified in a [Version object](#/reference/actors/version-object) passed as JSON in the POST payload. The request must specify `versionNumber` and `sourceType` parameters (as strings) in the JSON payload and a `Content-Type: application/json` HTTP header. Each `sourceType` requires its own additional properties to be passed to the JSON payload object. Th",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "versionNumber": {
      "type": [
       "string",
       "null"
      ],
      "description": "The version number of the Actor. Two numbers separated by a dot, that represent the `MAJOR.MINOR` part of the semantic versioning."
     },
     "sourceType": {
      "type": "string",
      "description": "Where the source code of the version lives."
     },
     "envVars": {
      "type": "string",
      "description": "Environment variables for the version."
     },
     "applyEnvVarsToBuild": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Whether to inject the environment variables at build time."
     },
     "buildTag": {
      "type": [
       "string",
       "null"
      ],
      "description": "The tag name to apply to a successful build of this version. Can be `null` when the version has no build tag."
     },
     "sourceFiles": {
      "type": "array",
      "description": ""
     },
     "gitRepoUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "URL of the Git repository to clone the source code from. Applies when the `sourceType` is `GIT_REPO`."
     },
     "tarballUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "URL of the tarball to download the source code from. Applies when the `sourceType` is `TARBALL`."
     },
     "gitHubGistUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "URL of the GitHub Gist to clone the source code from. Applies when the `sourceType` is `GITHUB_GIST`."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/versions",
    "path": [
     "actorId"
    ],
    "query": [],
    "header": [],
    "body": [
     "versionNumber",
     "sourceType",
     "envVars",
     "applyEnvVarsToBuild",
     "buildTag",
     "sourceFiles",
     "gitRepoUrl",
     "tarballUrl",
     "gitHubGistUrl"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actor_webhooks_get",
   "name": "Get list of webhooks",
   "description": "Gets the list of webhooks of a specific Actor. The response is a JSON with the list of objects, where each object contains basic information about a single webhook. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `createdAt` field in ascending order, to sort the records in descending",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order. By default, they are sorted in ascending order."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/webhooks",
    "path": [
     "actorId"
    ],
    "query": [
     "offset",
     "limit",
     "desc"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actors_builds_get",
   "name": "Get list of builds",
   "description": "Gets the list of builds of a specific Actor. The response is a JSON with the list of objects, where each object contains basic information about a single build. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `startedAt` field in ascending order, therefore you can use pagination to i",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `startedAt` field in descending order. By default, they are sorted in ascending order."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/builds",
    "path": [
     "actorId"
    ],
    "query": [
     "offset",
     "limit",
     "desc"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actors_builds_post",
   "name": "Build actor",
   "description": "Builds an Actor. The response is the build object as returned by the [Get build](#/reference/actors/build-object/get-build) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "version": {
      "type": "string",
      "description": "Actor version number to be built."
     },
     "useCache": {
      "type": "boolean",
      "description": "If `true` or `1`, the system will use a cache to speed up the build process. By default, cache is not used."
     },
     "betaPackages": {
      "type": "boolean",
      "description": "If `true` or `1` then the Actor is built with beta versions of Apify NPM packages. By default, the build uses `latest` packages."
     },
     "tag": {
      "type": "string",
      "description": "Tag to be applied to the build on success. By default, the tag is taken from Actor version's `buildTag` property."
     },
     "waitForFinish": {
      "type": "number",
      "description": "The maximum number of seconds the server waits for the build to finish. By default it is `0`, the maximum value is `60`. If the build finishes in time then the returned build object will have a terminal status (e.g. `SUCCEEDED`), otherwise it will have a transitional status (e.g. `RUNNING`)."
     }
    },
    "required": [
     "actorId",
     "version"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/builds",
    "path": [
     "actorId"
    ],
    "query": [
     "version",
     "useCache",
     "betaPackages",
     "tag",
     "waitForFinish"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actors_get",
   "name": "Get list of actors",
   "description": "Gets the list of all Actors that the user created or used. The response is a list of objects, where each object contains a basic information about a single Actor. To only get Actors created by the user, add the `my=1` query parameter. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `",
   "input": {
    "type": "object",
    "properties": {
     "my": {
      "type": "boolean",
      "description": "If `true` or `1` then the returned list only contains Actors owned by the user. The default value is `false`."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order. By default, they are sorted in ascending order."
     },
     "sortBy": {
      "type": "string",
      "description": "Field to sort the records by. The default is `createdAt`. You can also use `stats.lastRunStartedAt` to sort by the most recently ran Actors.",
      "enum": [
       "createdAt",
       "stats.lastRunStartedAt"
      ]
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors",
    "path": [],
    "query": [
     "my",
     "offset",
     "limit",
     "desc",
     "sortBy"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actors_post",
   "name": "Create actor",
   "description": "Creates an Actor with the settings specified in an `Actor` object passed as JSON in the POST payload. Returns the created `Actor` object. In the HTTP request, set the `Content-Type` header to `application/json`. ### Define a source code version An Actor must specify at least one version of the source code. For details, see [Actor versions](/api/v2/actors-actor-versions). ### Publish an Actor To ma",
   "input": {
    "type": "object",
    "properties": {
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": "The identifier of the Actor. Use lowercase letters, numbers, and hyphens. Spaces or special characters aren't allowed. Must be unique across your account."
     },
     "description": {
      "type": [
       "string",
       "null"
      ],
      "description": "Short description of the Actor, displayed in Apify Store and Console."
     },
     "title": {
      "type": [
       "string",
       "null"
      ],
      "description": "Human-readable name of the Actor, displayed in Apify Store and Console. Can contain spaces and capital letters. Recommended length is 40-50 characters. You can change this title without affecting the Actor's URL or SEO."
     },
     "isPublic": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Whether the Actor is available to users in Apify Store. If `false`, the Actor is private and only visible to you."
     },
     "seoTitle": {
      "type": [
       "string",
       "null"
      ],
      "description": "Name of the Actor to display by search engines such as Google. Can be different from the Actor's name displayed in Apify Store and Console. Recommended length is 40-50 characters."
     },
     "seoDescription": {
      "type": [
       "string",
       "null"
      ],
      "description": "Description of the Actor to display by search engines such as Google. Recommended length is 140-156 characters."
     },
     "restartOnError": {
      "type": "boolean",
      "description": ""
     },
     "versions": {
      "type": "string",
      "description": "An array of `Version` objects. Each object represents a specific version of the Actor's source code: its location, builds, and environment configuration."
     },
     "categories": {
      "type": [
       "array",
       "null"
      ],
      "description": "A list of categories that best define the Actor. Reflected in Apify Store's search and filtering options."
     },
     "defaultRunOptions": {
      "type": "object",
      "description": "The default settings applied to an Actor run. Can be overridden elsewhere."
     },
     "actorStandby": {
      "type": "string",
      "description": "The configuration of the Actor's standby mode. For details, see [Standby mode](https://docs.apify.com/platform/actors/development/programming-interface/standby)."
     },
     "exampleRunInput": {
      "type": "string",
      "description": "Sample input payload that demonstrates what a typical run input for an Actor looks like. Used when no explicit input for a run is provided."
     },
     "isDeprecated": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Whether the Actor is deprecated."
     }
    },
    "required": []
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "name",
     "description",
     "title",
     "isPublic",
     "seoTitle",
     "seoDescription",
     "restartOnError",
     "versions",
     "categories",
     "defaultRunOptions",
     "actorStandby",
     "exampleRunInput",
     "isDeprecated"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actors_runs_get",
   "name": "Get list of runs",
   "description": "Gets the list of runs of a specific Actor. The response is a list of objects, where each object contains basic information about a single Actor run. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 array elements. By default, the records are sorted by the `startedAt` field in ascending order, therefore you can use pagination to increm",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `startedAt` field in descending order. By default, they are sorted in ascending order."
     },
     "status": {
      "type": "array",
      "description": "Single status or comma-separated list of statuses, see ([available statuses](https://docs.apify.com/platform/actors/running/runs-and-builds#lifecycle)). Used to filter runs by the specified statuses only."
     },
     "startedAfter": {
      "type": "string",
      "description": "Filter runs that started after the specified date and time (inclusive). The value must be a valid ISO 8601 datetime string (UTC)."
     },
     "startedBefore": {
      "type": "string",
      "description": "Filter runs that started before the specified date and time (inclusive). The value must be a valid ISO 8601 datetime string (UTC)."
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs",
    "path": [
     "actorId"
    ],
    "query": [
     "offset",
     "limit",
     "desc",
     "status",
     "startedAfter",
     "startedBefore"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.actors_runs_post",
   "name": "Run actor",
   "description": "Runs an Actor and immediately returns without waiting for the run to finish. The POST payload including its `Content-Type` header is passed as `INPUT` to the Actor (usually `application/json`). The Actor is started with the default options; you can override them using various URL query parameters. The response is the Run object as returned by the [Get run](#/reference/actor-runs/run-object-and-its",
   "input": {
    "type": "object",
    "properties": {
     "actorId": {
      "type": "string",
      "description": "Actor ID or the username of the Actor owner and the Actor name, separated by a tilde (`~`)."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout from its configuration."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. You can set the amount of memory to a power of 2 with a minimum of 128. By default, the run uses the memory limit from its configuration. Don't change this value unless the Actor's documentation recommends it or you're aware of the consequences."
     },
     "maxItems": {
      "type": "number",
      "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors. This does NOT guarantee that the Actor will return only this many items. It only ensures you won't be charged for more than this number of items. Only works for pay-per-result Actors. Value can be accessed in the actor run using `ACTOR_MAX_PAID_DATASET_ITEMS` environment variable."
     },
     "maxTotalChargeUsd": {
      "type": "number",
      "description": "Specifies the maximum total cost of the run. Use it to cap the total amount charged for all pricing models. You can access the maximum cost in your Actor by using the `ACTOR_MAX_TOTAL_CHARGE_USD` environment variable."
     },
     "restartOnError": {
      "type": "boolean",
      "description": "Determines whether the run will be restarted if it fails."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run uses the build from its configuration (typically `latest`)."
     },
     "waitForFinish": {
      "type": "number",
      "description": "The maximum number of seconds the server waits for the run to finish. By default it is `0`, the maximum value is `60`. If the run finishes in time then the returned run object will have a terminal status (e.g. `SUCCEEDED`), otherwise it will have a transitional status (e.g. `RUNNING`)."
     },
     "webhooks": {
      "type": "string",
      "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a notification e.g. when the Actor finished or failed. The value is a Base64-encoded JSON array whose items follow the WebhookRepresentation schema. For more information, see [Webhooks documentation](https://docs.apify.com/platform/integrations/webhooks)."
     },
     "forcePermissionLevel": {
      "type": "string",
      "description": "Overrides the Actor's permission level for this specific run. Use to test restricted permissions before deploying changes to your Actor or to temporarily elevate or restrict access. If you don't specify this parameter, the Actor uses its configured default permission level. For more information on permissions, see the [documentation](https://docs.apify.com/platform/actors/development/permissions).",
      "enum": [
       "LIMITED_PERMISSIONS",
       "FULL_PERMISSIONS"
      ]
     }
    },
    "required": [
     "actorId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actors/{actorId}/runs",
    "path": [
     "actorId"
    ],
    "query": [
     "timeout",
     "memory",
     "maxItems",
     "maxTotalChargeUsd",
     "restartOnError",
     "build",
     "waitForFinish",
     "webhooks",
     "forcePermissionLevel"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.dataset_delete",
   "name": "Delete dataset",
   "description": "Deletes a specific dataset.",
   "input": {
    "type": "object",
    "properties": {
     "datasetId": {
      "type": "string",
      "description": "Dataset ID or `username~dataset-name`."
     }
    },
    "required": [
     "datasetId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/datasets/{datasetId}",
    "path": [
     "datasetId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.dataset_get",
   "name": "Get dataset",
   "description": "Returns dataset object for given dataset ID. This does not return dataset items, only information about the storage itself. To retrieve dataset items, use the [List dataset items](/api/v2/dataset-items-get) endpoint. :::note Keep in mind that attributes `itemCount` and `cleanItemCount` are not propagated right away after data are pushed into a dataset. ::: There is a short period (up to 5 seconds)",
   "input": {
    "type": "object",
    "properties": {
     "datasetId": {
      "type": "string",
      "description": "Dataset ID or `username~dataset-name`."
     }
    },
    "required": [
     "datasetId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/datasets/{datasetId}",
    "path": [
     "datasetId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.dataset_items_get",
   "name": "Get dataset items",
   "description": "Returns data stored in the dataset in a desired format. ### Response format The format of the response depends on format query parameter. The format parameter can have one of the following values: json, jsonl, xml, html, csv, xlsx and rss. The following table describes how each format is treated. Format Items json The response is a JSON, JSONL or XML array of raw item objects. jsonl xml html The r",
   "input": {
    "type": "object",
    "properties": {
     "datasetId": {
      "type": "string",
      "description": "Dataset ID or `username~dataset-name`."
     },
     "format": {
      "type": "string",
      "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml` and `rss`. The default value is `json`."
     },
     "clean": {
      "type": "boolean",
      "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i.e. fields starting with the # character). The `clean` parameter is just a shortcut for `skipHidden=true` and `skipEmpty=true` parameters. Note that since some objects might be skipped from the output, that the result might contain less items than the `limit` value."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. By default there is no limit."
     },
     "fields": {
      "type": "string",
      "description": "A comma-separated list of fields which should be picked from the items, only these fields will remain in the resulting record objects. Note that the fields in the outputted items are sorted the same way as they are specified in the `fields` query parameter. You can use this feature to effectively fix the output format."
     },
     "outputFields": {
      "type": "string",
      "description": "A comma-separated list of output field names that positionally rename the fields specified in the `fields` parameter. For example, `?fields=headline,url&outputFields=title,link` renames `headline` to `title` and `url` to `link` in the output. The number of names in `outputFields` must match the number of names in `fields`. Requires the `fields` parameter to be specified as well."
     },
     "omit": {
      "type": "string",
      "description": "A comma-separated list of fields which should be omitted from the items."
     },
     "unwind": {
      "type": "string",
      "description": "A comma-separated list of fields which should be unwound, in order which they should be processed. Each field should be either an array or an object. If the field is an array then every element of the array will become a separate record and merged with parent object. If the unwound field is an object then it is merged with the parent object. If the unwound field is missing or its value is neither "
     },
     "flatten": {
      "type": "string",
      "description": "A comma-separated list of fields which should transform nested objects into flat structures. For example, with `flatten=\"foo\"` the object `{\"foo\":{\"bar\": \"hello\"}}` is turned into `{\"foo.bar\": \"hello\"}`. The original object with properties is replaced with the flattened object."
     },
     "desc": {
      "type": "boolean",
      "description": "By default, results are returned in the same order as they were stored. To reverse the order, set this parameter to `true` or `1`."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header, forcing a web browser to download the file rather than to display it. By default this header is not present."
     },
     "delimiter": {
      "type": "string",
      "description": "A delimiter character for CSV files, only used if `format=csv`. You might need to URL-encode the character (e.g. use `%09` for tab or `%3B` for semicolon). The default delimiter is a simple comma (`,`)."
     },
     "bom": {
      "type": "boolean",
      "description": "All text responses are encoded in UTF-8 encoding. By default, the `format=csv` files are prefixed with the UTF-8 Byte Order Mark (BOM), while `json`, `jsonl`, `xml`, `html` and `rss` files are not. If you want to override this default behavior, specify `bom=1` query parameter to include the BOM or `bom=0` to skip it."
     },
     "xmlRoot": {
      "type": "string",
      "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
     },
     "xmlRow": {
      "type": "string",
      "description": "Overrides default element name that wraps each page or page function result object in `xml` output. By default the element name is `item`."
     },
     "skipHeaderRow": {
      "type": "boolean",
      "description": "If `true` or `1` then header row in the `csv` format is skipped."
     },
     "skipHidden": {
      "type": "boolean",
      "description": "If `true` or `1` then hidden fields are skipped from the output, i.e. fields starting with the `#` character."
     },
     "skipEmpty": {
      "type": "boolean",
      "description": "If `true` or `1` then empty items are skipped from the output. Note that if used, the results might contain less items than the limit value."
     },
     "simplified": {
      "type": "boolean",
      "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo` and `unwind=pageFunctionResult` query parameters. This feature is used to emulate simplified results provided by the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "view": {
      "type": "string",
      "description": "Defines the view configuration for dataset items based on the schema definition. This parameter determines how the data will be filtered and presented. For complete specification details, see the [dataset schema documentation](/storage/dataset-schema)."
     },
     "skipFailedPages": {
      "type": "boolean",
      "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the output. This feature is here to emulate functionality of API version 1 used for the legacy Apify Crawler product and it's not recommended to use it in new integrations."
     },
     "feedTitle": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the title defaults to `Dataset `."
     },
     "feedDescription": {
      "type": "string",
      "description": "Overrides the auto-generated RSS channel `` element. Only used when `format=rss`. If not provided, the description defaults to `Items in dataset with id \"\".`"
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "datasetId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/datasets/{datasetId}/items",
    "path": [
     "datasetId"
    ],
    "query": [
     "format",
     "clean",
     "offset",
     "limit",
     "fields",
     "outputFields",
     "omit",
     "unwind",
     "flatten",
     "desc",
     "attachment",
     "delimiter",
     "bom",
     "xmlRoot",
     "xmlRow",
     "skipHeaderRow",
     "skipHidden",
     "skipEmpty",
     "simplified",
     "view",
     "skipFailedPages",
     "feedTitle",
     "feedDescription",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.dataset_items_post",
   "name": "Store items",
   "description": "Appends an item or an array of items to the end of the dataset. The POST payload is a JSON object or a JSON array of objects to save into the dataset. If the data you attempt to store in the dataset is invalid (meaning any of the items received by the API fails the validation), the whole request is discarded and the API will return a response with status code 400. For more information about datase",
   "input": {
    "type": "object",
    "properties": {
     "datasetId": {
      "type": "string",
      "description": "Dataset ID or `username~dataset-name`."
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "datasetId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/datasets/{datasetId}/items",
    "path": [
     "datasetId"
    ],
    "query": [],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.dataset_put",
   "name": "Update dataset",
   "description": "Updates a dataset's name and general resource access level using a value specified by a JSON object passed in the PUT payload. The response is the updated dataset object, as returned by the [Get dataset](/api/v2/dataset-get) API endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "datasetId": {
      "type": "string",
      "description": "Dataset ID or `username~dataset-name`."
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "datasetId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/datasets/{datasetId}",
    "path": [
     "datasetId"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.dataset_statistics_get",
   "name": "Get dataset statistics",
   "description": "Returns statistics for given dataset. Provides only [field statistics](https://docs.apify.com/platform/actors/development/actor-definition/dataset-schema/validation#dataset-field-statistics).",
   "input": {
    "type": "object",
    "properties": {
     "datasetId": {
      "type": "string",
      "description": "Dataset ID or `username~dataset-name`."
     }
    },
    "required": [
     "datasetId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/datasets/{datasetId}/statistics",
    "path": [
     "datasetId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.datasets_get",
   "name": "Get list of datasets",
   "description": "Lists all of a user's datasets. The response is a JSON array of objects, where each object contains basic information about one dataset. By default, the objects are sorted by the `createdAt` field in ascending order, therefore you can use pagination to incrementally fetch all datasets while new ones are still being created. To sort them in descending order, use `desc=1` parameter. The endpoint sup",
   "input": {
    "type": "object",
    "properties": {
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order. By default, they are sorted in ascending order."
     },
     "unnamed": {
      "type": "boolean",
      "description": "If `true` or `1` then all the storages are returned. By default, only named storages are returned."
     },
     "ownership": {
      "type": "string",
      "description": "Filter by ownership. If this parameter is omitted, all accessible datasets are returned. - `ownedByMe`: Return only datasets owned by the user. - `sharedWithMe`: Return only datasets shared with the user by other users.",
      "enum": [
       "ownedByMe",
       "sharedWithMe"
      ]
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/datasets",
    "path": [],
    "query": [
     "offset",
     "limit",
     "desc",
     "unnamed",
     "ownership"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.datasets_post",
   "name": "Create dataset",
   "description": "Creates a dataset and returns its object. Keep in mind that data stored under unnamed dataset follows [data retention period](https://docs.apify.com/platform/storage#data-retention). It creates a dataset with the given name if the parameter name is used. If a dataset with the given name already exists then returns its object.",
   "input": {
    "type": "object",
    "properties": {
     "name": {
      "type": "string",
      "description": "Custom unique name to easily identify the dataset in the future."
     }
    },
    "required": []
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/datasets",
    "path": [],
    "query": [
     "name"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.key_value_store_delete",
   "name": "Delete store",
   "description": "Deletes a key-value store.",
   "input": {
    "type": "object",
    "properties": {
     "storeId": {
      "type": "string",
      "description": "Key-value store ID or `username~store-name`."
     }
    },
    "required": [
     "storeId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/key-value-stores/{storeId}",
    "path": [
     "storeId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.key_value_store_get",
   "name": "Get store",
   "description": "Gets an object that contains all the details about a specific key-value store.",
   "input": {
    "type": "object",
    "properties": {
     "storeId": {
      "type": "string",
      "description": "Key-value store ID or `username~store-name`."
     }
    },
    "required": [
     "storeId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/key-value-stores/{storeId}",
    "path": [
     "storeId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.key_value_store_keys_get",
   "name": "Get list of keys",
   "description": "Returns a list of objects describing keys of a given key-value store, as well as some information about the values (e.g. size). This endpoint is paginated using `exclusiveStartKey` and `limit` parameters - see [Pagination](/api/v2#using-key) for more details.",
   "input": {
    "type": "object",
    "properties": {
     "storeId": {
      "type": "string",
      "description": "Key-value store ID or `username~store-name`."
     },
     "exclusiveStartKey": {
      "type": "string",
      "description": "All keys up to this one (including) are skipped from the result."
     },
     "limit": {
      "type": "number",
      "description": "Number of keys to be returned."
     },
     "collection": {
      "type": "string",
      "description": "Limit the results to keys that belong to a specific collection from the key-value store schema. The key-value store need to have a schema defined for this parameter to work."
     },
     "prefix": {
      "type": "string",
      "description": "Limit the results to keys that start with a specific prefix."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "storeId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/key-value-stores/{storeId}/keys",
    "path": [
     "storeId"
    ],
    "query": [
     "exclusiveStartKey",
     "limit",
     "collection",
     "prefix",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.key_value_store_put",
   "name": "Update store",
   "description": "Updates a key-value store's name and general resource access level using a value specified by a JSON object passed in the PUT payload. The response is the updated key-value store object, as returned by the [Get store](#/reference/key-value-stores/store-object/get-store) API endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "storeId": {
      "type": "string",
      "description": "Key-value store ID or `username~store-name`."
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "storeId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/key-value-stores/{storeId}",
    "path": [
     "storeId"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.key_value_store_record_delete",
   "name": "Delete record",
   "description": "Removes a record specified by a key from the key-value store.",
   "input": {
    "type": "object",
    "properties": {
     "storeId": {
      "type": "string",
      "description": "Key-value store ID or `username~store-name`."
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     }
    },
    "required": [
     "recordKey",
     "storeId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/key-value-stores/{storeId}/records/{recordKey}",
    "path": [
     "storeId",
     "recordKey"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.key_value_store_record_get",
   "name": "Get record",
   "description": "Gets a value stored in the key-value store under a specific key. The response body has the same `Content-Encoding` header as it was set in [Put record](#tag/Key-value-storesRecord/operation/keyValueStore_record_put). If the request does not define the `Accept-Encoding` HTTP header with the right encoding, the record will be decompressed. Most HTTP clients support decompression by default. After us",
   "input": {
    "type": "object",
    "properties": {
     "storeId": {
      "type": "string",
      "description": "Key-value store ID or `username~store-name`."
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "attachment": {
      "type": "boolean",
      "description": "If `true` or `1`, the response will be served with `Content-Disposition: attachment` header, causing web browsers to offer downloading HTML records instead of displaying them."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "recordKey",
     "storeId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/key-value-stores/{storeId}/records/{recordKey}",
    "path": [
     "storeId",
     "recordKey"
    ],
    "query": [
     "attachment",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.key_value_store_record_post",
   "name": "Store record (post)",
   "description": "Stores a value under a specific key to the key-value store. This endpoint is an alias for the [`PUT` record](#tag/Key-value-storesRecord/operation/keyValueStore_record_put) method and behaves identically.",
   "input": {
    "type": "object",
    "properties": {
     "storeId": {
      "type": "string",
      "description": "Key-value store ID or `username~store-name`."
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "recordKey",
     "storeId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/key-value-stores/{storeId}/records/{recordKey}",
    "path": [
     "storeId",
     "recordKey"
    ],
    "query": [],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.key_value_store_record_put",
   "name": "Store record",
   "description": "Stores a value under a specific key to the key-value store. The value is passed as the PUT payload and it is stored with a MIME content type defined by the `Content-Type` header and with encoding defined by the `Content-Encoding` header. To save bandwidth, storage, and speed up your upload, send the request payload compressed with Gzip compression and add the `Content-Encoding: gzip` header. It is",
   "input": {
    "type": "object",
    "properties": {
     "storeId": {
      "type": "string",
      "description": "Key-value store ID or `username~store-name`."
     },
     "recordKey": {
      "type": "string",
      "description": "Key of the record."
     },
     "Content-Encoding": {
      "type": "string",
      "description": "Compression encoding of the request body.",
      "enum": [
       "br",
       "gzip",
       "deflate",
       "identity"
      ]
     }
    },
    "required": [
     "recordKey",
     "storeId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/key-value-stores/{storeId}/records/{recordKey}",
    "path": [
     "storeId",
     "recordKey"
    ],
    "query": [],
    "header": [
     "Content-Encoding"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.key_value_store_records_get",
   "name": "Download records",
   "description": "Downloads all records from the key-value store as a ZIP archive. Each record is stored as a separate file in the archive, with the filename equal to the record key. You can optionally filter the records by `collection` or `prefix` to download only a subset of the store.",
   "input": {
    "type": "object",
    "properties": {
     "storeId": {
      "type": "string",
      "description": "Key-value store ID or `username~store-name`."
     },
     "collection": {
      "type": "string",
      "description": "If specified, only records belonging to a specific collection from the key-value store schema. The key-value store need to have a schema defined for this parameter to work."
     },
     "prefix": {
      "type": "string",
      "description": "If specified, only records whose key starts with the given prefix are included in the archive."
     },
     "signature": {
      "type": "string",
      "description": "Signature used for the access."
     }
    },
    "required": [
     "storeId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/key-value-stores/{storeId}/records",
    "path": [
     "storeId"
    ],
    "query": [
     "collection",
     "prefix",
     "signature"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.key_value_stores_get",
   "name": "Get list of key value stores",
   "description": "Gets the list of key-value stores owned by the user. The response is a list of objects, where each objects contains a basic information about a single key-value store. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 array elements. By default, the records are sorted by the `createdAt` field in ascending order, therefore you can use p",
   "input": {
    "type": "object",
    "properties": {
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order. By default, they are sorted in ascending order."
     },
     "unnamed": {
      "type": "boolean",
      "description": "If `true` or `1` then all the storages are returned. By default, only named storages are returned."
     },
     "ownership": {
      "type": "string",
      "description": "Filter by ownership. If this parameter is omitted, all accessible key-value stores are returned. - `ownedByMe`: Return only key-value stores owned by the user. - `sharedWithMe`: Return only key-value stores shared with the user by other users.",
      "enum": [
       "ownedByMe",
       "sharedWithMe"
      ]
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/key-value-stores",
    "path": [],
    "query": [
     "offset",
     "limit",
     "desc",
     "unnamed",
     "ownership"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.key_value_stores_post",
   "name": "Create key value store",
   "description": "Creates a key-value store and returns its object. The response is the same object as returned by the [Get store](#/reference/key-value-stores/store-object/get-store) endpoint. Keep in mind that data stored under unnamed store follows [data retention period](https://docs.apify.com/platform/storage#data-retention). It creates a store with the given name if the parameter name is used. If there is ano",
   "input": {
    "type": "object",
    "properties": {
     "name": {
      "type": "string",
      "description": "Custom unique name to easily identify the store in the future."
     }
    },
    "required": []
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/key-value-stores",
    "path": [],
    "query": [
     "name"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.log_get",
   "name": "Get log",
   "description": "Retrieves logs for a specific Actor build or run.",
   "input": {
    "type": "object",
    "properties": {
     "buildOrRunId": {
      "type": "string",
      "description": "ID of the Actor build or run."
     },
     "stream": {
      "type": "boolean",
      "description": "If `true` or `1` then the logs will be streamed as long as the run or build is running."
     },
     "download": {
      "type": "boolean",
      "description": "If `true` or `1` then the web browser will download the log file rather than open it in a tab."
     },
     "raw": {
      "type": "boolean",
      "description": "If `true` or `1`, the logs will be kept verbatim. By default, the API removes ANSI escape codes from the logs, keeping only printable characters."
     }
    },
    "required": [
     "buildOrRunId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/logs/{buildOrRunId}",
    "path": [
     "buildOrRunId"
    ],
    "query": [
     "stream",
     "download",
     "raw"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.post_charge_run",
   "name": "Charge events in run",
   "description": "Charge for events in the run of your [pay per event Actor](https://docs.apify.com/platform/actors/running/actors-in-store#pay-per-event). The event you are charging for must be one of the configured events in your Actor. If the Actor is not set up as pay per event, or if the event is not configured, the endpoint will return an error. The endpoint must be called from the Actor run itself, with the ",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "idempotency-key": {
      "type": "string",
      "description": "Always pass a unique idempotency key (any unique string) for each charge to avoid double charging in case of retries or network errors."
     },
     "eventName": {
      "type": "string",
      "description": ""
     },
     "count": {
      "type": "integer",
      "description": ""
     }
    },
    "required": [
     "count",
     "eventName",
     "runId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/charge",
    "path": [
     "runId"
    ],
    "query": [],
    "header": [
     "idempotency-key"
    ],
    "body": [
     "eventName",
     "count"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.post_resurrect_run",
   "name": "Resurrect run",
   "description": "Resurrects a finished Actor run and returns an object that contains all the details about the resurrected run. Only finished runs, i.e. runs with status `FINISHED`, `FAILED`, `ABORTED` and `TIMED-OUT` can be resurrected. Run status will be updated to RUNNING and its container will be restarted with the same storages (the same behaviour as when the run gets migrated to the new server). For more inf",
   "input": {
    "type": "object",
    "properties": {
     "runId": {
      "type": "string",
      "description": "Actor run ID."
     },
     "build": {
      "type": "string",
      "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run is resurrected with the same build it originally used. Specifically, if a run was first started with the `latest` tag, which resolves to version `0.0.3` at the time, a run resurrected without this parameter will continue running with `0.0.3`, even if `latest` already points to a newer build."
     },
     "timeout": {
      "type": "number",
      "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout specified in the run that is being resurrected."
     },
     "memory": {
      "type": "number",
      "description": "Memory limit for the run, in megabytes. The amount of memory can be set to a power of 2 with a minimum of 128. By default, the run uses the memory limit specified in the run that is being resurrected."
     },
     "maxItems": {
      "type": "number",
      "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors. This does NOT guarantee that the Actor will return only this many items. It only ensures you won't be charged for more than this number of items. Only works for pay-per-result Actors. Value can be accessed in the actor run using `ACTOR_MAX_PAID_DATASET_ITEMS` environment variable."
     },
     "maxTotalChargeUsd": {
      "type": "number",
      "description": "Specifies the maximum total cost of the run. Use it to cap the total amount charged for all pricing models. You can access the maximum cost in your Actor by using the `ACTOR_MAX_TOTAL_CHARGE_USD` environment variable."
     },
     "restartOnError": {
      "type": "boolean",
      "description": "Determines whether the resurrected run will be restarted if it fails. By default, the resurrected run uses the same setting as before."
     }
    },
    "required": [
     "runId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/actor-runs/{runId}/resurrect",
    "path": [
     "runId"
    ],
    "query": [
     "build",
     "timeout",
     "memory",
     "maxItems",
     "maxTotalChargeUsd",
     "restartOnError"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_delete",
   "name": "Delete request queue",
   "description": "Deletes given queue.",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     }
    },
    "required": [
     "queueId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/request-queues/{queueId}",
    "path": [
     "queueId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_get",
   "name": "Get request queue",
   "description": "Returns queue object for given queue ID.",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     }
    },
    "required": [
     "queueId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/request-queues/{queueId}",
    "path": [
     "queueId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_head_get",
   "name": "Get head",
   "description": "Returns given number of first requests from the queue. The response contains the `hadMultipleClients` boolean field which indicates that the queue was accessed by more than one client (with unique or empty `clientKey`). This field is used by [Apify SDK](https://sdk.apify.com) to determine whether the local cache is consistent with the request queue, and thus optimize performance of certain operati",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "limit": {
      "type": "number",
      "description": "How many items from queue should be returned."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "queueId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/head",
    "path": [
     "queueId"
    ],
    "query": [
     "limit",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_head_lock_post",
   "name": "Get head and lock",
   "description": "Returns the given number of first requests from the queue and locks them for the given time. If this endpoint locks the request, no other client or run will be able to get and lock these requests. The response contains the `hadMultipleClients` boolean field which indicates that the queue was accessed by more than one client (with unique or empty `clientKey`).",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "lockSecs": {
      "type": "number",
      "description": "How long the requests will be locked for (in seconds)."
     },
     "limit": {
      "type": "number",
      "description": "How many items from the queue should be returned."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "lockSecs",
     "queueId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/head/lock",
    "path": [
     "queueId"
    ],
    "query": [
     "lockSecs",
     "limit",
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_put",
   "name": "Update request queue",
   "description": "Updates a request queue's name and general resource access level using a value specified by a JSON object passed in the PUT payload. The response is the updated request queue object, as returned by the [Get request queue](#/reference/request-queues/queue-collection/get-request-queue) API endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": "The new name for the request queue."
     },
     "generalAccess": {
      "type": "string",
      "description": "Defines the general access level for the resource.",
      "enum": [
       "ANYONE_WITH_ID_CAN_READ",
       "ANYONE_WITH_NAME_CAN_READ",
       "FOLLOW_USER_SETTING",
       "RESTRICTED"
      ]
     }
    },
    "required": [
     "queueId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/request-queues/{queueId}",
    "path": [
     "queueId"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "generalAccess"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_request_delete",
   "name": "Delete request",
   "description": "Deletes given request from queue.",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "queueId",
     "requestId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/requests/{requestId}",
    "path": [
     "queueId",
     "requestId"
    ],
    "query": [
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_request_get",
   "name": "Get request",
   "description": "Returns request from queue.",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     }
    },
    "required": [
     "queueId",
     "requestId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/requests/{requestId}",
    "path": [
     "queueId",
     "requestId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_request_lock_delete",
   "name": "Delete request lock",
   "description": "Deletes a request lock. The request lock can be deleted only by the client that has locked it using [Get and lock head operation](#/request-queue-head-lock-post). The clientKey identifier is used for locking and unlocking requests. You can delete or prolong the lock only for requests that were locked by the same client key or from the same Actor run.",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end after lock was removed."
     }
    },
    "required": [
     "queueId",
     "requestId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/requests/{requestId}/lock",
    "path": [
     "queueId",
     "requestId"
    ],
    "query": [
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_request_lock_put",
   "name": "Prolong request lock",
   "description": "Prolongs request lock. The request lock can be prolonged only by the client that has locked it using [Get and lock head operation](#/request-queue-head-lock-post). The clientKey identifier is used for locking and unlocking requests. You can delete or prolong the lock only for requests that were locked by the same client key or from the same Actor run.",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "lockSecs": {
      "type": "number",
      "description": "How long the requests will be locked for (in seconds)."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end after lock expires."
     }
    },
    "required": [
     "lockSecs",
     "queueId",
     "requestId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/requests/{requestId}/lock",
    "path": [
     "queueId",
     "requestId"
    ],
    "query": [
     "lockSecs",
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_request_put",
   "name": "Update request",
   "description": "Updates a request in a queue. Mark request as handled by setting `request.handledAt = new Date()`. If `handledAt` is set, the request will be removed from head of the queue (and unlocked, if applicable).",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "requestId": {
      "type": "string",
      "description": "Request ID."
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "uniqueKey": {
      "type": "string",
      "description": "A unique key used for request de-duplication. Requests with the same unique key are considered identical."
     },
     "url": {
      "type": "string",
      "description": "The URL of the request."
     },
     "method": {
      "type": "string",
      "description": "",
      "enum": [
       "GET",
       "HEAD",
       "POST",
       "PUT",
       "DELETE",
       "CONNECT",
       "OPTIONS",
       "TRACE",
       "PATCH"
      ]
     },
     "retryCount": {
      "type": "integer",
      "description": "The number of times this request has been retried."
     },
     "loadedUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "The final URL that was loaded, after redirects (if any)."
     },
     "payload": {
      "type": [
       "string",
       "object",
       "null"
      ],
      "description": "The request payload, typically used with POST or PUT requests."
     },
     "headers": {
      "type": [
       "object",
       "null"
      ],
      "description": "HTTP headers sent with the request."
     },
     "userData": {
      "type": "object",
      "description": "Custom user data attached to the request. Can contain arbitrary fields."
     },
     "noRetry": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Indicates whether the request should not be retried if processing fails."
     },
     "errorMessages": {
      "type": [
       "array",
       "null"
      ],
      "description": "Error messages recorded from failed processing attempts."
     },
     "handledAt": {
      "type": [
       "string",
       "null"
      ],
      "description": "The timestamp when the request was marked as handled, if applicable."
     }
    },
    "required": [
     "queueId",
     "requestId",
     "uniqueKey",
     "url"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/requests/{requestId}",
    "path": [
     "queueId",
     "requestId"
    ],
    "query": [
     "forefront",
     "clientKey"
    ],
    "header": [],
    "body": [
     "uniqueKey",
     "url",
     "method",
     "retryCount",
     "loadedUrl",
     "payload",
     "headers",
     "userData",
     "noRetry",
     "errorMessages",
     "handledAt"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_requests_batch_delete",
   "name": "Delete requests",
   "description": "Batch-deletes given requests from the queue. The number of requests in a batch is limited to 25. The response contains an array of unprocessed and processed requests. If any delete operation fails because the request queue rate limit is exceeded or an internal failure occurs, the failed request is returned in the `unprocessedRequests` response parameter. You can re-send these delete requests. It i",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "Content-Type": {
      "type": "string",
      "description": ""
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "Content-Type",
     "queueId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/requests/batch",
    "path": [
     "queueId"
    ],
    "query": [
     "clientKey"
    ],
    "header": [
     "Content-Type"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_requests_batch_post",
   "name": "Add requests",
   "description": "Adds requests to the queue in batch. The maximum requests in batch is limited to 25. The response contains an array of unprocessed and processed requests. If any add operation fails because the request queue rate limit is exceeded or an internal failure occurs, the failed request is returned in the unprocessedRequests response parameter. You can resend these requests to add. It is recommended to u",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     }
    },
    "required": [
     "queueId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/requests/batch",
    "path": [
     "queueId"
    ],
    "query": [
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_requests_get",
   "name": "List requests",
   "description": "Returns a list of requests. This endpoint is paginated using cursor (pagination by `exclusiveStartId` is deprecated) and limit parameters.",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "exclusiveStartId": {
      "type": "string",
      "description": "All requests up to this one (including) are skipped from the result. (Deprecated, use `cursor` instead.)"
     },
     "limit": {
      "type": "number",
      "description": "Number of keys to be returned. Maximum value is `10000`."
     },
     "cursor": {
      "type": "string",
      "description": "A cursor string for pagination, returned in the previous response as `nextCursor`. Use this to retrieve the next page of requests."
     },
     "filter": {
      "type": "array",
      "description": "Filter requests by their state. Possible values are `locked` and `pending`. You can combine multiple values separated by commas, which will mean the union of these filters \u2013 requests matching any of the specified states will be returned. (Not compatible with deprecated `exclusiveStartId` parameter.)"
     }
    },
    "required": [
     "queueId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/requests",
    "path": [
     "queueId"
    ],
    "query": [
     "clientKey",
     "exclusiveStartId",
     "limit",
     "cursor",
     "filter"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_requests_post",
   "name": "Add request",
   "description": "Adds request to the queue. Response contains ID of the request and info if request was already present in the queue or handled. If request with same `uniqueKey` was already present in the queue then returns an ID of existing request.",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     },
     "forefront": {
      "type": "string",
      "description": "Determines if request should be added to the head of the queue or to the end. Default value is `false` (end of queue)."
     },
     "uniqueKey": {
      "type": "string",
      "description": "A unique key used for request de-duplication. Requests with the same unique key are considered identical."
     },
     "url": {
      "type": "string",
      "description": "The URL of the request."
     },
     "method": {
      "type": "string",
      "description": "",
      "enum": [
       "GET",
       "HEAD",
       "POST",
       "PUT",
       "DELETE",
       "CONNECT",
       "OPTIONS",
       "TRACE",
       "PATCH"
      ]
     },
     "retryCount": {
      "type": "integer",
      "description": "The number of times this request has been retried."
     },
     "loadedUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": "The final URL that was loaded, after redirects (if any)."
     },
     "payload": {
      "type": [
       "string",
       "object",
       "null"
      ],
      "description": "The request payload, typically used with POST or PUT requests."
     },
     "headers": {
      "type": [
       "object",
       "null"
      ],
      "description": "HTTP headers sent with the request."
     },
     "userData": {
      "type": "object",
      "description": "Custom user data attached to the request. Can contain arbitrary fields."
     },
     "noRetry": {
      "type": [
       "boolean",
       "null"
      ],
      "description": "Indicates whether the request should not be retried if processing fails."
     },
     "errorMessages": {
      "type": [
       "array",
       "null"
      ],
      "description": "Error messages recorded from failed processing attempts."
     },
     "handledAt": {
      "type": [
       "string",
       "null"
      ],
      "description": "The timestamp when the request was marked as handled, if applicable."
     }
    },
    "required": [
     "queueId",
     "uniqueKey",
     "url"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/requests",
    "path": [
     "queueId"
    ],
    "query": [
     "clientKey",
     "forefront"
    ],
    "header": [],
    "body": [
     "uniqueKey",
     "url",
     "method",
     "retryCount",
     "loadedUrl",
     "payload",
     "headers",
     "userData",
     "noRetry",
     "errorMessages",
     "handledAt"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queue_requests_unlock_post",
   "name": "Unlock requests",
   "description": "Unlocks requests in the queue that are currently locked by the client. * If the client is within an Actor run, it unlocks all requests locked by that specific run plus all requests locked by the same clientKey. * If the client is outside of an Actor run, it unlocks all requests locked using the same clientKey.",
   "input": {
    "type": "object",
    "properties": {
     "queueId": {
      "type": "string",
      "description": "Queue ID or `username~queue-name`."
     },
     "clientKey": {
      "type": "string",
      "description": "A unique identifier of the client accessing the request queue. It must be a string between 1 and 32 characters long. This identifier is used to determine whether the queue was accessed by multiple clients. If `clientKey` is not provided, the system considers this API call to come from a new client. For details, see the `hadMultipleClients` field returned by the [Get head](#/reference/request-queue"
     }
    },
    "required": [
     "queueId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/request-queues/{queueId}/requests/unlock",
    "path": [
     "queueId"
    ],
    "query": [
     "clientKey"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queues_get",
   "name": "Get list of request queues",
   "description": "Lists all of a user's request queues. The response is a JSON array of objects, where each object contains basic information about one queue. By default, the objects are sorted by the `createdAt` field in ascending order, therefore you can use pagination to incrementally fetch all queues while new ones are still being created. To sort them in descending order, use `desc=1` parameter. The endpoint s",
   "input": {
    "type": "object",
    "properties": {
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order. By default, they are sorted in ascending order."
     },
     "unnamed": {
      "type": "boolean",
      "description": "If `true` or `1` then all the storages are returned. By default, only named storages are returned."
     },
     "ownership": {
      "type": "string",
      "description": "Filter by ownership. If this parameter is omitted, all accessible request queues are returned. - `ownedByMe`: Return only request queues owned by the user. - `sharedWithMe`: Return only request queues shared with the user by other users.",
      "enum": [
       "ownedByMe",
       "sharedWithMe"
      ]
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/request-queues",
    "path": [],
    "query": [
     "offset",
     "limit",
     "desc",
     "unnamed",
     "ownership"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.request_queues_post",
   "name": "Create request queue",
   "description": "Creates a request queue and returns its object. Keep in mind that requests stored under unnamed queue follows [data retention period](https://docs.apify.com/platform/storage#data-retention). It creates a queue of given name if the parameter name is used. If a queue with the given name already exists then the endpoint returns its object.",
   "input": {
    "type": "object",
    "properties": {
     "name": {
      "type": "string",
      "description": "Custom unique name to easily identify the queue in the future."
     }
    },
    "required": []
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/request-queues",
    "path": [],
    "query": [
     "name"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.schedule_delete",
   "name": "Delete schedule",
   "description": "Deletes a schedule.",
   "input": {
    "type": "object",
    "properties": {
     "scheduleId": {
      "type": "string",
      "description": "Schedule ID."
     }
    },
    "required": [
     "scheduleId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/schedules/{scheduleId}",
    "path": [
     "scheduleId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.schedule_get",
   "name": "Get schedule",
   "description": "Gets the schedule object with all details.",
   "input": {
    "type": "object",
    "properties": {
     "scheduleId": {
      "type": "string",
      "description": "Schedule ID."
     }
    },
    "required": [
     "scheduleId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/schedules/{scheduleId}",
    "path": [
     "scheduleId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.schedule_log_get",
   "name": "Get schedule log",
   "description": "Gets the schedule log as a JSON array containing information about up to a 1000 invocations of the schedule.",
   "input": {
    "type": "object",
    "properties": {
     "scheduleId": {
      "type": "string",
      "description": "Schedule ID."
     }
    },
    "required": [
     "scheduleId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/schedules/{scheduleId}/log",
    "path": [
     "scheduleId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.schedule_put",
   "name": "Update schedule",
   "description": "Updates a schedule using values specified by a schedule object passed as JSON in the POST payload. If the object does not define a specific property, its value will not be updated. The response is the full schedule object as returned by the [Get schedule](#/reference/schedules/schedule-object/get-schedule) endpoint. **The request needs to specify the `Content-Type: application/json` HTTP header!**",
   "input": {
    "type": "object",
    "properties": {
     "scheduleId": {
      "type": "string",
      "description": "Schedule ID."
     },
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "isEnabled": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     },
     "isExclusive": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     },
     "cronExpression": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "timezone": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "description": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "title": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "actions": {
      "type": [
       "array",
       "null"
      ],
      "description": ""
     }
    },
    "required": [
     "scheduleId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/schedules/{scheduleId}",
    "path": [
     "scheduleId"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "isEnabled",
     "isExclusive",
     "cronExpression",
     "timezone",
     "description",
     "title",
     "actions"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.schedules_get",
   "name": "Get list of schedules",
   "description": "Gets the list of schedules that the user created. The endpoint supports pagination using the `limit` and `offset` parameters. It will not return more than 1000 records. By default, the records are sorted by the `createdAt` field in ascending order. To sort the records in descending order, use the `desc=1` parameter.",
   "input": {
    "type": "object",
    "properties": {
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order. By default, they are sorted in ascending order."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/schedules",
    "path": [],
    "query": [
     "offset",
     "limit",
     "desc"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.schedules_post",
   "name": "Create schedule",
   "description": "Creates a new schedule with settings provided by the schedule object passed as JSON in the payload. The response is the created schedule object. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request's `Authorization` header, rather than the URL. ([More info](#/introduction/authentication)).",
   "input": {
    "type": "object",
    "properties": {
     "name": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "isEnabled": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     },
     "isExclusive": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     },
     "cronExpression": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "timezone": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "description": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "title": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "actions": {
      "type": [
       "array",
       "null"
      ],
      "description": ""
     }
    },
    "required": []
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/schedules",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "name",
     "isEnabled",
     "isExclusive",
     "cronExpression",
     "timezone",
     "description",
     "title",
     "actions"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.store_get",
   "name": "Get list of actors in store",
   "description": "Gets the list of public Actors in Apify Store. You can use `search` parameter to search Actors by string in title, name, description, username and readme. If you need detailed info about a specific Actor, use the [Get Actor](#/reference/actors/actor-object/get-actor) endpoint. The endpoint supports pagination using the `limit` and `offset` parameters. It will not return more than 1,000 records.",
   "input": {
    "type": "object",
    "properties": {
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "search": {
      "type": "string",
      "description": "String to search by. The search runs on the following fields: `title`, `name`, `description`, `username`, `readme`."
     },
     "sortBy": {
      "type": "string",
      "description": "Specifies the field by which to sort the results. The supported values are `relevance` (default), `popularity`, `newest` and `lastUpdate`."
     },
     "category": {
      "type": "string",
      "description": "Filters the results by the specified category."
     },
     "username": {
      "type": "string",
      "description": "Filters the results by the specified username."
     },
     "pricingModel": {
      "type": "string",
      "description": "Only return Actors with the specified pricing model.",
      "enum": [
       "FREE",
       "FLAT_PRICE_PER_MONTH",
       "PRICE_PER_DATASET_ITEM",
       "PAY_PER_EVENT"
      ]
     },
     "allowsAgenticUsers": {
      "type": "boolean",
      "description": "If true, only return Actors that allow agentic users. If false, only return Actors that do not allow agentic users."
     },
     "responseFormat": {
      "type": "string",
      "description": "Controls the shape of the response. Use `full` (default) for the complete response including image URLs and all fields. Use `agent` for a reduced field set optimized for LLM consumers, which only includes `id`, `title`, `name`, `username`, `description`, `notice`, `badge`, `categories`, and minimal `stats`.",
      "enum": [
       "full",
       "agent"
      ]
     },
     "includeUnrunnableActors": {
      "type": "boolean",
      "description": "By default, search results exclude Actors that are not safe to run automatically (e.g. Actors from developers who haven't passed KYC, or full-permission Actors without a large user base). Set to `true` to bypass this safety filtering and include all Actors in the results."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/store",
    "path": [],
    "query": [
     "limit",
     "offset",
     "search",
     "sortBy",
     "category",
     "username",
     "pricingModel",
     "allowsAgenticUsers",
     "responseFormat",
     "includeUnrunnableActors"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.tools_browser_info_delete",
   "name": "Get browser info",
   "description": "Returns information about the HTTP request, including the client IP address, country code, request headers, and body length. This endpoint is designed for proxy testing. It accepts any HTTP method so you can verify that your proxy correctly forwards requests of any type and that client IP addresses are anonymized.",
   "input": {
    "type": "object",
    "properties": {
     "skipHeaders": {
      "type": "boolean",
      "description": "If `true` or `1`, the response omits the `headers` field."
     },
     "rawHeaders": {
      "type": "boolean",
      "description": "If `true` or `1`, the response includes the `rawHeaders` field with the raw request headers."
     }
    },
    "required": []
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/browser-info",
    "path": [],
    "query": [
     "skipHeaders",
     "rawHeaders"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.tools_browser_info_get",
   "name": "Get browser info",
   "description": "Returns information about the HTTP request, including the client IP address, country code, request headers, and body length. This endpoint is designed for proxy testing. It accepts any HTTP method so you can verify that your proxy correctly forwards requests of any type and that client IP addresses are anonymized.",
   "input": {
    "type": "object",
    "properties": {
     "skipHeaders": {
      "type": "boolean",
      "description": "If `true` or `1`, the response omits the `headers` field."
     },
     "rawHeaders": {
      "type": "boolean",
      "description": "If `true` or `1`, the response includes the `rawHeaders` field with the raw request headers."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/browser-info",
    "path": [],
    "query": [
     "skipHeaders",
     "rawHeaders"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.tools_browser_info_post",
   "name": "Get browser info",
   "description": "Returns information about the HTTP request, including the client IP address, country code, request headers, and body length. This endpoint is designed for proxy testing. It accepts any HTTP method so you can verify that your proxy correctly forwards requests of any type and that client IP addresses are anonymized.",
   "input": {
    "type": "object",
    "properties": {
     "skipHeaders": {
      "type": "boolean",
      "description": "If `true` or `1`, the response omits the `headers` field."
     },
     "rawHeaders": {
      "type": "boolean",
      "description": "If `true` or `1`, the response includes the `rawHeaders` field with the raw request headers."
     }
    },
    "required": []
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/browser-info",
    "path": [],
    "query": [
     "skipHeaders",
     "rawHeaders"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.tools_browser_info_put",
   "name": "Get browser info",
   "description": "Returns information about the HTTP request, including the client IP address, country code, request headers, and body length. This endpoint is designed for proxy testing. It accepts any HTTP method so you can verify that your proxy correctly forwards requests of any type and that client IP addresses are anonymized.",
   "input": {
    "type": "object",
    "properties": {
     "skipHeaders": {
      "type": "boolean",
      "description": "If `true` or `1`, the response omits the `headers` field."
     },
     "rawHeaders": {
      "type": "boolean",
      "description": "If `true` or `1`, the response includes the `rawHeaders` field with the raw request headers."
     }
    },
    "required": []
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/browser-info",
    "path": [],
    "query": [
     "skipHeaders",
     "rawHeaders"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.tools_decode_and_verify_post",
   "name": "Decode and verify object",
   "description": "Decodes and verifies an encoded value previously created by the encode-and-sign endpoint. Returns the original decoded object along with information about the user who encoded it and whether that user is verified. **Important**: The request must specify the `Content-Type: application/json` HTTP header.",
   "input": {
    "type": "object",
    "properties": {
     "encoded": {
      "type": "string",
      "description": ""
     }
    },
    "required": [
     "encoded"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/tools/decode-and-verify",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "encoded"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.tools_encode_and_sign_post",
   "name": "Encode and sign object",
   "description": "Encodes and signs any JSON object. The encoded value includes a signature tied to the authenticated user's ID, which can later be verified using the decode-and-verify endpoint. **Important**: The request must specify the `Content-Type: application/json` HTTP header.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/tools/encode-and-sign",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.user_get",
   "name": "Get public user data",
   "description": "Returns public information about a specific user account, similar to what can be seen on public profile pages (e.g. https://apify.com/apify). This operation requires no authentication token.",
   "input": {
    "type": "object",
    "properties": {
     "userId": {
      "type": "string",
      "description": "User ID or username."
     }
    },
    "required": [
     "userId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/users/{userId}",
    "path": [
     "userId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.users_me_get",
   "name": "Get private user data",
   "description": "Returns information about the current user account, including both public and private information. The user account is identified by the provided authentication token. The fields `plan`, `email` and `profile` are omitted when this endpoint is accessed from Actor run.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/users/me",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.users_me_limits_get",
   "name": "Get limits",
   "description": "Returns a complete summary of your account's limits. It is the same information you will see on your account's [Limits page](https://console.apify.com/billing#/limits). The returned data includes the current usage cycle, a summary of your limits, and your current usage.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/users/me/limits",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.users_me_limits_put",
   "name": "Update limits",
   "description": "Updates the account's limits manageable on your account's [Limits page](https://console.apify.com/billing#/limits). Specifically the: `maxMonthlyUsageUsd` and `dataRetentionDays` limits (see request body schema for more details).",
   "input": {
    "type": "object",
    "properties": {
     "maxMonthlyUsageUsd": {
      "type": "number",
      "description": "If your platform usage in the billing period exceeds the prepaid usage, you will be charged extra. Setting this property you can update your hard limit on monthly platform usage to prevent accidental overage or to limit the extra charges."
     },
     "dataRetentionDays": {
      "type": "integer",
      "description": "Apify securely stores your ten most recent Actor runs indefinitely, ensuring they are always accessible. Unnamed storages and other Actor runs are automatically deleted after the retention period. If you're subscribed, you can change it to keep data for longer or to limit your usage. [Lear more](https://docs.apify.com/storage#data-retention)."
     }
    },
    "required": []
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/users/me/limits",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "maxMonthlyUsageUsd",
     "dataRetentionDays"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.users_me_usage_monthly_get",
   "name": "Get monthly usage",
   "description": "Returns a complete summary of your usage for the current monthly usage cycle, an overall sum, as well as a daily breakdown of usage. It is the same information you will see on your account's [Billing > Historical usage page](https://console.apify.com/billing/historical-usage). The information includes your use of Actors, compute, data transfer, and storage. Using the `date` parameter will show you",
   "input": {
    "type": "object",
    "properties": {
     "date": {
      "type": "string",
      "description": "Date in the YYYY-MM-DD format."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/users/me/usage/monthly",
    "path": [],
    "query": [
     "date"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.webhook_delete",
   "name": "Delete webhook",
   "description": "Deletes a webhook.",
   "input": {
    "type": "object",
    "properties": {
     "webhookId": {
      "type": "string",
      "description": "Webhook ID."
     }
    },
    "required": [
     "webhookId"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.apify.com/v2/webhooks/{webhookId}",
    "path": [
     "webhookId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.webhook_dispatch_get",
   "name": "Get webhook dispatch",
   "description": "Gets webhook dispatch object with all details.",
   "input": {
    "type": "object",
    "properties": {
     "dispatchId": {
      "type": "string",
      "description": "Webhook dispatch ID."
     }
    },
    "required": [
     "dispatchId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/webhook-dispatches/{dispatchId}",
    "path": [
     "dispatchId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.webhook_dispatches_get",
   "name": "Get list of webhook dispatches",
   "description": "Gets the list of webhook dispatches that the user have. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `createdAt` field in ascending order. To sort the records in descending order, use the `desc=1` parameter.",
   "input": {
    "type": "object",
    "properties": {
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order. By default, they are sorted in ascending order."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/webhook-dispatches",
    "path": [],
    "query": [
     "offset",
     "limit",
     "desc"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.webhook_get",
   "name": "Get webhook",
   "description": "Gets webhook object with all details.",
   "input": {
    "type": "object",
    "properties": {
     "webhookId": {
      "type": "string",
      "description": "Webhook ID."
     }
    },
    "required": [
     "webhookId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/webhooks/{webhookId}",
    "path": [
     "webhookId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.webhook_put",
   "name": "Update webhook",
   "description": "Updates a webhook using values specified by a webhook object passed as JSON in the POST payload. If the object does not define a specific property, its value will not be updated. The response is the full webhook object as returned by the [Get webhook](#/reference/webhooks/webhook-object/get-webhook) endpoint. The request needs to specify the `Content-Type: application/json` HTTP header! When provi",
   "input": {
    "type": "object",
    "properties": {
     "webhookId": {
      "type": "string",
      "description": "Webhook ID."
     },
     "isAdHoc": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     },
     "eventTypes": {
      "type": [
       "array",
       "null"
      ],
      "description": ""
     },
     "condition": {
      "type": "string",
      "description": ""
     },
     "ignoreSslErrors": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     },
     "doNotRetry": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     },
     "requestUrl": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "payloadTemplate": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "headersTemplate": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "description": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "shouldInterpolateStrings": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     }
    },
    "required": [
     "webhookId"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.apify.com/v2/webhooks/{webhookId}",
    "path": [
     "webhookId"
    ],
    "query": [],
    "header": [],
    "body": [
     "isAdHoc",
     "eventTypes",
     "condition",
     "ignoreSslErrors",
     "doNotRetry",
     "requestUrl",
     "payloadTemplate",
     "headersTemplate",
     "description",
     "shouldInterpolateStrings"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.webhook_test_post",
   "name": "Test webhook",
   "description": "Tests a webhook. Creates a webhook dispatch with a dummy payload.",
   "input": {
    "type": "object",
    "properties": {
     "webhookId": {
      "type": "string",
      "description": "Webhook ID."
     }
    },
    "required": [
     "webhookId"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/webhooks/{webhookId}/test",
    "path": [
     "webhookId"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.webhook_webhook_dispatches_get",
   "name": "Get collection",
   "description": "Gets a given webhook's list of dispatches.",
   "input": {
    "type": "object",
    "properties": {
     "webhookId": {
      "type": "string",
      "description": "Webhook ID."
     },
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order. By default, they are sorted in ascending order."
     }
    },
    "required": [
     "webhookId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/webhooks/{webhookId}/dispatches",
    "path": [
     "webhookId"
    ],
    "query": [
     "offset",
     "limit",
     "desc"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.webhooks_get",
   "name": "Get list of webhooks",
   "description": "Gets the list of webhooks that the user created. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `createdAt` field in ascending order. To sort the records in descending order, use the `desc=1` parameter.",
   "input": {
    "type": "object",
    "properties": {
     "offset": {
      "type": "number",
      "description": "Number of items that should be skipped at the start. The default value is `0`."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
     },
     "desc": {
      "type": "boolean",
      "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order. By default, they are sorted in ascending order."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.apify.com/v2/webhooks",
    "path": [],
    "query": [
     "offset",
     "limit",
     "desc"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "apify.webhooks_post",
   "name": "Create webhook",
   "description": "Creates a new webhook with settings provided by the webhook object passed as JSON in the payload. The response is the created webhook object. To avoid duplicating a webhook, use the `idempotencyKey` parameter in the request body. Multiple calls to create a webhook with the same `idempotencyKey` will only create the webhook with the first call and return the existing webhook on subsequent calls. Id",
   "input": {
    "type": "object",
    "properties": {
     "isAdHoc": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     },
     "eventTypes": {
      "type": "array",
      "description": ""
     },
     "condition": {
      "type": "object",
      "description": ""
     },
     "idempotencyKey": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "ignoreSslErrors": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     },
     "doNotRetry": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     },
     "requestUrl": {
      "type": "string",
      "description": ""
     },
     "payloadTemplate": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "headersTemplate": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "description": {
      "type": [
       "string",
       "null"
      ],
      "description": ""
     },
     "shouldInterpolateStrings": {
      "type": [
       "boolean",
       "null"
      ],
      "description": ""
     }
    },
    "required": [
     "condition",
     "eventTypes",
     "requestUrl"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.apify.com/v2/webhooks",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "isAdHoc",
     "eventTypes",
     "condition",
     "idempotencyKey",
     "ignoreSslErrors",
     "doNotRetry",
     "requestUrl",
     "payloadTemplate",
     "headersTemplate",
     "description",
     "shouldInterpolateStrings"
    ],
    "body_format": "json"
   }
  }
 ]
}
