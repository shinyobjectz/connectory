{
 "_": "GENERATED \u2014 what an authoring tool needs to FIND an operation. The calling table is the .lua beside it.",
 "provider": "box",
 "source": "https://raw.githubusercontent.com/box/box-openapi/main/openapi.json",
 "operations": [
  {
   "slug": "box.delete_ai_agents_id",
   "name": "Delete ai agent",
   "description": "Deletes an AI agent using the provided parameters.",
   "input": {
    "type": "object",
    "properties": {
     "agent_id": {
      "type": "string",
      "description": "The ID of the agent to delete."
     }
    },
    "required": [
     "agent_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/ai_agents/{agent_id}",
    "path": [
     "agent_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_collaboration_whitelist_entries_id",
   "name": "Remove domain from list of allowed collaboration domains",
   "description": "Removes a domain from the list of domains that have been deemed safe to create collaborations for within the current enterprise.",
   "input": {
    "type": "object",
    "properties": {
     "collaboration_whitelist_entry_id": {
      "type": "string",
      "description": "The ID of the entry in the list."
     }
    },
    "required": [
     "collaboration_whitelist_entry_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/collaboration_whitelist_entries/{collaboration_whitelist_entry_id}",
    "path": [
     "collaboration_whitelist_entry_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_collaboration_whitelist_exempt_targets_id",
   "name": "Remove user from list of users exempt from domain restrictions",
   "description": "Removes a user's exemption from the restrictions set out by the allowed list of domains for collaborations.",
   "input": {
    "type": "object",
    "properties": {
     "collaboration_whitelist_exempt_target_id": {
      "type": "string",
      "description": "The ID of the exemption to the list."
     }
    },
    "required": [
     "collaboration_whitelist_exempt_target_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/collaboration_whitelist_exempt_targets/{collaboration_whitelist_exempt_target_id}",
    "path": [
     "collaboration_whitelist_exempt_target_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_collaborations_id",
   "name": "Remove collaboration",
   "description": "Deletes a single collaboration.",
   "input": {
    "type": "object",
    "properties": {
     "collaboration_id": {
      "type": "string",
      "description": "The ID of the collaboration."
     }
    },
    "required": [
     "collaboration_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/collaborations/{collaboration_id}",
    "path": [
     "collaboration_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_comments_id",
   "name": "Remove comment",
   "description": "Permanently deletes a comment.",
   "input": {
    "type": "object",
    "properties": {
     "comment_id": {
      "type": "string",
      "description": "The ID of the comment."
     }
    },
    "required": [
     "comment_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/comments/{comment_id}",
    "path": [
     "comment_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_device_pinners_id",
   "name": "Remove device pin",
   "description": "Deletes an individual device pin.",
   "input": {
    "type": "object",
    "properties": {
     "device_pinner_id": {
      "type": "string",
      "description": "The ID of the device pin."
     }
    },
    "required": [
     "device_pinner_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/device_pinners/{device_pinner_id}",
    "path": [
     "device_pinner_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_file_requests_id",
   "name": "Delete file request",
   "description": "Deletes a file request permanently.",
   "input": {
    "type": "object",
    "properties": {
     "file_request_id": {
      "type": "string",
      "description": "The unique identifier that represent a file request. The ID for any file request can be determined by visiting a file request builder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/filerequest/123` the `file_request_id` is `123`."
     }
    },
    "required": [
     "file_request_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/file_requests/{file_request_id}",
    "path": [
     "file_request_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_files_id",
   "name": "Delete file",
   "description": "Deletes a file, either permanently or by moving it to the trash. The enterprise settings determine whether the item will be permanently deleted from Box or moved to the trash.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "if-match": {
      "type": "string",
      "description": "Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/files/{file_id}",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [
     "if-match"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_files_id_metadata_enterprise_security_classification_6_vmvochw_uwo",
   "name": "Remove classification from file",
   "description": "Removes any classifications from a file. This API can also be called by including the enterprise ID in the URL explicitly, for example `/files/:id//enterprise_12345/securityClassification-6VMVochwUWo`.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_files_id_metadata_global_box_skills_cards",
   "name": "Remove box skill cards from file",
   "description": "Removes any Box Skills cards metadata from a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/global/boxSkillsCards",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_files_id_metadata_id_id",
   "name": "Remove metadata instance from file",
   "description": "Deletes a piece of file metadata.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     }
    },
    "required": [
     "file_id",
     "scope",
     "template_key"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/{scope}/{template_key}",
    "path": [
     "file_id",
     "scope",
     "template_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_files_id_trash",
   "name": "Permanently remove file",
   "description": "Permanently deletes a file that is in the trash. This action cannot be undone.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/files/{file_id}/trash",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_files_id_versions_id",
   "name": "Remove file version",
   "description": "Move a file version to the trash. Versions are only tracked for Box users with premium accounts.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "file_version_id": {
      "type": "string",
      "description": "The ID of the file version."
     },
     "if-match": {
      "type": "string",
      "description": "Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since."
     }
    },
    "required": [
     "file_id",
     "file_version_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/files/{file_id}/versions/{file_version_id}",
    "path": [
     "file_id",
     "file_version_id"
    ],
    "query": [],
    "header": [
     "if-match"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_files_id_watermark",
   "name": "Remove watermark from file",
   "description": "Removes the watermark from a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/files/{file_id}/watermark",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_files_upload_sessions_id",
   "name": "Remove upload session",
   "description": "Abort an upload session and discard all data uploaded. This cannot be reversed. The actual endpoint URL is returned by the [`Create upload session`](https://developer.box.com/reference/post-files-upload-sessions) and [`Get upload session`](https://developer.box.com/reference/get-files-upload-sessions-id) endpoints.",
   "input": {
    "type": "object",
    "properties": {
     "upload_session_id": {
      "type": "string",
      "description": "The ID of the upload session."
     }
    },
    "required": [
     "upload_session_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/files/upload_sessions/{upload_session_id}",
    "path": [
     "upload_session_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_folder_locks_id",
   "name": "Delete folder lock",
   "description": "Deletes a folder lock on a given folder. You must be authenticated as the owner or co-owner of the folder to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "folder_lock_id": {
      "type": "string",
      "description": "The ID of the folder lock."
     }
    },
    "required": [
     "folder_lock_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/folder_locks/{folder_lock_id}",
    "path": [
     "folder_lock_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_folders_id",
   "name": "Delete folder",
   "description": "Deletes a folder, either permanently or by moving it to the trash.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "if-match": {
      "type": "string",
      "description": "Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since."
     },
     "recursive": {
      "type": "boolean",
      "description": "Delete a folder that is not empty by recursively deleting the folder and all of its content."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/folders/{folder_id}",
    "path": [
     "folder_id"
    ],
    "query": [
     "recursive"
    ],
    "header": [
     "if-match"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_folders_id_metadata_enterprise_security_classification_6_vmvochw_uwo",
   "name": "Remove classification from folder",
   "description": "Removes any classifications from a folder. This API can also be called by including the enterprise ID in the URL explicitly, for example `/folders/:id/enterprise_12345/securityClassification-6VMVochwUWo`.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/folders/{folder_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "path": [
     "folder_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_folders_id_metadata_id_id",
   "name": "Remove metadata instance from folder",
   "description": "Deletes a piece of folder metadata.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     }
    },
    "required": [
     "folder_id",
     "scope",
     "template_key"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/folders/{folder_id}/metadata/{scope}/{template_key}",
    "path": [
     "folder_id",
     "scope",
     "template_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_folders_id_trash",
   "name": "Permanently remove folder",
   "description": "Permanently deletes a folder that is in the trash. This action cannot be undone.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/folders/{folder_id}/trash",
    "path": [
     "folder_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_folders_id_watermark",
   "name": "Remove watermark from folder",
   "description": "Removes the watermark from a folder.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/folders/{folder_id}/watermark",
    "path": [
     "folder_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_group_memberships_id",
   "name": "Remove user from group",
   "description": "Deletes a specific group membership. Only admins of this group or users with admin-level permissions will be able to use this API.",
   "input": {
    "type": "object",
    "properties": {
     "group_membership_id": {
      "type": "string",
      "description": "The ID of the group membership."
     }
    },
    "required": [
     "group_membership_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/group_memberships/{group_membership_id}",
    "path": [
     "group_membership_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_groups_id",
   "name": "Remove group",
   "description": "Permanently deletes a group. Only users with admin-level permissions will be able to use this API.",
   "input": {
    "type": "object",
    "properties": {
     "group_id": {
      "type": "string",
      "description": "The ID of the group."
     }
    },
    "required": [
     "group_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/groups/{group_id}",
    "path": [
     "group_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_integration_mappings_slack_id",
   "name": "Delete slack integration mapping",
   "description": "Deletes a [Slack integration mapping](https://support.box.com/hc/en-us/articles/4415585987859-Box-as-the-Content-Layer-for-Slack). You need Admin or Co-Admin role to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "integration_mapping_id": {
      "type": "string",
      "description": "An ID of an integration mapping."
     }
    },
    "required": [
     "integration_mapping_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/integration_mappings/slack/{integration_mapping_id}",
    "path": [
     "integration_mapping_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_integration_mappings_teams_id",
   "name": "Delete teams integration mapping",
   "description": "Deletes a [Teams integration mapping](https://support.box.com/hc/en-us/articles/360044681474-Using-Box-for-Teams). You need Admin or Co-Admin role to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "integration_mapping_id": {
      "type": "string",
      "description": "An ID of an integration mapping."
     }
    },
    "required": [
     "integration_mapping_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/integration_mappings/teams/{integration_mapping_id}",
    "path": [
     "integration_mapping_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_legal_hold_policies_id",
   "name": "Remove legal hold policy",
   "description": "Delete an existing legal hold policy. This is an asynchronous process. The policy will not be fully deleted yet when the response returns.",
   "input": {
    "type": "object",
    "properties": {
     "legal_hold_policy_id": {
      "type": "string",
      "description": "The ID of the legal hold policy."
     }
    },
    "required": [
     "legal_hold_policy_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/legal_hold_policies/{legal_hold_policy_id}",
    "path": [
     "legal_hold_policy_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_legal_hold_policy_assignments_id",
   "name": "Unassign legal hold policy",
   "description": "Remove a legal hold from an item. This is an asynchronous process. The policy will not be fully removed yet when the response returns.",
   "input": {
    "type": "object",
    "properties": {
     "legal_hold_policy_assignment_id": {
      "type": "string",
      "description": "The ID of the legal hold policy assignment."
     }
    },
    "required": [
     "legal_hold_policy_assignment_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}",
    "path": [
     "legal_hold_policy_assignment_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_metadata_cascade_policies_id",
   "name": "Remove metadata cascade policy",
   "description": "Deletes a metadata cascade policy.",
   "input": {
    "type": "object",
    "properties": {
     "metadata_cascade_policy_id": {
      "type": "string",
      "description": "The ID of the metadata cascade policy."
     }
    },
    "required": [
     "metadata_cascade_policy_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/metadata_cascade_policies/{metadata_cascade_policy_id}",
    "path": [
     "metadata_cascade_policy_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_metadata_taxonomies_id_id",
   "name": "Remove metadata taxonomy",
   "description": "Delete a metadata taxonomy. This deletion is permanent and cannot be reverted.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     }
    },
    "required": [
     "namespace",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}",
    "path": [
     "namespace",
     "taxonomy_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_metadata_taxonomies_id_id_nodes_id",
   "name": "Remove metadata taxonomy node",
   "description": "Delete a metadata taxonomy node. This deletion is permanent and cannot be reverted. Only metadata taxonomy nodes without any children can be deleted.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     },
     "node_id": {
      "type": "string",
      "description": "The identifier of the metadata taxonomy node."
     }
    },
    "required": [
     "namespace",
     "node_id",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}/nodes/{node_id}",
    "path": [
     "namespace",
     "taxonomy_key",
     "node_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_metadata_templates_id_id_schema",
   "name": "Remove metadata template",
   "description": "Delete a metadata template and its instances. This deletion is permanent and can not be reversed.",
   "input": {
    "type": "object",
    "properties": {
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     }
    },
    "required": [
     "scope",
     "template_key"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/metadata_templates/{scope}/{template_key}/schema",
    "path": [
     "scope",
     "template_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_retention_policies_id",
   "name": "Delete retention policy",
   "description": "Permanently deletes a retention policy.",
   "input": {
    "type": "object",
    "properties": {
     "retention_policy_id": {
      "type": "string",
      "description": "The ID of the retention policy."
     }
    },
    "required": [
     "retention_policy_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/retention_policies/{retention_policy_id}",
    "path": [
     "retention_policy_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_retention_policy_assignments_id",
   "name": "Remove retention policy assignment",
   "description": "Removes a retention policy assignment applied to content.",
   "input": {
    "type": "object",
    "properties": {
     "retention_policy_assignment_id": {
      "type": "string",
      "description": "The ID of the retention policy assignment."
     }
    },
    "required": [
     "retention_policy_assignment_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/retention_policy_assignments/{retention_policy_assignment_id}",
    "path": [
     "retention_policy_assignment_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_shield_information_barrier_segment_members_id",
   "name": "Delete shield information barrier segment member by id",
   "description": "Deletes a shield information barrier segment member based on provided ID.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_segment_member_id": {
      "type": "string",
      "description": "The ID of the shield information barrier segment Member."
     }
    },
    "required": [
     "shield_information_barrier_segment_member_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/shield_information_barrier_segment_members/{shield_information_barrier_segment_member_id}",
    "path": [
     "shield_information_barrier_segment_member_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_shield_information_barrier_segment_restrictions_id",
   "name": "Delete shield information barrier segment restriction by id",
   "description": "Delete shield information barrier segment restriction based on provided ID.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_segment_restriction_id": {
      "type": "string",
      "description": "The ID of the shield information barrier segment Restriction."
     }
    },
    "required": [
     "shield_information_barrier_segment_restriction_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/shield_information_barrier_segment_restrictions/{shield_information_barrier_segment_restriction_id}",
    "path": [
     "shield_information_barrier_segment_restriction_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_shield_information_barrier_segments_id",
   "name": "Delete shield information barrier segment",
   "description": "Deletes the shield information barrier segment based on provided ID.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_segment_id": {
      "type": "string",
      "description": "The ID of the shield information barrier segment."
     }
    },
    "required": [
     "shield_information_barrier_segment_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/shield_information_barrier_segments/{shield_information_barrier_segment_id}",
    "path": [
     "shield_information_barrier_segment_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_storage_policy_assignments_id",
   "name": "Unassign storage policy",
   "description": "Delete a storage policy assignment. Deleting a storage policy assignment on a user will have the user inherit the enterprise's default storage policy. There is a rate limit for calling this endpoint of only twice per user in a 24 hour time frame.",
   "input": {
    "type": "object",
    "properties": {
     "storage_policy_assignment_id": {
      "type": "string",
      "description": "The ID of the storage policy assignment."
     }
    },
    "required": [
     "storage_policy_assignment_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/storage_policy_assignments/{storage_policy_assignment_id}",
    "path": [
     "storage_policy_assignment_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_task_assignments_id",
   "name": "Unassign task",
   "description": "Deletes a specific task assignment.",
   "input": {
    "type": "object",
    "properties": {
     "task_assignment_id": {
      "type": "string",
      "description": "The ID of the task assignment."
     }
    },
    "required": [
     "task_assignment_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/task_assignments/{task_assignment_id}",
    "path": [
     "task_assignment_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_tasks_id",
   "name": "Remove task",
   "description": "Removes a task from a file.",
   "input": {
    "type": "object",
    "properties": {
     "task_id": {
      "type": "string",
      "description": "The ID of the task."
     }
    },
    "required": [
     "task_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/tasks/{task_id}",
    "path": [
     "task_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_users_id",
   "name": "Delete user",
   "description": "Deletes a user. By default, this operation fails if the user still owns any content. To proceed, move their owned content first, or use the `force` parameter to delete the user and their files.",
   "input": {
    "type": "object",
    "properties": {
     "user_id": {
      "type": "string",
      "description": "The ID of the user."
     },
     "notify": {
      "type": "boolean",
      "description": "Whether the user will receive email notification of the deletion."
     },
     "force": {
      "type": "boolean",
      "description": "Specifies whether to delete the user even if they still own files."
     }
    },
    "required": [
     "user_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/users/{user_id}",
    "path": [
     "user_id"
    ],
    "query": [
     "notify",
     "force"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_users_id_avatar",
   "name": "Delete user avatar",
   "description": "Removes an existing user avatar. You cannot reverse this operation.",
   "input": {
    "type": "object",
    "properties": {
     "user_id": {
      "type": "string",
      "description": "The ID of the user."
     }
    },
    "required": [
     "user_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/users/{user_id}/avatar",
    "path": [
     "user_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_users_id_email_aliases_id",
   "name": "Remove email alias",
   "description": "Removes an email alias from a user.",
   "input": {
    "type": "object",
    "properties": {
     "user_id": {
      "type": "string",
      "description": "The ID of the user."
     },
     "email_alias_id": {
      "type": "string",
      "description": "The ID of the email alias."
     }
    },
    "required": [
     "email_alias_id",
     "user_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/users/{user_id}/email_aliases/{email_alias_id}",
    "path": [
     "user_id",
     "email_alias_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_web_links_id",
   "name": "Remove web link",
   "description": "Deletes a web link.",
   "input": {
    "type": "object",
    "properties": {
     "web_link_id": {
      "type": "string",
      "description": "The ID of the web link."
     }
    },
    "required": [
     "web_link_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/web_links/{web_link_id}",
    "path": [
     "web_link_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_web_links_id_trash",
   "name": "Permanently remove web link",
   "description": "Permanently deletes a web link that is in the trash. This action cannot be undone.",
   "input": {
    "type": "object",
    "properties": {
     "web_link_id": {
      "type": "string",
      "description": "The ID of the web link."
     }
    },
    "required": [
     "web_link_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/web_links/{web_link_id}/trash",
    "path": [
     "web_link_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.delete_webhooks_id",
   "name": "Remove webhook",
   "description": "Deletes a webhook.",
   "input": {
    "type": "object",
    "properties": {
     "webhook_id": {
      "type": "string",
      "description": "The ID of the webhook."
     }
    },
    "required": [
     "webhook_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.box.com/2.0/webhooks/{webhook_id}",
    "path": [
     "webhook_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_ai_agent_default",
   "name": "Get ai agent default configuration",
   "description": "Get the AI agent default config.",
   "input": {
    "type": "object",
    "properties": {
     "mode": {
      "type": "string",
      "description": "The mode to filter the agent config to return.",
      "enum": [
       "ask",
       "text_gen",
       "extract",
       "extract_structured"
      ]
     },
     "language": {
      "type": "string",
      "description": "The ISO language code to return the agent config for. If the language is not supported the default agent config is returned."
     },
     "model": {
      "type": "string",
      "description": "The model to return the default agent config for."
     }
    },
    "required": [
     "mode"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/ai_agent_default",
    "path": [],
    "query": [
     "mode",
     "language",
     "model"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_ai_agents",
   "name": "List ai agents",
   "description": "Lists AI agents based on the provided parameters.",
   "input": {
    "type": "object",
    "properties": {
     "mode": {
      "type": "array",
      "description": "The mode to filter the agent config to return. Possible values are: `ask`, `text_gen`, and `extract`."
     },
     "fields": {
      "type": "array",
      "description": "The fields to return in the response."
     },
     "agent_state": {
      "type": "array",
      "description": "The state of the agents to return. Possible values are: `enabled`, `disabled` and `enabled_for_selected_users`."
     },
     "include_box_default": {
      "type": "boolean",
      "description": "Whether to include the Box default agents in the response."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/ai_agents",
    "path": [],
    "query": [
     "mode",
     "fields",
     "agent_state",
     "include_box_default",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_ai_agents_id",
   "name": "Get ai agent by agent id",
   "description": "Gets an AI Agent using the `agent_id` parameter.",
   "input": {
    "type": "object",
    "properties": {
     "agent_id": {
      "type": "string",
      "description": "The agent id to get."
     },
     "fields": {
      "type": "array",
      "description": "The fields to return in the response."
     }
    },
    "required": [
     "agent_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/ai_agents/{agent_id}",
    "path": [
     "agent_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_authorize",
   "name": "Authorize user",
   "description": "Authorize a user by sending them through the [Box](https://box.com) website and request their permission to act on their behalf. This is the first step when authenticating a user using OAuth 2.0. To request a user's authorization to use the Box APIs on their behalf you will need to send a user to the URL with this format.",
   "input": {
    "type": "object",
    "properties": {
     "response_type": {
      "type": "string",
      "description": "The type of response we'd like to receive.",
      "enum": [
       "code"
      ]
     },
     "client_id": {
      "type": "string",
      "description": "The Client ID of the application that is requesting to authenticate the user. To get the Client ID for your application, log in to your Box developer console and click the **Edit Application** link for the application you're working with. In the OAuth 2.0 Parameters section of the configuration page, find the item labelled `client_id`. The text of that item is your application's Client ID."
     },
     "redirect_uri": {
      "type": "string",
      "description": "The URI to which Box redirects the browser after the user has granted or denied the application permission. This URI match one of the redirect URIs in the configuration of your application. It must be a valid HTTPS URI and it needs to be able to handle the redirection to complete the next step in the OAuth 2.0 flow. Although this parameter is optional, it must be a part of the authorization URL if"
     },
     "state": {
      "type": "string",
      "description": "A custom string of your choice. Box will pass the same string to the redirect URL when authentication is complete. This parameter can be used to identify a user on redirect, as well as protect against hijacked sessions and other exploits."
     },
     "scope": {
      "type": "string",
      "description": "A space-separated list of application scopes you'd like to authenticate the user for. This defaults to all the scopes configured for the application in its configuration page."
     }
    },
    "required": [
     "client_id",
     "response_type"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/authorize",
    "path": [],
    "query": [
     "response_type",
     "client_id",
     "redirect_uri",
     "state",
     "scope"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_collaboration_whitelist_entries",
   "name": "List allowed collaboration domains",
   "description": "Returns the list domains that have been deemed safe to create collaborations for within the current enterprise.",
   "input": {
    "type": "object",
    "properties": {
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/collaboration_whitelist_entries",
    "path": [],
    "query": [
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_collaboration_whitelist_entries_id",
   "name": "Get allowed collaboration domain",
   "description": "Returns a domain that has been deemed safe to create collaborations for within the current enterprise.",
   "input": {
    "type": "object",
    "properties": {
     "collaboration_whitelist_entry_id": {
      "type": "string",
      "description": "The ID of the entry in the list."
     }
    },
    "required": [
     "collaboration_whitelist_entry_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/collaboration_whitelist_entries/{collaboration_whitelist_entry_id}",
    "path": [
     "collaboration_whitelist_entry_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_collaboration_whitelist_exempt_targets",
   "name": "List users exempt from collaboration domain restrictions",
   "description": "Returns a list of users who have been exempt from the collaboration domain restrictions.",
   "input": {
    "type": "object",
    "properties": {
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/collaboration_whitelist_exempt_targets",
    "path": [],
    "query": [
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_collaboration_whitelist_exempt_targets_id",
   "name": "Get user exempt from collaboration domain restrictions",
   "description": "Returns a users who has been exempt from the collaboration domain restrictions.",
   "input": {
    "type": "object",
    "properties": {
     "collaboration_whitelist_exempt_target_id": {
      "type": "string",
      "description": "The ID of the exemption to the list."
     }
    },
    "required": [
     "collaboration_whitelist_exempt_target_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/collaboration_whitelist_exempt_targets/{collaboration_whitelist_exempt_target_id}",
    "path": [
     "collaboration_whitelist_exempt_target_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_collaborations",
   "name": "List pending collaborations",
   "description": "Retrieves all pending collaboration invites for this user.",
   "input": {
    "type": "object",
    "properties": {
     "status": {
      "type": "string",
      "description": "The status of the collaborations to retrieve.",
      "enum": [
       "pending"
      ]
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "status"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/collaborations",
    "path": [],
    "query": [
     "status",
     "fields",
     "offset",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_collaborations_id",
   "name": "Get collaboration",
   "description": "Retrieves a single collaboration.",
   "input": {
    "type": "object",
    "properties": {
     "collaboration_id": {
      "type": "string",
      "description": "The ID of the collaboration."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "collaboration_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/collaborations/{collaboration_id}",
    "path": [
     "collaboration_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_collections",
   "name": "List all collections",
   "description": "Retrieves all collections for a given user. Currently, only the `favorites` collection is supported.",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/collections",
    "path": [],
    "query": [
     "fields",
     "offset",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_collections_id",
   "name": "Get collection by id",
   "description": "Retrieves a collection by its ID.",
   "input": {
    "type": "object",
    "properties": {
     "collection_id": {
      "type": "string",
      "description": "The ID of the collection."
     }
    },
    "required": [
     "collection_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/collections/{collection_id}",
    "path": [
     "collection_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_collections_id_items",
   "name": "List collection items",
   "description": "Retrieves the files and/or folders contained within this collection.",
   "input": {
    "type": "object",
    "properties": {
     "collection_id": {
      "type": "string",
      "description": "The ID of the collection."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "collection_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/collections/{collection_id}/items",
    "path": [
     "collection_id"
    ],
    "query": [
     "fields",
     "offset",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_comments_id",
   "name": "Get comment",
   "description": "Retrieves the message and metadata for a specific comment, as well as information on the user who created the comment.",
   "input": {
    "type": "object",
    "properties": {
     "comment_id": {
      "type": "string",
      "description": "The ID of the comment."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "comment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/comments/{comment_id}",
    "path": [
     "comment_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_device_pinners_id",
   "name": "Get device pin",
   "description": "Retrieves information about an individual device pin.",
   "input": {
    "type": "object",
    "properties": {
     "device_pinner_id": {
      "type": "string",
      "description": "The ID of the device pin."
     }
    },
    "required": [
     "device_pinner_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/device_pinners/{device_pinner_id}",
    "path": [
     "device_pinner_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_enterprises_id_device_pinners",
   "name": "List enterprise device pins",
   "description": "Retrieves all the device pins within an enterprise. The user must have admin privileges, and the application needs the \"manage enterprise\" scope to make this call.",
   "input": {
    "type": "object",
    "properties": {
     "enterprise_id": {
      "type": "string",
      "description": "The ID of the enterprise."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "direction": {
      "type": "string",
      "description": "The direction to sort results in. This can be either in alphabetical ascending (`ASC`) or descending (`DESC`) order.",
      "enum": [
       "ASC",
       "DESC"
      ]
     }
    },
    "required": [
     "enterprise_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/enterprises/{enterprise_id}/device_pinners",
    "path": [
     "enterprise_id"
    ],
    "query": [
     "marker",
     "limit",
     "direction"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_events",
   "name": "List user and enterprise events",
   "description": "Returns up to a year of past events for a given user or for the entire enterprise. By default this returns events for the authenticated user. To retrieve events for the entire enterprise, set the `stream_type` to `admin_logs_streaming` for live monitoring of new events, or `admin_logs` for querying across historical events. The user making the API call will need to have admin privileges, and the a",
   "input": {
    "type": "object",
    "properties": {
     "stream_type": {
      "type": "string",
      "description": "Defines the type of events that are returned * `all` returns everything for a user and is the default * `changes` returns events that may cause file tree changes such as file updates or collaborations. * `sync` is similar to `changes` but only applies to synced folders * `admin_logs` returns all events for an entire enterprise and requires the user making the API call to have admin permissions. Th",
      "enum": [
       "all",
       "changes",
       "sync",
       "admin_logs",
       "admin_logs_streaming"
      ]
     },
     "stream_position": {
      "type": "string",
      "description": "The location in the event stream to start receiving events from. * `now` will return an empty list events and the latest stream position for initialization. * `0` or `null` will return all events."
     },
     "limit": {
      "type": "integer",
      "description": "Limits the number of events returned. Note: Sometimes, the events less than the limit requested can be returned even when there may be more events remaining. This is primarily done in the case where a number of events have already been retrieved and these retrieved events are returned rather than delaying for an unknown amount of time to see if there are any more results."
     },
     "event_type": {
      "type": "array",
      "description": "A comma-separated list of events to filter by. This can only be used when requesting the events with a `stream_type` of `admin_logs` or `adming_logs_streaming`. For any other `stream_type` this value will be ignored."
     },
     "created_after": {
      "type": "string",
      "description": "The lower bound date and time to return events for. This can only be used when requesting the events with a `stream_type` of `admin_logs`. For any other `stream_type` this value will be ignored."
     },
     "created_before": {
      "type": "string",
      "description": "The upper bound date and time to return events for. This can only be used when requesting the events with a `stream_type` of `admin_logs`. For any other `stream_type` this value will be ignored."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/events",
    "path": [],
    "query": [
     "stream_type",
     "stream_position",
     "limit",
     "event_type",
     "created_after",
     "created_before"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_file_requests_id",
   "name": "Get file request",
   "description": "Retrieves the information about a file request.",
   "input": {
    "type": "object",
    "properties": {
     "file_request_id": {
      "type": "string",
      "description": "The unique identifier that represent a file request. The ID for any file request can be determined by visiting a file request builder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/filerequest/123` the `file_request_id` is `123`."
     }
    },
    "required": [
     "file_request_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/file_requests/{file_request_id}",
    "path": [
     "file_request_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_file_version_legal_holds",
   "name": "List file version legal holds",
   "description": "Get a list of file versions on legal hold for a legal hold assignment. Due to ongoing re-architecture efforts this API might not return all file versions for this policy ID. Instead, this API will only return file versions held in the legacy architecture. Two new endpoints will available to request any file versions held in the new architecture. For file versions held in the new architecture, the ",
   "input": {
    "type": "object",
    "properties": {
     "policy_id": {
      "type": "string",
      "description": "The ID of the legal hold policy to get the file version legal holds for."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "policy_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/file_version_legal_holds",
    "path": [],
    "query": [
     "policy_id",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_file_version_legal_holds_id",
   "name": "Get file version legal hold",
   "description": "Retrieves information about the legal hold policies assigned to a file version.",
   "input": {
    "type": "object",
    "properties": {
     "file_version_legal_hold_id": {
      "type": "string",
      "description": "The ID of the file version legal hold."
     }
    },
    "required": [
     "file_version_legal_hold_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/file_version_legal_holds/{file_version_legal_hold_id}",
    "path": [
     "file_version_legal_hold_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_file_version_retentions",
   "name": "List file version retentions",
   "description": "Retrieves all file version retentions for the given enterprise. **Note**: File retention API is now **deprecated**. To get information about files and file versions under retention, see [files under retention](https://developer.box.com/reference/get-retention-policy-assignments-id-files-under-retention) or [file versions under retention](https://developer.box.com/reference/get-retention-policy-ass",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "Filters results by files with this ID."
     },
     "file_version_id": {
      "type": "string",
      "description": "Filters results by file versions with this ID."
     },
     "policy_id": {
      "type": "string",
      "description": "Filters results by the retention policy with this ID."
     },
     "disposition_action": {
      "type": "string",
      "description": "Filters results by the retention policy with this disposition action.",
      "enum": [
       "permanently_delete",
       "remove_retention"
      ]
     },
     "disposition_before": {
      "type": "string",
      "description": "Filters results by files that will have their disposition come into effect before this date."
     },
     "disposition_after": {
      "type": "string",
      "description": "Filters results by files that will have their disposition come into effect after this date."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/file_version_retentions",
    "path": [],
    "query": [
     "file_id",
     "file_version_id",
     "policy_id",
     "disposition_action",
     "disposition_before",
     "disposition_after",
     "limit",
     "marker"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_file_version_retentions_id",
   "name": "Get retention on file",
   "description": "Returns information about a file version retention. **Note**: File retention API is now **deprecated**. To get information about files and file versions under retention, see [files under retention](https://developer.box.com/reference/get-retention-policy-assignments-id-files-under-retention) or [file versions under retention](https://developer.box.com/reference/get-retention-policy-assignments-id-",
   "input": {
    "type": "object",
    "properties": {
     "file_version_retention_id": {
      "type": "string",
      "description": "The ID of the file version retention."
     }
    },
    "required": [
     "file_version_retention_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/file_version_retentions/{file_version_retention_id}",
    "path": [
     "file_version_retention_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id",
   "name": "Get file information",
   "description": "Retrieves the details about a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "if-none-match": {
      "type": "string",
      "description": "Ensures an item is only returned if it has changed. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `304 Not Modified` if the item has not changed since."
     },
     "boxapi": {
      "type": "string",
      "description": "The URL, and optional password, for the shared link of this item. This header can be used to access items that have not been explicitly shared with a user. Use the format `shared_link=[link]` or if a password is required then use `shared_link=[link]&shared_link_password=[password]`. This header can be used on the file or folder shared, as well as on any files or folders nested within the item."
     },
     "x-rep-hints": {
      "type": "string",
      "description": "A header required to request specific `representations` of a file. Use this in combination with the `fields` query parameter to request a specific file representation. The general format for these representations is `X-Rep-Hints: [...]` where `[...]` is one or many hints in the format `[fileType?query]`. For example, to request a `png` representation in `32x32` as well as `64x64` pixel dimensions "
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}",
    "path": [
     "file_id"
    ],
    "query": [
     "fields"
    ],
    "header": [
     "if-none-match",
     "boxapi",
     "x-rep-hints"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_app_item_associations",
   "name": "List file app item associations",
   "description": "**This is a beta feature, which means that its availability might be limited.** Returns all app items the file is associated with. This includes app items associated with ancestors of the file. Assuming the context user has access to the file, the type/ids are revealed even if the context user does not have **View** permission on the app item.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "application_type": {
      "type": "string",
      "description": "If given, only return app items for this application type."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/app_item_associations",
    "path": [
     "file_id"
    ],
    "query": [
     "limit",
     "marker",
     "application_type"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_collaborations",
   "name": "List file collaborations",
   "description": "Retrieves a list of pending and active collaborations for a file. This returns all the users that have access to the file or have been invited to the file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/collaborations",
    "path": [
     "file_id"
    ],
    "query": [
     "fields",
     "limit",
     "marker"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_comments",
   "name": "List file comments",
   "description": "Retrieves a list of comments for a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/comments",
    "path": [
     "file_id"
    ],
    "query": [
     "fields",
     "limit",
     "offset"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_content",
   "name": "Download file",
   "description": "Returns the contents of a file in binary format.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "range": {
      "type": "string",
      "description": "The byte range of the content to download. The format `bytes={start_byte}-{end_byte}` can be used to specify what section of the file to download."
     },
     "boxapi": {
      "type": "string",
      "description": "The URL, and optional password, for the shared link of this item. This header can be used to access items that have not been explicitly shared with a user. Use the format `shared_link=[link]` or if a password is required then use `shared_link=[link]&shared_link_password=[password]`. This header can be used on the file or folder shared, as well as on any files or folders nested within the item."
     },
     "version": {
      "type": "string",
      "description": "The file version to download."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/content",
    "path": [
     "file_id"
    ],
    "query": [
     "version"
    ],
    "header": [
     "range",
     "boxapi"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_get_shared_link",
   "name": "Get shared link for file",
   "description": "Gets the information for a shared link on a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     }
    },
    "required": [
     "fields",
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}#get_shared_link",
    "path": [
     "file_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_metadata",
   "name": "List metadata instances on file",
   "description": "Retrieves all metadata for a given file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "view": {
      "type": "string",
      "description": "Taxonomy field values are returned in `API view` by default, meaning the value is represented with a taxonomy node identifier. To retrieve the `Hydrated view`, where taxonomy values are represented with the full taxonomy node information, set this parameter to `hydrated`. This is the only supported value for this parameter."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata",
    "path": [
     "file_id"
    ],
    "query": [
     "view"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_metadata_enterprise_security_classification_6_vmvochw_uwo",
   "name": "Get classification on file",
   "description": "Retrieves the classification metadata instance that has been applied to a file. This API can also be called by including the enterprise ID in the URL explicitly, for example `/files/:id//enterprise_12345/securityClassification-6VMVochwUWo`.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_metadata_global_box_skills_cards",
   "name": "List box skill cards on file",
   "description": "List the Box Skills metadata cards that are attached to a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/global/boxSkillsCards",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_metadata_id_id",
   "name": "Get metadata instance on file",
   "description": "Retrieves the instance of a metadata template that has been applied to a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     },
     "view": {
      "type": "string",
      "description": "Taxonomy field values are returned in `API view` by default, meaning the value is represented with a taxonomy node identifier. To retrieve the `Hydrated view`, where taxonomy values are represented with the full taxonomy node information, set this parameter to `hydrated`. This is the only supported value for this parameter."
     }
    },
    "required": [
     "file_id",
     "scope",
     "template_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/{scope}/{template_key}",
    "path": [
     "file_id",
     "scope",
     "template_key"
    ],
    "query": [
     "view"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_tasks",
   "name": "List tasks on file",
   "description": "Retrieves a list of all the tasks for a file. This endpoint does not support pagination.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/tasks",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_thumbnail_id",
   "name": "Get file thumbnail",
   "description": "Retrieves a thumbnail, or smaller image representation, of a file. Sizes of `32x32`,`64x64`, `128x128`, and `256x256` can be returned in the `.png` format and sizes of `32x32`, `160x160`, and `320x320` can be returned in the `.jpg` format. Thumbnails can be generated for the image and video file formats listed [found on our community site][1]. [1]: https://community.box.com/t5/Migrating-and-Previe",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "extension": {
      "type": "string",
      "description": "The file format for the thumbnail.",
      "enum": [
       "png",
       "jpg"
      ]
     },
     "min_height": {
      "type": "integer",
      "description": "The minimum height of the thumbnail."
     },
     "min_width": {
      "type": "integer",
      "description": "The minimum width of the thumbnail."
     },
     "max_height": {
      "type": "integer",
      "description": "The maximum height of the thumbnail."
     },
     "max_width": {
      "type": "integer",
      "description": "The maximum width of the thumbnail."
     }
    },
    "required": [
     "extension",
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/thumbnail.{extension}",
    "path": [
     "file_id",
     "extension"
    ],
    "query": [
     "min_height",
     "min_width",
     "max_height",
     "max_width"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_trash",
   "name": "Get trashed file",
   "description": "Retrieves a file that has been moved to the trash. Please note that only if the file itself has been moved to the trash can it be retrieved with this API call. If instead one of its parent folders was moved to the trash, only that folder can be inspected using the [`GET /folders/:id/trash`](https://developer.box.com/reference/get-folders-id-trash) API. To list all items that have been moved to the",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/trash",
    "path": [
     "file_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_versions",
   "name": "List all file versions",
   "description": "Retrieve a list of the past versions for a file. Versions are only tracked by Box users with premium accounts. To fetch the ID of the current version of a file, use the `GET /file/:id` API.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/versions",
    "path": [
     "file_id"
    ],
    "query": [
     "fields",
     "limit",
     "offset"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_versions_id",
   "name": "Get file version",
   "description": "Retrieve a specific version of a file. Versions are only tracked for Box users with premium accounts.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "file_version_id": {
      "type": "string",
      "description": "The ID of the file version."
     }
    },
    "required": [
     "file_id",
     "file_version_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/versions/{file_version_id}",
    "path": [
     "file_id",
     "file_version_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_id_watermark",
   "name": "Get watermark on file",
   "description": "Retrieve the watermark for a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/watermark",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_upload_sessions_id",
   "name": "Get upload session",
   "description": "Return information about an upload session. The actual endpoint URL is returned by the [`Create upload session`](https://developer.box.com/reference/post-files-upload-sessions) endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "upload_session_id": {
      "type": "string",
      "description": "The ID of the upload session."
     }
    },
    "required": [
     "upload_session_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/upload_sessions/{upload_session_id}",
    "path": [
     "upload_session_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_files_upload_sessions_id_parts",
   "name": "List parts",
   "description": "Return a list of the chunks uploaded to the upload session so far. The actual endpoint URL is returned by the [`Create upload session`](https://developer.box.com/reference/post-files-upload-sessions) and [`Get upload session`](https://developer.box.com/reference/get-files-upload-sessions-id) endpoints.",
   "input": {
    "type": "object",
    "properties": {
     "upload_session_id": {
      "type": "string",
      "description": "The ID of the upload session."
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "upload_session_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/upload_sessions/{upload_session_id}/parts",
    "path": [
     "upload_session_id"
    ],
    "query": [
     "offset",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folder_locks",
   "name": "List folder locks",
   "description": "Retrieves folder lock details for a given folder. You must be authenticated as the owner or co-owner of the folder to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folder_locks",
    "path": [],
    "query": [
     "folder_id"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folders_id",
   "name": "Get folder information",
   "description": "Retrieves details for a folder, including the first 100 entries in the folder. Passing `sort`, `direction`, `offset`, and `limit` parameters in query allows you to manage the list of returned [folder items](https://developer.box.com/reference/resources/folder--full#param-item-collection). To fetch more items within the folder, use the [Get items in a folder](https://developer.box.com/reference/get",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "if-none-match": {
      "type": "string",
      "description": "Ensures an item is only returned if it has changed. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `304 Not Modified` if the item has not changed since."
     },
     "boxapi": {
      "type": "string",
      "description": "The URL, and optional password, for the shared link of this item. This header can be used to access items that have not been explicitly shared with a user. Use the format `shared_link=[link]` or if a password is required then use `shared_link=[link]&shared_link_password=[password]`. This header can be used on the file or folder shared, as well as on any files or folders nested within the item."
     },
     "sort": {
      "type": "string",
      "description": "Defines the **second** attribute by which items are sorted. The folder type affects the way the items are sorted: * **Standard folder**: Items are always sorted by their `type` first, with folders listed before files, and files listed before web links. * **Root folder**: This parameter is not supported for marker-based pagination on the root folder (the folder with an `id` of `0`). * **Shared fold",
      "enum": [
       "id",
       "name",
       "date",
       "size"
      ]
     },
     "direction": {
      "type": "string",
      "description": "The direction to sort results in. This can be either in alphabetical ascending (`ASC`) or descending (`DESC`) order.",
      "enum": [
       "ASC",
       "DESC"
      ]
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Offset-based pagination is not guaranteed to work reliably for high offset values and may fail for large datasets. In those cases, reduce the number of items in the folder (for example, by restructuring the folder into smaller subfolders) before retrying the request."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folders/{folder_id}",
    "path": [
     "folder_id"
    ],
    "query": [
     "fields",
     "sort",
     "direction",
     "offset",
     "limit"
    ],
    "header": [
     "if-none-match",
     "boxapi"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folders_id_app_item_associations",
   "name": "List folder app item associations",
   "description": "**This is a beta feature, which means that its availability might be limited.** Returns all app items the folder is associated with. This includes app items associated with ancestors of the folder. Assuming the context user has access to the folder, the type/ids are revealed even if the context user does not have **View** permission on the app item.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "application_type": {
      "type": "string",
      "description": "If given, returns only app items for this application type."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folders/{folder_id}/app_item_associations",
    "path": [
     "folder_id"
    ],
    "query": [
     "limit",
     "marker",
     "application_type"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folders_id_collaborations",
   "name": "List folder collaborations",
   "description": "Retrieves a list of pending and active collaborations for a folder. This returns all the users that have access to the folder or have been invited to the folder.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folders/{folder_id}/collaborations",
    "path": [
     "folder_id"
    ],
    "query": [
     "fields",
     "limit",
     "marker"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folders_id_get_shared_link",
   "name": "Get shared link for folder",
   "description": "Gets the information for a shared link on a folder.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     }
    },
    "required": [
     "fields",
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folders/{folder_id}#get_shared_link",
    "path": [
     "folder_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folders_id_items",
   "name": "List items in folder",
   "description": "Retrieves a page of items in a folder. These items can be files, folders, and web links. To request more information about the folder itself, like its size, use the [Get a folder](https://developer.box.com/reference/get-folders-id) endpoint instead.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "usemarker": {
      "type": "boolean",
      "description": "Specifies whether to use marker-based pagination instead of offset-based pagination. Only one pagination method can be used at a time. By setting this value to true, the API will return a `marker` field that can be passed as a parameter to this endpoint to get the next page of the response."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Offset-based pagination is not guaranteed to work reliably for high offset values and may fail for large datasets. In those cases, use marker-based pagination by setting `usemarker` to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "boxapi": {
      "type": "string",
      "description": "The URL, and optional password, for the shared link of this item. This header can be used to access items that have not been explicitly shared with a user. Use the format `shared_link=[link]` or if a password is required then use `shared_link=[link]&shared_link_password=[password]`. This header can be used on the file or folder shared, as well as on any files or folders nested within the item."
     },
     "sort": {
      "type": "string",
      "description": "Defines the **second** attribute by which items are sorted. The folder type affects the way the items are sorted: * **Standard folder**: Items are always sorted by their `type` first, with folders listed before files, and files listed before web links. * **Root folder**: This parameter is not supported for marker-based pagination on the root folder (the folder with an `id` of `0`). * **Shared fold",
      "enum": [
       "id",
       "name",
       "date",
       "size"
      ]
     },
     "direction": {
      "type": "string",
      "description": "The direction to sort results in. This can be either in alphabetical ascending (`ASC`) or descending (`DESC`) order.",
      "enum": [
       "ASC",
       "DESC"
      ]
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folders/{folder_id}/items",
    "path": [
     "folder_id"
    ],
    "query": [
     "fields",
     "usemarker",
     "marker",
     "offset",
     "limit",
     "sort",
     "direction"
    ],
    "header": [
     "boxapi"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folders_id_metadata",
   "name": "List metadata instances on folder",
   "description": "Retrieves all metadata for a given folder. This can not be used on the root folder with ID `0`.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "view": {
      "type": "string",
      "description": "Taxonomy field values are returned in `API view` by default, meaning the value is represented with a taxonomy node identifier. To retrieve the `Hydrated view`, where taxonomy values are represented with the full taxonomy node information, set this parameter to `hydrated`. This is the only supported value for this parameter."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folders/{folder_id}/metadata",
    "path": [
     "folder_id"
    ],
    "query": [
     "view"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folders_id_metadata_enterprise_security_classification_6_vmvochw_uwo",
   "name": "Get classification on folder",
   "description": "Retrieves the classification metadata instance that has been applied to a folder. This API can also be called by including the enterprise ID in the URL explicitly, for example `/folders/:id/enterprise_12345/securityClassification-6VMVochwUWo`.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folders/{folder_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "path": [
     "folder_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folders_id_metadata_id_id",
   "name": "Get metadata instance on folder",
   "description": "Retrieves the instance of a metadata template that has been applied to a folder. This can not be used on the root folder with ID `0`.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     }
    },
    "required": [
     "folder_id",
     "scope",
     "template_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folders/{folder_id}/metadata/{scope}/{template_key}",
    "path": [
     "folder_id",
     "scope",
     "template_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folders_id_trash",
   "name": "Get trashed folder",
   "description": "Retrieves a folder that has been moved to the trash. Please note that only if the folder itself has been moved to the trash can it be retrieved with this API call. If instead one of its parent folders was moved to the trash, only that folder can be inspected using the [`GET /folders/:id/trash`](https://developer.box.com/reference/get-folders-id-trash) API. To list all items that have been moved to",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folders/{folder_id}/trash",
    "path": [
     "folder_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folders_id_watermark",
   "name": "Get watermark for folder",
   "description": "Retrieve the watermark for a folder.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folders/{folder_id}/watermark",
    "path": [
     "folder_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_folders_trash_items",
   "name": "List trashed items",
   "description": "Retrieves the files and folders that have been moved to the trash. Any attribute in the full files or folders objects can be passed in with the `fields` parameter to retrieve those specific attributes that are not returned by default. This endpoint defaults to use offset-based pagination, yet also supports marker-based pagination using the `marker` parameter. The number of entries returned may be ",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     },
     "usemarker": {
      "type": "boolean",
      "description": "Specifies whether to use marker-based pagination instead of offset-based pagination. Only one pagination method can be used at a time. By setting this value to true, the API will return a `marker` field that can be passed as a parameter to this endpoint to get the next page of the response."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "direction": {
      "type": "string",
      "description": "The direction to sort results in. This can be either in alphabetical ascending (`ASC`) or descending (`DESC`) order.",
      "enum": [
       "ASC",
       "DESC"
      ]
     },
     "sort": {
      "type": "string",
      "description": "Defines the **second** attribute by which items are sorted. Items are always sorted by their `type` first, with folders listed before files, and files listed before web links. This parameter is not supported when using marker-based pagination.",
      "enum": [
       "name",
       "date",
       "size"
      ]
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/folders/trash/items",
    "path": [],
    "query": [
     "fields",
     "limit",
     "offset",
     "usemarker",
     "marker",
     "direction",
     "sort"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_group_memberships_id",
   "name": "Get group membership",
   "description": "Retrieves a specific group membership. Only admins of this group or users with admin-level permissions will be able to use this API.",
   "input": {
    "type": "object",
    "properties": {
     "group_membership_id": {
      "type": "string",
      "description": "The ID of the group membership."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "group_membership_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/group_memberships/{group_membership_id}",
    "path": [
     "group_membership_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_groups",
   "name": "List groups for enterprise",
   "description": "Retrieves all of the groups for a given enterprise. The user must have admin permissions to inspect enterprise's groups.",
   "input": {
    "type": "object",
    "properties": {
     "filter_term": {
      "type": "string",
      "description": "Limits the results to only groups whose `name` starts with the search term."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/groups",
    "path": [],
    "query": [
     "filter_term",
     "fields",
     "limit",
     "offset"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_groups_id",
   "name": "Get group",
   "description": "Retrieves information about a group. Only members of this group or users with admin-level permissions will be able to use this API.",
   "input": {
    "type": "object",
    "properties": {
     "group_id": {
      "type": "string",
      "description": "The ID of the group."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "group_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/groups/{group_id}",
    "path": [
     "group_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_groups_id_collaborations",
   "name": "List group collaborations",
   "description": "Retrieves all the collaborations for a group. The user must have admin permissions to inspect enterprise's groups. Each collaboration object has details on which files or folders the group has access to and with what role.",
   "input": {
    "type": "object",
    "properties": {
     "group_id": {
      "type": "string",
      "description": "The ID of the group."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     }
    },
    "required": [
     "group_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/groups/{group_id}/collaborations",
    "path": [
     "group_id"
    ],
    "query": [
     "limit",
     "offset"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_groups_id_memberships",
   "name": "List members of group",
   "description": "Retrieves all the members for a group. Only members of this group or users with admin-level permissions will be able to use this API.",
   "input": {
    "type": "object",
    "properties": {
     "group_id": {
      "type": "string",
      "description": "The ID of the group."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     }
    },
    "required": [
     "group_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/groups/{group_id}/memberships",
    "path": [
     "group_id"
    ],
    "query": [
     "limit",
     "offset"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_integration_mappings_slack",
   "name": "List slack integration mappings",
   "description": "Lists [Slack integration mappings](https://support.box.com/hc/en-us/articles/4415585987859-Box-as-the-Content-Layer-for-Slack) in a users' enterprise. You need Admin or Co-Admin role to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "partner_item_type": {
      "type": "string",
      "description": "Mapped item type, for which the mapping should be returned.",
      "enum": [
       "channel"
      ]
     },
     "partner_item_id": {
      "type": "string",
      "description": "ID of the mapped item, for which the mapping should be returned."
     },
     "box_item_id": {
      "type": "string",
      "description": "Box item ID, for which the mappings should be returned."
     },
     "box_item_type": {
      "type": "string",
      "description": "Box item type, for which the mappings should be returned.",
      "enum": [
       "folder"
      ]
     },
     "is_manually_created": {
      "type": "boolean",
      "description": "Whether the mapping has been manually created."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/integration_mappings/slack",
    "path": [],
    "query": [
     "marker",
     "limit",
     "partner_item_type",
     "partner_item_id",
     "box_item_id",
     "box_item_type",
     "is_manually_created"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_integration_mappings_teams",
   "name": "List teams integration mappings",
   "description": "Lists [Teams integration mappings](https://support.box.com/hc/en-us/articles/360044681474-Using-Box-for-Teams) in a users' enterprise. You need Admin or Co-Admin role to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "partner_item_type": {
      "type": "string",
      "description": "Mapped item type, for which the mapping should be returned.",
      "enum": [
       "channel",
       "team"
      ]
     },
     "partner_item_id": {
      "type": "string",
      "description": "ID of the mapped item, for which the mapping should be returned."
     },
     "box_item_id": {
      "type": "string",
      "description": "Box item ID, for which the mappings should be returned."
     },
     "box_item_type": {
      "type": "string",
      "description": "Box item type, for which the mappings should be returned.",
      "enum": [
       "folder"
      ]
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/integration_mappings/teams",
    "path": [],
    "query": [
     "partner_item_type",
     "partner_item_id",
     "box_item_id",
     "box_item_type"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_invites_id",
   "name": "Get user invite status",
   "description": "Returns the status of a user invite.",
   "input": {
    "type": "object",
    "properties": {
     "invite_id": {
      "type": "string",
      "description": "The ID of an invite."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "invite_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/invites/{invite_id}",
    "path": [
     "invite_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_legal_hold_policies",
   "name": "List all legal hold policies",
   "description": "Retrieves a list of legal hold policies that belong to an enterprise.",
   "input": {
    "type": "object",
    "properties": {
     "policy_name": {
      "type": "string",
      "description": "Limits results to policies for which the names start with this search term. This is a case-insensitive prefix."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/legal_hold_policies",
    "path": [],
    "query": [
     "policy_name",
     "fields",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_legal_hold_policies_id",
   "name": "Get legal hold policy",
   "description": "Retrieve a legal hold policy.",
   "input": {
    "type": "object",
    "properties": {
     "legal_hold_policy_id": {
      "type": "string",
      "description": "The ID of the legal hold policy."
     }
    },
    "required": [
     "legal_hold_policy_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/legal_hold_policies/{legal_hold_policy_id}",
    "path": [
     "legal_hold_policy_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_legal_hold_policy_assignments",
   "name": "List legal hold policy assignments",
   "description": "Retrieves a list of items a legal hold policy has been assigned to.",
   "input": {
    "type": "object",
    "properties": {
     "policy_id": {
      "type": "string",
      "description": "The ID of the legal hold policy."
     },
     "assign_to_type": {
      "type": "string",
      "description": "Filters the results by the type of item the policy was applied to.",
      "enum": [
       "file",
       "file_version",
       "folder",
       "user",
       "ownership",
       "interactions"
      ]
     },
     "assign_to_id": {
      "type": "string",
      "description": "Filters the results by the ID of item the policy was applied to."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "policy_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/legal_hold_policy_assignments",
    "path": [],
    "query": [
     "policy_id",
     "assign_to_type",
     "assign_to_id",
     "marker",
     "limit",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_legal_hold_policy_assignments_id",
   "name": "Get legal hold policy assignment",
   "description": "Retrieve a legal hold policy assignment.",
   "input": {
    "type": "object",
    "properties": {
     "legal_hold_policy_assignment_id": {
      "type": "string",
      "description": "The ID of the legal hold policy assignment."
     }
    },
    "required": [
     "legal_hold_policy_assignment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}",
    "path": [
     "legal_hold_policy_assignment_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_legal_hold_policy_assignments_id_file_versions_on_hold",
   "name": "List previous file versions for legal hold policy assignment",
   "description": "Get a list of previous file versions for a legal hold assignment. In some cases you may only need the latest file versions instead. In these cases, use the `GET /legal_hold_policy_assignments/:id/files_on_hold` API instead to return any current (latest) versions of a file for this legal hold policy assignment. Due to ongoing re-architecture efforts this API might not return all files held for this",
   "input": {
    "type": "object",
    "properties": {
     "legal_hold_policy_assignment_id": {
      "type": "string",
      "description": "The ID of the legal hold policy assignment."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "legal_hold_policy_assignment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}/file_versions_on_hold",
    "path": [
     "legal_hold_policy_assignment_id"
    ],
    "query": [
     "marker",
     "limit",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_legal_hold_policy_assignments_id_files_on_hold",
   "name": "List files with current file versions for legal hold policy assignment",
   "description": "Get a list of files with current file versions for a legal hold assignment. In some cases you may want to get previous file versions instead. In these cases, use the `GET /legal_hold_policy_assignments/:id/file_versions_on_hold` API instead to return any previous versions of a file for this legal hold policy assignment. Due to ongoing re-architecture efforts this API might not return all file vers",
   "input": {
    "type": "object",
    "properties": {
     "legal_hold_policy_assignment_id": {
      "type": "string",
      "description": "The ID of the legal hold policy assignment."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "legal_hold_policy_assignment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}/files_on_hold",
    "path": [
     "legal_hold_policy_assignment_id"
    ],
    "query": [
     "marker",
     "limit",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_cascade_policies",
   "name": "List metadata cascade policies",
   "description": "Retrieves a list of all the metadata cascade policies that are applied to a given folder. This can not be used on the root folder with ID `0`.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "Specifies which folder to return policies for. This can not be used on the root folder with ID `0`."
     },
     "owner_enterprise_id": {
      "type": "string",
      "description": "The ID of the enterprise ID for which to find metadata cascade policies. If not specified, it defaults to the current enterprise."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_cascade_policies",
    "path": [],
    "query": [
     "folder_id",
     "owner_enterprise_id",
     "marker",
     "offset"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_cascade_policies_id",
   "name": "Get metadata cascade policy",
   "description": "Retrieve a specific metadata cascade policy assigned to a folder.",
   "input": {
    "type": "object",
    "properties": {
     "metadata_cascade_policy_id": {
      "type": "string",
      "description": "The ID of the metadata cascade policy."
     }
    },
    "required": [
     "metadata_cascade_policy_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_cascade_policies/{metadata_cascade_policy_id}",
    "path": [
     "metadata_cascade_policy_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_taxonomies_id",
   "name": "Get metadata taxonomies for namespace",
   "description": "Used to retrieve all metadata taxonomies in a namespace.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "namespace"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}",
    "path": [
     "namespace"
    ],
    "query": [
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_taxonomies_id_id",
   "name": "Get metadata taxonomy by taxonomy key",
   "description": "Used to retrieve a metadata taxonomy by taxonomy key.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     }
    },
    "required": [
     "namespace",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}",
    "path": [
     "namespace",
     "taxonomy_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_taxonomies_id_id_nodes",
   "name": "List metadata taxonomy nodes",
   "description": "Used to retrieve metadata taxonomy nodes based on the parameters specified. Results are sorted in lexicographic order unless a `query` parameter is passed. With a `query` parameter specified, results are sorted in order of relevance.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     },
     "level": {
      "type": "array",
      "description": "Filters results by taxonomy level. Multiple values can be provided. Results include nodes that match any of the specified values."
     },
     "parent": {
      "type": "array",
      "description": "Node identifier of a direct parent node. Multiple values can be provided. Results include nodes that match any of the specified values."
     },
     "ancestor": {
      "type": "array",
      "description": "Node identifier of any ancestor node. Multiple values can be provided. Results include nodes that match any of the specified values."
     },
     "query": {
      "type": "string",
      "description": "Query text to search for the taxonomy nodes."
     },
     "include-total-result-count": {
      "type": "boolean",
      "description": "When set to `true` this provides the total number of nodes that matched the query. The response will compute counts of up to 10,000 elements. Defaults to `false`."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "namespace",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}/nodes",
    "path": [
     "namespace",
     "taxonomy_key"
    ],
    "query": [
     "level",
     "parent",
     "ancestor",
     "query",
     "include-total-result-count",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_taxonomies_id_id_nodes_id",
   "name": "Get metadata taxonomy node by id",
   "description": "Retrieves a metadata taxonomy node by its identifier.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     },
     "node_id": {
      "type": "string",
      "description": "The identifier of the metadata taxonomy node."
     }
    },
    "required": [
     "namespace",
     "node_id",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}/nodes/{node_id}",
    "path": [
     "namespace",
     "taxonomy_key",
     "node_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_templates",
   "name": "Find metadata template by instance id",
   "description": "Finds a metadata template by searching for the ID of an instance of the template.",
   "input": {
    "type": "object",
    "properties": {
     "metadata_instance_id": {
      "type": "string",
      "description": "The ID of an instance of the metadata template to find."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "metadata_instance_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_templates",
    "path": [],
    "query": [
     "metadata_instance_id",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_templates_enterprise",
   "name": "List all metadata templates for enterprise",
   "description": "Used to retrieve all metadata templates created to be used specifically within the user's enterprise.",
   "input": {
    "type": "object",
    "properties": {
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_templates/enterprise",
    "path": [],
    "query": [
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_templates_enterprise_security_classification_6_vmvochw_uwo_schema",
   "name": "List all classifications",
   "description": "Retrieves the classification metadata template and lists all the classifications available to this enterprise. This API can also be called by including the enterprise ID in the URL explicitly, for example `/metadata_templates/enterprise_12345/securityClassification-6VMVochwUWo/schema`.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_templates/enterprise/securityClassification-6VMVochwUWo/schema",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_templates_global",
   "name": "List all global metadata templates",
   "description": "Used to retrieve all generic, global metadata templates available to all enterprises using Box.",
   "input": {
    "type": "object",
    "properties": {
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_templates/global",
    "path": [],
    "query": [
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_templates_id",
   "name": "Get metadata template by id",
   "description": "Retrieves a metadata template by its ID.",
   "input": {
    "type": "object",
    "properties": {
     "template_id": {
      "type": "string",
      "description": "The ID of the template."
     }
    },
    "required": [
     "template_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_templates/{template_id}",
    "path": [
     "template_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_templates_id_id_fields_id_options",
   "name": "List metadata template's options for taxonomy field",
   "description": "Used to retrieve metadata taxonomy nodes which are available for the taxonomy field based on its configuration and the parameters specified. Results are sorted in lexicographic order unless a `query` parameter is passed. With a `query` parameter specified, results are sorted in order of relevance.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     },
     "field_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy field in the template."
     },
     "level": {
      "type": "array",
      "description": "Filters results by taxonomy level. Multiple values can be provided. Results include nodes that match any of the specified values."
     },
     "parent": {
      "type": "array",
      "description": "Node identifier of a direct parent node. Multiple values can be provided. Results include nodes that match any of the specified values."
     },
     "ancestor": {
      "type": "array",
      "description": "Node identifier of any ancestor node. Multiple values can be provided. Results include nodes that match any of the specified values."
     },
     "query": {
      "type": "string",
      "description": "Query text to search for the taxonomy nodes."
     },
     "include-total-result-count": {
      "type": "boolean",
      "description": "When set to `true` this provides the total number of nodes that matched the query. The response will compute counts of up to 10,000 elements. Defaults to `false`."
     },
     "only-selectable-options": {
      "type": "boolean",
      "description": "When set to `true`, this only returns valid selectable options for this template taxonomy field. Otherwise, it returns all taxonomy nodes, whether or not they are selectable. Defaults to `true`."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "field_key",
     "namespace",
     "template_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_templates/{namespace}/{template_key}/fields/{field_key}/options",
    "path": [
     "namespace",
     "template_key",
     "field_key"
    ],
    "query": [
     "level",
     "parent",
     "ancestor",
     "query",
     "include-total-result-count",
     "only-selectable-options",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_metadata_templates_id_id_schema",
   "name": "Get metadata template by name",
   "description": "Retrieves a metadata template by its `scope` and `templateKey` values. To find the `scope` and `templateKey` for a template, list all templates for an enterprise or globally, or list all templates applied to a file or folder.",
   "input": {
    "type": "object",
    "properties": {
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     }
    },
    "required": [
     "scope",
     "template_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/metadata_templates/{scope}/{template_key}/schema",
    "path": [
     "scope",
     "template_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_recent_items",
   "name": "List recently accessed items",
   "description": "Returns information about the recent items accessed by a user, either in the last 90 days or up to the last 1000 items accessed.",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/recent_items",
    "path": [],
    "query": [
     "fields",
     "limit",
     "marker"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_retention_policies",
   "name": "List retention policies",
   "description": "Retrieves all of the retention policies for an enterprise.",
   "input": {
    "type": "object",
    "properties": {
     "policy_name": {
      "type": "string",
      "description": "Filters results by a case sensitive prefix of the name of retention policies."
     },
     "policy_type": {
      "type": "string",
      "description": "Filters results by the type of retention policy.",
      "enum": [
       "finite",
       "indefinite"
      ]
     },
     "created_by_user_id": {
      "type": "string",
      "description": "Filters results by the ID of the user who created policy."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/retention_policies",
    "path": [],
    "query": [
     "policy_name",
     "policy_type",
     "created_by_user_id",
     "fields",
     "limit",
     "marker"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_retention_policies_id",
   "name": "Get retention policy",
   "description": "Retrieves a retention policy.",
   "input": {
    "type": "object",
    "properties": {
     "retention_policy_id": {
      "type": "string",
      "description": "The ID of the retention policy."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "retention_policy_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/retention_policies/{retention_policy_id}",
    "path": [
     "retention_policy_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_retention_policies_id_assignments",
   "name": "List retention policy assignments",
   "description": "Returns a list of all retention policy assignments associated with a specified retention policy.",
   "input": {
    "type": "object",
    "properties": {
     "retention_policy_id": {
      "type": "string",
      "description": "The ID of the retention policy."
     },
     "type": {
      "type": "string",
      "description": "The type of the retention policy assignment to retrieve.",
      "enum": [
       "folder",
       "enterprise",
       "metadata_template"
      ]
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "retention_policy_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/retention_policies/{retention_policy_id}/assignments",
    "path": [
     "retention_policy_id"
    ],
    "query": [
     "type",
     "fields",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_retention_policy_assignments_id",
   "name": "Get retention policy assignment",
   "description": "Retrieves a retention policy assignment.",
   "input": {
    "type": "object",
    "properties": {
     "retention_policy_assignment_id": {
      "type": "string",
      "description": "The ID of the retention policy assignment."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "retention_policy_assignment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/retention_policy_assignments/{retention_policy_assignment_id}",
    "path": [
     "retention_policy_assignment_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_retention_policy_assignments_id_file_versions_under_retention",
   "name": "Get file versions under retention",
   "description": "Returns a list of file versions under retention for a retention policy assignment.",
   "input": {
    "type": "object",
    "properties": {
     "retention_policy_assignment_id": {
      "type": "string",
      "description": "The ID of the retention policy assignment."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "retention_policy_assignment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/retention_policy_assignments/{retention_policy_assignment_id}/file_versions_under_retention",
    "path": [
     "retention_policy_assignment_id"
    ],
    "query": [
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_retention_policy_assignments_id_files_under_retention",
   "name": "Get files under retention",
   "description": "Returns a list of files under retention for a retention policy assignment.",
   "input": {
    "type": "object",
    "properties": {
     "retention_policy_assignment_id": {
      "type": "string",
      "description": "The ID of the retention policy assignment."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "retention_policy_assignment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/retention_policy_assignments/{retention_policy_assignment_id}/files_under_retention",
    "path": [
     "retention_policy_assignment_id"
    ],
    "query": [
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_search",
   "name": "Search for content",
   "description": "Searches for files, folders, web links, and shared files across the users content or across the entire enterprise.",
   "input": {
    "type": "object",
    "properties": {
     "query": {
      "type": "string",
      "description": "The string to search for. This query is matched against item names, descriptions, text content of files, and various other fields of the different item types. This parameter supports a variety of operators to further refine the results returns. * `\"\"` - by wrapping a query in double quotes only exact matches are returned by the API. Exact searches do not return search matches based on specific cha"
     },
     "scope": {
      "type": "string",
      "description": "Limits the search results to either the files that the user has access to, or to files available to the entire enterprise. The scope defaults to `user_content`, which limits the search results to content that is available to the currently authenticated user. The `enterprise_content` can be requested by an admin through our support channels. Once this scope has been enabled for a user, it will allo",
      "enum": [
       "user_content",
       "enterprise_content"
      ]
     },
     "file_extensions": {
      "type": "array",
      "description": "Limits the search results to any files that match any of the provided file extensions. This list is a comma-separated list of file extensions without the dots."
     },
     "created_at_range": {
      "type": "array",
      "description": "Limits the search results to any items created within a given date range. Date ranges are defined as comma separated RFC3339 timestamps. If the start date is omitted (`,2014-05-17T13:35:01-07:00`) anything created before the end date will be returned. If the end date is omitted (`2014-05-15T13:35:01-07:00,`) the current date will be used as the end date instead."
     },
     "updated_at_range": {
      "type": "array",
      "description": "Limits the search results to any items updated within a given date range. Date ranges are defined as comma separated RFC3339 timestamps. If the start date is omitted (`,2014-05-17T13:35:01-07:00`) anything updated before the end date will be returned. If the end date is omitted (`2014-05-15T13:35:01-07:00,`) the current date will be used as the end date instead."
     },
     "size_range": {
      "type": "array",
      "description": "Limits the search results to any items with a size within a given file size range. This applied to files and folders. Size ranges are defined as comma separated list of a lower and upper byte size limit (inclusive). The upper and lower bound can be omitted to create open ranges."
     },
     "owner_user_ids": {
      "type": "array",
      "description": "Limits the search results to any items that are owned by the given list of owners, defined as a list of comma separated user IDs. The items still need to be owned or shared with the currently authenticated user for them to show up in the search results. If the user does not have access to any files owned by any of the users an empty result set will be returned. To search across an entire enterpris"
     },
     "recent_updater_user_ids": {
      "type": "array",
      "description": "Limits the search results to any items that have been updated by the given list of users, defined as a list of comma separated user IDs. The items still need to be owned or shared with the currently authenticated user for them to show up in the search results. If the user does not have access to any files owned by any of the users an empty result set will be returned. This feature only searches ba"
     },
     "ancestor_folder_ids": {
      "type": "array",
      "description": "Limits the search results to items within the given list of folders, defined as a comma separated lists of folder IDs. Search results will also include items within any subfolders of those ancestor folders. The folders still need to be owned or shared with the currently authenticated user. If the folder is not accessible by this user, or it does not exist, a `HTTP 404` error code will be returned "
     },
     "content_types": {
      "type": "array",
      "description": "Limits the search results to any items that match the search query for a specific part of the file, for example the file description. Content types are defined as a comma separated lists of Box recognized content types. The allowed content types are as follows. * `name` - The name of the item, as defined by its `name` field. * `description` - The description of the item, as defined by its `descrip"
     },
     "type": {
      "type": "string",
      "description": "Limits the search results to any items of this type. This parameter only takes one value. By default the API returns items that match any of these types. * `file` - Limits the search results to files, * `folder` - Limits the search results to folders, * `web_link` - Limits the search results to web links, also known as bookmarks.",
      "enum": [
       "file",
       "folder",
       "web_link"
      ]
     },
     "trash_content": {
      "type": "string",
      "description": "Determines if the search should look in the trash for items. By default, this API only returns search results for items not currently in the trash (`non_trashed_only`). * `trashed_only` - Only searches for items currently in the trash * `non_trashed_only` - Only searches for items currently not in the trash * `all_items` - Searches for both trashed and non-trashed items.",
      "enum": [
       "non_trashed_only",
       "trashed_only",
       "all_items"
      ]
     },
     "mdfilters": {
      "type": "array",
      "description": "Limits the search results to any items for which the metadata matches the provided filter. This parameter is a list that specifies exactly **one** metadata template used to filter the search results. The parameter is required unless the `query` parameter is provided."
     },
     "sort": {
      "type": "string",
      "description": "Defines the order in which search results are returned. This API defaults to returning items by relevance unless this parameter is explicitly specified. * `relevance` (default) returns the results sorted by relevance to the query search term. The relevance is based on the occurrence of the search term in the items name, description, content, and additional properties. * `modified_at` returns the r",
      "enum": [
       "modified_at",
       "relevance"
      ]
     },
     "direction": {
      "type": "string",
      "description": "Defines the direction in which search results are ordered. This API defaults to returning items in descending (`DESC`) order unless this parameter is explicitly specified. When results are sorted by `relevance` the ordering is locked to returning items in descending order of relevance, and this parameter is ignored.",
      "enum": [
       "DESC",
       "ASC"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "Defines the maximum number of items to return as part of a page of results."
     },
     "include_recent_shared_links": {
      "type": "boolean",
      "description": "Defines whether the search results should include any items that the user recently accessed through a shared link. When this parameter has been set to true, the format of the response of this API changes to return a list of [Search Results with Shared Links](https://developer.box.com/reference/resources/search-results-with-shared-links)."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     },
     "deleted_user_ids": {
      "type": "array",
      "description": "Limits the search results to items that were deleted by the given list of users, defined as a list of comma separated user IDs. The `trash_content` parameter needs to be set to `trashed_only`. If searching in trash is not performed, an empty result set is returned. The items need to be owned or shared with the currently authenticated user for them to show up in the search results. If the user does"
     },
     "deleted_at_range": {
      "type": "array",
      "description": "Limits the search results to any items deleted within a given date range. Date ranges are defined as comma separated RFC3339 timestamps. If the start date is omitted (`2014-05-17T13:35:01-07:00`), anything deleted before the end date will be returned. If the end date is omitted (`2014-05-15T13:35:01-07:00`), the current date will be used as the end date instead. The `trash_content` parameter needs"
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/search",
    "path": [],
    "query": [
     "query",
     "scope",
     "file_extensions",
     "created_at_range",
     "updated_at_range",
     "size_range",
     "owner_user_ids",
     "recent_updater_user_ids",
     "ancestor_folder_ids",
     "content_types",
     "type",
     "trash_content",
     "mdfilters",
     "sort",
     "direction",
     "limit",
     "include_recent_shared_links",
     "fields",
     "offset",
     "deleted_user_ids",
     "deleted_at_range"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shared_items",
   "name": "Find file for shared link",
   "description": "Returns the file represented by a shared link. A shared file can be represented by a shared link, which can originate within the current enterprise or within another. This endpoint allows an application to retrieve information about a shared file when only given a shared link. The `shared_link_permission_options` array field can be returned by requesting it in the `fields` query parameter.",
   "input": {
    "type": "object",
    "properties": {
     "if-none-match": {
      "type": "string",
      "description": "Ensures an item is only returned if it has changed. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `304 Not Modified` if the item has not changed since."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "boxapi": {
      "type": "string",
      "description": "A header containing the shared link and optional password for the shared link. The format for this header is as follows: `shared_link=[link]&shared_link_password=[password]`."
     }
    },
    "required": [
     "boxapi"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shared_items",
    "path": [],
    "query": [
     "fields"
    ],
    "header": [
     "if-none-match",
     "boxapi"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shared_items_app_items",
   "name": "Find app item for shared link",
   "description": "Returns the app item represented by a shared link. The link can originate from the current enterprise or another.",
   "input": {
    "type": "object",
    "properties": {
     "boxapi": {
      "type": "string",
      "description": "A header containing the shared link and optional password for the shared link. The format for this header is `shared_link=[link]&shared_link_password=[password]`."
     }
    },
    "required": [
     "boxapi"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shared_items#app_items",
    "path": [],
    "query": [],
    "header": [
     "boxapi"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shared_items_folders",
   "name": "Find folder for shared link",
   "description": "Return the folder represented by a shared link. A shared folder can be represented by a shared link, which can originate within the current enterprise or within another. This endpoint allows an application to retrieve information about a shared folder when only given a shared link.",
   "input": {
    "type": "object",
    "properties": {
     "if-none-match": {
      "type": "string",
      "description": "Ensures an item is only returned if it has changed. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `304 Not Modified` if the item has not changed since."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "boxapi": {
      "type": "string",
      "description": "A header containing the shared link and optional password for the shared link. The format for this header is as follows: `shared_link=[link]&shared_link_password=[password]`."
     }
    },
    "required": [
     "boxapi"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shared_items#folders",
    "path": [],
    "query": [
     "fields"
    ],
    "header": [
     "if-none-match",
     "boxapi"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shared_items_web_links",
   "name": "Find web link for shared link",
   "description": "Returns the web link represented by a shared link. A shared web link can be represented by a shared link, which can originate within the current enterprise or within another. This endpoint allows an application to retrieve information about a shared web link when only given a shared link.",
   "input": {
    "type": "object",
    "properties": {
     "if-none-match": {
      "type": "string",
      "description": "Ensures an item is only returned if it has changed. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `304 Not Modified` if the item has not changed since."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "boxapi": {
      "type": "string",
      "description": "A header containing the shared link and optional password for the shared link. The format for this header is as follows: `shared_link=[link]&shared_link_password=[password]`."
     }
    },
    "required": [
     "boxapi"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shared_items#web_links",
    "path": [],
    "query": [
     "fields"
    ],
    "header": [
     "if-none-match",
     "boxapi"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shield_information_barrier_reports",
   "name": "List shield information barrier reports",
   "description": "Lists shield information barrier reports.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_id": {
      "type": "string",
      "description": "The ID of the shield information barrier."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "shield_information_barrier_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shield_information_barrier_reports",
    "path": [],
    "query": [
     "shield_information_barrier_id",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shield_information_barrier_reports_id",
   "name": "Get shield information barrier report by id",
   "description": "Retrieves a shield information barrier report by its ID.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_report_id": {
      "type": "string",
      "description": "The ID of the shield information barrier Report."
     }
    },
    "required": [
     "shield_information_barrier_report_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shield_information_barrier_reports/{shield_information_barrier_report_id}",
    "path": [
     "shield_information_barrier_report_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shield_information_barrier_segment_members",
   "name": "List shield information barrier segment members",
   "description": "Lists shield information barrier segment members based on provided segment IDs.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_segment_id": {
      "type": "string",
      "description": "The ID of the shield information barrier segment."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "shield_information_barrier_segment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shield_information_barrier_segment_members",
    "path": [],
    "query": [
     "shield_information_barrier_segment_id",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shield_information_barrier_segment_members_id",
   "name": "Get shield information barrier segment member by id",
   "description": "Retrieves a shield information barrier segment member by its ID.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_segment_member_id": {
      "type": "string",
      "description": "The ID of the shield information barrier segment Member."
     }
    },
    "required": [
     "shield_information_barrier_segment_member_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shield_information_barrier_segment_members/{shield_information_barrier_segment_member_id}",
    "path": [
     "shield_information_barrier_segment_member_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shield_information_barrier_segment_restrictions",
   "name": "List shield information barrier segment restrictions",
   "description": "Lists shield information barrier segment restrictions based on provided segment ID.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_segment_id": {
      "type": "string",
      "description": "The ID of the shield information barrier segment."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "shield_information_barrier_segment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shield_information_barrier_segment_restrictions",
    "path": [],
    "query": [
     "shield_information_barrier_segment_id",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shield_information_barrier_segment_restrictions_id",
   "name": "Get shield information barrier segment restriction by id",
   "description": "Retrieves a shield information barrier segment restriction based on provided ID.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_segment_restriction_id": {
      "type": "string",
      "description": "The ID of the shield information barrier segment Restriction."
     }
    },
    "required": [
     "shield_information_barrier_segment_restriction_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shield_information_barrier_segment_restrictions/{shield_information_barrier_segment_restriction_id}",
    "path": [
     "shield_information_barrier_segment_restriction_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shield_information_barrier_segments",
   "name": "List shield information barrier segments",
   "description": "Retrieves a list of shield information barrier segment objects for the specified Information Barrier ID.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_id": {
      "type": "string",
      "description": "The ID of the shield information barrier."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": [
     "shield_information_barrier_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shield_information_barrier_segments",
    "path": [],
    "query": [
     "shield_information_barrier_id",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shield_information_barrier_segments_id",
   "name": "Get shield information barrier segment with specified id",
   "description": "Retrieves shield information barrier segment based on provided ID..",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_segment_id": {
      "type": "string",
      "description": "The ID of the shield information barrier segment."
     }
    },
    "required": [
     "shield_information_barrier_segment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shield_information_barrier_segments/{shield_information_barrier_segment_id}",
    "path": [
     "shield_information_barrier_segment_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shield_information_barriers",
   "name": "List shield information barriers",
   "description": "Retrieves a list of shield information barrier objects for the enterprise of JWT.",
   "input": {
    "type": "object",
    "properties": {
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shield_information_barriers",
    "path": [],
    "query": [
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_shield_information_barriers_id",
   "name": "Get shield information barrier with specified id",
   "description": "Get shield information barrier based on provided ID.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_id": {
      "type": "string",
      "description": "The ID of the shield information barrier."
     }
    },
    "required": [
     "shield_information_barrier_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/shield_information_barriers/{shield_information_barrier_id}",
    "path": [
     "shield_information_barrier_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_sign_requests",
   "name": "List box sign requests",
   "description": "Gets signature requests created by a user. If the `sign_files` and/or `parent_folder` are deleted, the signature request will not return in the list.",
   "input": {
    "type": "object",
    "properties": {
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "senders": {
      "type": "array",
      "description": "A list of sender emails to filter the signature requests by sender. If provided, `shared_requests` must be set to `true`."
     },
     "shared_requests": {
      "type": "boolean",
      "description": "If set to `true`, only includes requests that user is not an owner, but user is a collaborator. Collaborator access is determined by the user access level of the sign files of the request. Default is `false`. Must be set to `true` if `senders` are provided."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/sign_requests",
    "path": [],
    "query": [
     "marker",
     "limit",
     "senders",
     "shared_requests"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_sign_requests_id",
   "name": "Get box sign request by id",
   "description": "Gets a sign request by ID.",
   "input": {
    "type": "object",
    "properties": {
     "sign_request_id": {
      "type": "string",
      "description": "The ID of the signature request."
     }
    },
    "required": [
     "sign_request_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/sign_requests/{sign_request_id}",
    "path": [
     "sign_request_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_sign_templates",
   "name": "List box sign templates",
   "description": "Gets Box Sign templates created by a user.",
   "input": {
    "type": "object",
    "properties": {
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/sign_templates",
    "path": [],
    "query": [
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_sign_templates_id",
   "name": "Get box sign template by id",
   "description": "Fetches details of a specific Box Sign template.",
   "input": {
    "type": "object",
    "properties": {
     "template_id": {
      "type": "string",
      "description": "The ID of a Box Sign template."
     }
    },
    "required": [
     "template_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/sign_templates/{template_id}",
    "path": [
     "template_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_storage_policies",
   "name": "List storage policies",
   "description": "Fetches all the storage policies in the enterprise.",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/storage_policies",
    "path": [],
    "query": [
     "fields",
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_storage_policies_id",
   "name": "Get storage policy",
   "description": "Fetches a specific storage policy.",
   "input": {
    "type": "object",
    "properties": {
     "storage_policy_id": {
      "type": "string",
      "description": "The ID of the storage policy."
     }
    },
    "required": [
     "storage_policy_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/storage_policies/{storage_policy_id}",
    "path": [
     "storage_policy_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_storage_policy_assignments",
   "name": "List storage policy assignments",
   "description": "Fetches all the storage policy assignment for an enterprise or user.",
   "input": {
    "type": "object",
    "properties": {
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "resolved_for_type": {
      "type": "string",
      "description": "The target type to return assignments for.",
      "enum": [
       "user",
       "enterprise"
      ]
     },
     "resolved_for_id": {
      "type": "string",
      "description": "The ID of the user or enterprise to return assignments for."
     }
    },
    "required": [
     "resolved_for_id",
     "resolved_for_type"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/storage_policy_assignments",
    "path": [],
    "query": [
     "marker",
     "resolved_for_type",
     "resolved_for_id"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_storage_policy_assignments_id",
   "name": "Get storage policy assignment",
   "description": "Fetches a specific storage policy assignment.",
   "input": {
    "type": "object",
    "properties": {
     "storage_policy_assignment_id": {
      "type": "string",
      "description": "The ID of the storage policy assignment."
     }
    },
    "required": [
     "storage_policy_assignment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/storage_policy_assignments/{storage_policy_assignment_id}",
    "path": [
     "storage_policy_assignment_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_task_assignments_id",
   "name": "Get task assignment",
   "description": "Retrieves information about a task assignment.",
   "input": {
    "type": "object",
    "properties": {
     "task_assignment_id": {
      "type": "string",
      "description": "The ID of the task assignment."
     }
    },
    "required": [
     "task_assignment_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/task_assignments/{task_assignment_id}",
    "path": [
     "task_assignment_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_tasks_id",
   "name": "Get task",
   "description": "Retrieves information about a specific task.",
   "input": {
    "type": "object",
    "properties": {
     "task_id": {
      "type": "string",
      "description": "The ID of the task."
     }
    },
    "required": [
     "task_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/tasks/{task_id}",
    "path": [
     "task_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_tasks_id_assignments",
   "name": "List task assignments",
   "description": "Lists all of the assignments for a given task.",
   "input": {
    "type": "object",
    "properties": {
     "task_id": {
      "type": "string",
      "description": "The ID of the task."
     }
    },
    "required": [
     "task_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/tasks/{task_id}/assignments",
    "path": [
     "task_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_terms_of_service_user_statuses",
   "name": "List terms of service user statuses",
   "description": "Retrieves an overview of users and their status for a terms of service, including Whether they have accepted the terms and when.",
   "input": {
    "type": "object",
    "properties": {
     "tos_id": {
      "type": "string",
      "description": "The ID of the terms of service."
     },
     "user_id": {
      "type": "string",
      "description": "Limits results to the given user ID."
     }
    },
    "required": [
     "tos_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/terms_of_service_user_statuses",
    "path": [],
    "query": [
     "tos_id",
     "user_id"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_terms_of_services",
   "name": "List terms of services",
   "description": "Returns the current terms of service text and settings for the enterprise.",
   "input": {
    "type": "object",
    "properties": {
     "tos_type": {
      "type": "string",
      "description": "Limits the results to the terms of service of the given type.",
      "enum": [
       "external",
       "managed"
      ]
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/terms_of_services",
    "path": [],
    "query": [
     "tos_type"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_terms_of_services_id",
   "name": "Get terms of service",
   "description": "Fetches a specific terms of service.",
   "input": {
    "type": "object",
    "properties": {
     "terms_of_service_id": {
      "type": "string",
      "description": "The ID of the terms of service."
     }
    },
    "required": [
     "terms_of_service_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/terms_of_services/{terms_of_service_id}",
    "path": [
     "terms_of_service_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_users",
   "name": "List enterprise users",
   "description": "Returns a list of all users for the Enterprise along with their `user_id`, `public_name`, and `login`. The application and the authenticated user need to have the permission to look up users in the entire enterprise.",
   "input": {
    "type": "object",
    "properties": {
     "filter_term": {
      "type": "string",
      "description": "Limits the results to only users who's `name` or `login` start with the search term. For externally managed users, the search term needs to completely match the in order to find the user, and it will only return one user at a time."
     },
     "user_type": {
      "type": "string",
      "description": "Limits the results to the kind of user specified. * `all` returns every kind of user for whom the `login` or `name` partially matches the `filter_term`. It will only return an external user if the login matches the `filter_term` completely, and in that case it will only return that user. * `managed` returns all managed and app users for whom the `login` or `name` partially matches the `filter_term",
      "enum": [
       "all",
       "managed",
       "external"
      ]
     },
     "external_app_user_id": {
      "type": "string",
      "description": "Limits the results to app users with the given `external_app_user_id` value. When creating an app user, an `external_app_user_id` value can be set. This value can then be used in this endpoint to find any users that match that `external_app_user_id` value."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "usemarker": {
      "type": "boolean",
      "description": "Specifies whether to use marker-based pagination instead of offset-based pagination. Only one pagination method can be used at a time. By setting this value to true, the API will return a `marker` field that can be passed as a parameter to this endpoint to get the next page of the response."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/users",
    "path": [],
    "query": [
     "filter_term",
     "user_type",
     "external_app_user_id",
     "fields",
     "offset",
     "limit",
     "usemarker",
     "marker"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_users_id",
   "name": "Get user",
   "description": "Retrieves information about a user in the enterprise. The application and the authenticated user need to have the permission to look up users in the entire enterprise. This endpoint also returns a limited set of information for external users who are collaborated on content owned by the enterprise for authenticated users with the right scopes. In this case, disallowed fields will return null inste",
   "input": {
    "type": "object",
    "properties": {
     "user_id": {
      "type": "string",
      "description": "The ID of the user."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "user_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/users/{user_id}",
    "path": [
     "user_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_users_id_avatar",
   "name": "Get user avatar",
   "description": "Retrieves an image of a the user's avatar.",
   "input": {
    "type": "object",
    "properties": {
     "user_id": {
      "type": "string",
      "description": "The ID of the user."
     }
    },
    "required": [
     "user_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/users/{user_id}/avatar",
    "path": [
     "user_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_users_id_email_aliases",
   "name": "List user's email aliases",
   "description": "Retrieves all email aliases for a user. The collection does not include the primary login for the user.",
   "input": {
    "type": "object",
    "properties": {
     "user_id": {
      "type": "string",
      "description": "The ID of the user."
     }
    },
    "required": [
     "user_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/users/{user_id}/email_aliases",
    "path": [
     "user_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_users_id_memberships",
   "name": "List user's groups",
   "description": "Retrieves all the groups for a user. Only members of this group or users with admin-level permissions will be able to use this API.",
   "input": {
    "type": "object",
    "properties": {
     "user_id": {
      "type": "string",
      "description": "The ID of the user."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "offset": {
      "type": "integer",
      "description": "The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
     }
    },
    "required": [
     "user_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/users/{user_id}/memberships",
    "path": [
     "user_id"
    ],
    "query": [
     "limit",
     "offset"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_users_me",
   "name": "Get current user",
   "description": "Retrieves information about the user who is currently authenticated. In the case of a client-side authenticated OAuth 2.0 application this will be the user who authorized the app. In the case of a JWT, server-side authenticated application this will be the service account that belongs to the application by default. Use the `As-User` header to change who this API call is made on behalf of.",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/users/me",
    "path": [],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_web_links_id",
   "name": "Get web link",
   "description": "Retrieve information about a web link.",
   "input": {
    "type": "object",
    "properties": {
     "web_link_id": {
      "type": "string",
      "description": "The ID of the web link."
     },
     "boxapi": {
      "type": "string",
      "description": "The URL, and optional password, for the shared link of this item. This header can be used to access items that have not been explicitly shared with a user. Use the format `shared_link=[link]` or if a password is required then use `shared_link=[link]&shared_link_password=[password]`. This header can be used on the file or folder shared, as well as on any files or folders nested within the item."
     }
    },
    "required": [
     "web_link_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/web_links/{web_link_id}",
    "path": [
     "web_link_id"
    ],
    "query": [],
    "header": [
     "boxapi"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_web_links_id_get_shared_link",
   "name": "Get shared link for web link",
   "description": "Gets the information for a shared link on a web link.",
   "input": {
    "type": "object",
    "properties": {
     "web_link_id": {
      "type": "string",
      "description": "The ID of the web link."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     }
    },
    "required": [
     "fields",
     "web_link_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/web_links/{web_link_id}#get_shared_link",
    "path": [
     "web_link_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_web_links_id_trash",
   "name": "Get trashed web link",
   "description": "Retrieves a web link that has been moved to the trash.",
   "input": {
    "type": "object",
    "properties": {
     "web_link_id": {
      "type": "string",
      "description": "The ID of the web link."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     }
    },
    "required": [
     "web_link_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/web_links/{web_link_id}/trash",
    "path": [
     "web_link_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_webhooks",
   "name": "List all webhooks",
   "description": "Returns all defined webhooks for the requesting application. This API only returns webhooks that are applied to files or folders that are owned by the authenticated user. This means that an admin can not see webhooks created by a service account unless the admin has access to those folders, and vice versa.",
   "input": {
    "type": "object",
    "properties": {
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/webhooks",
    "path": [],
    "query": [
     "marker",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_webhooks_id",
   "name": "Get webhook",
   "description": "Retrieves a specific webhook.",
   "input": {
    "type": "object",
    "properties": {
     "webhook_id": {
      "type": "string",
      "description": "The ID of the webhook."
     }
    },
    "required": [
     "webhook_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/webhooks/{webhook_id}",
    "path": [
     "webhook_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_workflows",
   "name": "List workflows",
   "description": "Returns list of workflows that act on a given `folder ID`, and have a flow with a trigger type of `WORKFLOW_MANUAL_START`. You application must be authorized to use the `Manage Box Relay` application scope within the developer console in to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "trigger_type": {
      "type": "string",
      "description": "Type of trigger to search for."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of items to return per page."
     },
     "marker": {
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/workflows",
    "path": [],
    "query": [
     "folder_id",
     "trigger_type",
     "limit",
     "marker"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_zip_downloads_id_content",
   "name": "Download zip archive",
   "description": "Returns the contents of a `zip` archive in binary format. This URL does not require any form of authentication and could be used in a user's browser to download the archive to a user's device. By default, this URL is only valid for a few seconds from the creation of the request for this archive. Once a download has started it can not be stopped and resumed, instead a new request for a zip archive ",
   "input": {
    "type": "object",
    "properties": {
     "zip_download_id": {
      "type": "string",
      "description": "The unique identifier that represent this `zip` archive."
     }
    },
    "required": [
     "zip_download_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/zip_downloads/{zip_download_id}/content",
    "path": [
     "zip_download_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.get_zip_downloads_id_status",
   "name": "Get zip download status",
   "description": "Returns the download status of a `zip` archive, allowing an application to inspect the progress of the download as well as the number of items that might have been skipped. This endpoint can only be accessed once the download has started. Subsequently this endpoint is valid for 12 hours from the start of the download. The URL of this endpoint should not be considered as fixed. Instead, use the [Cr",
   "input": {
    "type": "object",
    "properties": {
     "zip_download_id": {
      "type": "string",
      "description": "The unique identifier that represent this `zip` archive."
     }
    },
    "required": [
     "zip_download_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.box.com/2.0/zip_downloads/{zip_download_id}/status",
    "path": [
     "zip_download_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.patch_metadata_taxonomies_id_id",
   "name": "Update metadata taxonomy",
   "description": "Updates an existing metadata taxonomy.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     },
     "displayName": {
      "type": "string",
      "description": "The display name of the taxonomy."
     }
    },
    "required": [
     "displayName",
     "namespace",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}",
    "path": [
     "namespace",
     "taxonomy_key"
    ],
    "query": [],
    "header": [],
    "body": [
     "displayName"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.patch_metadata_taxonomies_id_id_levels_id",
   "name": "Update metadata taxonomy level",
   "description": "Updates an existing metadata taxonomy level.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     },
     "level_index": {
      "type": "integer",
      "description": "The index of the metadata taxonomy level."
     },
     "displayName": {
      "type": "string",
      "description": "The display name of the taxonomy level."
     },
     "description": {
      "type": "string",
      "description": "The description of the taxonomy level."
     }
    },
    "required": [
     "displayName",
     "level_index",
     "namespace",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}/levels/{level_index}",
    "path": [
     "namespace",
     "taxonomy_key",
     "level_index"
    ],
    "query": [],
    "header": [],
    "body": [
     "displayName",
     "description"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.patch_metadata_taxonomies_id_id_nodes_id",
   "name": "Update metadata taxonomy node",
   "description": "Updates an existing metadata taxonomy node.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     },
     "node_id": {
      "type": "string",
      "description": "The identifier of the metadata taxonomy node."
     },
     "displayName": {
      "type": "string",
      "description": "The display name of the taxonomy node."
     }
    },
    "required": [
     "namespace",
     "node_id",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}/nodes/{node_id}",
    "path": [
     "namespace",
     "taxonomy_key",
     "node_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "displayName"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_ai_agents",
   "name": "Create ai agent",
   "description": "Creates an AI agent. At least one of the following capabilities must be provided: `ask`, `text_gen`, `extract`.",
   "input": {
    "type": "object",
    "properties": {
     "type": {
      "type": "string",
      "description": "The type of agent used to handle queries.",
      "enum": [
       "ai_agent"
      ]
     },
     "name": {
      "type": "string",
      "description": "The name of the AI Agent."
     },
     "access_state": {
      "type": "string",
      "description": "The state of the AI Agent. Possible values are: `enabled`, `disabled`, and `enabled_for_selected_users`."
     },
     "icon_reference": {
      "type": "string",
      "description": "The icon reference of the AI Agent. It should have format of the URL `https://cdn01.boxcdn.net/app-assets/aistudio/avatars/` where possible values of `file_name` are: `logo_boxAi.png`,`logo_stamp.png`,`logo_legal.png`,`logo_finance.png`,`logo_config.png`,`logo_handshake.png`,`logo_analytics.png`,`logo_classification.png`."
     },
     "allowed_entities": {
      "type": "array",
      "description": "List of allowed users or groups."
     },
     "ask": {
      "type": "object",
      "description": "The AI agent to be used to handle queries."
     },
     "text_gen": {
      "type": "object",
      "description": "The AI agent to be used to generate text."
     },
     "extract": {
      "type": "object",
      "description": "The AI agent to be used for metadata extraction."
     }
    },
    "required": [
     "access_state",
     "name",
     "type"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/ai_agents",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "type",
     "name",
     "access_state",
     "icon_reference",
     "allowed_entities",
     "ask",
     "text_gen",
     "extract"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_ai_ask",
   "name": "Ask question",
   "description": "Sends an AI request to supported LLMs and returns an answer specifically focused on the user's question given the provided context. You can ask a question about a single file, several files, or the entire contents of a Box Hub. To search across and ask questions about everything in a Box Hub, send a single item with `type` set to `hubs` and the Hub's ID as the `id`. Box AI answers the question usi",
   "input": {
    "type": "object",
    "properties": {
     "mode": {
      "type": "string",
      "description": "Box AI handles text documents with text representations up to 2MB in size, or a maximum of 25 files, whichever comes first. If the text file size exceeds 2MB, the first 2MB of text representation will be processed. Box AI handles image documents with a resolution of 1024 x 1024 pixels, with a maximum of 5 images or 5 pages for multi-page images. If the number of image or image pages exceeds 5, the",
      "enum": [
       "multiple_item_qa",
       "single_item_qa"
      ]
     },
     "prompt": {
      "type": "string",
      "description": "The prompt provided by the client to be answered by the LLM. The prompt's length is limited to 10000 characters."
     },
     "items": {
      "type": "array",
      "description": "The items to be processed by the LLM, often files. To search across and ask questions about the contents of a Box Hub, pass a single item with `type` set to `hubs`. See the item `type` property for details."
     },
     "dialogue_history": {
      "type": "array",
      "description": "The history of prompts and answers previously passed to the LLM. This provides additional context to the LLM in generating the response."
     },
     "include_citations": {
      "type": "boolean",
      "description": "A flag to indicate whether citations should be returned."
     },
     "ai_agent": {
      "type": "object",
      "description": "The AI agent to be used to handle the AI ask request."
     }
    },
    "required": [
     "items",
     "mode",
     "prompt"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/ai/ask",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "mode",
     "prompt",
     "items",
     "dialogue_history",
     "include_citations",
     "ai_agent"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_ai_extract",
   "name": "Extract metadata (freeform)",
   "description": "Sends an AI request to supported Large Language Models (LLMs) and extracts metadata in form of key-value pairs. In this request, both the prompt and the output can be freeform. Metadata template setup before sending the request is not required.",
   "input": {
    "type": "object",
    "properties": {
     "prompt": {
      "type": "string",
      "description": "The prompt provided to a Large Language Model (LLM) in the request. The prompt can be up to 10000 characters long and it can be an XML or a JSON schema."
     },
     "items": {
      "type": "array",
      "description": "The items that LLM will process. Currently, you can use files only."
     },
     "ai_agent": {
      "type": "object",
      "description": "The AI agent to be used to handle the AI metadata freeform extraction request."
     }
    },
    "required": [
     "items",
     "prompt"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/ai/extract",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "prompt",
     "items",
     "ai_agent"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_ai_extract_structured",
   "name": "Extract metadata (structured)",
   "description": "Sends an AI request to supported Large Language Models (LLMs) and returns extracted metadata as a set of key-value pairs. To define the extraction structure, provide either a metadata template or a list of fields. To learn more about creating templates, see [Creating metadata templates in the Admin Console](https://support.box.com/hc/en-us/articles/360044194033-Customizing-Metadata-Templates) or u",
   "input": {
    "type": "object",
    "properties": {
     "items": {
      "type": "array",
      "description": "The items to be processed by the LLM. Currently you can use files only."
     },
     "metadata_template": {
      "type": "object",
      "description": "The metadata template containing the fields to extract. For your request to work, you must provide either `metadata_template` or `fields`, but not both."
     },
     "fields": {
      "type": "array",
      "description": "The fields to be extracted from the provided items. For your request to work, you must provide either `metadata_template` or `fields`, but not both."
     },
     "ai_agent": {
      "type": "object",
      "description": "The AI agent to be used to handle the AI extract structured request."
     },
     "include_confidence_score": {
      "type": "boolean",
      "description": "A flag to indicate whether confidence scores for every extracted field should be returned."
     },
     "include_reference": {
      "type": "boolean",
      "description": "A flag to indicate whether references for every extracted field should be returned."
     },
     "taxonomy_sources": {
      "type": "array",
      "description": "The taxonomy sources to be used for the structured extraction. They can either be an existing file or a taxonomy. For your request to work, `fields` must also be provided. `taxonomy_sources` is not supported with `metadata_template`."
     }
    },
    "required": [
     "items"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/ai/extract_structured",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "items",
     "metadata_template",
     "fields",
     "ai_agent",
     "include_confidence_score",
     "include_reference",
     "taxonomy_sources"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_ai_text_gen",
   "name": "Generate text",
   "description": "Sends an AI request to supported Large Language Models (LLMs) and returns generated text based on the provided prompt.",
   "input": {
    "type": "object",
    "properties": {
     "prompt": {
      "type": "string",
      "description": "The prompt provided by the client to be answered by the LLM. The prompt's length is limited to 10000 characters."
     },
     "items": {
      "type": "array",
      "description": "The items to be processed by the LLM, often files. The array can include **exactly one** element. **Note**: Box AI handles documents with text representations up to 2MB in size. If the file size exceeds 2MB, the first 2MB of text representation will be processed."
     },
     "dialogue_history": {
      "type": "array",
      "description": "The history of prompts and answers previously passed to the LLM. This parameter provides the additional context to the LLM when generating the response."
     },
     "ai_agent": {
      "type": "object",
      "description": "The AI agent to be used to handle the AI text generation request."
     }
    },
    "required": [
     "items",
     "prompt"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/ai/text_gen",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "prompt",
     "items",
     "dialogue_history",
     "ai_agent"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_collaboration_whitelist_entries",
   "name": "Add domain to list of allowed collaboration domains",
   "description": "Creates a new entry in the list of allowed domains to allow collaboration for.",
   "input": {
    "type": "object",
    "properties": {
     "domain": {
      "type": "string",
      "description": "The domain to add to the list of allowed domains."
     },
     "direction": {
      "type": "string",
      "description": "The direction in which to allow collaborations.",
      "enum": [
       "inbound",
       "outbound",
       "both"
      ]
     }
    },
    "required": [
     "direction",
     "domain"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/collaboration_whitelist_entries",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "domain",
     "direction"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_collaboration_whitelist_exempt_targets",
   "name": "Create user exemption from collaboration domain restrictions",
   "description": "Exempts a user from the restrictions set out by the allowed list of domains for collaborations.",
   "input": {
    "type": "object",
    "properties": {
     "user": {
      "type": "object",
      "description": "The user to exempt."
     }
    },
    "required": [
     "user"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/collaboration_whitelist_exempt_targets",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "user"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_collaborations",
   "name": "Create collaboration",
   "description": "Adds a collaboration for a single user or a single group to a file or folder. Collaborations can be created using email address, user IDs, or a group IDs. If a collaboration is being created with a group, access to this endpoint is dependent on the group's ability to be invited. If collaboration is in `pending` status, field `name` is redacted when: - a collaboration was created using `user_id`, -",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "notify": {
      "type": "boolean",
      "description": "Determines if users should receive email notification for the action performed."
     },
     "item": {
      "type": "object",
      "description": "The item to attach the comment to."
     },
     "accessible_by": {
      "type": "object",
      "description": "The user or group to give access to the item."
     },
     "role": {
      "type": "string",
      "description": "The level of access granted.",
      "enum": [
       "editor",
       "viewer",
       "previewer",
       "uploader",
       "previewer uploader",
       "viewer uploader",
       "co-owner"
      ]
     },
     "is_access_only": {
      "type": "boolean",
      "description": "If set to `true`, collaborators have access to shared items, but such items won't be visible in the All Files list. Additionally, collaborators won't see the path to the root folder for the shared item."
     },
     "can_view_path": {
      "type": "boolean",
      "description": "Determines if the invited users can see the entire parent path to the associated folder. The user will not gain privileges in any parent folder and therefore can not see content the user is not collaborated on. Be aware that this meaningfully increases the time required to load the invitee's **All Files** page. We recommend you limit the number of collaborations with `can_view_path` enabled to 1,0"
     },
     "expires_at": {
      "type": "string",
      "description": "Set the expiration date for the collaboration. At this date, the collaboration will be automatically removed from the item. This feature will only work if the **Automatically remove invited collaborators: Allow folder owners to extend the expiry date** setting has been enabled in the **Enterprise Settings** of the **Admin Console**. When the setting is not enabled, collaborations can not have an e"
     }
    },
    "required": [
     "accessible_by",
     "item",
     "role"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/collaborations",
    "path": [],
    "query": [
     "fields",
     "notify"
    ],
    "header": [],
    "body": [
     "item",
     "accessible_by",
     "role",
     "is_access_only",
     "can_view_path",
     "expires_at"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_comments",
   "name": "Create comment",
   "description": "Adds a comment by the user to a specific file, or as a reply to an other comment.",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "message": {
      "type": "string",
      "description": "The text of the comment. To mention a user, use the `tagged_message` parameter instead."
     },
     "tagged_message": {
      "type": "string",
      "description": "The text of the comment, including `@[user_id:name]` somewhere in the message to mention another user, which will send them an email notification, letting them know they have been mentioned. The `user_id` is the target user's ID, where the `name` can be any custom phrase. In the Box UI this name will link to the user's profile. If you are not mentioning another user, use `message` instead."
     },
     "item": {
      "type": "object",
      "description": "The item to attach the comment to."
     }
    },
    "required": [
     "item",
     "message"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/comments",
    "path": [],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "message",
     "tagged_message",
     "item"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_file_requests_id_copy",
   "name": "Copy file request",
   "description": "Copies an existing file request that is already present on one folder, and applies it to another folder.",
   "input": {
    "type": "object",
    "properties": {
     "file_request_id": {
      "type": "string",
      "description": "The unique identifier that represent a file request. The ID for any file request can be determined by visiting a file request builder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/filerequest/123` the `file_request_id` is `123`."
     },
     "title": {
      "type": "string",
      "description": "An optional new title for the file request. This can be used to change the title of the file request. This will default to the value on the existing file request."
     },
     "description": {
      "type": "string",
      "description": "An optional new description for the file request. This can be used to change the description of the file request. This will default to the value on the existing file request."
     },
     "status": {
      "type": "string",
      "description": "An optional new status of the file request. When the status is set to `inactive`, the file request will no longer accept new submissions, and any visitor to the file request URL will receive a `HTTP 404` status code. This will default to the value on the existing file request.",
      "enum": [
       "active",
       "inactive"
      ]
     },
     "is_email_required": {
      "type": "boolean",
      "description": "Whether a file request submitter is required to provide their email address. When this setting is set to true, the Box UI will show an email field on the file request form. This will default to the value on the existing file request."
     },
     "is_description_required": {
      "type": "boolean",
      "description": "Whether a file request submitter is required to provide a description of the files they are submitting. When this setting is set to true, the Box UI will show a description field on the file request form. This will default to the value on the existing file request."
     },
     "expires_at": {
      "type": "string",
      "description": "The date after which a file request will no longer accept new submissions. After this date, the `status` will automatically be set to `inactive`. This will default to the value on the existing file request."
     }
    },
    "required": [
     "file_request_id",
     "folder"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/file_requests/{file_request_id}/copy",
    "path": [
     "file_request_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "title",
     "description",
     "status",
     "is_email_required",
     "is_description_required",
     "expires_at"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_content",
   "name": "Upload file",
   "description": "Uploads a small file to Box. For file sizes over 50MB we recommend using the Chunk Upload APIs. The `attributes` part of the body must come **before** the `file` part. Requests that do not follow this format when uploading the file will receive a HTTP `400` error with a `metadata_after_file_contents` error code.",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "content-md5": {
      "type": "string",
      "description": "An optional header containing the SHA1 hash of the file to ensure that the file was not corrupted in transit."
     },
     "attributes": {
      "type": "object",
      "description": "The additional attributes of the file being uploaded. Mainly the name and the parent folder. These attributes are part of the multi part request body and are in JSON format. The `attributes` part of the body must come **before** the `file` part. Requests that do not follow this format when uploading the file will receive a HTTP `400` error with a `metadata_after_file_contents` error code."
     },
     "file": {
      "type": "string",
      "description": "The content of the file to upload to Box. The `attributes` part of the body must come **before** the `file` part. Requests that do not follow this format when uploading the file will receive a HTTP `400` error with a `metadata_after_file_contents` error code."
     }
    },
    "required": [
     "attributes",
     "file"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/content",
    "path": [],
    "query": [
     "fields"
    ],
    "header": [
     "content-md5"
    ],
    "body": [
     "attributes",
     "file"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_id",
   "name": "Restore file",
   "description": "Restores a file that has been moved to the trash. An optional new parent ID can be provided to restore the file to in case the original folder has been deleted.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "name": {
      "type": "string",
      "description": "An optional new name for the file."
     },
     "parent": {
      "type": "object",
      "description": "The parent for this item."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/{file_id}",
    "path": [
     "file_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "name",
     "parent"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_id_content",
   "name": "Upload file version",
   "description": "Update a file's content. For file sizes over 50MB we recommend using the Chunk Upload APIs. The `attributes` part of the body must come **before** the `file` part. Requests that do not follow this format when uploading the file will receive a HTTP `400` error with a `metadata_after_file_contents` error code.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "if-match": {
      "type": "string",
      "description": "Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "content-md5": {
      "type": "string",
      "description": "An optional header containing the SHA1 hash of the file to ensure that the file was not corrupted in transit."
     },
     "attributes": {
      "type": "object",
      "description": "The additional attributes of the file being uploaded. Mainly the name and the parent folder. These attributes are part of the multi part request body and are in JSON format. The `attributes` part of the body must come **before** the `file` part. Requests that do not follow this format when uploading the file will receive a HTTP `400` error with a `metadata_after_file_contents` error code."
     },
     "file": {
      "type": "string",
      "description": "The content of the file to upload to Box. The `attributes` part of the body must come **before** the `file` part. Requests that do not follow this format when uploading the file will receive a HTTP `400` error with a `metadata_after_file_contents` error code."
     }
    },
    "required": [
     "attributes",
     "file",
     "file_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/{file_id}/content",
    "path": [
     "file_id"
    ],
    "query": [
     "fields"
    ],
    "header": [
     "if-match",
     "content-md5"
    ],
    "body": [
     "attributes",
     "file"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_id_copy",
   "name": "Copy file",
   "description": "Creates a copy of a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "name": {
      "type": "string",
      "description": "An optional new name for the copied file. There are some restrictions to the file name. Names containing non-printable ASCII characters, forward and backward slashes (`/`, `\\`), and protected names like `.` and `..` are automatically sanitized by removing the non-allowed characters."
     },
     "version": {
      "type": "string",
      "description": "An optional ID of the specific file version to copy."
     },
     "parent": {
      "type": "object",
      "description": "The destination folder to copy the file to."
     }
    },
    "required": [
     "file_id",
     "parent"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/{file_id}/copy",
    "path": [
     "file_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "name",
     "version",
     "parent"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_id_metadata_enterprise_security_classification_6_vmvochw_uwo",
   "name": "Add classification to file",
   "description": "Adds a classification to a file by specifying the label of the classification to add. This API can also be called by including the enterprise ID in the URL explicitly, for example `/files/:id//enterprise_12345/securityClassification-6VMVochwUWo`.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "Box__Security__Classification__Key": {
      "type": "string",
      "description": "The name of the classification to apply to this file. To list the available classifications in an enterprise, use the classification API to retrieve the [classification template](https://developer.box.com/reference/get-metadata-templates-enterprise-securityClassification-6VMVochwUWo-schema) which lists all available classification keys."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "Box__Security__Classification__Key"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_id_metadata_global_box_skills_cards",
   "name": "Create box skill cards on file",
   "description": "Applies one or more Box Skills metadata cards to a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "cards": {
      "type": "array",
      "description": "A list of Box Skill cards to apply to this file."
     }
    },
    "required": [
     "cards",
     "file_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/global/boxSkillsCards",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "cards"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_id_metadata_id_id",
   "name": "Create metadata instance on file",
   "description": "Applies an instance of a metadata template to a file. In most cases only values that are present in the metadata template will be accepted, except for the `global.properties` template which accepts any key-value pair.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     }
    },
    "required": [
     "file_id",
     "scope",
     "template_key"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/{scope}/{template_key}",
    "path": [
     "file_id",
     "scope",
     "template_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_id_upload_sessions",
   "name": "Create upload session for existing file",
   "description": "Creates an upload session for an existing file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "file_size": {
      "type": "integer",
      "description": "The total number of bytes of the file to be uploaded."
     },
     "file_name": {
      "type": "string",
      "description": "The optional new name of new file."
     }
    },
    "required": [
     "file_id",
     "file_size"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/{file_id}/upload_sessions",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "file_size",
     "file_name"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_id_versions_current",
   "name": "Promote file version",
   "description": "Promote a specific version of a file. If previous versions exist, this method can be used to promote one of the older versions to the top of the version history. This creates a new copy of the old version and puts it at the top of the versions history. The file will have the exact same contents as the older version, with the same hash digest, `etag`, and name as the original. Other properties such",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "id": {
      "type": "string",
      "description": "The file version ID."
     },
     "type": {
      "type": "string",
      "description": "The type to promote.",
      "enum": [
       "file_version"
      ]
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/{file_id}/versions/current",
    "path": [
     "file_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "id",
     "type"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_upload_sessions",
   "name": "Create upload session",
   "description": "Creates an upload session for a new file.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The ID of the folder to upload the new file to."
     },
     "file_size": {
      "type": "integer",
      "description": "The total number of bytes of the file to be uploaded."
     },
     "file_name": {
      "type": "string",
      "description": "The name of new file."
     }
    },
    "required": [
     "file_name",
     "file_size",
     "folder_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/upload_sessions",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "folder_id",
     "file_size",
     "file_name"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_upload_sessions_id_commit",
   "name": "Commit upload session",
   "description": "Close an upload session and create a file from the uploaded chunks. The actual endpoint URL is returned by the [`Create upload session`](https://developer.box.com/reference/post-files-upload-sessions) and [`Get upload session`](https://developer.box.com/reference/get-files-upload-sessions-id) endpoints.",
   "input": {
    "type": "object",
    "properties": {
     "upload_session_id": {
      "type": "string",
      "description": "The ID of the upload session."
     },
     "digest": {
      "type": "string",
      "description": "The [RFC3230][1] message digest of the whole file. Only SHA1 is supported. The SHA1 digest must be Base64 encoded. The format of this header is as `sha=BASE64_ENCODED_DIGEST`. [1]: https://tools.ietf.org/html/rfc3230"
     },
     "if-match": {
      "type": "string",
      "description": "Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since."
     },
     "if-none-match": {
      "type": "string",
      "description": "Ensures an item is only returned if it has changed. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `304 Not Modified` if the item has not changed since."
     },
     "parts": {
      "type": "array",
      "description": "The list details for the uploaded parts."
     }
    },
    "required": [
     "digest",
     "parts",
     "upload_session_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/upload_sessions/{upload_session_id}/commit",
    "path": [
     "upload_session_id"
    ],
    "query": [],
    "header": [
     "digest",
     "if-match",
     "if-none-match"
    ],
    "body": [
     "parts"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_files_upload_sessions_id_plan",
   "name": "Plan upload session",
   "description": "Plan an upload session by checking which parts already exist on the server. This endpoint allows clients to optimize uploads by skipping parts that have already been uploaded (cache hits) and only uploading missing parts. The actual endpoint URL is returned by the [`Create upload session`](e://post-files-upload-sessions) and [`Get upload session`](e://get-files-upload-sessions-id) endpoints.",
   "input": {
    "type": "object",
    "properties": {
     "upload_session_id": {
      "type": "string",
      "description": "The ID of the upload session."
     },
     "parts": {
      "type": "array",
      "description": "The list of parts to check for existence."
     }
    },
    "required": [
     "parts",
     "upload_session_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/files/upload_sessions/{upload_session_id}/plan",
    "path": [
     "upload_session_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "parts"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_folder_locks",
   "name": "Create folder lock",
   "description": "Creates a folder lock on a folder, preventing it from being moved and/or deleted. You must be authenticated as the owner or co-owner of the folder to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "locked_operations": {
      "type": "object",
      "description": "The operations to lock for the folder. If `locked_operations` is included in the request, both `move` and `delete` must also be included and both set to `true`."
     },
     "folder": {
      "type": "object",
      "description": "The folder to apply the lock to."
     }
    },
    "required": [
     "folder"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/folder_locks",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "locked_operations",
     "folder"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_folders",
   "name": "Create folder",
   "description": "Creates a new empty folder within the specified parent folder.",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "name": {
      "type": "string",
      "description": "The name for the new folder. The following restrictions to folder names apply: names containing non-printable ASCII characters, forward and backward slashes (`/`, `\\`), names with trailing spaces, and names `.` and `..` are not allowed. Folder names must be unique within their parent folder. The name check is case-insensitive, so a folder named `New Folder` cannot be created in a parent folder tha"
     },
     "parent": {
      "type": "object",
      "description": "The parent folder to create the new folder within."
     },
     "folder_upload_email": {
      "type": "object",
      "description": "The Write Folder Upload Email object."
     },
     "sync_state": {
      "type": "string",
      "description": "Specifies whether a folder should be synced to a user's device or not. This is used by Box Sync (discontinued) and is not used by Box Drive.",
      "enum": [
       "synced",
       "not_synced",
       "partially_synced"
      ]
     }
    },
    "required": [
     "name",
     "parent"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/folders",
    "path": [],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "name",
     "parent",
     "folder_upload_email",
     "sync_state"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_folders_id",
   "name": "Restore folder",
   "description": "Restores a folder that has been moved to the trash. An optional new parent ID can be provided to restore the folder to in case the original folder has been deleted. During this operation, part of the file tree will be locked, mainly the source folder and all of its descendants, as well as the destination folder. For the duration of the operation, no other move, copy, delete, or restore operation c",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "name": {
      "type": "string",
      "description": "An optional new name for the folder."
     },
     "parent": {
      "type": "object",
      "description": "The parent for this item."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/folders/{folder_id}",
    "path": [
     "folder_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "name",
     "parent"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_folders_id_copy",
   "name": "Copy folder",
   "description": "Creates a copy of a folder within a destination folder. The original folder will not be changed.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier of the folder to copy. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder with the ID `0` can not be copied."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "name": {
      "type": "string",
      "description": "An optional new name for the copied folder. There are some restrictions to the file name. Names containing non-printable ASCII characters, forward and backward slashes (`/`, `\\`), as well as names with trailing spaces are prohibited. Additionally, the names `.` and `..` are not allowed either."
     },
     "parent": {
      "type": "object",
      "description": "The destination folder to copy the folder to."
     }
    },
    "required": [
     "folder_id",
     "parent"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/folders/{folder_id}/copy",
    "path": [
     "folder_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "name",
     "parent"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_folders_id_metadata_enterprise_security_classification_6_vmvochw_uwo",
   "name": "Add classification to folder",
   "description": "Adds a classification to a folder by specifying the label of the classification to add. This API can also be called by including the enterprise ID in the URL explicitly, for example `/folders/:id/enterprise_12345/securityClassification-6VMVochwUWo`.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "Box__Security__Classification__Key": {
      "type": "string",
      "description": "The name of the classification to apply to this folder. To list the available classifications in an enterprise, use the classification API to retrieve the [classification template](https://developer.box.com/reference/get-metadata-templates-enterprise-securityClassification-6VMVochwUWo-schema) which lists all available classification keys."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/folders/{folder_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "path": [
     "folder_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "Box__Security__Classification__Key"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_folders_id_metadata_id_id",
   "name": "Create metadata instance on folder",
   "description": "Applies an instance of a metadata template to a folder. In most cases only values that are present in the metadata template will be accepted, except for the `global.properties` template which accepts any key-value pair. To display the metadata template in the Box web app the enterprise needs to be configured to enable **Cascading Folder Level Metadata** for the user in the admin console.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     }
    },
    "required": [
     "folder_id",
     "scope",
     "template_key"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/folders/{folder_id}/metadata/{scope}/{template_key}",
    "path": [
     "folder_id",
     "scope",
     "template_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_group_memberships",
   "name": "Add user to group",
   "description": "Creates a group membership. Only users with admin-level permissions will be able to use this API.",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "user": {
      "type": "object",
      "description": "The user to add to the group."
     },
     "group": {
      "type": "object",
      "description": "The group to add the user to."
     },
     "role": {
      "type": "string",
      "description": "The role of the user in the group.",
      "enum": [
       "member",
       "admin"
      ]
     },
     "configurable_permissions": {
      "type": "object",
      "description": "Custom configuration for the permissions an admin if a group will receive. This option has no effect on members with a role of `member`. Setting these permissions overwrites the default access levels of an admin. Specifying a value of `null` for this object will disable all configurable permissions. Specifying permissions will set them accordingly, omitted permissions will be enabled by default."
     }
    },
    "required": [
     "group",
     "user"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/group_memberships",
    "path": [],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "user",
     "group",
     "role",
     "configurable_permissions"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_groups",
   "name": "Create group",
   "description": "Creates a new group of users in an enterprise. Only users with admin permissions can create new groups.",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "name": {
      "type": "string",
      "description": "The name of the new group to be created. This name must be unique within the enterprise."
     },
     "provenance": {
      "type": "string",
      "description": "Keeps track of which external source this group is coming, for example `Active Directory`, or `Okta`. Setting this will also prevent Box admins from editing the group name and its members directly via the Box web application. This is desirable for one-way syncing of groups."
     },
     "external_sync_identifier": {
      "type": "string",
      "description": "An arbitrary identifier that can be used by external group sync tools to link this Box Group to an external group. Example values of this field could be an **Active Directory Object ID** or a **Google Group ID**. We recommend you use of this field in order to avoid issues when group names are updated in either Box or external systems."
     },
     "description": {
      "type": "string",
      "description": "A human readable description of the group."
     },
     "invitability_level": {
      "type": "string",
      "description": "Specifies who can invite the group to collaborate on folders. When set to `admins_only` the enterprise admin, co-admins, and the group's admin can invite the group. When set to `admins_and_members` all the admins listed above and group members can invite the group. When set to `all_managed_users` all managed users in the enterprise can invite the group.",
      "enum": [
       "admins_only",
       "admins_and_members",
       "all_managed_users"
      ]
     },
     "member_viewability_level": {
      "type": "string",
      "description": "Specifies who can see the members of the group. * `admins_only` - the enterprise admin, co-admins, group's group admin. * `admins_and_members` - all admins and group members. * `all_managed_users` - all managed users in the enterprise.",
      "enum": [
       "admins_only",
       "admins_and_members",
       "all_managed_users"
      ]
     }
    },
    "required": [
     "name"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/groups",
    "path": [],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "name",
     "provenance",
     "external_sync_identifier",
     "description",
     "invitability_level",
     "member_viewability_level"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_groups_terminate_sessions",
   "name": "Create jobs to terminate user group session",
   "description": "Validates the roles and permissions of the group, and creates asynchronous jobs to terminate the group's sessions. Returns the status for the POST request.",
   "input": {
    "type": "object",
    "properties": {
     "group_ids": {
      "type": "array",
      "description": "A list of group IDs."
     }
    },
    "required": [
     "group_ids"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/groups/terminate_sessions",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "group_ids"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_integration_mappings_slack",
   "name": "Create slack integration mapping",
   "description": "Creates a [Slack integration mapping](https://support.box.com/hc/en-us/articles/4415585987859-Box-as-the-Content-Layer-for-Slack) by mapping a Slack channel to a Box item. You need Admin or Co-Admin role to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "partner_item": {
      "type": "object",
      "description": "The schema for an integration mapping mapped item object for type Slack. Depending if Box for Slack is installed at the org or workspace level, provide **either** `slack_org_id` **or** `slack_workspace_id`. Do not use both parameters at the same time."
     },
     "box_item": {
      "type": "object",
      "description": "The schema for an integration mapping Box item object for type Slack."
     },
     "options": {
      "type": "object",
      "description": "The schema for an integration mapping options object for Slack type."
     }
    },
    "required": [
     "box_item",
     "partner_item"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/integration_mappings/slack",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "partner_item",
     "box_item",
     "options"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_integration_mappings_teams",
   "name": "Create teams integration mapping",
   "description": "Creates a [Teams integration mapping](https://support.box.com/hc/en-us/articles/360044681474-Using-Box-for-Teams) by mapping a Teams channel to a Box item. You need Admin or Co-Admin role to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "partner_item": {
      "type": "object",
      "description": "The schema for an integration mapping mapped item object for type Teams."
     },
     "box_item": {
      "type": "object",
      "description": "Folder reference."
     }
    },
    "required": [
     "box_item",
     "partner_item"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/integration_mappings/teams",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "partner_item",
     "box_item"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_invites",
   "name": "Create user invite",
   "description": "Invites an existing external user to join an enterprise. The existing user can not be part of another enterprise and must already have a Box account. Once invited, the user will receive an email and are prompted to accept the invitation within the Box web application. This method requires the \"Manage An Enterprise\" scope enabled for the application, which can be enabled within the developer consol",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "enterprise": {
      "type": "object",
      "description": "The enterprise to invite the user to."
     },
     "actionable_by": {
      "type": "object",
      "description": "The user to invite."
     }
    },
    "required": [
     "actionable_by",
     "enterprise"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/invites",
    "path": [],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "enterprise",
     "actionable_by"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_legal_hold_policies",
   "name": "Create legal hold policy",
   "description": "Create a new legal hold policy.",
   "input": {
    "type": "object",
    "properties": {
     "policy_name": {
      "type": "string",
      "description": "The name of the policy."
     },
     "description": {
      "type": "string",
      "description": "A description for the policy."
     },
     "filter_started_at": {
      "type": "string",
      "description": "The filter start date. When this policy is applied using a `custodian` legal hold assignments, it will only apply to file versions created or uploaded inside of the date range. Other assignment types, such as folders and files, will ignore the date filter. Required if `is_ongoing` is set to `false`."
     },
     "filter_ended_at": {
      "type": "string",
      "description": "The filter end date. When this policy is applied using a `custodian` legal hold assignments, it will only apply to file versions created or uploaded inside of the date range. Other assignment types, such as folders and files, will ignore the date filter. Required if `is_ongoing` is set to `false`."
     },
     "is_ongoing": {
      "type": "boolean",
      "description": "Whether new assignments under this policy should continue applying to files even after initialization. When this policy is applied using a legal hold assignment, it will continue applying the policy to any new file versions even after it has been applied. For example, if a legal hold assignment is placed on a user today, and that user uploads a file tomorrow, that file will get held. This will con"
     }
    },
    "required": [
     "policy_name"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/legal_hold_policies",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "policy_name",
     "description",
     "filter_started_at",
     "filter_ended_at",
     "is_ongoing"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_legal_hold_policy_assignments",
   "name": "Assign legal hold policy",
   "description": "Assign a legal hold to an item type of: file, file version, folder, user, ownership, or interactions.",
   "input": {
    "type": "object",
    "properties": {
     "policy_id": {
      "type": "string",
      "description": "The ID of the policy to assign."
     },
     "assign_to": {
      "type": "object",
      "description": "The item to assign the policy to."
     }
    },
    "required": [
     "assign_to",
     "policy_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/legal_hold_policy_assignments",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "policy_id",
     "assign_to"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_metadata_cascade_policies",
   "name": "Create metadata cascade policy",
   "description": "Creates a new metadata cascade policy that applies a given metadata template to a given folder and automatically cascades it down to any files within that folder. In order for the policy to be applied a metadata instance must first be applied to the folder the policy is to be applied to.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The ID of the folder to apply the policy to. This folder will need to already have an instance of the targeted metadata template applied to it."
     },
     "scope": {
      "type": "string",
      "description": "The scope of the targeted metadata template. This template will need to already have an instance applied to the targeted folder.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "templateKey": {
      "type": "string",
      "description": "The key of the targeted metadata template. This template will need to already have an instance applied to the targeted folder. In many cases the template key is automatically derived of its display name, for example `Contract Template` would become `contractTemplate`. In some cases the creator of the template will have provided its own template key. Please [list the templates for an enterprise][li"
     }
    },
    "required": [
     "folder_id",
     "scope",
     "templateKey"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/metadata_cascade_policies",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "folder_id",
     "scope",
     "templateKey"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_metadata_cascade_policies_id_apply",
   "name": "Force apply metadata cascade policy to folder",
   "description": "Force the metadata on a folder with a metadata cascade policy to be applied to all of its children. This can be used after creating a new cascade policy to enforce the metadata to be cascaded down to all existing files within that folder.",
   "input": {
    "type": "object",
    "properties": {
     "metadata_cascade_policy_id": {
      "type": "string",
      "description": "The ID of the cascade policy to force-apply."
     },
     "conflict_resolution": {
      "type": "string",
      "description": "Describes the desired behavior when dealing with the conflict where a metadata template already has an instance applied to a child. * `none` will preserve the existing value on the file * `overwrite` will force-apply the templates values over any existing values.",
      "enum": [
       "none",
       "overwrite"
      ]
     }
    },
    "required": [
     "conflict_resolution",
     "metadata_cascade_policy_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/metadata_cascade_policies/{metadata_cascade_policy_id}/apply",
    "path": [
     "metadata_cascade_policy_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "conflict_resolution"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_metadata_queries_execute_read",
   "name": "Query files folders by metadata",
   "description": "Create a search using SQL-like syntax to return items that match specific metadata. By default, this endpoint returns only the most basic info about the items for which the query matches. To get additional fields for each item, including any of the metadata, use the `fields` attribute in the query.",
   "input": {
    "type": "object",
    "properties": {
     "from": {
      "type": "string",
      "description": "Specifies the template used in the query. Must be in the form `scope.templateKey`. Not all templates can be used in this field, most notably the built-in, Box-provided classification templates can not be used in a query."
     },
     "query": {
      "type": "string",
      "description": "The query to perform. A query is a logical expression that is very similar to a SQL `SELECT` statement. Values in the search query can be turned into parameters specified in the `query_param` arguments list to prevent having to manually insert search values into the query string. For example, a value of `:amount` would represent the `amount` value in `query_params` object."
     },
     "query_params": {
      "type": "object",
      "description": "Set of arguments corresponding to the parameters specified in the `query`. The type of each parameter used in the `query_params` must match the type of the corresponding metadata template field."
     },
     "ancestor_folder_id": {
      "type": "string",
      "description": "The ID of the folder that you are restricting the query to. A value of zero will return results from all folders you have access to. A non-zero value will only return results found in the folder corresponding to the ID or in any of its subfolders."
     },
     "order_by": {
      "type": "array",
      "description": "A list of template fields and directions to sort the metadata query results by. The ordering `direction` must be the same for each item in the array."
     },
     "limit": {
      "type": "integer",
      "description": "A value between 0 and 100 that indicates the maximum number of results to return for a single request. This only specifies a maximum boundary and will not guarantee the minimum number of results returned."
     },
     "marker": {
      "type": "string",
      "description": "Marker to use for requesting the next page."
     },
     "fields": {
      "type": "array",
      "description": "By default, this endpoint returns only the most basic info about the items for which the query matches. This attribute can be used to specify a list of additional attributes to return for any item, including its metadata. This attribute takes a list of item fields, metadata template identifiers, or metadata template field identifiers. For example: * `created_by` will add the details of the user wh"
     }
    },
    "required": [
     "ancestor_folder_id",
     "from"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/metadata_queries/execute_read",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "from",
     "query",
     "query_params",
     "ancestor_folder_id",
     "order_by",
     "limit",
     "marker",
     "fields"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_metadata_taxonomies",
   "name": "Create metadata taxonomy",
   "description": "Creates a new metadata taxonomy that can be used in metadata templates.",
   "input": {
    "type": "object",
    "properties": {
     "displayName": {
      "type": "string",
      "description": "The display name of the taxonomy."
     },
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy to create."
     }
    },
    "required": [
     "displayName",
     "namespace"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/metadata_taxonomies",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "displayName",
     "namespace"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_metadata_taxonomies_id_id_levels",
   "name": "Create metadata taxonomy levels",
   "description": "Creates new metadata taxonomy levels.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     }
    },
    "required": [
     "namespace",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}/levels",
    "path": [
     "namespace",
     "taxonomy_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_metadata_taxonomies_id_id_levels_append",
   "name": "Add metadata taxonomy level",
   "description": "Creates a new metadata taxonomy level and appends it to the existing levels. If there are no levels defined yet, this will create the first level.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     },
     "displayName": {
      "type": "string",
      "description": "The display name of the taxonomy level."
     },
     "description": {
      "type": "string",
      "description": "The description of the taxonomy level."
     }
    },
    "required": [
     "displayName",
     "namespace",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}/levels:append",
    "path": [
     "namespace",
     "taxonomy_key"
    ],
    "query": [],
    "header": [],
    "body": [
     "displayName",
     "description"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_metadata_taxonomies_id_id_levels_trim",
   "name": "Delete metadata taxonomy level",
   "description": "Deletes the last level of the metadata taxonomy.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     }
    },
    "required": [
     "namespace",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}/levels:trim",
    "path": [
     "namespace",
     "taxonomy_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_metadata_taxonomies_id_id_nodes",
   "name": "Create metadata taxonomy node",
   "description": "Creates a new metadata taxonomy node.",
   "input": {
    "type": "object",
    "properties": {
     "namespace": {
      "type": "string",
      "description": "The namespace of the metadata taxonomy."
     },
     "taxonomy_key": {
      "type": "string",
      "description": "The key of the metadata taxonomy."
     },
     "displayName": {
      "type": "string",
      "description": "The display name of the taxonomy node."
     },
     "level": {
      "type": "integer",
      "description": "The level of the taxonomy node."
     },
     "parentId": {
      "type": "string",
      "description": "The identifier of the parent taxonomy node. Omit this field for root-level nodes."
     }
    },
    "required": [
     "displayName",
     "level",
     "namespace",
     "taxonomy_key"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/metadata_taxonomies/{namespace}/{taxonomy_key}/nodes",
    "path": [
     "namespace",
     "taxonomy_key"
    ],
    "query": [],
    "header": [],
    "body": [
     "displayName",
     "level",
     "parentId"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_metadata_templates_schema",
   "name": "Create metadata template",
   "description": "Creates a new metadata template that can be applied to files and folders.",
   "input": {
    "type": "object",
    "properties": {
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template to create. Applications can only create templates for use within the authenticated user's enterprise. This value needs to be set to `enterprise`, as `global` scopes can not be created by applications."
     },
     "templateKey": {
      "type": "string",
      "description": "A unique identifier for the template. This identifier needs to be unique across the enterprise for which the metadata template is being created. When not provided, the API will create a unique `templateKey` based on the value of the `displayName`."
     },
     "displayName": {
      "type": "string",
      "description": "The display name of the template."
     },
     "hidden": {
      "type": "boolean",
      "description": "Defines if this template is visible in the Box web app UI, or if it is purely intended for usage through the API."
     },
     "fields": {
      "type": "array",
      "description": "An ordered list of template fields which are part of the template. Each field can be a regular text field, date field, number field, as well as a single or multi-select list."
     },
     "copyInstanceOnItemCopy": {
      "type": "boolean",
      "description": "Whether or not to copy any metadata attached to a file or folder when it is copied. By default, metadata is not copied along with a file or folder when it is copied."
     }
    },
    "required": [
     "displayName",
     "scope"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/metadata_templates/schema",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "scope",
     "templateKey",
     "displayName",
     "hidden",
     "fields",
     "copyInstanceOnItemCopy"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_metadata_templates_schema_classifications",
   "name": "Add initial classifications",
   "description": "When an enterprise does not yet have any classifications, this API call initializes the classification template with an initial set of classifications. If an enterprise already has a classification, the template will already exist and instead an API call should be made to add additional classifications.",
   "input": {
    "type": "object",
    "properties": {
     "scope": {
      "type": "string",
      "description": "The scope in which to create the classifications. This should be `enterprise` or `enterprise_{id}` where `id` is the unique ID of the enterprise.",
      "enum": [
       "enterprise"
      ]
     },
     "templateKey": {
      "type": "string",
      "description": "Defines the list of metadata templates.",
      "enum": [
       "securityClassification-6VMVochwUWo"
      ]
     },
     "displayName": {
      "type": "string",
      "description": "The name of the template as shown in web and mobile interfaces.",
      "enum": [
       "Classification"
      ]
     },
     "hidden": {
      "type": "boolean",
      "description": "Determines if the classification template is hidden or available on web and mobile devices."
     },
     "copyInstanceOnItemCopy": {
      "type": "boolean",
      "description": "Determines if classifications are copied along when the file or folder is copied."
     },
     "fields": {
      "type": "array",
      "description": "The classification template requires exactly one field, which holds all the valid classification values."
     }
    },
    "required": [
     "displayName",
     "fields",
     "scope",
     "templateKey"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/metadata_templates/schema#classifications",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "scope",
     "templateKey",
     "displayName",
     "hidden",
     "copyInstanceOnItemCopy",
     "fields"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_oauth2_revoke",
   "name": "Revoke access token",
   "description": "Revoke an active Access Token, effectively logging a user out that has been previously authenticated.",
   "input": {
    "type": "object",
    "properties": {
     "client_id": {
      "type": "string",
      "description": "The Client ID of the application requesting to revoke the access token."
     },
     "client_secret": {
      "type": "string",
      "description": "The client secret of the application requesting to revoke an access token."
     }
    },
    "required": [
     "grant_type"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/oauth2/revoke",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "client_id",
     "client_secret"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_oauth2_token",
   "name": "Request access token",
   "description": "Request an Access Token using either a client-side obtained OAuth 2.0 authorization code or a server-side JWT assertion. An Access Token is a string that enables Box to verify that a request belongs to an authorized session. In the normal order of operations you will begin by requesting authentication from the [authorize](https://developer.box.com/reference/get-authorize) endpoint and Box will sen",
   "input": {
    "type": "object",
    "properties": {
     "grant_type": {
      "type": "string",
      "description": "The type of request being made, either using a client-side obtained authorization code, a refresh token, a JWT assertion, client credentials grant or another access token for the purpose of downscoping a token.",
      "enum": [
       "authorization_code",
       "refresh_token",
       "client_credentials",
       "urn:ietf:params:oauth:grant-type:jwt-bearer",
       "urn:ietf:params:oauth:grant-type:token-exchange"
      ]
     },
     "client_id": {
      "type": "string",
      "description": "The Client ID of the application requesting an access token. Used in combination with `authorization_code`, `client_credentials`, or `urn:ietf:params:oauth:grant-type:jwt-bearer` as the `grant_type`."
     },
     "client_secret": {
      "type": "string",
      "description": "The client secret of the application requesting an access token. Used in combination with `authorization_code`, `client_credentials`, or `urn:ietf:params:oauth:grant-type:jwt-bearer` as the `grant_type`."
     },
     "code": {
      "type": "string",
      "description": "The client-side authorization code passed to your application by Box in the browser redirect after the user has successfully granted your application permission to make API calls on their behalf. Used in combination with `authorization_code` as the `grant_type`."
     },
     "refresh_token": {
      "type": "string",
      "description": "A refresh token used to get a new access token with. Used in combination with `refresh_token` as the `grant_type`."
     },
     "assertion": {
      "type": "string",
      "description": "A JWT assertion for which to request a new access token. Used in combination with `urn:ietf:params:oauth:grant-type:jwt-bearer` as the `grant_type`."
     },
     "subject_token": {
      "type": "string",
      "description": "The token to exchange for a downscoped token. This can be a regular access token, a JWT assertion, or an app token. Used in combination with `urn:ietf:params:oauth:grant-type:token-exchange` as the `grant_type`."
     },
     "subject_token_type": {
      "type": "string",
      "description": "The type of `subject_token` passed in. Used in combination with `urn:ietf:params:oauth:grant-type:token-exchange` as the `grant_type`.",
      "enum": [
       "urn:ietf:params:oauth:token-type:access_token"
      ]
     },
     "actor_token": {
      "type": "string",
      "description": "The token used to create an annotator token. This is a JWT assertion. Used in combination with `urn:ietf:params:oauth:grant-type:token-exchange` as the `grant_type`."
     },
     "actor_token_type": {
      "type": "string",
      "description": "The type of `actor_token` passed in. Used in combination with `urn:ietf:params:oauth:grant-type:token-exchange` as the `grant_type`.",
      "enum": [
       "urn:ietf:params:oauth:token-type:id_token"
      ]
     },
     "scope": {
      "type": "string",
      "description": "The space-delimited list of scopes that you want apply to the new access token. The `subject_token` will need to have all of these scopes or the call will error with **401 Unauthorized**.."
     },
     "resource": {
      "type": "string",
      "description": "Full URL for the file that the token should be generated for."
     },
     "box_subject_type": {
      "type": "string",
      "description": "Used in combination with `client_credentials` as the `grant_type`.",
      "enum": [
       "enterprise",
       "user"
      ]
     },
     "box_subject_id": {
      "type": "string",
      "description": "Used in combination with `client_credentials` as the `grant_type`. Value is determined by `box_subject_type`. If `user` use user ID and if `enterprise` use enterprise ID."
     },
     "box_shared_link": {
      "type": "string",
      "description": "Full URL of the shared link on the file or folder that the token should be generated for."
     }
    },
    "required": [
     "grant_type"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/oauth2/token",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "grant_type",
     "client_id",
     "client_secret",
     "code",
     "refresh_token",
     "assertion",
     "subject_token",
     "subject_token_type",
     "actor_token",
     "actor_token_type",
     "scope",
     "resource",
     "box_subject_type",
     "box_subject_id",
     "box_shared_link"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_oauth2_token_refresh",
   "name": "Refresh access token",
   "description": "Refresh an Access Token using its client ID, secret, and refresh token.",
   "input": {
    "type": "object",
    "properties": {
     "grant_type": {
      "type": "string",
      "description": "The type of request being made, in this case a refresh request.",
      "enum": [
       "refresh_token"
      ]
     },
     "client_id": {
      "type": "string",
      "description": "The client ID of the application requesting to refresh the token."
     },
     "client_secret": {
      "type": "string",
      "description": "The client secret of the application requesting to refresh the token."
     },
     "refresh_token": {
      "type": "string",
      "description": "The refresh token to refresh."
     }
    },
    "required": [
     "client_id",
     "client_secret",
     "grant_type",
     "refresh_token"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/oauth2/token#refresh",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "grant_type",
     "client_id",
     "client_secret",
     "refresh_token"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_retention_policies",
   "name": "Create retention policy",
   "description": "Creates a retention policy.",
   "input": {
    "type": "object",
    "properties": {
     "policy_name": {
      "type": "string",
      "description": "The name for the retention policy."
     },
     "description": {
      "type": "string",
      "description": "The additional text description of the retention policy."
     },
     "policy_type": {
      "type": "string",
      "description": "The type of the retention policy. A retention policy type can either be `finite`, where a specific amount of time to retain the content is known upfront, or `indefinite`, where the amount of time to retain the content is still unknown.",
      "enum": [
       "finite",
       "indefinite"
      ]
     },
     "disposition_action": {
      "type": "string",
      "description": "The disposition action of the retention policy. `permanently_delete` deletes the content retained by the policy permanently. `remove_retention` lifts retention policy from the content, allowing it to be deleted by users once the retention policy has expired.",
      "enum": [
       "permanently_delete",
       "remove_retention"
      ]
     },
     "retention_length": {
      "type": "string",
      "description": "The length of the retention policy. This value specifies the duration in days that the retention policy will be active for after being assigned to content. If the policy has a `policy_type` of `indefinite`, the `retention_length` will also be `indefinite`."
     },
     "retention_type": {
      "type": "string",
      "description": "Specifies the retention type: * `modifiable`: You can modify the retention policy. For example, you can add or remove folders, shorten or lengthen the policy duration, or delete the assignment. Use this type if your retention policy is not related to any regulatory purposes. * `non_modifiable`: You can modify the retention policy only in a limited way: add a folder, lengthen the duration, retire t",
      "enum": [
       "modifiable",
       "non_modifiable"
      ]
     },
     "can_owner_extend_retention": {
      "type": "boolean",
      "description": "Whether the owner of a file will be allowed to extend the retention."
     },
     "max_extension_length": {
      "type": "string",
      "description": "The maximum extension length of the retention date. This value specifies the duration in days for which the retention date of the file under policy can be extended. It can be specified only for the 'finite' policy type where the disposition action is 'permanently delete', otherwise the server will return status 400. If this value is 'none', it won't be possible to extend the retention."
     },
     "are_owners_notified": {
      "type": "boolean",
      "description": "Whether owner and co-owners of a file are notified when the policy nears expiration."
     },
     "custom_notification_recipients": {
      "type": "array",
      "description": "A list of users notified when the retention policy duration is about to end."
     }
    },
    "required": [
     "disposition_action",
     "policy_name",
     "policy_type"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/retention_policies",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "policy_name",
     "description",
     "policy_type",
     "disposition_action",
     "retention_length",
     "retention_type",
     "can_owner_extend_retention",
     "max_extension_length",
     "are_owners_notified",
     "custom_notification_recipients"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_retention_policy_assignments",
   "name": "Assign retention policy",
   "description": "Assigns a retention policy to an item.",
   "input": {
    "type": "object",
    "properties": {
     "policy_id": {
      "type": "string",
      "description": "The ID of the retention policy to assign."
     },
     "assign_to": {
      "type": "object",
      "description": "The item to assign the policy to."
     },
     "filter_fields": {
      "type": "array",
      "description": "If the `assign_to` type is `metadata_template`, then optionally add the `filter_fields` parameter which will require an array of objects with a field entry and a value entry. Currently only one object of `field` and `value` is supported."
     },
     "start_date_field": {
      "type": "string",
      "description": "The date the retention policy assignment begins. If the `assigned_to` type is `metadata_template`, this field can be a date field's metadata attribute key id."
     }
    },
    "required": [
     "assign_to",
     "policy_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/retention_policy_assignments",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "policy_id",
     "assign_to",
     "filter_fields",
     "start_date_field"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_shield_information_barrier_reports",
   "name": "Create shield information barrier report",
   "description": "Creates a shield information barrier report for a given barrier.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier": {
      "type": "object",
      "description": "A base representation of a shield information barrier object."
     }
    },
    "required": []
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/shield_information_barrier_reports",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "shield_information_barrier"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_shield_information_barrier_segment_members",
   "name": "Create shield information barrier segment member",
   "description": "Creates a new shield information barrier segment member.",
   "input": {
    "type": "object",
    "properties": {
     "type": {
      "type": "string",
      "description": "A type of the shield barrier segment member.",
      "enum": [
       "shield_information_barrier_segment_member"
      ]
     },
     "shield_information_barrier": {
      "type": "object",
      "description": "A base representation of a shield information barrier object."
     },
     "shield_information_barrier_segment": {
      "type": "object",
      "description": "The `type` and `id` of the requested shield information barrier segment."
     },
     "user": {
      "type": "object",
      "description": "User to which restriction will be applied."
     }
    },
    "required": [
     "shield_information_barrier_segment",
     "user"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/shield_information_barrier_segment_members",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "type",
     "shield_information_barrier",
     "shield_information_barrier_segment",
     "user"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_shield_information_barrier_segment_restrictions",
   "name": "Create shield information barrier segment restriction",
   "description": "Creates a shield information barrier segment restriction object.",
   "input": {
    "type": "object",
    "properties": {
     "type": {
      "type": "string",
      "description": "The type of the shield barrier segment restriction for this member.",
      "enum": [
       "shield_information_barrier_segment_restriction"
      ]
     },
     "shield_information_barrier": {
      "type": "object",
      "description": "A base representation of a shield information barrier object."
     },
     "shield_information_barrier_segment": {
      "type": "object",
      "description": "The `type` and `id` of the requested shield information barrier segment."
     },
     "restricted_segment": {
      "type": "object",
      "description": "The `type` and `id` of the restricted shield information barrier segment."
     }
    },
    "required": [
     "restricted_segment",
     "shield_information_barrier_segment",
     "type"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/shield_information_barrier_segment_restrictions",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "type",
     "shield_information_barrier",
     "shield_information_barrier_segment",
     "restricted_segment"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_shield_information_barrier_segments",
   "name": "Create shield information barrier segment",
   "description": "Creates a shield information barrier segment.",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier": {
      "type": "object",
      "description": "A base representation of a shield information barrier object."
     },
     "name": {
      "type": "string",
      "description": "Name of the shield information barrier segment."
     },
     "description": {
      "type": "string",
      "description": "Description of the shield information barrier segment."
     }
    },
    "required": [
     "name",
     "shield_information_barrier"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/shield_information_barrier_segments",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "shield_information_barrier",
     "name",
     "description"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_shield_information_barriers",
   "name": "Create shield information barrier",
   "description": "Creates a shield information barrier to separate individuals/groups within the same firm and prevents confidential information passing between them.",
   "input": {
    "type": "object",
    "properties": {
     "enterprise": {
      "type": "object",
      "description": "The `type` and `id` of enterprise this barrier is under."
     }
    },
    "required": [
     "enterprise"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/shield_information_barriers",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "enterprise"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_shield_information_barriers_change_status",
   "name": "Add changed status of shield information barrier with specified id",
   "description": "Change status of shield information barrier with the specified ID.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "string",
      "description": "The ID of the shield information barrier."
     },
     "status": {
      "type": "string",
      "description": "The desired status for the shield information barrier.",
      "enum": [
       "pending",
       "disabled"
      ]
     }
    },
    "required": [
     "id",
     "status"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/shield_information_barriers/change_status",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "id",
     "status"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_sign_requests",
   "name": "Create box sign request",
   "description": "Creates a signature request. This involves preparing a document for signing and sending the signature request to signers.",
   "input": {
    "type": "object",
    "properties": {
     "is_document_preparation_needed": {
      "type": "boolean",
      "description": "Indicates if the sender should receive a `prepare_url` in the response to complete document preparation using the UI."
     },
     "redirect_url": {
      "type": "string",
      "description": "When specified, the signature request will be redirected to this url when a document is signed."
     },
     "declined_redirect_url": {
      "type": "string",
      "description": "The uri that a signer will be redirected to after declining to sign a document."
     },
     "are_text_signatures_enabled": {
      "type": "boolean",
      "description": "Disables the usage of signatures generated by typing (text)."
     },
     "email_subject": {
      "type": "string",
      "description": "Subject of sign request email. This is cleaned by sign request. If this field is not passed, a default subject will be used."
     },
     "email_message": {
      "type": "string",
      "description": "Message to include in sign request email. The field is cleaned through sanitization of specific characters. However, some html tags are allowed. Links included in the message are also converted to hyperlinks in the email. The message may contain the following html tags including `a`, `abbr`, `acronym`, `b`, `blockquote`, `code`, `em`, `i`, `ul`, `li`, `ol`, and `strong`. Be aware that when the tex"
     },
     "are_reminders_enabled": {
      "type": "boolean",
      "description": "Reminds signers to sign a document on day 3, 8, 13 and 18. Reminders are only sent to outstanding signers."
     },
     "name": {
      "type": "string",
      "description": "Name of the signature request."
     },
     "prefill_tags": {
      "type": "array",
      "description": "When a document contains sign-related tags in the content, you can prefill them using this `prefill_tags` by referencing the 'id' of the tag as the `external_id` field of the prefill tag."
     },
     "days_valid": {
      "type": "integer",
      "description": "Set the number of days after which the created signature request will automatically expire if not completed. By default, we do not apply any expiration date on signature requests, and the signature request does not expire."
     },
     "external_id": {
      "type": "string",
      "description": "This can be used to reference an ID in an external system that the sign request is related to."
     },
     "template_id": {
      "type": "string",
      "description": "When a signature request is created from a template this field will indicate the id of that template."
     },
     "external_system_name": {
      "type": "string",
      "description": "Used as an optional system name to appear in the signature log next to the signers who have been assigned the `embed_url_external_id`."
     },
     "request_flow": {
      "type": "string",
      "description": "The flow type of the sign request. Values can include `standard` or `cfr11`. When not specified during creation, a default is chosen based on admin settings."
     }
    },
    "required": [
     "signers"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/sign_requests",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "is_document_preparation_needed",
     "redirect_url",
     "declined_redirect_url",
     "are_text_signatures_enabled",
     "email_subject",
     "email_message",
     "are_reminders_enabled",
     "name",
     "prefill_tags",
     "days_valid",
     "external_id",
     "template_id",
     "external_system_name",
     "request_flow"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_sign_requests_id_cancel",
   "name": "Cancel box sign request",
   "description": "Cancels a sign request.",
   "input": {
    "type": "object",
    "properties": {
     "sign_request_id": {
      "type": "string",
      "description": "The ID of the signature request."
     },
     "reason": {
      "type": "string",
      "description": "An optional reason for cancelling the sign request."
     }
    },
    "required": [
     "sign_request_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/sign_requests/{sign_request_id}/cancel",
    "path": [
     "sign_request_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "reason"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_sign_requests_id_resend",
   "name": "Resend box sign request",
   "description": "Resends a signature request email to all outstanding signers.",
   "input": {
    "type": "object",
    "properties": {
     "sign_request_id": {
      "type": "string",
      "description": "The ID of the signature request."
     }
    },
    "required": [
     "sign_request_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/sign_requests/{sign_request_id}/resend",
    "path": [
     "sign_request_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_storage_policy_assignments",
   "name": "Assign storage policy",
   "description": "Creates a storage policy assignment for an enterprise or user.",
   "input": {
    "type": "object",
    "properties": {
     "storage_policy": {
      "type": "object",
      "description": "The storage policy to assign to the user or enterprise."
     },
     "assigned_to": {
      "type": "object",
      "description": "The user or enterprise to assign the storage policy to."
     }
    },
    "required": [
     "assigned_to",
     "storage_policy"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/storage_policy_assignments",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "storage_policy",
     "assigned_to"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_task_assignments",
   "name": "Assign task",
   "description": "Assigns a task to a user. A task can be assigned to more than one user by creating multiple assignments.",
   "input": {
    "type": "object",
    "properties": {
     "task": {
      "type": "object",
      "description": "The task to assign to a user."
     },
     "assign_to": {
      "type": "object",
      "description": "The user to assign the task to."
     }
    },
    "required": [
     "assign_to",
     "task"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/task_assignments",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "task",
     "assign_to"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_tasks",
   "name": "Create task",
   "description": "Creates a single task on a file. This task is not assigned to any user and will need to be assigned separately.",
   "input": {
    "type": "object",
    "properties": {
     "item": {
      "type": "object",
      "description": "The file to attach the task to."
     },
     "action": {
      "type": "string",
      "description": "The action the task assignee will be prompted to do. Must be * `review` defines an approval task that can be approved or, rejected * `complete` defines a general task which can be completed.",
      "enum": [
       "review",
       "complete"
      ]
     },
     "message": {
      "type": "string",
      "description": "An optional message to include with the task."
     },
     "due_at": {
      "type": "string",
      "description": "Defines when the task is due. Defaults to `null` if not provided."
     },
     "completion_rule": {
      "type": "string",
      "description": "Defines which assignees need to complete this task before the task is considered completed. * `all_assignees` (default) requires all assignees to review or approve the task in order for it to be considered completed. * `any_assignee` accepts any one assignee to review or approve the task in order for it to be considered completed.",
      "enum": [
       "all_assignees",
       "any_assignee"
      ]
     }
    },
    "required": [
     "item"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/tasks",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "item",
     "action",
     "message",
     "due_at",
     "completion_rule"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_terms_of_service_user_statuses",
   "name": "Create terms of service status for new user",
   "description": "Sets the status for a terms of service for a user.",
   "input": {
    "type": "object",
    "properties": {
     "tos": {
      "type": "object",
      "description": "The terms of service to set the status for."
     },
     "user": {
      "type": "object",
      "description": "The user to set the status for."
     },
     "is_accepted": {
      "type": "boolean",
      "description": "Whether the user has accepted the terms."
     }
    },
    "required": [
     "is_accepted",
     "tos",
     "user"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/terms_of_service_user_statuses",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "tos",
     "user",
     "is_accepted"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_terms_of_services",
   "name": "Create terms of service",
   "description": "Creates a terms of service for a given enterprise and type of user.",
   "input": {
    "type": "object",
    "properties": {
     "status": {
      "type": "string",
      "description": "Whether this terms of service is active.",
      "enum": [
       "enabled",
       "disabled"
      ]
     },
     "tos_type": {
      "type": "string",
      "description": "The type of user to set the terms of service for.",
      "enum": [
       "external",
       "managed"
      ]
     },
     "text": {
      "type": "string",
      "description": "The terms of service text to display to users. The text can be set to empty if the `status` is set to `disabled`."
     }
    },
    "required": [
     "status",
     "text"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/terms_of_services",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "status",
     "tos_type",
     "text"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_users",
   "name": "Create user",
   "description": "Creates a new managed user in an enterprise. This endpoint is only available to users and applications with the right admin permissions.",
   "input": {
    "type": "object",
    "properties": {
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "name": {
      "type": "string",
      "description": "The name of the user."
     },
     "login": {
      "type": "string",
      "description": "The email address the user uses to log in Required, unless `is_platform_access_only` is set to `true`."
     },
     "is_platform_access_only": {
      "type": "boolean",
      "description": "Specifies that the user is an app user."
     },
     "role": {
      "type": "string",
      "description": "The user\u2019s enterprise role.",
      "enum": [
       "coadmin",
       "user"
      ]
     },
     "language": {
      "type": "string",
      "description": "The language of the user, formatted in modified version of the [ISO 639-1](https://developer.box.com/guides/api-calls/language-codes) format."
     },
     "is_sync_enabled": {
      "type": "boolean",
      "description": "Whether the user can use Box Sync."
     },
     "job_title": {
      "type": "string",
      "description": "The user\u2019s job title."
     },
     "phone": {
      "type": "string",
      "description": "The user\u2019s phone number."
     },
     "address": {
      "type": "string",
      "description": "The user\u2019s address."
     },
     "space_amount": {
      "type": "integer",
      "description": "The user\u2019s total available space in bytes. Set this to `-1` to indicate unlimited storage."
     },
     "tracking_codes": {
      "type": "array",
      "description": "Tracking codes allow an admin to generate reports from the admin console and assign an attribute to a specific group of users. This setting must be enabled for an enterprise before it can be used."
     },
     "can_see_managed_users": {
      "type": "boolean",
      "description": "Whether the user can see other enterprise users in their contact list."
     },
     "timezone": {
      "type": "string",
      "description": "The user's timezone."
     },
     "is_external_collab_restricted": {
      "type": "boolean",
      "description": "Whether the user is allowed to collaborate with users outside their enterprise."
     },
     "is_exempt_from_device_limits": {
      "type": "boolean",
      "description": "Whether to exempt the user from enterprise device limits."
     },
     "is_exempt_from_login_verification": {
      "type": "boolean",
      "description": "Whether the user must use two-factor authentication."
     },
     "status": {
      "type": "string",
      "description": "The user's account status.",
      "enum": [
       "active",
       "inactive",
       "cannot_delete_edit",
       "cannot_delete_edit_upload"
      ]
     },
     "external_app_user_id": {
      "type": "string",
      "description": "An external identifier for an app user, which can be used to look up the user. This can be used to tie user IDs from external identity providers to Box users."
     }
    },
    "required": [
     "name"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/users",
    "path": [],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "name",
     "login",
     "is_platform_access_only",
     "role",
     "language",
     "is_sync_enabled",
     "job_title",
     "phone",
     "address",
     "space_amount",
     "tracking_codes",
     "can_see_managed_users",
     "timezone",
     "is_external_collab_restricted",
     "is_exempt_from_device_limits",
     "is_exempt_from_login_verification",
     "status",
     "external_app_user_id"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_users_id_avatar",
   "name": "Add or update user avatar",
   "description": "Adds or updates a user avatar.",
   "input": {
    "type": "object",
    "properties": {
     "user_id": {
      "type": "string",
      "description": "The ID of the user."
     },
     "pic": {
      "type": "string",
      "description": "The image file to be uploaded to Box. Accepted file extensions are `.jpg` or `.png`. The maximum file size is 1MB."
     }
    },
    "required": [
     "pic",
     "user_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/users/{user_id}/avatar",
    "path": [
     "user_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "pic"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_users_id_email_aliases",
   "name": "Create email alias",
   "description": "Adds a new email alias to a user account..",
   "input": {
    "type": "object",
    "properties": {
     "user_id": {
      "type": "string",
      "description": "The ID of the user."
     },
     "email": {
      "type": "string",
      "description": "The email address to add to the account as an alias. Note: The domain of the email alias needs to be registered to your enterprise. See the [domain verification guide]( https://support.box.com/hc/en-us/articles/4408619650579-Domain-Verification ) for steps to add a new domain."
     }
    },
    "required": [
     "email",
     "user_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/users/{user_id}/email_aliases",
    "path": [
     "user_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "email"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_users_terminate_sessions",
   "name": "Create jobs to terminate users session",
   "description": "Validates the roles and permissions of the user, and creates asynchronous jobs to terminate the user's sessions. Returns the status for the POST request.",
   "input": {
    "type": "object",
    "properties": {
     "user_ids": {
      "type": "array",
      "description": "A list of user IDs."
     },
     "user_logins": {
      "type": "array",
      "description": "A list of user logins."
     }
    },
    "required": [
     "user_ids",
     "user_logins"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/users/terminate_sessions",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "user_ids",
     "user_logins"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_web_links",
   "name": "Create web link",
   "description": "Creates a web link object within a folder.",
   "input": {
    "type": "object",
    "properties": {
     "url": {
      "type": "string",
      "description": "The URL that this web link links to. Must start with `\"http://\"` or `\"https://\"`."
     },
     "parent": {
      "type": "object",
      "description": "The parent folder to create the web link within."
     },
     "name": {
      "type": "string",
      "description": "Name of the web link. Defaults to the URL if not set."
     },
     "description": {
      "type": "string",
      "description": "Description of the web link."
     }
    },
    "required": [
     "parent",
     "url"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/web_links",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "url",
     "parent",
     "name",
     "description"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_web_links_id",
   "name": "Restore web link",
   "description": "Restores a web link that has been moved to the trash. An optional new parent ID can be provided to restore the web link to in case the original folder has been deleted.",
   "input": {
    "type": "object",
    "properties": {
     "web_link_id": {
      "type": "string",
      "description": "The ID of the web link."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "name": {
      "type": "string",
      "description": "An optional new name for the web link."
     },
     "parent": {
      "type": "object",
      "description": "The parent for this item."
     }
    },
    "required": [
     "web_link_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/web_links/{web_link_id}",
    "path": [
     "web_link_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "name",
     "parent"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_webhooks",
   "name": "Create webhook",
   "description": "Creates a webhook.",
   "input": {
    "type": "object",
    "properties": {
     "target": {
      "type": "object",
      "description": "The item that will trigger the webhook."
     },
     "address": {
      "type": "string",
      "description": "The URL that is notified by this webhook."
     },
     "triggers": {
      "type": "array",
      "description": "An array of event names that this webhook is to be triggered for."
     }
    },
    "required": [
     "address",
     "target",
     "triggers"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/webhooks",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "target",
     "address",
     "triggers"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_workflows_id_start",
   "name": "Starts workflow based on request body",
   "description": "Initiates a flow with a trigger type of `WORKFLOW_MANUAL_START`. You application must be authorized to use the `Manage Box Relay` application scope within the developer console.",
   "input": {
    "type": "object",
    "properties": {
     "workflow_id": {
      "type": "string",
      "description": "The ID of the workflow."
     },
     "type": {
      "type": "string",
      "description": "The type of the parameters object.",
      "enum": [
       "workflow_parameters"
      ]
     },
     "flow": {
      "type": "object",
      "description": "The flow that will be triggered."
     },
     "files": {
      "type": "array",
      "description": "The array of files for which the workflow should start. All files must be in the workflow's configured folder."
     },
     "folder": {
      "type": "object",
      "description": "The folder object for which the workflow is configured."
     },
     "outcomes": {
      "type": "array",
      "description": "A configurable outcome the workflow should complete."
     }
    },
    "required": [
     "files",
     "flow",
     "folder",
     "workflow_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/workflows/{workflow_id}/start",
    "path": [
     "workflow_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "type",
     "flow",
     "files",
     "folder",
     "outcomes"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.post_zip_downloads",
   "name": "Create zip download",
   "description": "Creates a request to download multiple files and folders as a single `zip` archive file. This API does not return the archive but instead performs all the checks to ensure that the user has access to all the items, and then returns a `download_url` and a `status_url` that can be used to download the archive. The limit for an archive is either the Account's upload limit or 10,000 files, whichever i",
   "input": {
    "type": "object",
    "properties": {
     "items": {
      "type": "array",
      "description": "A list of items to add to the `zip` archive. These can be folders or files."
     },
     "download_file_name": {
      "type": "string",
      "description": "The optional name of the `zip` archive. This name will be appended by the `.zip` file extension, for example `January Financials.zip`."
     }
    },
    "required": [
     "items"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.box.com/2.0/zip_downloads",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "items",
     "download_file_name"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_ai_agents_id",
   "name": "Update ai agent",
   "description": "Updates an AI agent.",
   "input": {
    "type": "object",
    "properties": {
     "agent_id": {
      "type": "string",
      "description": "The ID of the agent to update."
     },
     "type": {
      "type": "string",
      "description": "The type of agent used to handle queries.",
      "enum": [
       "ai_agent"
      ]
     },
     "name": {
      "type": "string",
      "description": "The name of the AI Agent."
     },
     "access_state": {
      "type": "string",
      "description": "The state of the AI Agent. Possible values are: `enabled`, `disabled`, and `enabled_for_selected_users`."
     },
     "icon_reference": {
      "type": "string",
      "description": "The icon reference of the AI Agent. It should have format of the URL `https://cdn01.boxcdn.net/app-assets/aistudio/avatars/` where possible values of `file_name` are: `logo_boxAi.png`,`logo_stamp.png`,`logo_legal.png`,`logo_finance.png`,`logo_config.png`,`logo_handshake.png`,`logo_analytics.png`,`logo_classification.png`."
     },
     "allowed_entities": {
      "type": "array",
      "description": "List of allowed users or groups."
     },
     "ask": {
      "type": "object",
      "description": "The AI agent to be used to handle queries."
     },
     "text_gen": {
      "type": "object",
      "description": "The AI agent to be used to generate text."
     },
     "extract": {
      "type": "object",
      "description": "The AI agent to be used for metadata extraction."
     }
    },
    "required": [
     "access_state",
     "agent_id",
     "name",
     "type"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/ai_agents/{agent_id}",
    "path": [
     "agent_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "type",
     "name",
     "access_state",
     "icon_reference",
     "allowed_entities",
     "ask",
     "text_gen",
     "extract"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_collaborations_id",
   "name": "Update collaboration",
   "description": "Updates a collaboration. Can be used to change the owner of an item, or to accept collaboration invites. In case of accepting collaboration invite, role is not required.",
   "input": {
    "type": "object",
    "properties": {
     "collaboration_id": {
      "type": "string",
      "description": "The ID of the collaboration."
     },
     "role": {
      "type": "string",
      "description": "The level of access granted.",
      "enum": [
       "editor",
       "viewer",
       "previewer",
       "uploader",
       "previewer uploader",
       "viewer uploader",
       "co-owner",
       "owner"
      ]
     },
     "status": {
      "type": "string",
      "description": "Set the status of a `pending` collaboration invitation, effectively accepting, or rejecting the invite.",
      "enum": [
       "pending",
       "accepted",
       "rejected"
      ]
     },
     "expires_at": {
      "type": "string",
      "description": "Update the expiration date for the collaboration. At this date, the collaboration will be automatically removed from the item. This feature will only work if the **Automatically remove invited collaborators: Allow folder owners to extend the expiry date** setting has been enabled in the **Enterprise Settings** of the **Admin Console**. When the setting is not enabled, collaborations can not have a"
     },
     "can_view_path": {
      "type": "boolean",
      "description": "Determines if the invited users can see the entire parent path to the associated folder. The user will not gain privileges in any parent folder and therefore can not see content the user is not collaborated on. Be aware that this meaningfully increases the time required to load the invitee's **All Files** page. We recommend you limit the number of collaborations with `can_view_path` enabled to 1,0"
     }
    },
    "required": [
     "collaboration_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/collaborations/{collaboration_id}",
    "path": [
     "collaboration_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "role",
     "status",
     "expires_at",
     "can_view_path"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_comments_id",
   "name": "Update comment",
   "description": "Update the message of a comment.",
   "input": {
    "type": "object",
    "properties": {
     "comment_id": {
      "type": "string",
      "description": "The ID of the comment."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "message": {
      "type": "string",
      "description": "The text of the comment to update."
     }
    },
    "required": [
     "comment_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/comments/{comment_id}",
    "path": [
     "comment_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "message"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_file_requests_id",
   "name": "Update file request",
   "description": "Updates a file request. This can be used to activate or deactivate a file request.",
   "input": {
    "type": "object",
    "properties": {
     "file_request_id": {
      "type": "string",
      "description": "The unique identifier that represent a file request. The ID for any file request can be determined by visiting a file request builder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/filerequest/123` the `file_request_id` is `123`."
     },
     "if-match": {
      "type": "string",
      "description": "Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since."
     },
     "title": {
      "type": "string",
      "description": "An optional new title for the file request. This can be used to change the title of the file request. This will default to the value on the existing file request."
     },
     "description": {
      "type": "string",
      "description": "An optional new description for the file request. This can be used to change the description of the file request. This will default to the value on the existing file request."
     },
     "status": {
      "type": "string",
      "description": "An optional new status of the file request. When the status is set to `inactive`, the file request will no longer accept new submissions, and any visitor to the file request URL will receive a `HTTP 404` status code. This will default to the value on the existing file request.",
      "enum": [
       "active",
       "inactive"
      ]
     },
     "is_email_required": {
      "type": "boolean",
      "description": "Whether a file request submitter is required to provide their email address. When this setting is set to true, the Box UI will show an email field on the file request form. This will default to the value on the existing file request."
     },
     "is_description_required": {
      "type": "boolean",
      "description": "Whether a file request submitter is required to provide a description of the files they are submitting. When this setting is set to true, the Box UI will show a description field on the file request form. This will default to the value on the existing file request."
     },
     "expires_at": {
      "type": "string",
      "description": "The date after which a file request will no longer accept new submissions. After this date, the `status` will automatically be set to `inactive`. This will default to the value on the existing file request."
     }
    },
    "required": [
     "file_request_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/file_requests/{file_request_id}",
    "path": [
     "file_request_id"
    ],
    "query": [],
    "header": [
     "if-match"
    ],
    "body": [
     "title",
     "description",
     "status",
     "is_email_required",
     "is_description_required",
     "expires_at"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_files_id",
   "name": "Update file",
   "description": "Updates a file. This can be used to rename or move a file, create a shared link, or lock a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "if-match": {
      "type": "string",
      "description": "Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since."
     },
     "name": {
      "type": "string",
      "description": "An optional different name for the file. This can be used to rename the file. File names must be unique within their parent folder. The name check is case-insensitive, so a file named `New File` cannot be created in a parent folder that already contains a folder named `new file`."
     },
     "description": {
      "type": "string",
      "description": "The description for a file. This can be seen in the right-hand sidebar panel when viewing a file in the Box web app. Additionally, this index is used in the search index of the file, allowing users to find the file by the content in the description."
     },
     "parent": {
      "type": "object",
      "description": "The parent for this item."
     },
     "shared_link": {
      "type": "object",
      "description": "Defines a shared link for an item. Set this to `null` to remove the shared link."
     },
     "lock": {
      "type": "object",
      "description": "Defines a lock on an item. This prevents the item from being moved, renamed, or otherwise changed by anyone other than the user who created the lock. Set this to `null` to remove the lock."
     },
     "disposition_at": {
      "type": "string",
      "description": "The retention expiration timestamp for the given file. This date cannot be shortened once set on a file."
     },
     "permissions": {
      "type": "object",
      "description": "Defines who can download a file."
     },
     "collections": {
      "type": "array",
      "description": "An array of collections to make this file a member of. Currently we only support the `favorites` collection. To get the ID for a collection, use the [List all collections][1] endpoint. Passing an empty array `[]` or `null` will remove the file from all collections. [1]: https://developer.box.com/reference/get-collections"
     },
     "tags": {
      "type": "array",
      "description": "The tags for this item. These tags are shown in the Box web app and mobile apps next to an item. To add or remove a tag, retrieve the item's current tags, modify them, and then update this field. There is a limit of 100 tags per item, and 10,000 unique tags per enterprise."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/files/{file_id}",
    "path": [
     "file_id"
    ],
    "query": [
     "fields"
    ],
    "header": [
     "if-match"
    ],
    "body": [
     "name",
     "description",
     "parent",
     "shared_link",
     "lock",
     "disposition_at",
     "permissions",
     "collections",
     "tags"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_files_id_add_shared_link",
   "name": "Add shared link to file",
   "description": "Adds a shared link to a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     },
     "shared_link": {
      "type": "object",
      "description": "The settings for the shared link to create on the file. Use an empty object (`{}`) to use the default settings for shared links."
     }
    },
    "required": [
     "fields",
     "file_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/files/{file_id}#add_shared_link",
    "path": [
     "file_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "shared_link"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_files_id_metadata_enterprise_security_classification_6_vmvochw_uwo",
   "name": "Update classification on file",
   "description": "Updates a classification on a file. The classification can only be updated if a classification has already been applied to the file before. When editing classifications, only values are defined for the enterprise will be accepted.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_files_id_metadata_global_box_skills_cards",
   "name": "Update box skill cards on file",
   "description": "Updates one or more Box Skills metadata cards to a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     }
    },
    "required": [
     "file_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/global/boxSkillsCards",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_files_id_metadata_id_id",
   "name": "Update metadata instance on file",
   "description": "Updates a piece of metadata on a file. The metadata instance can only be updated if the template has already been applied to the file before. When editing metadata, only values that match the metadata template schema will be accepted. The update is applied atomically. If any errors occur during the application of the operations, the metadata instance will not be changed.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     }
    },
    "required": [
     "file_id",
     "scope",
     "template_key"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/files/{file_id}/metadata/{scope}/{template_key}",
    "path": [
     "file_id",
     "scope",
     "template_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_files_id_remove_shared_link",
   "name": "Remove shared link from file",
   "description": "Removes a shared link from a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     },
     "shared_link": {
      "type": "object",
      "description": "By setting this value to `null`, the shared link is removed from the file."
     }
    },
    "required": [
     "fields",
     "file_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/files/{file_id}#remove_shared_link",
    "path": [
     "file_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "shared_link"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_files_id_update_shared_link",
   "name": "Update shared link on file",
   "description": "Updates a shared link on a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     },
     "shared_link": {
      "type": "object",
      "description": "The settings for the shared link to update."
     }
    },
    "required": [
     "fields",
     "file_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/files/{file_id}#update_shared_link",
    "path": [
     "file_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "shared_link"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_files_id_versions_id",
   "name": "Restore file version",
   "description": "Restores a specific version of a file after it was deleted. Don't use this endpoint to restore Box Notes, as it works with file formats such as PDF, DOC, PPTX or similar.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "file_version_id": {
      "type": "string",
      "description": "The ID of the file version."
     },
     "trashed_at": {
      "type": "string",
      "description": "Set this to `null` to clear the date and restore the file."
     }
    },
    "required": [
     "file_id",
     "file_version_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/files/{file_id}/versions/{file_version_id}",
    "path": [
     "file_id",
     "file_version_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "trashed_at"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_files_id_watermark",
   "name": "Apply watermark to file",
   "description": "Applies or update a watermark on a file.",
   "input": {
    "type": "object",
    "properties": {
     "file_id": {
      "type": "string",
      "description": "The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
     },
     "watermark": {
      "type": "object",
      "description": "The watermark to imprint on the file."
     }
    },
    "required": [
     "file_id",
     "watermark"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/files/{file_id}/watermark",
    "path": [
     "file_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "watermark"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_files_upload_sessions_id",
   "name": "Upload part of file",
   "description": "Uploads a chunk of a file for an upload session. The actual endpoint URL is returned by the [`Create upload session`](https://developer.box.com/reference/post-files-upload-sessions) and [`Get upload session`](https://developer.box.com/reference/get-files-upload-sessions-id) endpoints.",
   "input": {
    "type": "object",
    "properties": {
     "upload_session_id": {
      "type": "string",
      "description": "The ID of the upload session."
     },
     "digest": {
      "type": "string",
      "description": "The [RFC3230][1] message digest of the chunk uploaded. Only SHA1 is supported. The SHA1 digest must be base64 encoded. The format of this header is as `sha=BASE64_ENCODED_DIGEST`. To get the value for the `SHA` digest, use the openSSL command to encode the file part: `openssl sha1 -binary | base64`. [1]: https://tools.ietf.org/html/rfc3230"
     },
     "content-range": {
      "type": "string",
      "description": "The byte range of the chunk. Must not overlap with the range of a part already uploaded this session. Each part\u2019s size must be exactly equal in size to the part size specified in the upload session that you created. One exception is the last part of the file, as this can be smaller. When providing the value for `content-range`, remember that: * The lower bound of each part's byte range must be a m"
     }
    },
    "required": [
     "content-range",
     "digest",
     "upload_session_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/files/upload_sessions/{upload_session_id}",
    "path": [
     "upload_session_id"
    ],
    "query": [],
    "header": [
     "digest",
     "content-range"
    ],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_folders_id",
   "name": "Update folder",
   "description": "Updates a folder. This can be also be used to move the folder, create shared links, update collaborations, and more.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "if-match": {
      "type": "string",
      "description": "Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since."
     },
     "name": {
      "type": "string",
      "description": "The optional new name for this folder. The following restrictions to folder names apply: names containing non-printable ASCII characters, forward and backward slashes (`/`, `\\`), names with trailing spaces, and names `.` and `..` are not allowed. Folder names must be unique within their parent folder. The name check is case-insensitive, so a folder named `New Folder` cannot be created in a parent "
     },
     "description": {
      "type": "string",
      "description": "The optional description of this folder."
     },
     "sync_state": {
      "type": "string",
      "description": "Specifies whether a folder should be synced to a user's device or not. This is used by Box Sync (discontinued) and is not used by Box Drive.",
      "enum": [
       "synced",
       "not_synced",
       "partially_synced"
      ]
     },
     "can_non_owners_invite": {
      "type": "boolean",
      "description": "Specifies if users who are not the owner of the folder can invite new collaborators to the folder."
     },
     "parent": {
      "type": "object",
      "description": "The parent for this item."
     },
     "shared_link": {
      "type": "object",
      "description": "Defines a shared link for an item. Set this to `null` to remove the shared link."
     },
     "folder_upload_email": {
      "type": "object",
      "description": "The Write Folder Upload Email object."
     },
     "tags": {
      "type": "array",
      "description": "The tags for this item. These tags are shown in the Box web app and mobile apps next to an item. To add or remove a tag, retrieve the item's current tags, modify them, and then update this field. There is a limit of 100 tags per item, and 10,000 unique tags per enterprise."
     },
     "is_collaboration_restricted_to_enterprise": {
      "type": "boolean",
      "description": "Specifies if new invites to this folder are restricted to users within the enterprise. This does not affect existing collaborations."
     },
     "collections": {
      "type": "array",
      "description": "An array of collections to make this folder a member of. Currently we only support the `favorites` collection. To get the ID for a collection, use the [List all collections][1] endpoint. Passing an empty array `[]` or `null` will remove the folder from all collections. [1]: https://developer.box.com/reference/get-collections"
     },
     "can_non_owners_view_collaborators": {
      "type": "boolean",
      "description": "Restricts collaborators who are not the owner of this folder from viewing other collaborations on this folder. It also restricts non-owners from inviting new collaborators. When setting this field to `false`, it is required to also set `can_non_owners_invite_collaborators` to `false` if it has not already been set."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/folders/{folder_id}",
    "path": [
     "folder_id"
    ],
    "query": [
     "fields"
    ],
    "header": [
     "if-match"
    ],
    "body": [
     "name",
     "description",
     "sync_state",
     "can_non_owners_invite",
     "parent",
     "shared_link",
     "folder_upload_email",
     "tags",
     "is_collaboration_restricted_to_enterprise",
     "collections",
     "can_non_owners_view_collaborators"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_folders_id_add_shared_link",
   "name": "Add shared link to folder",
   "description": "Adds a shared link to a folder.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     },
     "shared_link": {
      "type": "object",
      "description": "The settings for the shared link to create on the folder. Use an empty object (`{}`) to use the default settings for shared links."
     }
    },
    "required": [
     "fields",
     "folder_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/folders/{folder_id}#add_shared_link",
    "path": [
     "folder_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "shared_link"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_folders_id_metadata_enterprise_security_classification_6_vmvochw_uwo",
   "name": "Update classification on folder",
   "description": "Updates a classification on a folder. The classification can only be updated if a classification has already been applied to the folder before. When editing classifications, only values are defined for the enterprise will be accepted.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/folders/{folder_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "path": [
     "folder_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_folders_id_metadata_id_id",
   "name": "Update metadata instance on folder",
   "description": "Updates a piece of metadata on a folder. The metadata instance can only be updated if the template has already been applied to the folder before. When editing metadata, only values that match the metadata template schema will be accepted. The update is applied atomically. If any errors occur during the application of the operations, the metadata instance will not be changed.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     }
    },
    "required": [
     "folder_id",
     "scope",
     "template_key"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/folders/{folder_id}/metadata/{scope}/{template_key}",
    "path": [
     "folder_id",
     "scope",
     "template_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_folders_id_remove_shared_link",
   "name": "Remove shared link from folder",
   "description": "Removes a shared link from a folder.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     },
     "shared_link": {
      "type": "object",
      "description": "By setting this value to `null`, the shared link is removed from the folder."
     }
    },
    "required": [
     "fields",
     "folder_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/folders/{folder_id}#remove_shared_link",
    "path": [
     "folder_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "shared_link"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_folders_id_update_shared_link",
   "name": "Update shared link on folder",
   "description": "Updates a shared link on a folder.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     },
     "shared_link": {
      "type": "object",
      "description": "The settings for the shared link to update."
     }
    },
    "required": [
     "fields",
     "folder_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/folders/{folder_id}#update_shared_link",
    "path": [
     "folder_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "shared_link"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_folders_id_watermark",
   "name": "Apply watermark to folder",
   "description": "Applies or update a watermark on a folder.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "The unique identifier that represent a folder. The ID for any folder can be determined by visiting this folder in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/folder/123` the `folder_id` is `123`. The root folder of a Box account is always represented by the ID `0`."
     },
     "watermark": {
      "type": "object",
      "description": "The watermark to imprint on the folder."
     }
    },
    "required": [
     "folder_id",
     "watermark"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/folders/{folder_id}/watermark",
    "path": [
     "folder_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "watermark"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_group_memberships_id",
   "name": "Update group membership",
   "description": "Updates a user's group membership. Only admins of this group or users with admin-level permissions will be able to use this API.",
   "input": {
    "type": "object",
    "properties": {
     "group_membership_id": {
      "type": "string",
      "description": "The ID of the group membership."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "role": {
      "type": "string",
      "description": "The role of the user in the group.",
      "enum": [
       "member",
       "admin"
      ]
     },
     "configurable_permissions": {
      "type": "object",
      "description": "Custom configuration for the permissions an admin if a group will receive. This option has no effect on members with a role of `member`. Setting these permissions overwrites the default access levels of an admin. Specifying a value of `null` for this object will disable all configurable permissions. Specifying permissions will set them accordingly, omitted permissions will be enabled by default."
     }
    },
    "required": [
     "group_membership_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/group_memberships/{group_membership_id}",
    "path": [
     "group_membership_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "role",
     "configurable_permissions"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_groups_id",
   "name": "Update group",
   "description": "Updates a specific group. Only admins of this group or users with admin-level permissions will be able to use this API.",
   "input": {
    "type": "object",
    "properties": {
     "group_id": {
      "type": "string",
      "description": "The ID of the group."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "name": {
      "type": "string",
      "description": "The name of the new group to be created. Must be unique within the enterprise."
     },
     "provenance": {
      "type": "string",
      "description": "Keeps track of which external source this group is coming, for example `Active Directory`, or `Okta`. Setting this will also prevent Box admins from editing the group name and its members directly via the Box web application. This is desirable for one-way syncing of groups."
     },
     "external_sync_identifier": {
      "type": "string",
      "description": "An arbitrary identifier that can be used by external group sync tools to link this Box Group to an external group. Example values of this field could be an **Active Directory Object ID** or a **Google Group ID**. We recommend you use of this field in order to avoid issues when group names are updated in either Box or external systems."
     },
     "description": {
      "type": "string",
      "description": "A human readable description of the group."
     },
     "invitability_level": {
      "type": "string",
      "description": "Specifies who can invite the group to collaborate on folders. When set to `admins_only` the enterprise admin, co-admins, and the group's admin can invite the group. When set to `admins_and_members` all the admins listed above and group members can invite the group. When set to `all_managed_users` all managed users in the enterprise can invite the group.",
      "enum": [
       "admins_only",
       "admins_and_members",
       "all_managed_users"
      ]
     },
     "member_viewability_level": {
      "type": "string",
      "description": "Specifies who can see the members of the group. * `admins_only` - the enterprise admin, co-admins, group's group admin. * `admins_and_members` - all admins and group members. * `all_managed_users` - all managed users in the enterprise.",
      "enum": [
       "admins_only",
       "admins_and_members",
       "all_managed_users"
      ]
     }
    },
    "required": [
     "group_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/groups/{group_id}",
    "path": [
     "group_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "name",
     "provenance",
     "external_sync_identifier",
     "description",
     "invitability_level",
     "member_viewability_level"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_integration_mappings_slack_id",
   "name": "Update slack integration mapping",
   "description": "Updates a [Slack integration mapping](https://support.box.com/hc/en-us/articles/4415585987859-Box-as-the-Content-Layer-for-Slack). Supports updating the Box folder ID and options. You need Admin or Co-Admin role to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "integration_mapping_id": {
      "type": "string",
      "description": "An ID of an integration mapping."
     },
     "box_item": {
      "type": "object",
      "description": "The schema for an integration mapping Box item object for type Slack."
     },
     "options": {
      "type": "object",
      "description": "The schema for an integration mapping options object for Slack type."
     }
    },
    "required": [
     "integration_mapping_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/integration_mappings/slack/{integration_mapping_id}",
    "path": [
     "integration_mapping_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "box_item",
     "options"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_integration_mappings_teams_id",
   "name": "Update teams integration mapping",
   "description": "Updates a [Teams integration mapping](https://support.box.com/hc/en-us/articles/360044681474-Using-Box-for-Teams). Supports updating the Box folder ID and options. You need Admin or Co-Admin role to use this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "integration_mapping_id": {
      "type": "string",
      "description": "An ID of an integration mapping."
     },
     "box_item": {
      "type": "object",
      "description": "Folder reference."
     }
    },
    "required": [
     "integration_mapping_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/integration_mappings/teams/{integration_mapping_id}",
    "path": [
     "integration_mapping_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "box_item"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_legal_hold_policies_id",
   "name": "Update legal hold policy",
   "description": "Update legal hold policy.",
   "input": {
    "type": "object",
    "properties": {
     "legal_hold_policy_id": {
      "type": "string",
      "description": "The ID of the legal hold policy."
     },
     "policy_name": {
      "type": "string",
      "description": "The name of the policy."
     },
     "description": {
      "type": "string",
      "description": "A description for the policy."
     },
     "release_notes": {
      "type": "string",
      "description": "Notes around why the policy was released."
     }
    },
    "required": [
     "legal_hold_policy_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/legal_hold_policies/{legal_hold_policy_id}",
    "path": [
     "legal_hold_policy_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "policy_name",
     "description",
     "release_notes"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_metadata_templates_enterprise_security_classification_6_vmvochw_uwo_schema_add",
   "name": "Add classification",
   "description": "Adds one or more new classifications to the list of classifications available to the enterprise. This API can also be called by including the enterprise ID in the URL explicitly, for example `/metadata_templates/enterprise_12345/securityClassification-6VMVochwUWo/schema`.",
   "input": {
    "type": "object",
    "properties": {},
    "required": [
     "items"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/metadata_templates/enterprise/securityClassification-6VMVochwUWo/schema#add",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_metadata_templates_enterprise_security_classification_6_vmvochw_uwo_schema_update",
   "name": "Update classification",
   "description": "Updates the labels and descriptions of one or more classifications available to the enterprise. This API can also be called by including the enterprise ID in the URL explicitly, for example `/metadata_templates/enterprise_12345/securityClassification-6VMVochwUWo/schema`.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/metadata_templates/enterprise/securityClassification-6VMVochwUWo/schema#update",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_metadata_templates_id_id_schema",
   "name": "Update metadata template",
   "description": "Updates a metadata template. The metadata template can only be updated if the template already exists. The update is applied atomically. If any errors occur during the application of the operations, the metadata template will not be changed.",
   "input": {
    "type": "object",
    "properties": {
     "scope": {
      "type": "string",
      "description": "The scope of the metadata template.",
      "enum": [
       "global",
       "enterprise"
      ]
     },
     "template_key": {
      "type": "string",
      "description": "The name of the metadata template."
     }
    },
    "required": [
     "scope",
     "template_key"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/metadata_templates/{scope}/{template_key}/schema",
    "path": [
     "scope",
     "template_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_retention_policies_id",
   "name": "Update retention policy",
   "description": "Updates a retention policy.",
   "input": {
    "type": "object",
    "properties": {
     "retention_policy_id": {
      "type": "string",
      "description": "The ID of the retention policy."
     },
     "policy_name": {
      "type": "string",
      "description": "The name for the retention policy."
     },
     "description": {
      "type": "string",
      "description": "The additional text description of the retention policy."
     },
     "disposition_action": {
      "type": "string",
      "description": "The disposition action of the retention policy. This action can be `permanently_delete`, which will cause the content retained by the policy to be permanently deleted, or `remove_retention`, which will lift the retention policy from the content, allowing it to be deleted by users, once the retention policy has expired. You can use `null` if you don't want to change `disposition_action`."
     },
     "retention_type": {
      "type": "string",
      "description": "Specifies the retention type: * `modifiable`: You can modify the retention policy. For example, you can add or remove folders, shorten or lengthen the policy duration, or delete the assignment. Use this type if your retention policy is not related to any regulatory purposes. * `non-modifiable`: You can modify the retention policy only in a limited way: add a folder, lengthen the duration, retire t"
     },
     "retention_length": {
      "type": "string",
      "description": "The length of the retention policy. This value specifies the duration in days that the retention policy will be active for after being assigned to content. If the policy has a `policy_type` of `indefinite`, the `retention_length` will also be `indefinite`."
     },
     "status": {
      "type": "string",
      "description": "Used to retire a retention policy. If not retiring a policy, do not include this parameter or set it to `null`."
     },
     "can_owner_extend_retention": {
      "type": "boolean",
      "description": "Determines if the owner of items under the policy can extend the retention when the original retention duration is about to end."
     },
     "max_extension_length": {
      "type": "string",
      "description": "The maximum extension length of the retention date. This value specifies the duration in days for which the retention date of the file under policy can be extended. It can be specified only for the 'finite' policy type where the disposition action is 'permanently delete', otherwise the server will return status 400. If this value is 'none', it won't be possible to extend the retention."
     },
     "are_owners_notified": {
      "type": "boolean",
      "description": "Determines if owners and co-owners of items under the policy are notified when the retention duration is about to end."
     },
     "custom_notification_recipients": {
      "type": "array",
      "description": "A list of users notified when the retention duration is about to end."
     }
    },
    "required": [
     "retention_policy_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/retention_policies/{retention_policy_id}",
    "path": [
     "retention_policy_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "policy_name",
     "description",
     "disposition_action",
     "retention_type",
     "retention_length",
     "status",
     "can_owner_extend_retention",
     "max_extension_length",
     "are_owners_notified",
     "custom_notification_recipients"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_shield_information_barrier_segments_id",
   "name": "Update shield information barrier segment with specified id",
   "description": "Updates the shield information barrier segment based on provided ID..",
   "input": {
    "type": "object",
    "properties": {
     "shield_information_barrier_segment_id": {
      "type": "string",
      "description": "The ID of the shield information barrier segment."
     },
     "name": {
      "type": "string",
      "description": "The updated name for the shield information barrier segment."
     },
     "description": {
      "type": "string",
      "description": "The updated description for the shield information barrier segment."
     }
    },
    "required": [
     "shield_information_barrier_segment_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/shield_information_barrier_segments/{shield_information_barrier_segment_id}",
    "path": [
     "shield_information_barrier_segment_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "description"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_skill_invocations_id",
   "name": "Update all box skill cards on file",
   "description": "An alternative method that can be used to overwrite and update all Box Skill metadata cards on a file.",
   "input": {
    "type": "object",
    "properties": {
     "skill_id": {
      "type": "string",
      "description": "The ID of the skill to apply this metadata for."
     },
     "status": {
      "type": "string",
      "description": "Defines the status of this invocation. Set this to `success` when setting Skill cards.",
      "enum": [
       "invoked",
       "processing",
       "success",
       "transient_failure",
       "permanent_failure"
      ]
     },
     "metadata": {
      "type": "object",
      "description": "The metadata to set for this skill. This is a list of Box Skills cards. These cards will overwrite any existing Box skill cards on the file."
     },
     "file": {
      "type": "object",
      "description": "The file to assign the cards to."
     },
     "file_version": {
      "type": "object",
      "description": "The optional file version to assign the cards to."
     },
     "usage": {
      "type": "object",
      "description": "A descriptor that defines what items are affected by this call. Set this to the default values when setting a card to a `success` state, and leave it out in most other situations."
     }
    },
    "required": [
     "file",
     "metadata",
     "skill_id",
     "status"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/skill_invocations/{skill_id}",
    "path": [
     "skill_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "status",
     "metadata",
     "file",
     "file_version",
     "usage"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_storage_policy_assignments_id",
   "name": "Update storage policy assignment",
   "description": "Updates a specific storage policy assignment.",
   "input": {
    "type": "object",
    "properties": {
     "storage_policy_assignment_id": {
      "type": "string",
      "description": "The ID of the storage policy assignment."
     },
     "storage_policy": {
      "type": "object",
      "description": "The storage policy to assign to the user or enterprise."
     }
    },
    "required": [
     "storage_policy",
     "storage_policy_assignment_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/storage_policy_assignments/{storage_policy_assignment_id}",
    "path": [
     "storage_policy_assignment_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "storage_policy"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_task_assignments_id",
   "name": "Update task assignment",
   "description": "Updates a task assignment. This endpoint can be used to update the state of a task assigned to a user.",
   "input": {
    "type": "object",
    "properties": {
     "task_assignment_id": {
      "type": "string",
      "description": "The ID of the task assignment."
     },
     "message": {
      "type": "string",
      "description": "An optional message by the assignee that can be added to the task."
     },
     "resolution_state": {
      "type": "string",
      "description": "The state of the task assigned to the user. * For a task with an `action` value of `complete` this can be `incomplete` or `completed`. * For a task with an `action` of `review` this can be `incomplete`, `approved`, or `rejected`.",
      "enum": [
       "completed",
       "incomplete",
       "approved",
       "rejected"
      ]
     }
    },
    "required": [
     "task_assignment_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/task_assignments/{task_assignment_id}",
    "path": [
     "task_assignment_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "message",
     "resolution_state"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_tasks_id",
   "name": "Update task",
   "description": "Updates a task. This can be used to update a task's configuration, or to update its completion state.",
   "input": {
    "type": "object",
    "properties": {
     "task_id": {
      "type": "string",
      "description": "The ID of the task."
     },
     "action": {
      "type": "string",
      "description": "The action the task assignee will be prompted to do. Must be * `review` defines an approval task that can be approved or rejected, * `complete` defines a general task which can be completed.",
      "enum": [
       "review",
       "complete"
      ]
     },
     "message": {
      "type": "string",
      "description": "The message included with the task."
     },
     "due_at": {
      "type": "string",
      "description": "When the task is due at."
     },
     "completion_rule": {
      "type": "string",
      "description": "Defines which assignees need to complete this task before the task is considered completed. * `all_assignees` (default) requires all assignees to review or approve the task in order for it to be considered completed. * `any_assignee` accepts any one assignee to review or approve the task in order for it to be considered completed.",
      "enum": [
       "all_assignees",
       "any_assignee"
      ]
     }
    },
    "required": [
     "task_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/tasks/{task_id}",
    "path": [
     "task_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "action",
     "message",
     "due_at",
     "completion_rule"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_terms_of_service_user_statuses_id",
   "name": "Update terms of service status for existing user",
   "description": "Updates the status for a terms of service for a user.",
   "input": {
    "type": "object",
    "properties": {
     "terms_of_service_user_status_id": {
      "type": "string",
      "description": "The ID of the terms of service status."
     },
     "is_accepted": {
      "type": "boolean",
      "description": "Whether the user has accepted the terms."
     }
    },
    "required": [
     "is_accepted",
     "terms_of_service_user_status_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/terms_of_service_user_statuses/{terms_of_service_user_status_id}",
    "path": [
     "terms_of_service_user_status_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "is_accepted"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_terms_of_services_id",
   "name": "Update terms of service",
   "description": "Updates a specific terms of service.",
   "input": {
    "type": "object",
    "properties": {
     "terms_of_service_id": {
      "type": "string",
      "description": "The ID of the terms of service."
     },
     "status": {
      "type": "string",
      "description": "Whether this terms of service is active.",
      "enum": [
       "enabled",
       "disabled"
      ]
     },
     "text": {
      "type": "string",
      "description": "The terms of service text to display to users. The text can be set to empty if the `status` is set to `disabled`."
     }
    },
    "required": [
     "status",
     "terms_of_service_id",
     "text"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/terms_of_services/{terms_of_service_id}",
    "path": [
     "terms_of_service_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "status",
     "text"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_users_id",
   "name": "Update user",
   "description": "Updates a managed or app user in an enterprise. This endpoint is only available to users and applications with the right admin permissions.",
   "input": {
    "type": "object",
    "properties": {
     "user_id": {
      "type": "string",
      "description": "The ID of the user."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "enterprise": {
      "type": "string",
      "description": "Set this to `null` to roll the user out of the enterprise and make them a free user."
     },
     "notify": {
      "type": "boolean",
      "description": "Whether the user should receive an email when they are rolled out of an enterprise."
     },
     "name": {
      "type": "string",
      "description": "The name of the user."
     },
     "login": {
      "type": "string",
      "description": "The email address the user uses to log in Note: If the target user's email is not confirmed, then the primary login address cannot be changed."
     },
     "role": {
      "type": "string",
      "description": "The user\u2019s enterprise role.",
      "enum": [
       "coadmin",
       "user"
      ]
     },
     "language": {
      "type": "string",
      "description": "The language of the user, formatted in modified version of the [ISO 639-1](https://developer.box.com/guides/api-calls/language-codes) format."
     },
     "is_sync_enabled": {
      "type": "boolean",
      "description": "Whether the user can use Box Sync."
     },
     "job_title": {
      "type": "string",
      "description": "The user\u2019s job title."
     },
     "phone": {
      "type": "string",
      "description": "The user\u2019s phone number."
     },
     "address": {
      "type": "string",
      "description": "The user\u2019s address."
     },
     "tracking_codes": {
      "type": "array",
      "description": "Tracking codes allow an admin to generate reports from the admin console and assign an attribute to a specific group of users. This setting must be enabled for an enterprise before it can be used."
     },
     "can_see_managed_users": {
      "type": "boolean",
      "description": "Whether the user can see other enterprise users in their contact list."
     },
     "timezone": {
      "type": "string",
      "description": "The user's timezone."
     },
     "is_external_collab_restricted": {
      "type": "boolean",
      "description": "Whether the user is allowed to collaborate with users outside their enterprise."
     },
     "is_exempt_from_device_limits": {
      "type": "boolean",
      "description": "Whether to exempt the user from enterprise device limits."
     },
     "is_exempt_from_login_verification": {
      "type": "boolean",
      "description": "Whether the user must use two-factor authentication."
     },
     "is_password_reset_required": {
      "type": "boolean",
      "description": "Whether the user is required to reset their password."
     },
     "is_collaborated_content_available_when_owner_inactive": {
      "type": "boolean",
      "description": "Whether collaborators can access content owned by the user when the user is inactive. This setting preserves existing collaborator access and does not grant new permissions."
     },
     "status": {
      "type": "string",
      "description": "The user's account status.",
      "enum": [
       "active",
       "inactive",
       "cannot_delete_edit",
       "cannot_delete_edit_upload"
      ]
     },
     "space_amount": {
      "type": "integer",
      "description": "The user\u2019s total available space in bytes. Set this to `-1` to indicate unlimited storage."
     },
     "notification_email": {
      "type": "object",
      "description": "An alternate notification email address to which email notifications are sent. When it's confirmed, this will be the email address to which notifications are sent instead of to the primary email address. Set this value to `null` to remove the notification email."
     },
     "external_app_user_id": {
      "type": "string",
      "description": "An external identifier for an app user, which can be used to look up the user. This can be used to tie user IDs from external identity providers to Box users. Note: In order to update this field, you need to request a token using the application that created the app user."
     }
    },
    "required": [
     "user_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/users/{user_id}",
    "path": [
     "user_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "enterprise",
     "notify",
     "name",
     "login",
     "role",
     "language",
     "is_sync_enabled",
     "job_title",
     "phone",
     "address",
     "tracking_codes",
     "can_see_managed_users",
     "timezone",
     "is_external_collab_restricted",
     "is_exempt_from_device_limits",
     "is_exempt_from_login_verification",
     "is_password_reset_required",
     "is_collaborated_content_available_when_owner_inactive",
     "status",
     "space_amount",
     "notification_email",
     "external_app_user_id"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_users_id_folders_0",
   "name": "Transfer owned folders",
   "description": "Move all of the items (files, folders and workflows) owned by a user into another user's account. Only the root folder (`0`) can be transferred. Folders can only be moved across users by users with administrative permissions. All existing shared links and folder-level collaborations are transferred during the operation. Please note that while collaborations at the individual file-level are transfe",
   "input": {
    "type": "object",
    "properties": {
     "user_id": {
      "type": "string",
      "description": "The ID of the user."
     },
     "fields": {
      "type": "array",
      "description": "A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields reques"
     },
     "notify": {
      "type": "boolean",
      "description": "Determines if users should receive email notification for the action performed."
     },
     "owned_by": {
      "type": "object",
      "description": "The user who the folder will be transferred to."
     }
    },
    "required": [
     "owned_by",
     "user_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/users/{user_id}/folders/0",
    "path": [
     "user_id"
    ],
    "query": [
     "fields",
     "notify"
    ],
    "header": [],
    "body": [
     "owned_by"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_web_links_id",
   "name": "Update web link",
   "description": "Updates a web link object.",
   "input": {
    "type": "object",
    "properties": {
     "web_link_id": {
      "type": "string",
      "description": "The ID of the web link."
     },
     "url": {
      "type": "string",
      "description": "The new URL that the web link links to. Must start with `\"http://\"` or `\"https://\"`."
     },
     "parent": {
      "type": "object",
      "description": "The parent for this item."
     },
     "name": {
      "type": "string",
      "description": "A new name for the web link. Defaults to the URL if not set."
     },
     "description": {
      "type": "string",
      "description": "A new description of the web link."
     },
     "shared_link": {
      "type": "object",
      "description": "The settings for the shared link to update."
     }
    },
    "required": [
     "web_link_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/web_links/{web_link_id}",
    "path": [
     "web_link_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "url",
     "parent",
     "name",
     "description",
     "shared_link"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_web_links_id_add_shared_link",
   "name": "Add shared link to web link",
   "description": "Adds a shared link to a web link.",
   "input": {
    "type": "object",
    "properties": {
     "web_link_id": {
      "type": "string",
      "description": "The ID of the web link."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     },
     "shared_link": {
      "type": "object",
      "description": "The settings for the shared link to create on the web link. Use an empty object (`{}`) to use the default settings for shared links."
     }
    },
    "required": [
     "fields",
     "web_link_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/web_links/{web_link_id}#add_shared_link",
    "path": [
     "web_link_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "shared_link"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_web_links_id_remove_shared_link",
   "name": "Remove shared link from web link",
   "description": "Removes a shared link from a web link.",
   "input": {
    "type": "object",
    "properties": {
     "web_link_id": {
      "type": "string",
      "description": "The ID of the web link."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     },
     "shared_link": {
      "type": "object",
      "description": "By setting this value to `null`, the shared link is removed from the web link."
     }
    },
    "required": [
     "fields",
     "web_link_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/web_links/{web_link_id}#remove_shared_link",
    "path": [
     "web_link_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "shared_link"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_web_links_id_update_shared_link",
   "name": "Update shared link on web link",
   "description": "Updates a shared link on a web link.",
   "input": {
    "type": "object",
    "properties": {
     "web_link_id": {
      "type": "string",
      "description": "The ID of the web link."
     },
     "fields": {
      "type": "string",
      "description": "Explicitly request the `shared_link` fields to be returned for this item."
     },
     "shared_link": {
      "type": "object",
      "description": "The settings for the shared link to update."
     }
    },
    "required": [
     "fields",
     "web_link_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/web_links/{web_link_id}#update_shared_link",
    "path": [
     "web_link_id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [
     "shared_link"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "box.put_webhooks_id",
   "name": "Update webhook",
   "description": "Updates a webhook.",
   "input": {
    "type": "object",
    "properties": {
     "webhook_id": {
      "type": "string",
      "description": "The ID of the webhook."
     },
     "target": {
      "type": "object",
      "description": "The item that will trigger the webhook."
     },
     "address": {
      "type": "string",
      "description": "The URL that is notified by this webhook."
     },
     "triggers": {
      "type": "array",
      "description": "An array of event names that this webhook is to be triggered for."
     }
    },
    "required": [
     "webhook_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.box.com/2.0/webhooks/{webhook_id}",
    "path": [
     "webhook_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "target",
     "address",
     "triggers"
    ],
    "body_format": "json"
   }
  }
 ]
}
