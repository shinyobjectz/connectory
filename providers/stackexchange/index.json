{
 "_": "GENERATED \u2014 what an authoring tool needs to FIND an operation. The calling table is the .lua beside it.",
 "provider": "stackexchange",
 "source": "https://api.apis.guru/v2/specs/stackexchange.com/2.0/openapi.json",
 "operations": [
  {
   "slug": "stackexchange.get_access_tokens_accessTokens",
   "name": "Stackexchange get access tokens access tokens",
   "description": "Reads the properties for a set of access tokens. {accessTokens} can contain up to 20 access tokens. These are obtained by authenticating a user using OAuth 2.0. This method returns a list of access_tokens.",
   "input": {
    "type": "object",
    "properties": {
     "accessTokens": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": [
     "accessTokens"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/access-tokens/{accessTokens}",
    "path": [
     "accessTokens"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_access_tokens_accessTokens_invalidate",
   "name": "Stackexchange get access tokens access tokens invalidate",
   "description": "Immediately expires the access tokens passed. This method is meant to allow an application to discard any active access tokens it no longer needs. {accessTokens} can contain up to 20 access tokens. These are obtained by authenticating a user using OAuth 2.0. This method returns a list of access_tokens.",
   "input": {
    "type": "object",
    "properties": {
     "accessTokens": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": [
     "accessTokens"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/access-tokens/{accessTokens}/invalidate",
    "path": [
     "accessTokens"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_answers",
   "name": "Stackexchange get answers",
   "description": "Returns all the undeleted answers in the system. The sorts accepted by this method operate on the follow fields of the answer object: - activity - last_activity_date - creation - creation_date - votes - score activity is the default sort. It is possible to create moderately complex queries using sort, min, max, fromdate, and todate. This method returns a list of answers.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/answers",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_answers_ids",
   "name": "Stackexchange get answers ids",
   "description": "Gets the set of answers identified by ids. This is meant for batch fetcing of questions. A useful trick to poll for updates is to sort by activity, with a minimum date of the last time you polled. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for answer_id on answer objects. The sorts accepted by this method operate on the follow fields of the answer object:",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/answers/{ids}",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_answers_ids_comments",
   "name": "Stackexchange get answers ids comments",
   "description": "Gets the comments on a set of answers. If you know that you have an answer id and need the comments, use this method. If you know you have a question id, use /questions/{id}/comments. If you are unsure, use /posts/{id}/comments. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for answer_id on answer objects. The sorts accepted by this method operate on the fol",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/answers/{ids}/comments",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_apps_accessTokens_de_authenticate",
   "name": "Stackexchange get apps access tokens de authenticate",
   "description": "Passing valid access_tokens to this method causes the application that created them to be de-authorized by the user associated with each access_token. This will remove the application from their apps tab, and cause all other existing access_tokens to be destroyed. This method is meant for uninstalling applications, recovering from access_token leaks, or simply as a stronger form of /access-tokens/",
   "input": {
    "type": "object",
    "properties": {
     "accessTokens": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": [
     "accessTokens"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/apps/{accessTokens}/de-authenticate",
    "path": [
     "accessTokens"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_badges",
   "name": "Stackexchange get badges",
   "description": "Returns all the badges in the system. Badge sorts are a tad complicated. For the purposes of sorting (and min/max) tag_based is considered to be greater than named. This means that you can get a list of all tag based badges by passing min=tag_based, and conversely all the named badges by passing max=named, with sort=type. For ranks, bronze is greater than silver which is greater than gold. Along w",
   "input": {
    "type": "object",
    "properties": {
     "inname": {
      "type": "string",
      "description": ""
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = rank => string sort = name => string sort = type => string"
     },
     "min": {
      "type": "string",
      "description": "sort = rank => string sort = name => string sort = type => string"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "rank",
       "name",
       "type"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/badges",
    "path": [],
    "query": [
     "inname",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_badges_ids",
   "name": "Stackexchange get badges ids",
   "description": "Gets the badges identified in id. Note that badge ids are not constant across sites, and thus should be looked up via the /badges method. A badge id on a single site is, however, guaranteed to be stable. Badge sorts are a tad complicated. For the purposes of sorting (and min/max) tag_based is considered to be greater than named. This means that you can get a list of all tag based badges by passing",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = rank => string sort = name => string sort = type => string"
     },
     "min": {
      "type": "string",
      "description": "sort = rank => string sort = name => string sort = type => string"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "rank",
       "name",
       "type"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/badges/{ids}",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_badges_ids_recipients",
   "name": "Stackexchange get badges ids recipients",
   "description": "Returns recently awarded badges in the system, constrained to a certain set of badges. As these badges have been awarded, they will have the badge.user property set. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for badge_id on badge objects. This method returns a list of badges.",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/badges/{ids}/recipients",
    "path": [
     "ids"
    ],
    "query": [
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_badges_name",
   "name": "Stackexchange get badges name",
   "description": "Gets all explicitly named badges in the system. A named badged stands in opposition to a tag-based badge. These are referred to as general badges on the sites themselves. For the rank sort, bronze is greater than silver which is greater than gold. Along with sort=rank, set max=gold for just gold badges, max=silver&min=silver for just silver, and min=bronze for just bronze. rank is the default sort",
   "input": {
    "type": "object",
    "properties": {
     "inname": {
      "type": "string",
      "description": ""
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = rank => string sort = name => string"
     },
     "min": {
      "type": "string",
      "description": "sort = rank => string sort = name => string"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "rank",
       "name"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/badges/name",
    "path": [],
    "query": [
     "inname",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_badges_recipients",
   "name": "Stackexchange get badges recipients",
   "description": "Returns recently awarded badges in the system. As these badges have been awarded, they will have the badge.user property set. This method returns a list of badges.",
   "input": {
    "type": "object",
    "properties": {
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/badges/recipients",
    "path": [],
    "query": [
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_badges_tags",
   "name": "Stackexchange get badges tags",
   "description": "Returns the badges that are awarded for participation in specific tags. For the rank sort, bronze is greater than silver which is greater than gold. Along with sort=rank, set max=gold for just gold badges, max=silver&min=silver for just silver, and min=bronze for just bronze. rank is the default sort. This method returns a list of badges.",
   "input": {
    "type": "object",
    "properties": {
     "inname": {
      "type": "string",
      "description": ""
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = rank => string sort = name => string"
     },
     "min": {
      "type": "string",
      "description": "sort = rank => string sort = name => string"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "rank",
       "name"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/badges/tags",
    "path": [],
    "query": [
     "inname",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_comments",
   "name": "Stackexchange get comments",
   "description": "Gets all the comments on the site. If you're filtering for interesting comments (by score, creation date, etc.) make use of the sort paramter with appropriate min and max values. If you're looking to query conversations between users, instead use the /users/{ids}/mentioned and /users/{ids}/comments/{toid} methods. The sorts accepted by this method operate on the follow fields of the comment object",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/comments",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_comments_ids",
   "name": "Stackexchange get comments ids",
   "description": "Gets the comments identified in id. This method is most useful if you have a cache of comment ids obtained through other means (such as /questions/{id}/comments) but suspect the data may be stale. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for comment_id on comment objects. The sorts accepted by this method operate on the follow fields of the comment obje",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/comments/{ids}",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_errors",
   "name": "Stackexchange get errors",
   "description": "Returns the various error codes that can be produced by the API. This method is provided for discovery, documentation, and testing purposes, it is not expected many applications will consume it during normal operation. For testing purposes, look into the /errors/{id} method which simulates errors given a code. This method returns a list of errors.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/errors",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_errors_id",
   "name": "Stackexchange get errors id",
   "description": "This method allows you to generate an error. This method is only intended for use when testing an application or library. Unlike other methods in the API, its contract is not frozen, and new error codes may be added at any time. This method results in an error, which will be expressed with a 400 HTTP status code and setting the error* properties on the wrapper object.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     }
    },
    "required": [
     "id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/errors/{id}",
    "path": [
     "id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_events",
   "name": "Stackexchange get events",
   "description": "Returns a stream of events that have occurred on the site. The API considers the following \"events\": - posting a question - posting an answer - posting a comment - editing a post - creating a user Events are only accessible for 15 minutes after they occurred, and by default only events in the last 5 minutes are returned. You can specify the age of the oldest event returned by setting the since par",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     },
     "since": {
      "type": "integer",
      "description": "Unix date."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/events",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site",
     "since"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_filters_create",
   "name": "Stackexchange get filters create",
   "description": "Creates a new filter given a list of includes, excludes, a base filter, and whether or not this filter should be \"unsafe\". Filter \"safety\" is defined as follows. Any string returned as a result of an API call with a safe filter will be inline-able into HTML without script-injection concerns. That is to say, no additional sanitizing (encoding, HTML tag stripping, etc.) will be necessary on returned",
   "input": {
    "type": "object",
    "properties": {
     "base": {
      "type": "string",
      "description": ""
     },
     "exclude": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "include": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "unsafe": {
      "type": "boolean",
      "description": ""
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/filters/create",
    "path": [],
    "query": [
     "base",
     "exclude",
     "include",
     "unsafe"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_filters_filters",
   "name": "Stackexchange get filters filters",
   "description": "Returns the fields included by the given filters, and the \"safeness\" of those filters. It is not expected that this method will be consumed by many applications at runtime, it is provided to aid in debugging. {filters} can contain up to 20 semicolon delimited filters. Filters are obtained via calls to /filters/create, or by using a built-in filter. This method returns a list of filters.",
   "input": {
    "type": "object",
    "properties": {
     "filters": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     }
    },
    "required": [
     "filters"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/filters/{filters}",
    "path": [
     "filters"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_inbox",
   "name": "Stackexchange get inbox",
   "description": "Returns a user's inbox. This method requires an access_token, with a scope containing \"read_inbox\". This method returns a list of inbox items.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/inbox",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_inbox_unread",
   "name": "Stackexchange get inbox unread",
   "description": "Returns the unread items in a user's inbox. This method requires an access_token, with a scope containing \"read_inbox\". This method returns a list of inbox items.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "since": {
      "type": "integer",
      "description": "Unix date."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/inbox/unread",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "since"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_info",
   "name": "Stackexchange get info",
   "description": "Returns a collection of statistics about the site. Data to facilitate per-site customization, discover related sites, and aggregate statistics is all returned by this method. This data is cached very aggressively, by design. Query sparingly, ideally no more than once an hour. This method returns an info object.",
   "input": {
    "type": "object",
    "properties": {
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/info",
    "path": [],
    "query": [
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me",
   "name": "Stackexchange get me",
   "description": "Returns the user associated with the passed access_token. This method returns a user.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = reputation => number sort = creation => date sort = name => string sort = modified => date"
     },
     "min": {
      "type": "string",
      "description": "sort = reputation => number sort = creation => date sort = name => string sort = modified => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "reputation",
       "creation",
       "name",
       "modified"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_answers",
   "name": "Stackexchange get me answers",
   "description": "Returns the answers owned by the user associated with the given access_token. This method returns a list of answers.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/answers",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_associated",
   "name": "Stackexchange get me associated",
   "description": "Returns all of a user's associated accounts, given an access_token for them. This method returns a list of network users.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/associated",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_badges",
   "name": "Stackexchange get me badges",
   "description": "Returns the badges earned by the user associated with the given access_token. This method returns a list of badges.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = rank => string sort = name => string sort = type => string"
     },
     "min": {
      "type": "string",
      "description": "sort = rank => string sort = name => string sort = type => string"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "rank",
       "name",
       "type"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/badges",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_comments",
   "name": "Stackexchange get me comments",
   "description": "Returns the comments owned by the user associated with the given access_token. This method returns a list of comments.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/comments",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_comments_toId",
   "name": "Stackexchange get me comments to id",
   "description": "Returns the comments owned by the user associated with the given access_token that are in reply to the user identified by {toId}. This method returns a list of comments.",
   "input": {
    "type": "object",
    "properties": {
     "toId": {
      "type": "integer",
      "description": ""
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site",
     "toId"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/comments/{toId}",
    "path": [
     "toId"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_favorites",
   "name": "Stackexchange get me favorites",
   "description": "Returns the questions favorites by the user associated with the given access_token. This method returns a list of questions.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = added => date"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = added => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes",
       "added"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/favorites",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_inbox",
   "name": "Stackexchange get me inbox",
   "description": "Returns the user identified by access_token's inbox. This method requires an access_token, with a scope containing \"read_inbox\". This method returns a list of inbox items.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/inbox",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_inbox_unread",
   "name": "Stackexchange get me inbox unread",
   "description": "Returns the unread items in the user identified by access_token's inbox. This method requires an access_token, with a scope containing \"read_inbox\". This method returns a list of inbox items.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     },
     "since": {
      "type": "integer",
      "description": "Unix date."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/inbox/unread",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site",
     "since"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_mentioned",
   "name": "Stackexchange get me mentioned",
   "description": "Returns the comments mentioning the user associated with the given access_token. This method returns a list of comments.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/mentioned",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_merges",
   "name": "Stackexchange get me merges",
   "description": "Returns a record of merges that have occurred involving a user identified by an access_token. This method allows you to take now invalid account ids and find what account they've become, or take currently valid account ids and find which ids were equivalent in the past. This is most useful when confirming that an account_id is in fact \"new\" to an application. Account merges can happen for a wide r",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/merges",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_notifications",
   "name": "Stackexchange get me notifications",
   "description": "Returns a user's notifications, given an access_token. This method requires an access_token, with a scope containing \"read_inbox\". This method returns a list of notifications.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/notifications",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_notifications_unread",
   "name": "Stackexchange get me notifications unread",
   "description": "Returns a user's unread notifications, given an access_token. This method requires an access_token, with a scope containing \"read_inbox\". This method returns a list of notifications.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/notifications/unread",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_privileges",
   "name": "Stackexchange get me privileges",
   "description": "Returns the privileges the user identified by access_token has. This method returns a list of privileges.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/privileges",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_questions",
   "name": "Stackexchange get me questions",
   "description": "Returns the questions owned by the user associated with the given access_token. This method returns a list of questions.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/questions",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_questions_featured",
   "name": "Stackexchange get me questions featured",
   "description": "Returns the questions that have active bounties offered by the user associated with the given access_token. This method returns a list of questions.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/questions/featured",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_questions_no_answers",
   "name": "Stackexchange get me questions no answers",
   "description": "Returns the questions owned by the user associated with the given access_token that have no answers. This method returns a list of questions.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/questions/no-answers",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_questions_unaccepted",
   "name": "Stackexchange get me questions unaccepted",
   "description": "Returns the questions owned by the user associated with the given access_token that have no accepted answer. This method returns a list of questions.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/questions/unaccepted",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_questions_unanswered",
   "name": "Stackexchange get me questions unanswered",
   "description": "Returns the questions owned by the user associated with the given access_token that are not considered answered. This method returns a list of questions.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/questions/unanswered",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_reputation",
   "name": "Stackexchange get me reputation",
   "description": "Returns the reputation changed for the user associated with the given access_token. This method returns a list of reputation changes.",
   "input": {
    "type": "object",
    "properties": {
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/reputation",
    "path": [],
    "query": [
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_reputation_history",
   "name": "Stackexchange get me reputation history",
   "description": "Returns user's public reputation history. This method returns a list of reputation_history.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/reputation-history",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_reputation_history_full",
   "name": "Stackexchange get me reputation history full",
   "description": "Returns user's full reputation history, including private events. This method requires an access_token, with a scope containing \"private_info\". This method returns a list of reputation_history.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/reputation-history/full",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_suggested_edits",
   "name": "Stackexchange get me suggested edits",
   "description": "Returns the suggested edits the user identified by access_token has submitted. This method returns a list of suggested-edits.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = approval => date sort = rejection => date"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = approval => date sort = rejection => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "approval",
       "rejection"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/suggested-edits",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_tags",
   "name": "Stackexchange get me tags",
   "description": "Returns the tags the user identified by the access_token passed is active in. This method returns a list of tags.",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "min": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "popular",
       "activity",
       "name"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/tags",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_tags_tags_top_answers",
   "name": "Stackexchange get me tags tags top answers",
   "description": "Returns the top 30 answers the user associated with the given access_token has posted in response to questions with the given tags. This method returns a list of answers.",
   "input": {
    "type": "object",
    "properties": {
     "tags": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site",
     "tags"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/tags/{tags}/top-answers",
    "path": [
     "tags"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_tags_tags_top_questions",
   "name": "Stackexchange get me tags tags top questions",
   "description": "Returns the top 30 questions the user associated with the given access_token has posted in response to questions with the given tags. This method returns a list of questions.",
   "input": {
    "type": "object",
    "properties": {
     "tags": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = hot => none sort = week => none sort = month => none sort = relevance => none"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = hot => none sort = week => none sort = month => none sort = relevance => none"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes",
       "hot",
       "week",
       "month",
       "relevance"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site",
     "tags"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/tags/{tags}/top-questions",
    "path": [
     "tags"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_timeline",
   "name": "Stackexchange get me timeline",
   "description": "Returns a subset of the actions the user identified by the passed access_token has taken on the site. This method returns a list of user timeline objects.",
   "input": {
    "type": "object",
    "properties": {
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/timeline",
    "path": [],
    "query": [
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_top_answer_tags",
   "name": "Stackexchange get me top answer tags",
   "description": "Returns the user identified by access_token's top 30 tags by answer score. This method returns a list of top tag objects.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/top-answer-tags",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_top_question_tags",
   "name": "Stackexchange get me top question tags",
   "description": "Returns the user identified by access_token's top 30 tags by question score. This method returns a list of top tag objects.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/top-question-tags",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_me_write_permissions",
   "name": "Stackexchange get me write permissions",
   "description": "Returns the write permissions a user has via the api, given an access token. The Stack Exchange API gives users the ability to create, edit, and delete certain types. This method returns whether the passed user is capable of performing those actions at all, as well as how many times a day they can. This method does not consider the user's current quota (ie. if they've already exhausted it for toda",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/me/write-permissions",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_notifications",
   "name": "Stackexchange get notifications",
   "description": "Returns a user's notifications. This method requires an access_token, with a scope containing \"read_inbox\". This method returns a list of notifications.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/notifications",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_notifications_unread",
   "name": "Stackexchange get notifications unread",
   "description": "Returns a user's unread notifications. This method requires an access_token, with a scope containing \"read_inbox\". This method returns a list of notifications.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/notifications/unread",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_posts",
   "name": "Stackexchange get posts",
   "description": "Fetches all posts (questions and answers) on the site. In many ways this method is the union of /questions and /answers, returning both sets of data albeit only the common fields. Most applications should use the question or answer specific methods, but /posts is available for those rare cases where any activity is of intereset. Examples of such queries would be: \"all posts on Jan. 1st 2011\" or \"t",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/posts",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_posts_ids",
   "name": "Stackexchange get posts ids",
   "description": "Fetches a set of posts by ids. This method is meant for grabbing an object when unsure whether an id identifies a question or an answer. This is most common with user entered data. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for post_id, answer_id, or question_id on post, answer, and question objects respectively. The sorts accepted by this method operate ",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/posts/{ids}",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_posts_ids_comments",
   "name": "Stackexchange get posts ids comments",
   "description": "Gets the comments on the posts identified in ids, regardless of the type of the posts. This method is meant for cases when you are unsure of the type of the post id provided. Generally, this would be due to obtaining the post id directly from a user. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for post_id, answer_id, or question_id on post, answer, and que",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/posts/{ids}/comments",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_posts_ids_revisions",
   "name": "Stackexchange get posts ids revisions",
   "description": "Returns edit revisions for the posts identified in ids. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for post_id, answer_id, or question_id on post, answer, and question objects respectively. This method returns a list of revisions.",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/posts/{ids}/revisions",
    "path": [
     "ids"
    ],
    "query": [
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_posts_ids_suggested_edits",
   "name": "Stackexchange get posts ids suggested edits",
   "description": "Returns suggsted edits on the posts identified in ids. - creation - creation_date - approval - approval_date - rejection - rejection_date creation is the default sort. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for post_id, answer_id, or question_id on post, answer, and question objects respectively. This method returns a list of suggested-edits.",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = approval => date sort = rejection => date"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = approval => date sort = rejection => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "approval",
       "rejection"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/posts/{ids}/suggested-edits",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_privileges",
   "name": "Stackexchange get privileges",
   "description": "Returns the earnable privileges on a site. Privileges define abilities a user can earn (via reputation) on any Stack Exchange site. While fairly stable, over time they do change. New ones are introduced with new features, and the reputation requirements change as a site matures. This method returns a list of privileges.",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/privileges",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_questions",
   "name": "Stackexchange get questions",
   "description": "Gets all the questions on the site. This method allows you make fairly flexible queries across the entire corpus of questions on a site. For example, getting all questions asked in the the week of Jan 1st 2011 with scores of 10 or more is a single query with parameters sort=votes&min=10&fromdate=1293840000&todate=1294444800. To constrain questions returned to those with a set of tags, use the tagg",
   "input": {
    "type": "object",
    "properties": {
     "tagged": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = hot => none sort = week => none sort = month => none sort = relevance => none"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = hot => none sort = week => none sort = month => none sort = relevance => none"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes",
       "hot",
       "week",
       "month",
       "relevance"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/questions",
    "path": [],
    "query": [
     "tagged",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_questions_featured",
   "name": "Stackexchange get questions featured",
   "description": "Returns all the questions with active bounties in the system. The sorts accepted by this method operate on the follow fields of the question object: - activity - last_activity_date - creation - creation_date - votes - score activity is the default sort. It is possible to create moderately complex queries using sort, min, max, fromdate, and todate. This method returns a list of questions.",
   "input": {
    "type": "object",
    "properties": {
     "tagged": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/questions/featured",
    "path": [],
    "query": [
     "tagged",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_questions_ids",
   "name": "Stackexchange get questions ids",
   "description": "Returns the questions identified in {ids}. This is most useful for fetching fresh data when maintaining a cache of question ids, or polling for changes. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for question_id on question objects. The sorts accepted by this method operate on the follow fields of the question object: - activity - last_activity_date - cre",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/questions/{ids}",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_questions_ids_answers",
   "name": "Stackexchange get questions ids answers",
   "description": "Gets the answers to a set of questions identified in id. This method is most useful if you have a set of interesting questions, and you wish to obtain all of their answers at once or if you are polling for new or updates answers (in conjunction with sort=activity). {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for question_id on question objects. The sorts a",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/questions/{ids}/answers",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_questions_ids_comments",
   "name": "Stackexchange get questions ids comments",
   "description": "Gets the comments on a question. If you know that you have an question id and need the comments, use this method. If you know you have a answer id, use /answers/{ids}/comments. If you are unsure, use /posts/{ids}/comments. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for question_id on question objects. The sorts accepted by this method operate on the follo",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/questions/{ids}/comments",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_questions_ids_linked",
   "name": "Stackexchange get questions ids linked",
   "description": "Gets questions which link to those questions identified in {ids}. This method only considers questions that are linked within a site, and will never return questions from another Stack Exchange site. A question is considered \"linked\" when it explicitly includes a hyperlink to another question, there are no other heuristics. {ids} can contain up to 100 semicolon delimited ids, to find ids programat",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = rank => none"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = rank => none"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes",
       "rank"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/questions/{ids}/linked",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_questions_ids_related",
   "name": "Stackexchange get questions ids related",
   "description": "Returns questions that the site considers related to those identified in {ids}. The algorithm for determining if questions are related is not documented, and subject to change at any time. Futhermore, these values are very heavily cached, and may not update immediately after a question has been editted. It is also not guaranteed that a question will be considered related to any number (even non-ze",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = rank => none"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = rank => none"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes",
       "rank"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/questions/{ids}/related",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_questions_ids_timeline",
   "name": "Stackexchange get questions ids timeline",
   "description": "Returns a subset of the events that have happened to the questions identified in id. This provides data similar to that found on a question's timeline page. Voting data is scrubbed to deter inferencing of voter identity. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for question_id on question objects. This method returns a list of question timeline events.",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/questions/{ids}/timeline",
    "path": [
     "ids"
    ],
    "query": [
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_questions_no_answers",
   "name": "Stackexchange get questions no answers",
   "description": "Returns questions which have received no answers. Compare with /questions/unanswered which mearly returns questions that the sites consider insufficiently well answered. This method corresponds roughly with the this site tab. To constrain questions returned to those with a set of tags, use the tagged parameter with a semi-colon delimited list of tags. This is an and contraint, passing tagged=c;jav",
   "input": {
    "type": "object",
    "properties": {
     "tagged": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/questions/no-answers",
    "path": [],
    "query": [
     "tagged",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_questions_unanswered",
   "name": "Stackexchange get questions unanswered",
   "description": "Returns questions the site considers to be unanswered. Note that just because a question has an answer, that does not mean it is considered answered. While the rules are subject to change, at this time a question must have at least one upvoted answer to be considered answered. To constrain questions returned to those with a set of tags, use the tagged parameter with a semi-colon delimited list of ",
   "input": {
    "type": "object",
    "properties": {
     "tagged": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/questions/unanswered",
    "path": [],
    "query": [
     "tagged",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_revisions_ids",
   "name": "Stackexchange get revisions ids",
   "description": "Returns edit revisions identified by ids in {ids}. {ids} can contain up to 20 semicolon delimited ids, to find ids programatically look for revision_guid on revision objects. Note that unlike most other id types in the API, revision_guid is a string. This method returns a list of revisions.",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Guid list (semicolon delimited)."
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/revisions/{ids}",
    "path": [
     "ids"
    ],
    "query": [
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_search",
   "name": "Stackexchange get search",
   "description": "Searches a site for any questions which fit the given criteria. This method is intentionally quite limited. For more general searching, you should use a proper internet search engine restricted to the domain of the site in question. At least one of tagged or intitle must be set on this method. nottagged is only used if tagged is also set, for performance reasons. tagged and nottagged are semi-colo",
   "input": {
    "type": "object",
    "properties": {
     "tagged": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = relevance => none"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = relevance => none"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes",
       "relevance"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     },
     "intitle": {
      "type": "string",
      "description": ""
     },
     "nottagged": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/search",
    "path": [],
    "query": [
     "tagged",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site",
     "intitle",
     "nottagged"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_search_advanced",
   "name": "Stackexchange get search advanced",
   "description": "Searches a site for any questions which fit the given criteria. Search criteria are expressed using the following parameters: - q - a free form text parameter, will match all question properties based on an undocumented algorithm. - accepted - true to return only questions with accepted answers, false to return only those without. Omit to elide constraint. - answers - the minimum number of answers",
   "input": {
    "type": "object",
    "properties": {
     "tagged": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = relevance => none"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = relevance => none"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes",
       "relevance"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     },
     "accepted": {
      "type": "string",
      "description": "",
      "enum": [
       true,
       false
      ]
     },
     "answers": {
      "type": "integer",
      "description": ""
     },
     "body": {
      "type": "string",
      "description": ""
     },
     "closed": {
      "type": "string",
      "description": "",
      "enum": [
       true,
       false
      ]
     },
     "migrated": {
      "type": "string",
      "description": "",
      "enum": [
       true,
       false
      ]
     },
     "notice": {
      "type": "string",
      "description": "",
      "enum": [
       true,
       false
      ]
     },
     "nottagged": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "q": {
      "type": "string",
      "description": ""
     },
     "title": {
      "type": "string",
      "description": ""
     },
     "url": {
      "type": "string",
      "description": ""
     },
     "user": {
      "type": "integer",
      "description": ""
     },
     "views": {
      "type": "integer",
      "description": ""
     },
     "wiki": {
      "type": "string",
      "description": "",
      "enum": [
       true,
       false
      ]
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/search/advanced",
    "path": [],
    "query": [
     "tagged",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site",
     "accepted",
     "answers",
     "body",
     "closed",
     "migrated",
     "notice",
     "nottagged",
     "q",
     "title",
     "url",
     "user",
     "views",
     "wiki"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_similar",
   "name": "Stackexchange get similar",
   "description": "Returns questions which are similar to a hypothetical one based on a title and tag combination. This method is roughly equivalent to a site's related questions suggestion on the ask page. This method is useful for correlating data outside of a Stack Exchange site with similar content within one. Note that title must always be passed as a parameter. tagged and nottagged are optional, semi-colon del",
   "input": {
    "type": "object",
    "properties": {
     "tagged": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = relevance => none"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = relevance => none"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes",
       "relevance"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     },
     "nottagged": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "title": {
      "type": "string",
      "description": ""
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/similar",
    "path": [],
    "query": [
     "tagged",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site",
     "nottagged",
     "title"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_sites",
   "name": "Stackexchange get sites",
   "description": "Returns all sites in the network. This method allows for discovery of new sites, and changes to existing ones. Be aware that unlike normal API methods, this method should be fetched very infrequently, it is very unusual for these values to change more than once on any given day. It is suggested that you cache its return for at least one day, unless your app encounters evidence that it has changed ",
   "input": {
    "type": "object",
    "properties": {
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/sites",
    "path": [],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_suggested_edits",
   "name": "Stackexchange get suggested edits",
   "description": "Returns all the suggested edits in the systems. This method returns a list of suggested-edits. The sorts accepted by this method operate on the follow fields of the suggested_edit object: - creation - creation_date - approval - approval_date Does not return unapproved suggested_edits - rejection - rejection_date Does not return unrejected suggested_edits creation is the default sort. It is possibl",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = approval => date sort = rejection => date"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = approval => date sort = rejection => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "approval",
       "rejection"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/suggested-edits",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_suggested_edits_ids",
   "name": "Stackexchange get suggested edits ids",
   "description": "Returns suggested edits identified in ids. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for suggested_edit_id on suggested_edit objects. The sorts accepted by this method operate on the follow fields of the suggested_edit object: - creation - creation_date - approval - approval_date Does not return unapproved suggested_edits - rejection - rejection_date Doe",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = approval => date sort = rejection => date"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = approval => date sort = rejection => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "approval",
       "rejection"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/suggested-edits/{ids}",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_tags",
   "name": "Stackexchange get tags",
   "description": "Returns the tags found on a site. The inname parameter lets a consumer filter down to tags that contain a certain substring. For example, inname=own would return both \"download\" and \"owner\" amongst others. This method returns a list of tags. The sorts accepted by this method operate on the follow fields of the tag object: - popular - count - activity - the creation_date of the last question asked ",
   "input": {
    "type": "object",
    "properties": {
     "inname": {
      "type": "string",
      "description": ""
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "min": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "popular",
       "activity",
       "name"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/tags",
    "path": [],
    "query": [
     "inname",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_tags_moderator_only",
   "name": "Stackexchange get tags moderator only",
   "description": "Returns the tags found on a site that only moderators can use. The inname parameter lets a consumer filter down to tags that contain a certain substring. For example, inname=own would return both \"download\" and \"owner\" amongst others. This method returns a list of tags. The sorts accepted by this method operate on the follow fields of the tag object: - popular - count - activity - the creation_dat",
   "input": {
    "type": "object",
    "properties": {
     "inname": {
      "type": "string",
      "description": ""
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "min": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "popular",
       "activity",
       "name"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/tags/moderator-only",
    "path": [],
    "query": [
     "inname",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_tags_required",
   "name": "Stackexchange get tags required",
   "description": "Returns the tags found on a site that fulfill required tag constraints on questions. The inname parameter lets a consumer filter down to tags that contain a certain substring. For example, inname=own would return both \"download\" and \"owner\" amongst others. This method returns a list of tags. The sorts accepted by this method operate on the follow fields of the tag object: - popular - count - activ",
   "input": {
    "type": "object",
    "properties": {
     "inname": {
      "type": "string",
      "description": ""
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "min": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "popular",
       "activity",
       "name"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/tags/required",
    "path": [],
    "query": [
     "inname",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_tags_synonyms",
   "name": "Stackexchange get tags synonyms",
   "description": "Returns all tag synonyms found a site. When searching for synonyms of specific tags, it is better to use /tags/{tags}/synonyms over this method. The sorts accepted by this method operate on the follow fields of the tag_synonym object: - creation - creation_date - applied - applied_count - activity - last_applied_date creation is the default sort. It is possible to create moderately complex queries",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = applied => number sort = activity => date"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = applied => number sort = activity => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "applied",
       "activity"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/tags/synonyms",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_tags_tag_top_answerers_period",
   "name": "Stackexchange get tags tag top answerers period",
   "description": "Returns the top 30 answerers active in a single tag, of either all-time or the last 30 days. This is a view onto the data presented on the tag info page on the sites. This method returns a list of tag score objects.",
   "input": {
    "type": "object",
    "properties": {
     "tag": {
      "type": "string",
      "description": ""
     },
     "period": {
      "type": "string",
      "description": "",
      "enum": [
       "all_time",
       "month"
      ]
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "period",
     "site",
     "tag"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/tags/{tag}/top-answerers/{period}",
    "path": [
     "tag",
     "period"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_tags_tag_top_askers_period",
   "name": "Stackexchange get tags tag top askers period",
   "description": "Returns the top 30 askers active in a single tag, of either all-time or the last 30 days. This is a view onto the data presented on the tag info page on the sites. This method returns a list of tag score objects.",
   "input": {
    "type": "object",
    "properties": {
     "tag": {
      "type": "string",
      "description": ""
     },
     "period": {
      "type": "string",
      "description": "",
      "enum": [
       "all_time",
       "month"
      ]
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "period",
     "site",
     "tag"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/tags/{tag}/top-askers/{period}",
    "path": [
     "tag",
     "period"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_tags_tags_faq",
   "name": "Stackexchange get tags tags faq",
   "description": "Returns the frequently asked questions for the given set of tags in {tags}. For a question to be returned, it must have all the tags in {tags} and be considered \"frequently asked\". The exact algorithm for determining whether a question is considered a FAQ is subject to change at any time. {tags} can contain up to 5 individual tags per request. This method returns a list of questions.",
   "input": {
    "type": "object",
    "properties": {
     "tags": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site",
     "tags"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/tags/{tags}/faq",
    "path": [
     "tags"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_tags_tags_info",
   "name": "Stackexchange get tags tags info",
   "description": "Returns tag objects representing the tags in {tags} found on the site. This method diverges from the standard naming patterns to avoid to conflicting with existing methods, due to the free form nature of tag names. This method returns a list of tags. The sorts accepted by this method operate on the follow fields of the tag object: - popular - count - activity - the creation_date of the last questi",
   "input": {
    "type": "object",
    "properties": {
     "tags": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "min": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "popular",
       "activity",
       "name"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site",
     "tags"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/tags/{tags}/info",
    "path": [
     "tags"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_tags_tags_related",
   "name": "Stackexchange get tags tags related",
   "description": "Returns the tags that are most related to those in {tags}. Including multiple tags in {tags} is equivalent to asking for \"tags related to tag #1 and tag #2\" not \"tags related to tag #1 or tag #2\". count on tag objects returned is the number of question with that tag that also share all those in {tags}. {tags} can contain up to 4 individual tags per request. This method returns a list of tags.",
   "input": {
    "type": "object",
    "properties": {
     "tags": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site",
     "tags"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/tags/{tags}/related",
    "path": [
     "tags"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_tags_tags_synonyms",
   "name": "Stackexchange get tags tags synonyms",
   "description": "Gets all the synonyms that point to the tags identified in {tags}. If you're looking to discover all the tag synonyms on a site, use the /tags/synonyms methods instead of call this method on all tags. {tags} can contain up to 20 individual tags per request. The sorts accepted by this method operate on the follow fields of the tag_synonym object: - creation - creation_date - applied - applied_count",
   "input": {
    "type": "object",
    "properties": {
     "tags": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = applied => number sort = activity => date"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = applied => number sort = activity => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "applied",
       "activity"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site",
     "tags"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/tags/{tags}/synonyms",
    "path": [
     "tags"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_tags_tags_wikis",
   "name": "Stackexchange get tags tags wikis",
   "description": "Returns the wikis that go with the given set of tags in {tags}. Be aware that not all tags have wikis. {tags} can contain up to 20 individual tags per request. This method returns a list of tag wikis.",
   "input": {
    "type": "object",
    "properties": {
     "tags": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site",
     "tags"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/tags/{tags}/wikis",
    "path": [
     "tags"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users",
   "name": "Stackexchange get users",
   "description": "Returns all users on a site. This method returns a list of users. The sorts accepted by this method operate on the follow fields of the user object: - reputation - reputation - creation - creation_date - name - display_name - modified - last_modified_date reputation is the default sort. It is possible to create moderately complex queries using sort, min, max, fromdate, and todate. The inname param",
   "input": {
    "type": "object",
    "properties": {
     "inname": {
      "type": "string",
      "description": ""
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = reputation => number sort = creation => date sort = name => string sort = modified => date"
     },
     "min": {
      "type": "string",
      "description": "sort = reputation => number sort = creation => date sort = name => string sort = modified => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "reputation",
       "creation",
       "name",
       "modified"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users",
    "path": [],
    "query": [
     "inname",
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_id_inbox",
   "name": "Stackexchange get users id inbox",
   "description": "Returns a user's inbox. This method requires an access_token, with a scope containing \"read_inbox\". This method is effectively an alias for /inbox. It is provided for consumers who make strong assumptions about operating within the context of a single site rather than the Stack Exchange network as a whole. {id} can contain a single id, to find it programatically look for user_id on user or shallow",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{id}/inbox",
    "path": [
     "id"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_id_inbox_unread",
   "name": "Stackexchange get users id inbox unread",
   "description": "Returns the unread items in a user's inbox. This method requires an access_token, with a scope containing \"read_inbox\". This method is effectively an alias for /inbox/unread. It is provided for consumers who make strong assumptions about operating within the context of a single site rather than the Stack Exchange network as a whole. {id} can contain a single id, to find it programatically look for",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     },
     "since": {
      "type": "integer",
      "description": "Unix date."
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{id}/inbox/unread",
    "path": [
     "id"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site",
     "since"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_id_notifications",
   "name": "Stackexchange get users id notifications",
   "description": "Returns a user's notifications. This method requires an access_token, with a scope containing \"read_inbox\". This method returns a list of notifications.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{id}/notifications",
    "path": [
     "id"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_id_notifications_unread",
   "name": "Stackexchange get users id notifications unread",
   "description": "Returns a user's unread notifications. This method requires an access_token, with a scope containing \"read_inbox\". This method returns a list of notifications.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{id}/notifications/unread",
    "path": [
     "id"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_id_privileges",
   "name": "Stackexchange get users id privileges",
   "description": "Returns the privileges a user has. Applications are encouraged to calculate privileges themselves, without repeated queries to this method. A simple check against the results returned by /privileges and user.user_type would be sufficient. {id} can contain only a single, to find it programatically look for user_id on user or shallow_user objects. This method returns a list of privileges.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{id}/privileges",
    "path": [
     "id"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_id_reputation_history_full",
   "name": "Stackexchange get users id reputation history full",
   "description": "Returns a user's full reputation history, including private events. This method requires an access_token, with a scope containing \"private_info\". This method returns a list of reputation_history.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{id}/reputation-history/full",
    "path": [
     "id"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_id_tags_tags_top_answers",
   "name": "Stackexchange get users id tags tags top answers",
   "description": "Returns the top 30 answers a user has posted in response to questions with the given tags. {id} can contain a single id, to find it programatically look for user_id on user or shallow_user objects. {tags} is limited to 5 tags, passing more will result in an error. The sorts accepted by this method operate on the follow fields of the answer object: - activity - last_activity_date - creation - creat",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "tags": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "id",
     "site",
     "tags"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{id}/tags/{tags}/top-answers",
    "path": [
     "id",
     "tags"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_id_tags_tags_top_questions",
   "name": "Stackexchange get users id tags tags top questions",
   "description": "Returns the top 30 questions a user has asked with the given tags. {id} can contain a single id, to find it programatically look for user_id on user or shallow_user objects. {tags} is limited to 5 tags, passing more will result in an error. The sorts accepted by this method operate on the follow fields of the question object: - activity - last_activity_date - creation - creation_date - votes - sco",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "tags": {
      "type": "string",
      "description": "String list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "id",
     "site",
     "tags"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{id}/tags/{tags}/top-questions",
    "path": [
     "id",
     "tags"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_id_top_answer_tags",
   "name": "Stackexchange get users id top answer tags",
   "description": "Returns a single user's top tags by answer score. This a subset of the data returned on a user's tags tab. {id} can contain a single id, to find it programatically look for user_id on user or shallow_user objects. This method returns a list of top_tag objects.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{id}/top-answer-tags",
    "path": [
     "id"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_id_top_question_tags",
   "name": "Stackexchange get users id top question tags",
   "description": "Returns a single user's top tags by question score. This a subset of the data returned on a user's tags tab. {id} can contain a single id, to find it programatically look for user_id on user or shallow_user objects. This method returns a list of top_tag objects.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{id}/top-question-tags",
    "path": [
     "id"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_id_write_permissions",
   "name": "Stackexchange get users id write permissions",
   "description": "Returns the write permissions a user has via the api. The Stack Exchange API gives users the ability to create, edit, and delete certain types. This method returns whether the passed user is capable of performing those actions at all, as well as how many times a day they can. This method does not consider the user's current quota (ie. if they've already exhausted it for today) nor any additional r",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{id}/write-permissions",
    "path": [
     "id"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids",
   "name": "Stackexchange get users ids",
   "description": "Gets the users identified in ids in {ids}. Typically this method will be called to fetch user profiles when you have obtained user ids from some other source, such as /questions. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_user objects. The sorts accepted by this method operate on the follow fields of the user object: - reput",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = reputation => number sort = creation => date sort = name => string sort = modified => date"
     },
     "min": {
      "type": "string",
      "description": "sort = reputation => number sort = creation => date sort = name => string sort = modified => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "reputation",
       "creation",
       "name",
       "modified"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_answers",
   "name": "Stackexchange get users ids answers",
   "description": "Returns the answers the users in {ids} have posted. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_user objects. The sorts accepted by this method operate on the follow fields of the answer object: - activity - last_activity_date - creation - creation_date - votes - score activity is the default sort. It is possible to create mo",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/answers",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_associated",
   "name": "Stackexchange get users ids associated",
   "description": "Returns all of a user's associated accounts, given their account_ids in {ids}. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for account_id on user objects. This method returns a list of network_users.",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": [
     "ids"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/associated",
    "path": [
     "ids"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_badges",
   "name": "Stackexchange get users ids badges",
   "description": "Get the badges the users in {ids} have earned. Badge sorts are a tad complicated. For the purposes of sorting (and min/max) tag_based is considered to be greater than named. This means that you can get a list of all tag based badges a user has by passing min=tag_based, and conversely all the named badges by passing max=named, with sort=type. For ranks, bronze is greater than silver which is greate",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = rank => string sort = name => string sort = type => string sort = awarded => date"
     },
     "min": {
      "type": "string",
      "description": "sort = rank => string sort = name => string sort = type => string sort = awarded => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "rank",
       "name",
       "type",
       "awarded"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/badges",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_comments",
   "name": "Stackexchange get users ids comments",
   "description": "Get the comments posted by users in {ids}. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_user objects. The sorts accepted by this method operate on the follow fields of the comment object: - creation - creation_date - votes - score creation is the default sort. It is possible to create moderately complex queries using sort, min",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/comments",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_comments_toid",
   "name": "Stackexchange get users ids comments toid",
   "description": "Get the comments that the users in {ids} have posted in reply to the single user identified in {toid}. This method is useful for extracting conversations, especially over time or across multiple posts. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_user objects. {toid} can contain only 1 id, found in the same manner as those in ",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "toid": {
      "type": "integer",
      "description": ""
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site",
     "toid"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/comments/{toid}",
    "path": [
     "ids",
     "toid"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_favorites",
   "name": "Stackexchange get users ids favorites",
   "description": "Get the questions that users in {ids} have favorited. This method is effectively a view onto a user's favorites tab. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_user objects. The sorts accepted by this method operate on the follow fields of the question object: - activity - last_activity_date - creation - creation_date - vote",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = added => date"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number sort = added => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes",
       "added"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/favorites",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_mentioned",
   "name": "Stackexchange get users ids mentioned",
   "description": "Gets all the comments that the users in {ids} were mentioned in. Note, to count as a mention the comment must be considered to be \"in reply to\" a user. Most importantly, this means that a comment can only be in reply to a single user. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_user objects. The sorts accepted by this method ",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/mentioned",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_merges",
   "name": "Stackexchange get users ids merges",
   "description": "Returns a record of merges that have occurred involving the passed account ids. This method allows you to take now invalid account ids and find what account they've become, or take currently valid account ids and find which ids were equivalent in the past. This is most useful when confirming that an account_id is in fact \"new\" to an application. Account merges can happen for a wide range of reason",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     }
    },
    "required": [
     "ids"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/merges",
    "path": [
     "ids"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_questions",
   "name": "Stackexchange get users ids questions",
   "description": "Gets the questions asked by the users in {ids}. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_user objects. The sorts accepted by this method operate on the follow fields of the question object: - activity - last_activity_date - creation - creation_date - votes - score activity is the default sort. It is possible to create mode",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/questions",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_questions_featured",
   "name": "Stackexchange get users ids questions featured",
   "description": "Gets the questions on which the users in {ids} have active bounties. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_user objects. The sorts accepted by this method operate on the follow fields of the question object: - activity - last_activity_date - creation - creation_date - votes - score activity is the default sort. It is po",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/questions/featured",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_questions_no_answers",
   "name": "Stackexchange get users ids questions no answers",
   "description": "Gets the questions asked by the users in {ids} which have no answers. Questions returns by this method actually have zero undeleted answers. It is completely disjoint /users/{ids}/questions/unanswered and /users/{ids}/questions/unaccepted, which only return questions with at least one answer, subject to other contraints. {ids} can contain up to 100 semicolon delimited ids, to find ids programatica",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/questions/no-answers",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_questions_unaccepted",
   "name": "Stackexchange get users ids questions unaccepted",
   "description": "Gets the questions asked by the users in {ids} which have at least one answer, but no accepted answer. Questions returned by this method have answers, but the owner has not opted to accept any of them. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_user objects. The sorts accepted by this method operate on the follow fields of t",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/questions/unaccepted",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_questions_unanswered",
   "name": "Stackexchange get users ids questions unanswered",
   "description": "Gets the questions asked by the users in {ids} which the site consideres unanswered, while still having at least one answer posted. These rules are subject to change, but currently any question without at least one upvoted or accepted answer is considered unanswered. To get the set of questions that a user probably considers unanswered, the returned questions should be unioned with those returned ",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "min": {
      "type": "string",
      "description": "sort = activity => date sort = creation => date sort = votes => number"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "activity",
       "creation",
       "votes"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/questions/unanswered",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_reputation",
   "name": "Stackexchange get users ids reputation",
   "description": "Gets a subset of the reputation changes for users in {ids}. Reputation changes are intentionally scrubbed of some data to make it difficult to correlate votes on particular posts with user reputation changes. That being said, this method returns enough data for reasonable display of reputation trends. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/reputation",
    "path": [
     "ids"
    ],
    "query": [
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_reputation_history",
   "name": "Stackexchange get users ids reputation history",
   "description": "Returns users' public reputation history. This method returns a list of reputation_history.",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/reputation-history",
    "path": [
     "ids"
    ],
    "query": [
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_suggested_edits",
   "name": "Stackexchange get users ids suggested edits",
   "description": "Returns the suggested edits a users in {ids} have submitted. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_user objects. The sorts accepted by this method operate on the follow fields of the suggested_edit object: - creation - creation_date - approval - approval_date Does not return unapproved suggested_edits - rejection - reje",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = creation => date sort = approval => date sort = rejection => date"
     },
     "min": {
      "type": "string",
      "description": "sort = creation => date sort = approval => date sort = rejection => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "creation",
       "approval",
       "rejection"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/suggested-edits",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_tags",
   "name": "Stackexchange get users ids tags",
   "description": "Returns the tags the users identified in {ids} have been active in. This route corresponds roughly to user's stats tab, but does not include tag scores. A subset of tag scores are available (on a single user basis) in /users/{id}/top-answer-tags and /users/{id}/top-question-tags. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_us",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "min": {
      "type": "string",
      "description": "sort = popular => number sort = activity => date sort = name => string"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "popular",
       "activity",
       "name"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/tags",
    "path": [
     "ids"
    ],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_ids_timeline",
   "name": "Stackexchange get users ids timeline",
   "description": "Returns a subset of the actions the users in {ids} have taken on the site. This method returns users' posts, edits, and earned badges in the order they were accomplished. It is possible to filter to just a window of activity using the fromdate and todate parameters. {ids} can contain up to 100 semicolon delimited ids, to find ids programatically look for user_id on user or shallow_user objects. Th",
   "input": {
    "type": "object",
    "properties": {
     "ids": {
      "type": "string",
      "description": "Number list (semicolon delimited)."
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "ids",
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/{ids}/timeline",
    "path": [
     "ids"
    ],
    "query": [
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_moderators",
   "name": "Stackexchange get users moderators",
   "description": "Gets those users on a site who can exercise moderation powers. Note, employees of Stack Exchange Inc. will be returned if they have been granted moderation powers on a site even if they have never been appointed or elected explicitly. This method checks abilities, not the manner in which they were obtained. The sorts accepted by this method operate on the follow fields of the user object: - reputa",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = reputation => number sort = creation => date sort = name => string sort = modified => date"
     },
     "min": {
      "type": "string",
      "description": "sort = reputation => number sort = creation => date sort = name => string sort = modified => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "reputation",
       "creation",
       "name",
       "modified"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/moderators",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.get_users_moderators_elected",
   "name": "Stackexchange get users moderators elected",
   "description": "Returns those users on a site who both have moderator powers, and were actually elected. This method excludes Stack Exchange Inc. employees, unless they were actually elected moderators on a site (which can only have happened prior to their employment). The sorts accepted by this method operate on the follow fields of the user object: - reputation - reputation - creation - creation_date - name - d",
   "input": {
    "type": "object",
    "properties": {
     "order": {
      "type": "string",
      "description": "",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "max": {
      "type": "string",
      "description": "sort = reputation => number sort = creation => date sort = name => string sort = modified => date"
     },
     "min": {
      "type": "string",
      "description": "sort = reputation => number sort = creation => date sort = name => string sort = modified => date"
     },
     "sort": {
      "type": "string",
      "description": "",
      "enum": [
       "reputation",
       "creation",
       "name",
       "modified"
      ]
     },
     "fromdate": {
      "type": "integer",
      "description": "Unix date."
     },
     "todate": {
      "type": "integer",
      "description": "Unix date."
     },
     "pagesize": {
      "type": "integer",
      "description": ""
     },
     "page": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     }
    },
    "required": [
     "site"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.stackexchange.com/users/moderators/elected",
    "path": [],
    "query": [
     "order",
     "max",
     "min",
     "sort",
     "fromdate",
     "todate",
     "pagesize",
     "page",
     "filter",
     "callback",
     "site"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.post_comments_id_delete",
   "name": "Stackexchange post comments id delete",
   "description": "Deletes a comment. Use an access_token with write_access to delete a comment. In practice, this method will never return an object.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     },
     "preview": {
      "type": "boolean",
      "description": ""
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.stackexchange.com/comments/{id}/delete",
    "path": [
     "id"
    ],
    "query": [
     "filter",
     "callback",
     "site",
     "preview"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.post_comments_id_edit",
   "name": "Stackexchange post comments id edit",
   "description": "Edit an existing comment. Use an access_token with write_access to edit an existing comment. This method return the created comment.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     },
     "body": {
      "type": "string",
      "description": ""
     },
     "preview": {
      "type": "boolean",
      "description": ""
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.stackexchange.com/comments/{id}/edit",
    "path": [
     "id"
    ],
    "query": [
     "filter",
     "callback",
     "site",
     "body",
     "preview"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "stackexchange.post_posts_id_comments_add",
   "name": "Stackexchange post posts id comments add",
   "description": "Create a new comment. Use an access_token with write_access to create a new comment on a post. This method returns the created comment.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": ""
     },
     "filter": {
      "type": "string",
      "description": "#Discussion The Stack Exchange API allows applications to exclude almost every field returned. For example, if an application did not care about a user's badge counts it could exclude user.badge_counts whenever it calls a method that returns users. An application excludes fields by creating a filter (via /filter/create) and passing it to a method in the filter parameter. Filters are immutable and "
     },
     "callback": {
      "type": "string",
      "description": "All API responses are JSON, we do support JSONP with the callback query parameter."
     },
     "site": {
      "type": "string",
      "description": "Each of these methods operates on a single site at a time, identified by the site parameter. This parameter can be the full domain name (ie. \"stackoverflow.com\"), or a short form identified by api_site_parameter on the site object."
     },
     "body": {
      "type": "string",
      "description": ""
     },
     "preview": {
      "type": "boolean",
      "description": ""
     }
    },
    "required": [
     "id",
     "site"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.stackexchange.com/posts/{id}/comments/add",
    "path": [
     "id"
    ],
    "query": [
     "filter",
     "callback",
     "site",
     "body",
     "preview"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  }
 ]
}
