{
 "_": "GENERATED \u2014 what an authoring tool needs to FIND an operation. The calling table is the .lua beside it.",
 "provider": "shortcut",
 "source": "https://shortcut.com/openapi.json",
 "operations": [
  {
   "slug": "shortcut.add_doc_to_collection",
   "name": "Add doc to collection",
   "description": "Add Doc to Collection adds the Doc to the chosen Collection, preserving its other Collection memberships, and returns the updated Doc. Requires write access to the Doc.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "doc-public-id": {
      "type": "string",
      "description": "The ID of the Doc."
     },
     "collection-public-id": {
      "type": "string",
      "description": "The ID of the Collection."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "collection-public-id",
     "doc-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/documents/{doc-public-id}/collections/{collection-public-id}",
    "path": [
     "workspace-slug",
     "doc-public-id",
     "collection-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_categories",
   "name": "Bulk create categories",
   "description": "Bulk Create Categories creates multiple Categories, and returns a paginated list of the created Categories.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/categories/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_docs",
   "name": "Bulk create docs",
   "description": "Bulk Create Docs creates multiple Docs, and returns a paginated list of the created Docs.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/documents/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_entity_templates",
   "name": "Bulk create entity templates",
   "description": "Bulk Create Entity Templates creates multiple Entity Templates, and returns a paginated list of the created Entity Templates.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/entity-templates/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_epic_comments",
   "name": "Bulk create epic comments",
   "description": "Bulk Create Epic Comments creates multiple Comments on an Epic, and returns a paginated list of the created Comments.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/comments/bulk",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_epics",
   "name": "Bulk create epics",
   "description": "Bulk Create Epics creates multiple Epics, and returns a paginated list of the created Epics.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_iterations",
   "name": "Bulk create iterations",
   "description": "Bulk Create Iterations creates multiple Iterations, and returns a paginated list of the created Iterations.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "start_date"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_labels",
   "name": "Bulk create labels",
   "description": "Bulk Create Labels creates multiple Labels, and returns a paginated list of the created Labels.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/labels/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_linked_files",
   "name": "Bulk create linked files",
   "description": "Bulk Create Linked Files creates multiple Linked Files, and returns a paginated list of the created Linked Files.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/linked-files/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_objectives",
   "name": "Bulk create objectives",
   "description": "Bulk Create Objectives creates multiple Objectives, and returns a paginated list of the created Objectives.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_stories",
   "name": "Bulk create stories",
   "description": "Bulk Create Stories creates multiple Stories, and returns a paginated list of the created Stories.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_story_checklist_items",
   "name": "Bulk create story checklist items",
   "description": "Bulk Create Story Checklist Items creates multiple Checklist Items on a Story, and returns a paginated list of the created Checklist Items.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "position"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/checklist-items/bulk",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_story_comments",
   "name": "Bulk create story comments",
   "description": "Bulk Create Story Comments creates multiple Comments on a Story, and returns a paginated list of the created Comments.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "position"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/bulk",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_story_links",
   "name": "Bulk create story links",
   "description": "Bulk Create Story Links creates multiple Story Links, and returns a paginated list of the created Story Links.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/story-links/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_create_teams",
   "name": "Bulk create teams",
   "description": "Creates multiple Teams and returns a paginated list of the created Teams.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/teams/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_categories",
   "name": "Bulk delete categories",
   "description": "Bulk Delete Categories deletes multiple Categories.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/categories/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_custom_fields",
   "name": "Bulk delete custom fields",
   "description": "Bulk Delete Custom Fields removes multiple Custom Fields from the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/custom-fields/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_entity_templates",
   "name": "Bulk delete entity templates",
   "description": "Bulk Delete Entity Templates removes multiple Entity Templates from the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/entity-templates/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_epic_comments",
   "name": "Bulk delete epic comments",
   "description": "Bulk Delete Epic Comments deletes multiple Comments from an Epic.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/comments/bulk",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_epics",
   "name": "Bulk delete epics",
   "description": "Bulk Delete Epics deletes multiple Epics. The Epics must be archived unless `force` is set.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "force": {
      "type": "boolean",
      "description": "Forces deletion of the Epics, overriding the check that they are archived."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "force"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_files",
   "name": "Bulk delete files",
   "description": "Bulk Delete Files deletes multiple Files from the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/files/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_iterations",
   "name": "Bulk delete iterations",
   "description": "Bulk Delete Iterations deletes multiple Iterations.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_labels",
   "name": "Bulk delete labels",
   "description": "Bulk Delete Labels deletes multiple Labels.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/labels/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_linked_files",
   "name": "Bulk delete linked files",
   "description": "Bulk Delete Linked Files deletes multiple Linked Files.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/linked-files/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_objectives",
   "name": "Bulk delete objectives",
   "description": "Bulk Delete Objectives deletes multiple Objectives.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_stories",
   "name": "Bulk delete stories",
   "description": "Bulk Delete Stories deletes multiple Stories. The Stories must be archived unless `force` is set.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "force": {
      "type": "boolean",
      "description": "Forces deletion of the Stories, overriding the check that they are archived."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "force"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_story_checklist_items",
   "name": "Bulk delete story checklist items",
   "description": "Bulk Delete Story Checklist Items deletes multiple Checklist Items from a Story.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/checklist-items/bulk",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_story_comments",
   "name": "Bulk delete story comments",
   "description": "Bulk Delete Story Comments deletes multiple Comments from a Story.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/bulk",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_delete_story_links",
   "name": "Bulk delete story links",
   "description": "Bulk Delete Story Links deletes multiple Story Links.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/story-links/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_categories",
   "name": "Bulk update categories",
   "description": "Bulk Update Categories updates multiple Categories, and returns a paginated list of the updated Categories.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/categories/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_custom_fields",
   "name": "Bulk update custom fields",
   "description": "Bulk Update Custom Fields updates multiple Custom Fields, and returns a paginated list of the updated Custom Fields.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/custom-fields/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_entity_templates",
   "name": "Bulk update entity templates",
   "description": "Bulk Update Entity Templates updates multiple Entity Templates, and returns a paginated list of the updated Entity Templates.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/entity-templates/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_epic_comments",
   "name": "Bulk update epic comments",
   "description": "Bulk Update Epic Comments updates multiple Comments on an Epic, and returns a paginated list of the updated Comments.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/comments/bulk",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_epics",
   "name": "Bulk update epics",
   "description": "Bulk Update Epics updates multiple Epics, and returns a paginated list of the updated Epics.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_iterations",
   "name": "Bulk update iterations",
   "description": "Bulk Update Iterations updates multiple Iterations, and returns a paginated list of the updated Iterations.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "start_date"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_labels",
   "name": "Bulk update labels",
   "description": "Bulk Update Labels updates multiple Labels, and returns a paginated list of the updated Labels.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/labels/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_linked_files",
   "name": "Bulk update linked files",
   "description": "Bulk Update Linked Files updates multiple Linked Files, and returns a paginated list of the updated Linked Files.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/linked-files/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_members",
   "name": "Bulk update members",
   "description": "Bulk Update Members updates multiple Members and returns a paginated list of the updated Members.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/members/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_objectives",
   "name": "Bulk update objectives",
   "description": "Bulk Update Objectives updates multiple Objectives, and returns a paginated list of the updated Objectives.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_stories",
   "name": "Bulk update stories",
   "description": "Bulk Update Stories updates multiple Stories, and returns a paginated list of the updated Stories.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_story_checklist_items",
   "name": "Bulk update story checklist items",
   "description": "Bulk Update Story Checklist Items updates multiple Checklist Items on a Story, and returns a paginated list of the updated Checklist Items.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "position"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/checklist-items/bulk",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_story_comments",
   "name": "Bulk update story comments",
   "description": "Bulk Update Story Comments updates multiple Comments on a Story, and returns a paginated list of the updated Comments.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "position"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/bulk",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_story_links",
   "name": "Bulk update story links",
   "description": "Bulk Update Story Links updates multiple Story Links, and returns a paginated list of the updated Story Links.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/story-links/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.bulk_update_teams",
   "name": "Bulk update teams",
   "description": "Updates multiple Teams and returns a paginated list of the updated Teams.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/teams/bulk",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_category",
   "name": "Create category",
   "description": "Create Category allows you to create a new Category in Shortcut.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/categories",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_collection",
   "name": "Create collection",
   "description": "Create Collection allows you to create a new Collection in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/collections",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_doc",
   "name": "Create doc",
   "description": "Create Doc allows you to create a new Doc in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/documents",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_entity_template",
   "name": "Create entity template",
   "description": "Create Entity Template creates a new Entity Template in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/entity-templates",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_epic",
   "name": "Create epic",
   "description": "Create Epic is used to create a new Epic in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_epic_comment",
   "name": "Create epic comment",
   "description": "Create Epic Comment is used to add a new Comment to an Epic. To reply to an existing Comment, provide a parent_comment_id.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/comments",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_epic_health",
   "name": "Create epic health",
   "description": "Creates a new Health update for the specified Epic.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/epic-healths",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_iteration",
   "name": "Create iteration",
   "description": "Create Iteration is used to create a new Iteration in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_label",
   "name": "Create label",
   "description": "Create Label allows you to create a new Label in Shortcut.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/labels",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_linked_file",
   "name": "Create linked file",
   "description": "Create Linked File is used to add a new Linked File to your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/linked-files",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_objective",
   "name": "Create objective",
   "description": "Create Objective is used to create a new Objective in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_objective_health",
   "name": "Create objective health",
   "description": "Creates a new Health update for the specified Objective.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "objective-public-id": {
      "type": "integer",
      "description": "The ID of the Objective."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "objective-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/{objective-public-id}/objective-healths",
    "path": [
     "workspace-slug",
     "objective-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_story",
   "name": "Create story",
   "description": "Create Story is used to add a new Story to your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_story_checklist_item",
   "name": "Create story checklist item",
   "description": "Create Story Checklist Item is used to add a new Checklist Item to a Story.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/checklist-items",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_story_comment",
   "name": "Create story comment",
   "description": "Create Story is used to add a new Comment to a Story.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_story_comment_reaction",
   "name": "Create story comment reaction",
   "description": "Create a Reaction to a Story Comment.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "comment-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/{comment-public-id}/reactions",
    "path": [
     "workspace-slug",
     "story-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_story_link",
   "name": "Create story link",
   "description": "Create Story Link is used to create a semantic relationship between two Stories. The subject Story acts on the object Story. For example, \"Story A blocks Story B\".",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/story-links",
    "path": [
     "workspace-slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.create_team",
   "name": "Create team",
   "description": "Creates a new Team in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/teams",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_category",
   "name": "Delete category",
   "description": "Delete Category removes a Category from the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "category-public-id": {
      "type": "integer",
      "description": "The ID of the Category."
     }
    },
    "required": [
     "category-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/categories/{category-public-id}",
    "path": [
     "workspace-slug",
     "category-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_custom_field",
   "name": "Delete custom field",
   "description": "Delete Custom Field removes a Custom Field from the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "custom-field-public-id": {
      "type": "string",
      "description": "The unique ID of the Custom Field."
     }
    },
    "required": [
     "custom-field-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/custom-fields/{custom-field-public-id}",
    "path": [
     "workspace-slug",
     "custom-field-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_entity_template",
   "name": "Delete entity template",
   "description": "Delete Entity Template removes an Entity Template from the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "entity-template-public-id": {
      "type": "string",
      "description": "The unique ID of the entity template."
     }
    },
    "required": [
     "entity-template-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/entity-templates/{entity-template-public-id}",
    "path": [
     "workspace-slug",
     "entity-template-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_epic",
   "name": "Delete epic",
   "description": "Delete Epic removes an Epic from the Workspace. The Epic must be archived unless `force` is set.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "force": {
      "type": "boolean",
      "description": "Forces deletion of the Epic, overriding the check that it is archived."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "force"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_epic_comment",
   "name": "Delete epic comment",
   "description": "Delete a Comment from an Epic.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "comment-public-id",
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/comments/{comment-public-id}",
    "path": [
     "workspace-slug",
     "epic-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_file",
   "name": "Delete file",
   "description": "Delete File removes a previously uploaded File from the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "file-public-id": {
      "type": "integer",
      "description": "The ID of the File."
     }
    },
    "required": [
     "file-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/files/{file-public-id}",
    "path": [
     "workspace-slug",
     "file-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_iteration",
   "name": "Delete iteration",
   "description": "Delete Iteration removes an Iteration from the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "iteration-public-id": {
      "type": "integer",
      "description": "The unique ID of the Iteration."
     }
    },
    "required": [
     "iteration-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/{iteration-public-id}",
    "path": [
     "workspace-slug",
     "iteration-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_label",
   "name": "Delete label",
   "description": "Delete Label removes a Label from the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "label-public-id": {
      "type": "integer",
      "description": "The ID of the Label."
     }
    },
    "required": [
     "label-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/labels/{label-public-id}",
    "path": [
     "workspace-slug",
     "label-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_linked_file",
   "name": "Delete linked file",
   "description": "Delete Linked File removes a Linked File from the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "linked-file-public-id": {
      "type": "integer",
      "description": "The ID of the Linked File."
     }
    },
    "required": [
     "linked-file-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/linked-files/{linked-file-public-id}",
    "path": [
     "workspace-slug",
     "linked-file-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_objective",
   "name": "Delete objective",
   "description": "Delete Objective removes an Objective from the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "objective-public-id": {
      "type": "integer",
      "description": "The ID of the Objective."
     }
    },
    "required": [
     "objective-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/{objective-public-id}",
    "path": [
     "workspace-slug",
     "objective-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_story",
   "name": "Delete story",
   "description": "Delete Story can be used to delete a Story. The Story must be archived unless `force` is set.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "force": {
      "type": "boolean",
      "description": "Forces deletion of the Story, overriding the check that it is archived."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "force"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_story_checklist_item",
   "name": "Delete story checklist item",
   "description": "Delete a Checklist Item from a Story.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "checklist-item-public-id": {
      "type": "integer",
      "description": "The ID of the Checklist Item."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "checklist-item-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/checklist-items/{checklist-item-public-id}",
    "path": [
     "workspace-slug",
     "story-public-id",
     "checklist-item-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_story_comment",
   "name": "Delete story comment",
   "description": "Delete a Comment from a Story.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "comment-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/{comment-public-id}",
    "path": [
     "workspace-slug",
     "story-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_story_comment_reaction",
   "name": "Delete story comment reaction",
   "description": "Delete a Reaction from a Story Comment.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "reaction-public-id": {
      "type": "string",
      "description": "The ID of the Reaction."
     }
    },
    "required": [
     "comment-public-id",
     "reaction-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/{comment-public-id}/reactions/{reaction-public-id}",
    "path": [
     "workspace-slug",
     "story-public-id",
     "comment-public-id",
     "reaction-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_story_link",
   "name": "Delete story link",
   "description": "Delete Story Link removes the relationship between the Stories for the given Story Link.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-link-public-id": {
      "type": "integer",
      "description": "The ID of the Story Link."
     }
    },
    "required": [
     "story-link-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/story-links/{story-link-public-id}",
    "path": [
     "workspace-slug",
     "story-link-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.delete_workspace_token",
   "name": "Delete workspace token",
   "description": "Delete Workspace Token disables an API Token in the Workspace. Requires admin role.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "token-public-id": {
      "type": "string",
      "description": "The unique ID of the Token."
     }
    },
    "required": [
     "token-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/admin/tokens/{token-public-id}",
    "path": [
     "workspace-slug",
     "token-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.favorite_collection",
   "name": "Favorite collection",
   "description": "Favorite Collection marks the Collection as a favorite of the current Member, and returns the updated Collection.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "collection-public-id": {
      "type": "string",
      "description": "The ID of the Collection."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "collection-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/collections/{collection-public-id}/favorite",
    "path": [
     "workspace-slug",
     "collection-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_branch",
   "name": "Get branch",
   "description": "Get Branch returns information about a chosen Branch.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "branch-public-id": {
      "type": "integer",
      "description": "The ID of the Branch."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "branch-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/branches/{branch-public-id}",
    "path": [
     "workspace-slug",
     "branch-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_category",
   "name": "Get category",
   "description": "Get Category returns information about a chosen Category.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "category-public-id": {
      "type": "integer",
      "description": "The ID of the Category."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "category-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/categories/{category-public-id}",
    "path": [
     "workspace-slug",
     "category-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_collection",
   "name": "Get collection",
   "description": "Get Collection returns information about a chosen Collection.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "collection-public-id": {
      "type": "string",
      "description": "The ID of the Collection."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "collection-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/collections/{collection-public-id}",
    "path": [
     "workspace-slug",
     "collection-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_commit",
   "name": "Get commit",
   "description": "Get Commit returns information about a chosen Commit.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "commit-public-id": {
      "type": "integer",
      "description": "The ID of the Commit."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "commit-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/commits/{commit-public-id}",
    "path": [
     "workspace-slug",
     "commit-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_current_iterations",
   "name": "Get current iterations",
   "description": "Returns all currently started iterations for the authenticated user based on their team memberships. The 'current' iterations are determined by finding started iterations associated with teams the user belongs to. If multiple iterations match, all are returned sorted by start date (ascending), then end date (ascending), then ID (ascending).",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "start_date"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/current",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_custom_field",
   "name": "Get custom field",
   "description": "Get Custom Field returns information about a chosen Custom Field.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "custom-field-public-id": {
      "type": "string",
      "description": "The unique ID of the Custom Field."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "custom-field-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/custom-fields/{custom-field-public-id}",
    "path": [
     "workspace-slug",
     "custom-field-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_doc",
   "name": "Get doc",
   "description": "Get Doc returns information about a chosen Doc. Pass content_format=markdown (default) or content_format=html to include content in the response.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "doc-public-id": {
      "type": "string",
      "description": "The ID of the Doc."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "doc-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/documents/{doc-public-id}",
    "path": [
     "workspace-slug",
     "doc-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_entity_template",
   "name": "Get entity template",
   "description": "Get Entity Template returns information about a given Entity Template.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "entity-template-public-id": {
      "type": "string",
      "description": "The unique ID of the entity template."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "entity-template-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/entity-templates/{entity-template-public-id}",
    "path": [
     "workspace-slug",
     "entity-template-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_epic",
   "name": "Get epic",
   "description": "Get Epic returns information about a chosen Epic.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_epic_comment",
   "name": "Get epic comment",
   "description": "Get Epic Comment returns information about a chosen Epic Comment.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "comment-public-id",
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/comments/{comment-public-id}",
    "path": [
     "workspace-slug",
     "epic-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_epic_health",
   "name": "Get epic health",
   "description": "Returns a Health for the specified Epic.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "epic-health-public-id": {
      "type": "string",
      "description": "The unique ID of the Health."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-health-public-id",
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/epic-healths/{epic-health-public-id}",
    "path": [
     "workspace-slug",
     "epic-public-id",
     "epic-health-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_epic_stats",
   "name": "Get epic stats",
   "description": "Get Epic Stats returns calculated stats for a chosen Epic. Stats are served from this dedicated sub-endpoint (rather than the Epic response itself) to avoid the performance impact of computing them on bulk list requests.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/stats",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_epic_workflow",
   "name": "Get epic workflow",
   "description": "Returns the Epic Workflow configuration for the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epic-workflow",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_file",
   "name": "Get file",
   "description": "Get File returns information about the chosen File.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "file-public-id": {
      "type": "integer",
      "description": "The ID of the File."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "file-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/files/{file-public-id}",
    "path": [
     "workspace-slug",
     "file-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_iteration",
   "name": "Get iteration",
   "description": "Get Iteration returns information about a chosen Iteration.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "iteration-public-id": {
      "type": "integer",
      "description": "The unique ID of the Iteration."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "iteration-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/{iteration-public-id}",
    "path": [
     "workspace-slug",
     "iteration-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_key_result",
   "name": "Get key result",
   "description": "Returns the specified Key Result.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "key-result-public-id": {
      "type": "string",
      "description": "The unique ID of the Key Result."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "key-result-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/key-results/{key-result-public-id}",
    "path": [
     "workspace-slug",
     "key-result-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_label",
   "name": "Get label",
   "description": "Get Label returns information about a chosen Label.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "label-public-id": {
      "type": "integer",
      "description": "The ID of the Label."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "label-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/labels/{label-public-id}",
    "path": [
     "workspace-slug",
     "label-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_linked_file",
   "name": "Get linked file",
   "description": "Get Linked File returns information about the chosen Linked File.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "linked-file-public-id": {
      "type": "integer",
      "description": "The ID of the Linked File."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "linked-file-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/linked-files/{linked-file-public-id}",
    "path": [
     "workspace-slug",
     "linked-file-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_member",
   "name": "Get member",
   "description": "Get Member returns information about a Member of the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "member-public-id": {
      "type": "string",
      "description": "The unique ID of the Member."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "member-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/members/{member-public-id}",
    "path": [
     "workspace-slug",
     "member-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_multiple_stories_history",
   "name": "Get multiple stories history",
   "description": "Returns history of changes for multiple Stories in chronological order (oldest first by default; newest first with order_dir=desc), across all attributes, with a deterministic tie-break for changes at the same instant. Ordering is stable across pages. Requires ?ids= parameter. Supports cursor-based pagination. History is currently tracked for these attributes: archived, completed, custom_field_val",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "ids": {
      "type": "string",
      "description": "Comma-separated Story IDs (max 100)."
     },
     "fields": {
      "type": "string",
      "description": "Filter history to specific attributes (e.g., workflow_state,epic)."
     },
     "order_dir": {
      "type": "string",
      "description": "Sort direction by change instant: asc (default, oldest first) or desc (newest first). Entries at the same instant keep the same deterministic tie-break order in both directions.",
      "enum": [
       "desc",
       "asc"
      ]
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/history",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "ids",
     "fields",
     "order_dir"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_objective",
   "name": "Get objective",
   "description": "Get Objective returns information about a chosen Objective.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "objective-public-id": {
      "type": "integer",
      "description": "The ID of the Objective."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "objective-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/{objective-public-id}",
    "path": [
     "workspace-slug",
     "objective-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_objective_health",
   "name": "Get objective health",
   "description": "Returns a Health for the specified Objective.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "objective-public-id": {
      "type": "integer",
      "description": "The ID of the Objective."
     },
     "objective-health-public-id": {
      "type": "string",
      "description": "The unique ID of the Health."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "objective-health-public-id",
     "objective-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/{objective-public-id}/objective-healths/{objective-health-public-id}",
    "path": [
     "workspace-slug",
     "objective-public-id",
     "objective-health-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_pull_request",
   "name": "Get pull request",
   "description": "Get Pull Request returns information about a chosen Pull Request.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "pull-request-public-id": {
      "type": "integer",
      "description": "The ID of the Pull Request."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "pull-request-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/pull-requests/{pull-request-public-id}",
    "path": [
     "workspace-slug",
     "pull-request-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_repository",
   "name": "Get repository",
   "description": "Get Repository returns information about a connected Repository.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "repository-public-id": {
      "type": "integer",
      "description": "The ID of the Repository."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "repository-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/repositories/{repository-public-id}",
    "path": [
     "workspace-slug",
     "repository-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_schema",
   "name": "Get schema",
   "description": "Gets the json-schema for a route.",
   "input": {
    "type": "object",
    "properties": {
     "schema-name": {
      "type": "string",
      "description": "The name of the JSON schema."
     }
    },
    "required": [
     "schema-name"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/schemas/{schema-name}",
    "path": [
     "schema-name"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_story",
   "name": "Get story",
   "description": "Get Story returns information about a chosen Story.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_story_checklist_item",
   "name": "Get story checklist item",
   "description": "Get Story Checklist Item returns information about a chosen Checklist Item for a Story.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "checklist-item-public-id": {
      "type": "integer",
      "description": "The ID of the Checklist Item."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "checklist-item-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/checklist-items/{checklist-item-public-id}",
    "path": [
     "workspace-slug",
     "story-public-id",
     "checklist-item-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_story_comment",
   "name": "Get story comment",
   "description": "Get Story Comment returns information about a chosen Story Comment.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "comment-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/{comment-public-id}",
    "path": [
     "workspace-slug",
     "story-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_story_history",
   "name": "Get story history",
   "description": "Returns history of changes for a Story in chronological order (oldest first by default; newest first with order_dir=desc), across all attributes, with a deterministic tie-break for changes at the same instant. Ordering is stable across pages. Supports cursor-based pagination and optional field filtering. History is currently tracked for these attributes: archived, completed, custom_field_values, d",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "fields": {
      "type": "string",
      "description": "Filter history to specific attributes (e.g., workflow_state,epic)."
     },
     "order_dir": {
      "type": "string",
      "description": "Sort direction by change instant: asc (default, oldest first) or desc (newest first). Entries at the same instant keep the same deterministic tie-break order in both directions.",
      "enum": [
       "desc",
       "asc"
      ]
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/history",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "fields",
     "order_dir"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_story_link",
   "name": "Get story link",
   "description": "Get Story Link returns information about a chosen Story Link.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-link-public-id": {
      "type": "integer",
      "description": "The ID of the Story Link."
     }
    },
    "required": [
     "story-link-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/story-links/{story-link-public-id}",
    "path": [
     "workspace-slug",
     "story-link-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_team",
   "name": "Get team",
   "description": "Returns the specified Team.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "team-public-id": {
      "type": "string",
      "description": "The UUID of the Team."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "team-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/teams/{team-public-id}",
    "path": [
     "workspace-slug",
     "team-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_whoami",
   "name": "Get whoami",
   "description": "Returns details about the currently-authenticated member and token authorization.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/whoami",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_workflow",
   "name": "Get workflow",
   "description": "Returns the specified Workflow.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "workflow-public-id": {
      "type": "integer",
      "description": "The ID of the Workflow."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workflow-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/workflows/{workflow-public-id}",
    "path": [
     "workspace-slug",
     "workflow-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.get_workflow_state",
   "name": "Get workflow state",
   "description": "Returns the specified Workflow State.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "workflow-state-public-id": {
      "type": "integer",
      "description": "The ID of the Workflow State."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workflow-state-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/workflow-states/{workflow-state-public-id}",
    "path": [
     "workspace-slug",
     "workflow-state-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_branch_pull_requests",
   "name": "List branch pull requests",
   "description": "List Branch Pull Requests returns a paginated list of all Pull Requests associated with a Branch in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "branch-public-id": {
      "type": "integer",
      "description": "The ID of the Branch."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :include_closed).",
      "enum": [
       "exclude_closed",
       "include_closed"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "branch-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/branches/{branch-public-id}/pull-requests",
    "path": [
     "workspace-slug",
     "branch-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_branch_stories",
   "name": "List branch stories",
   "description": "List Branch Stories returns a paginated list of all Stories associated with a Branch in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "branch-public-id": {
      "type": "integer",
      "description": "The ID of the Branch."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "branch-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/branches/{branch-public-id}/stories",
    "path": [
     "workspace-slug",
     "branch-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_branches",
   "name": "List branches",
   "description": "List Branches returns a paginated list of all Branches in the Workspace that have either been associated with a Story, or used in a Pull Request that was associated with a Story.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_deleted).",
      "enum": [
       "exclude_deleted",
       "include_deleted"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/branches",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_categories",
   "name": "List categories",
   "description": "List Categories returns a paginated list of all Categories in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/categories",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_category_objectives",
   "name": "List category objectives",
   "description": "List Category Objectives returns a paginated list of all Objectives associated with a Category in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "category-public-id": {
      "type": "integer",
      "description": "The ID of the Category."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "category-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/categories/{category-public-id}/objectives",
    "path": [
     "workspace-slug",
     "category-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_collections",
   "name": "List collections",
   "description": "List Collections returns a paginated list of all Collections in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived",
       "favorited"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/collections",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_commit_stories",
   "name": "List commit stories",
   "description": "List Commit Stories returns a paginated list of all Stories associated with a Commit in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "commit-public-id": {
      "type": "integer",
      "description": "The ID of the Commit."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "commit-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/commits/{commit-public-id}/stories",
    "path": [
     "workspace-slug",
     "commit-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_commits",
   "name": "List commits",
   "description": "List Commits returns a paginated list of all Commits in the Workspace that have been associated with Stories.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/commits",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_custom_field_stories",
   "name": "List custom field stories",
   "description": "Returns a paginated list of all Stories that have a value set for the specified Custom Field.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "custom-field-public-id": {
      "type": "string",
      "description": "The unique ID of the Custom Field."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "custom-field-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/custom-fields/{custom-field-public-id}/stories",
    "path": [
     "workspace-slug",
     "custom-field-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_custom_fields",
   "name": "List custom fields",
   "description": "List Custom Fields returns a paginated list of all Custom Fields in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/custom-fields",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_doc_collections",
   "name": "List doc collections",
   "description": "List Doc Collections returns a paginated list of the Collections the Doc belongs to.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "doc-public-id": {
      "type": "string",
      "description": "The ID of the Doc."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived",
       "favorited"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "doc-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/documents/{doc-public-id}/collections",
    "path": [
     "workspace-slug",
     "doc-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_doc_followers",
   "name": "List doc followers",
   "description": "List Doc Followers returns a paginated list of all Members following a Doc in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "doc-public-id": {
      "type": "string",
      "description": "The ID of the Doc."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "doc-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/documents/{doc-public-id}/followers",
    "path": [
     "workspace-slug",
     "doc-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_docs",
   "name": "List docs",
   "description": "List Docs returns a paginated list of all accessible Docs in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/documents",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_entity_templates",
   "name": "List entity templates",
   "description": "List Entity Templates returns a paginated list of all Entity Templates in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/entity-templates",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_comment_comments",
   "name": "List epic comment comments",
   "description": "List Epic Comment Comments returns a paginated list of all threaded reply Comments for an Epic Comment in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "comment-public-id",
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/comments/{comment-public-id}/comments",
    "path": [
     "workspace-slug",
     "epic-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_comment_mentioned_members",
   "name": "List epic comment mentioned members",
   "description": "List Epic Comment Mentioned Members returns a paginated list of all Members mentioned in an Epic Comment in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "comment-public-id",
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/comments/{comment-public-id}/mentioned-members",
    "path": [
     "workspace-slug",
     "epic-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_comment_mentioned_teams",
   "name": "List epic comment mentioned teams",
   "description": "List Epic Comment Mentioned Teams returns a paginated list of all Teams mentioned in an Epic Comment in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "comment-public-id",
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/comments/{comment-public-id}/mentioned-teams",
    "path": [
     "workspace-slug",
     "epic-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_comments",
   "name": "List epic comments",
   "description": "List Epic Comments returns a paginated list of all Comments for an Epic in the Workspace. Note that deleted comments will also be returned, but will have minimal information. The deleted comments are included as they may be parents to other comments.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "ids": {
      "type": "string",
      "description": "A comma-separated list of ids. If included, only those entities will be considered for listing."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/comments",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "ids",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_followers",
   "name": "List epic followers",
   "description": "List Epic Followers returns a paginated list of all Members who are following an Epic in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/followers",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_healths",
   "name": "List epic healths",
   "description": "Returns a paginated list of Health updates for the specified Epic.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/epic-healths",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_labels",
   "name": "List epic labels",
   "description": "List Epic Labels returns a paginated list of all Labels for an Epic in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/labels",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_mentioned_members",
   "name": "List epic mentioned members",
   "description": "List Epic Mentioned Members returns a paginated list of all Members mentioned in an Epic in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/mentioned-members",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_mentioned_teams",
   "name": "List epic mentioned teams",
   "description": "List Epic Mentioned Teams returns a paginated list of all Teams mentioned in an Epic in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/mentioned-teams",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_objectives",
   "name": "List epic objectives",
   "description": "List Epic Objectives returns a paginated list of all Objectives associated with an Epic in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/objectives",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_owners",
   "name": "List epic owners",
   "description": "List Epic Owners returns a paginated list of all Members who own an Epic in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/owners",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_stories",
   "name": "List epic stories",
   "description": "List Epic Stories returns a paginated list of all Stories associated with an Epic in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/stories",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_teams",
   "name": "List epic teams",
   "description": "List Epic Teams returns a paginated list of all Teams associated with an Epic in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/teams",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epic_workflow_states",
   "name": "List epic workflow states",
   "description": "Returns a paginated list of all Epic States in the Workspace's Epic Workflow.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "position"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epic-workflow/states",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_epics",
   "name": "List epics",
   "description": "List Epics returns a paginated list of all Epics in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_file_mentioned_members",
   "name": "List file mentioned members",
   "description": "List File Mentioned Members returns a paginated list of all Members mentioned in a File in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "file-public-id": {
      "type": "integer",
      "description": "The ID of the File."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "file-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/files/{file-public-id}/mentioned-members",
    "path": [
     "workspace-slug",
     "file-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_file_mentioned_teams",
   "name": "List file mentioned teams",
   "description": "List File Mentioned Teams returns a paginated list of all Teams mentioned in a File in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "file-public-id": {
      "type": "integer",
      "description": "The ID of the File."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "file-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/files/{file-public-id}/mentioned-teams",
    "path": [
     "workspace-slug",
     "file-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_file_stories",
   "name": "List file stories",
   "description": "List File Stories returns a paginated list of all Stories associated with a File in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "file-public-id": {
      "type": "integer",
      "description": "The ID of the File."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "file-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/files/{file-public-id}/stories",
    "path": [
     "workspace-slug",
     "file-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_files",
   "name": "List files",
   "description": "List Files returns a paginated list of all Files in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/files",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_iteration_associated_teams",
   "name": "List iteration associated teams",
   "description": "List Iteration Associated Teams returns a paginated list of all Teams that own Stories in an Iteration but are not directly associated with the Iteration.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "iteration-public-id": {
      "type": "integer",
      "description": "The unique ID of the Iteration."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "iteration-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/{iteration-public-id}/associated-teams",
    "path": [
     "workspace-slug",
     "iteration-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_iteration_followers",
   "name": "List iteration followers",
   "description": "List Iteration Followers returns a paginated list of all Members who are following an Iteration in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "iteration-public-id": {
      "type": "integer",
      "description": "The unique ID of the Iteration."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "iteration-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/{iteration-public-id}/followers",
    "path": [
     "workspace-slug",
     "iteration-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_iteration_labels",
   "name": "List iteration labels",
   "description": "List Iteration Labels returns a paginated list of all Labels for an Iteration in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "iteration-public-id": {
      "type": "integer",
      "description": "The unique ID of the Iteration."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "iteration-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/{iteration-public-id}/labels",
    "path": [
     "workspace-slug",
     "iteration-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_iteration_mentioned_members",
   "name": "List iteration mentioned members",
   "description": "List Iteration Mentioned Members returns a paginated list of all Members mentioned in an Iteration in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "iteration-public-id": {
      "type": "integer",
      "description": "The unique ID of the Iteration."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "iteration-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/{iteration-public-id}/mentioned-members",
    "path": [
     "workspace-slug",
     "iteration-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_iteration_mentioned_teams",
   "name": "List iteration mentioned teams",
   "description": "List Iteration Mentioned Teams returns a paginated list of all Teams mentioned in an Iteration in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "iteration-public-id": {
      "type": "integer",
      "description": "The unique ID of the Iteration."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "iteration-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/{iteration-public-id}/mentioned-teams",
    "path": [
     "workspace-slug",
     "iteration-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_iteration_stories",
   "name": "List iteration stories",
   "description": "List Iteration Stories returns a paginated list of all Stories in an Iteration in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "iteration-public-id": {
      "type": "integer",
      "description": "The unique ID of the Iteration."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "iteration-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/{iteration-public-id}/stories",
    "path": [
     "workspace-slug",
     "iteration-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_iteration_teams",
   "name": "List iteration teams",
   "description": "List Iteration Teams returns a paginated list of all Teams directly associated with an Iteration in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "iteration-public-id": {
      "type": "integer",
      "description": "The unique ID of the Iteration."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "iteration-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/{iteration-public-id}/teams",
    "path": [
     "workspace-slug",
     "iteration-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_iterations",
   "name": "List iterations",
   "description": "List Iterations returns a paginated list of all Iterations in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "start_date"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_key_results",
   "name": "List key results",
   "description": "Returns a paginated list of all Key Results in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/key-results",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_label_epics",
   "name": "List label epics",
   "description": "List Label Epics returns a paginated list of all Epics that have a given Label.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "label-public-id": {
      "type": "integer",
      "description": "The ID of the Label."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "label-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/labels/{label-public-id}/epics",
    "path": [
     "workspace-slug",
     "label-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_label_stories",
   "name": "List label stories",
   "description": "List Label Stories returns a paginated list of all Stories that have a given Label.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "label-public-id": {
      "type": "integer",
      "description": "The ID of the Label."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "label-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/labels/{label-public-id}/stories",
    "path": [
     "workspace-slug",
     "label-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_labels",
   "name": "List labels",
   "description": "List Labels returns a paginated list of all Labels in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/labels",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_linked_file_mentioned_members",
   "name": "List linked file mentioned members",
   "description": "List Linked File Mentioned Members returns a paginated list of all Members mentioned in a Linked File in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "linked-file-public-id": {
      "type": "integer",
      "description": "The ID of the Linked File."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "linked-file-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/linked-files/{linked-file-public-id}/mentioned-members",
    "path": [
     "workspace-slug",
     "linked-file-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_linked_file_mentioned_teams",
   "name": "List linked file mentioned teams",
   "description": "List Linked File Mentioned Teams returns a paginated list of all Teams mentioned in a Linked File in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "linked-file-public-id": {
      "type": "integer",
      "description": "The ID of the Linked File."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "linked-file-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/linked-files/{linked-file-public-id}/mentioned-teams",
    "path": [
     "workspace-slug",
     "linked-file-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_linked_file_stories",
   "name": "List linked file stories",
   "description": "List Linked File Stories returns a paginated list of all Stories associated with a Linked File in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "linked-file-public-id": {
      "type": "integer",
      "description": "The ID of the Linked File."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "linked-file-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/linked-files/{linked-file-public-id}/stories",
    "path": [
     "workspace-slug",
     "linked-file-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_linked_files",
   "name": "List linked files",
   "description": "List Linked Files returns a paginated list of all Linked Files in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/linked-files",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_member_stories",
   "name": "List member stories",
   "description": "Returns a paginated list of all Stories owned by the specified Member.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "member-public-id": {
      "type": "string",
      "description": "The unique ID of the Member."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "member-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/members/{member-public-id}/stories",
    "path": [
     "workspace-slug",
     "member-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_member_teams",
   "name": "List member teams",
   "description": "Returns a paginated list of all Teams the Member belongs to.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "member-public-id": {
      "type": "string",
      "description": "The unique ID of the Member."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "member-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/members/{member-public-id}/teams",
    "path": [
     "workspace-slug",
     "member-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_members",
   "name": "List members",
   "description": "List Members returns a paginated list of all Members in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/members",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_objective_categories",
   "name": "List objective categories",
   "description": "List Objective Categories returns a paginated list of all Categories associated with an Objective in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "objective-public-id": {
      "type": "integer",
      "description": "The ID of the Objective."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "objective-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/{objective-public-id}/categories",
    "path": [
     "workspace-slug",
     "objective-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_objective_epics",
   "name": "List objective epics",
   "description": "List Objective Epics returns a paginated list of all Epics associated with an Objective in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "objective-public-id": {
      "type": "integer",
      "description": "The ID of the Objective."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "objective-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/{objective-public-id}/epics",
    "path": [
     "workspace-slug",
     "objective-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_objective_healths",
   "name": "List objective healths",
   "description": "Returns a paginated list of Health updates for the specified Objective.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "objective-public-id": {
      "type": "integer",
      "description": "The ID of the Objective."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "objective-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/{objective-public-id}/objective-healths",
    "path": [
     "workspace-slug",
     "objective-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_objective_key_results",
   "name": "List objective key results",
   "description": "List Objective Key Results returns a paginated list of all Key Results associated with an Objective in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "objective-public-id": {
      "type": "integer",
      "description": "The ID of the Objective."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "objective-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/{objective-public-id}/key-results",
    "path": [
     "workspace-slug",
     "objective-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_objective_owners",
   "name": "List objective owners",
   "description": "List Objective Owners returns a paginated list of all Members who own an Objective in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "objective-public-id": {
      "type": "integer",
      "description": "The ID of the Objective."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "objective-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/{objective-public-id}/owners",
    "path": [
     "workspace-slug",
     "objective-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_objective_teams",
   "name": "List objective teams",
   "description": "List Objective Teams returns a paginated list of all Teams associated with an Objective in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "objective-public-id": {
      "type": "integer",
      "description": "The ID of the Objective."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "objective-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/{objective-public-id}/teams",
    "path": [
     "workspace-slug",
     "objective-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_objectives",
   "name": "List objectives",
   "description": "List Objectives returns a paginated list of all Objectives in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_pull_request_stories",
   "name": "List pull request stories",
   "description": "List Pull Request Stories returns a paginated list of all Stories associated with a Pull Request in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "pull-request-public-id": {
      "type": "integer",
      "description": "The ID of the Pull Request."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "pull-request-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/pull-requests/{pull-request-public-id}/stories",
    "path": [
     "workspace-slug",
     "pull-request-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_pull_requests",
   "name": "List pull requests",
   "description": "List Pull Requests returns a paginated list of all Pull Requests in the Workspace that have been associated with a Story.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :include_closed).",
      "enum": [
       "exclude_closed",
       "include_closed"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/pull-requests",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_repositories",
   "name": "List repositories",
   "description": "List Repositories returns a paginated list of all connected Repositories in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/repositories",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_repository_branches",
   "name": "List repository branches",
   "description": "List Repository Branches returns a paginated list of all Branches associated with a Repository in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "repository-public-id": {
      "type": "integer",
      "description": "The ID of the Repository."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_deleted).",
      "enum": [
       "exclude_deleted",
       "include_deleted"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "repository-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/repositories/{repository-public-id}/branches",
    "path": [
     "workspace-slug",
     "repository-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_repository_commits",
   "name": "List repository commits",
   "description": "List Repository Commits returns a paginated list of all Commits associated with a Repository in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "repository-public-id": {
      "type": "integer",
      "description": "The ID of the Repository."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "repository-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/repositories/{repository-public-id}/commits",
    "path": [
     "workspace-slug",
     "repository-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_repository_pull_requests",
   "name": "List repository pull requests",
   "description": "List Repository Pull Requests returns a paginated list of all Pull Requests associated with a Repository in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "repository-public-id": {
      "type": "integer",
      "description": "The ID of the Repository."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :include_closed).",
      "enum": [
       "exclude_closed",
       "include_closed"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "repository-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/repositories/{repository-public-id}/pull-requests",
    "path": [
     "workspace-slug",
     "repository-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_stories",
   "name": "List stories",
   "description": "List Stories returns a paginated list of all Stories in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "ids": {
      "type": "string",
      "description": "A comma-separated list of ids. If included, only those entities will be considered for listing."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "ids",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_branches",
   "name": "List story branches",
   "description": "List Story Branches returns a paginated list of all Branches for a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_deleted).",
      "enum": [
       "exclude_deleted",
       "include_deleted"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/branches",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_checklist_item_mentioned_members",
   "name": "List story checklist item mentioned members",
   "description": "List Story Checklist Item Mentioned Members returns a paginated list of all Members mentioned in a Story Checklist Item in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "checklist-item-public-id": {
      "type": "integer",
      "description": "The ID of the Checklist Item."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "checklist-item-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/checklist-items/{checklist-item-public-id}/mentioned-members",
    "path": [
     "workspace-slug",
     "story-public-id",
     "checklist-item-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_checklist_item_mentioned_teams",
   "name": "List story checklist item mentioned teams",
   "description": "List Story Checklist Item Mentioned Teams returns a paginated list of all Teams mentioned in a Story Checklist Item in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "checklist-item-public-id": {
      "type": "integer",
      "description": "The ID of the Checklist Item."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "checklist-item-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/checklist-items/{checklist-item-public-id}/mentioned-teams",
    "path": [
     "workspace-slug",
     "story-public-id",
     "checklist-item-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_checklist_items",
   "name": "List story checklist items",
   "description": "List Story Checklist Items returns a paginated list of all Checklist Items for a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "ids": {
      "type": "string",
      "description": "A comma-separated list of ids. If included, only those entities will be considered for listing."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "position"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/checklist-items",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "ids",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_comment_comments",
   "name": "List story comment comments",
   "description": "List Story Comment Comments returns a paginated list of all threaded reply Comments for a Story Comment in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "position"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "comment-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/{comment-public-id}/comments",
    "path": [
     "workspace-slug",
     "story-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_comment_mentioned_members",
   "name": "List story comment mentioned members",
   "description": "List Story Comment Mentioned Members returns a paginated list of all Members mentioned in a Story Comment in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "comment-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/{comment-public-id}/mentioned-members",
    "path": [
     "workspace-slug",
     "story-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_comment_mentioned_teams",
   "name": "List story comment mentioned teams",
   "description": "List Story Comment Mentioned Teams returns a paginated list of all Teams mentioned in a Story Comment in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "comment-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/{comment-public-id}/mentioned-teams",
    "path": [
     "workspace-slug",
     "story-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_comment_reactions",
   "name": "List story comment reactions",
   "description": "List Story Comment Reactions returns a paginated list of all Reactions to a Story Comment in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "created_at"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "comment-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/{comment-public-id}/reactions",
    "path": [
     "workspace-slug",
     "story-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_comments",
   "name": "List story comments",
   "description": "List Story Comments returns a paginated list of all Comments for a Story in the Workspace. Note that deleted comments will also be returned, but will have minimal information. The deleted comments are included as they may be parents to other comments.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "ids": {
      "type": "string",
      "description": "A comma-separated list of ids. If included, only those entities will be considered for listing."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "position"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "fields",
     "order_dir",
     "ids",
     "limit",
     "order_by",
     "filter",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_commits",
   "name": "List story commits",
   "description": "List Story Commits returns a paginated list of all Commits associated with a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/commits",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_custom_field_values",
   "name": "List story custom field values",
   "description": "List Story Custom Field Values returns a paginated list of all Custom Field Values for a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "field_name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/custom-field-values",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_external_links",
   "name": "List story external links",
   "description": "List Story External Links returns a paginated list of all External Links for a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "value"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/external-links",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_files",
   "name": "List story files",
   "description": "List Story Files returns a paginated list of all Files attached to a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/files",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_followers",
   "name": "List story followers",
   "description": "List Story Followers returns a paginated list of all Members who are following a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/followers",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_iterations",
   "name": "List story iterations",
   "description": "List Story Iterations returns a paginated list of all Iterations associated with a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "start_date"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/iterations",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_labels",
   "name": "List story labels",
   "description": "List Story Labels returns a paginated list of all Labels for a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/labels",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_linked_files",
   "name": "List story linked files",
   "description": "List Story Linked Files returns a paginated list of all Linked Files attached to a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/linked-files",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_links",
   "name": "List story links",
   "description": "List Story Links returns a paginated list of all Story Links in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/story-links",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_mentioned_members",
   "name": "List story mentioned members",
   "description": "List Story Mentioned Members returns a paginated list of all Members mentioned in a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/mentioned-members",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_mentioned_teams",
   "name": "List story mentioned teams",
   "description": "List Story Mentioned Teams returns a paginated list of all Teams mentioned in a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/mentioned-teams",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_owners",
   "name": "List story owners",
   "description": "List Story Owners returns a paginated list of all Members who own a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/owners",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_pull_requests",
   "name": "List story pull requests",
   "description": "List Story Pull Requests returns a paginated list of all Pull Requests associated with a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :include_closed).",
      "enum": [
       "exclude_closed",
       "include_closed"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/pull-requests",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_story_links",
   "name": "List story story links",
   "description": "List Story Story Links returns a paginated list of all Story Links associated with a Story in the Workspace. This includes links where the Story is either the subject or the object of the relationship.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/story-links",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_story_sub_task_stories",
   "name": "List story sub task stories",
   "description": "List Story Sub-Task Stories returns a paginated list of all Sub-Task Stories for a Story in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/sub-task-stories",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_team_members",
   "name": "List team members",
   "description": "Returns a paginated list of all Members for the specified Team.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "team-public-id": {
      "type": "string",
      "description": "The UUID of the Team."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "team-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/teams/{team-public-id}/members",
    "path": [
     "workspace-slug",
     "team-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_team_stories",
   "name": "List team stories",
   "description": "Returns a paginated list of all Stories assigned to the specified Team.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "team-public-id": {
      "type": "string",
      "description": "The UUID of the Team."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "team-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/teams/{team-public-id}/stories",
    "path": [
     "workspace-slug",
     "team-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_team_workflows",
   "name": "List team workflows",
   "description": "Returns a paginated list of all Workflows for the specified Team.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "team-public-id": {
      "type": "string",
      "description": "The UUID of the Team."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "team-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/teams/{team-public-id}/workflows",
    "path": [
     "workspace-slug",
     "team-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_teams",
   "name": "List teams",
   "description": "Returns a paginated list of all Teams in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/teams",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_workflow_state_stories",
   "name": "List workflow state stories",
   "description": "Returns a paginated list of all Stories in the specified Workflow State.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "workflow-state-public-id": {
      "type": "integer",
      "description": "The ID of the Workflow State."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workflow-state-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/workflow-states/{workflow-state-public-id}/stories",
    "path": [
     "workspace-slug",
     "workflow-state-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_workflow_states",
   "name": "List workflow states",
   "description": "Returns a paginated list of all Workflow States in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "position"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/workflow-states",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_workflow_states_within_aworkflow",
   "name": "List workflow states within a workflow",
   "description": "Returns a paginated list of all Workflow States within the specified Workflow.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "workflow-public-id": {
      "type": "integer",
      "description": "The ID of the Workflow."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "position"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workflow-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/workflows/{workflow-public-id}/states",
    "path": [
     "workspace-slug",
     "workflow-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_workflow_stories",
   "name": "List workflow stories",
   "description": "Returns a paginated list of all Stories associated with the specified Workflow.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "workflow-public-id": {
      "type": "integer",
      "description": "The ID of the Workflow."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "updated_at",
       "position",
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workflow-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/workflows/{workflow-public-id}/stories",
    "path": [
     "workspace-slug",
     "workflow-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_workflow_teams",
   "name": "List workflow teams",
   "description": "Returns a paginated list of all Teams associated with the specified Workflow.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "workflow-public-id": {
      "type": "integer",
      "description": "The ID of the Workflow."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name",
       "updated_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_archived).",
      "enum": [
       "exclude_archived",
       "include_archived"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workflow-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/workflows/{workflow-public-id}/teams",
    "path": [
     "workspace-slug",
     "workflow-public-id"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_workflows",
   "name": "List workflows",
   "description": "Returns a paginated list of all Workflows in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "name"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :all).",
      "enum": [
       "all"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/workflows",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.list_workspace_tokens",
   "name": "List workspace tokens",
   "description": "List Workspace Tokens returns a paginated list of all API Tokens in the Workspace. Requires admin role.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "order_by": {
      "type": "string",
      "description": "Order the results by this property and direction.",
      "enum": [
       "created_at"
      ]
     },
     "order_dir": {
      "type": "string",
      "description": "Order order_by in this direction. If order_by not provided, the default order property for this entity type will be used.",
      "enum": [
       "desc",
       "asc"
      ]
     },
     "filter": {
      "type": "string",
      "description": "Filter options (default: :exclude_disabled).",
      "enum": [
       "exclude_disabled",
       "include_disabled"
      ]
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/admin/tokens",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "order_by",
     "order_dir",
     "filter",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.remove_doc_from_collection",
   "name": "Remove doc from collection",
   "description": "Remove Doc from Collection removes the Doc from the chosen Collection, preserving its other Collection memberships. Requires write access to the Doc.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "doc-public-id": {
      "type": "string",
      "description": "The ID of the Doc."
     },
     "collection-public-id": {
      "type": "string",
      "description": "The ID of the Collection."
     }
    },
    "required": [
     "collection-public-id",
     "doc-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/documents/{doc-public-id}/collections/{collection-public-id}",
    "path": [
     "workspace-slug",
     "doc-public-id",
     "collection-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.search",
   "name": "Search",
   "description": "Search lets you search across Epics, Stories, Iterations, Objectives, and Docs with a single query, returning one combined, paginated list of results.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "query": {
      "type": "string",
      "description": "The search query. Required if `cursor` is not sent. See our help center article on [search operators](https://help.shortcut.com/hc/en-us/articles/360000046646-Search-Operators)."
     },
     "entity_types": {
      "type": "string",
      "description": "A comma-separated list of entity types to search. Defaults to all supported types. Supported: epic, story, iteration, objective, doc. Docs are only included when the docs feature is enabled for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/search",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "query",
     "entity_types"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.search_docs",
   "name": "Search docs",
   "description": "Search Docs lets you search Docs by title, with optional archived/created_by_me/followed_by_me filters.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "query": {
      "type": "string",
      "description": "Search text to match against Doc titles. Supports fuzzy matching. Required if `cursor` is not sent."
     },
     "archived": {
      "type": "boolean",
      "description": "When true, find archived Docs. When false, find non-archived Docs."
     },
     "created_by_me": {
      "type": "boolean",
      "description": "When true, find Docs created by the current user. When false, find Docs NOT created by the current user."
     },
     "followed_by_me": {
      "type": "boolean",
      "description": "When true, find Docs that the current user is following. When false, find Docs NOT followed by the current user."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/documents/search",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "query",
     "archived",
     "created_by_me",
     "followed_by_me",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.search_epics",
   "name": "Search epics",
   "description": "Search Epics lets you search Epics based on a query.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "query": {
      "type": "string",
      "description": "The search query. Required if `cursor` is not sent. See our help center article on [search operators](https://help.shortcut.com/hc/en-us/articles/360000046646-Search-Operators)."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/search",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "query",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.search_iterations",
   "name": "Search iterations",
   "description": "Search Iterations lets you search Iterations based on a query.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "query": {
      "type": "string",
      "description": "The search query. Required if `cursor` is not sent. See our help center article on [search operators](https://help.shortcut.com/hc/en-us/articles/360000046646-Search-Operators)."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/search",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "query",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.search_objectives",
   "name": "Search objectives",
   "description": "Search Objectives lets you search Objectives based on a query.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "query": {
      "type": "string",
      "description": "The search query. Required if `cursor` is not sent. See our help center article on [search operators](https://help.shortcut.com/hc/en-us/articles/360000046646-Search-Operators)."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/search",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "query",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.search_stories",
   "name": "Search stories",
   "description": "Search Stories lets you search Stories based on a query.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "limit": {
      "type": "integer",
      "description": "The number of results to include in a page. Can be between 1 and 100. Defaults to 10."
     },
     "cursor": {
      "type": "string",
      "description": "The cursor indicating where we are in a paged response. If sent, it (and an optional `fields` parameter) must be the only parameters."
     },
     "query": {
      "type": "string",
      "description": "The search query. Required if `cursor` is not sent. See our help center article on [search operators](https://help.shortcut.com/hc/en-us/articles/360000046646-Search-Operators)."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/search",
    "path": [
     "workspace-slug"
    ],
    "query": [
     "limit",
     "cursor",
     "query",
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.unfavorite_collection",
   "name": "Unfavorite collection",
   "description": "Unfavorite Collection removes the Collection from the current Member's favorites.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "collection-public-id": {
      "type": "string",
      "description": "The ID of the Collection."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "collection-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/collections/{collection-public-id}/favorite",
    "path": [
     "workspace-slug",
     "collection-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_category",
   "name": "Update category",
   "description": "Update Category allows you to modify a Category in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "category-public-id": {
      "type": "integer",
      "description": "The ID of the Category."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "category-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/categories/{category-public-id}",
    "path": [
     "workspace-slug",
     "category-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_custom_field",
   "name": "Update custom field",
   "description": "Update Custom Field allows you to modify a Custom Field definition in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "custom-field-public-id": {
      "type": "string",
      "description": "The unique ID of the Custom Field."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "custom-field-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/custom-fields/{custom-field-public-id}",
    "path": [
     "workspace-slug",
     "custom-field-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_entity_template",
   "name": "Update entity template",
   "description": "Update Entity Template updates an existing Entity Template in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "entity-template-public-id": {
      "type": "string",
      "description": "The unique ID of the entity template."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "entity-template-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/entity-templates/{entity-template-public-id}",
    "path": [
     "workspace-slug",
     "entity-template-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_epic",
   "name": "Update epic",
   "description": "Update Epic is used to modify an Epic in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}",
    "path": [
     "workspace-slug",
     "epic-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_epic_comment",
   "name": "Update epic comment",
   "description": "Update Epic Comment is used to modify an existing Epic Comment.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "epic-public-id": {
      "type": "integer",
      "description": "The ID of the Epic."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "comment-public-id",
     "epic-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/epics/{epic-public-id}/comments/{comment-public-id}",
    "path": [
     "workspace-slug",
     "epic-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_iteration",
   "name": "Update iteration",
   "description": "Update Iteration is used to modify an Iteration in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "iteration-public-id": {
      "type": "integer",
      "description": "The unique ID of the Iteration."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "iteration-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/iterations/{iteration-public-id}",
    "path": [
     "workspace-slug",
     "iteration-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_key_result",
   "name": "Update key result",
   "description": "Updates the specified Key Result.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "key-result-public-id": {
      "type": "string",
      "description": "The unique ID of the Key Result."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "key-result-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/key-results/{key-result-public-id}",
    "path": [
     "workspace-slug",
     "key-result-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_label",
   "name": "Update label",
   "description": "Update Label allows you to modify a Label in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "label-public-id": {
      "type": "integer",
      "description": "The ID of the Label."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "label-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/labels/{label-public-id}",
    "path": [
     "workspace-slug",
     "label-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_linked_file",
   "name": "Update linked file",
   "description": "Update Linked File is used to update an existing Linked File in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "linked-file-public-id": {
      "type": "integer",
      "description": "The ID of the Linked File."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "linked-file-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/linked-files/{linked-file-public-id}",
    "path": [
     "workspace-slug",
     "linked-file-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_member",
   "name": "Update member",
   "description": "Update Member modifies a Member's role or disabled status in the Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "member-public-id": {
      "type": "string",
      "description": "The unique ID of the Member."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "member-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/members/{member-public-id}",
    "path": [
     "workspace-slug",
     "member-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_objective",
   "name": "Update objective",
   "description": "Update Objective is used to modify an Objective in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "objective-public-id": {
      "type": "integer",
      "description": "The ID of the Objective."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "objective-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/objectives/{objective-public-id}",
    "path": [
     "workspace-slug",
     "objective-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_story",
   "name": "Update story",
   "description": "Update Story is used to modify a Story in your Shortcut Workspace.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}",
    "path": [
     "workspace-slug",
     "story-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_story_checklist_item",
   "name": "Update story checklist item",
   "description": "Update Story Checklist Item is used to modify a Checklist Item on a Story.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "checklist-item-public-id": {
      "type": "integer",
      "description": "The ID of the Checklist Item."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "checklist-item-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/checklist-items/{checklist-item-public-id}",
    "path": [
     "workspace-slug",
     "story-public-id",
     "checklist-item-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_story_comment",
   "name": "Update story comment",
   "description": "Update Story Comment is used to modify a Story Comment.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-public-id": {
      "type": "integer",
      "description": "The ID of the Story."
     },
     "comment-public-id": {
      "type": "integer",
      "description": "The ID of the Comment."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "comment-public-id",
     "story-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/stories/{story-public-id}/comments/{comment-public-id}",
    "path": [
     "workspace-slug",
     "story-public-id",
     "comment-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_story_link",
   "name": "Update story link",
   "description": "Update Story Link is used to modify the stories or verb of an existing Story Link.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "story-link-public-id": {
      "type": "integer",
      "description": "The ID of the Story Link."
     }
    },
    "required": [
     "story-link-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/story-links/{story-link-public-id}",
    "path": [
     "workspace-slug",
     "story-link-public-id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.update_team",
   "name": "Update team",
   "description": "Updates the specified Team.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     },
     "team-public-id": {
      "type": "string",
      "description": "The UUID of the Team."
     },
     "fields": {
      "type": "string",
      "description": "Fields to include in the response, as a comma-separated string. If provided, only these fields will be included for each entity in the response. If not present, all fields will be returned."
     }
    },
    "required": [
     "team-public-id",
     "workspace-slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/teams/{team-public-id}",
    "path": [
     "workspace-slug",
     "team-public-id"
    ],
    "query": [
     "fields"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "shortcut.upload_files",
   "name": "Upload files",
   "description": "Upload Files uploads one or more files and optionally associates them with a Story. Use the multipart/form-data content-type to upload. Each `file` key should contain a separate file.",
   "input": {
    "type": "object",
    "properties": {
     "workspace-slug": {
      "type": "string",
      "description": "The slug for the Workspace."
     }
    },
    "required": [
     "workspace-slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.app.shortcut.com/api/v4/{workspace-slug}/files",
    "path": [
     "workspace-slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  }
 ]
}
