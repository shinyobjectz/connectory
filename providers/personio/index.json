{
 "_": "GENERATED \u2014 what an authoring tool needs to FIND an operation. The calling table is the .lua beside it.",
 "provider": "personio",
 "source": "https://api.apis.guru/v2/specs/personio.de/personnel/1.0/openapi.json",
 "operations": [
  {
   "slug": "personio.delete_company_attendances_id",
   "name": "Personio delete company attendances id",
   "description": "This endpoint is responsible for deleting attendance data for the company employees.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": "ID of the attendance period to delete"
     }
    },
    "required": [
     "id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.personio.de/v1/company/attendances/{id}",
    "path": [
     "id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.delete_company_time_offs_id",
   "name": "Personio delete company time offs id",
   "description": "This endpoint is responsible for deleting absence period data for the company employees.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": "ID of the absence period to delete"
     }
    },
    "required": [
     "id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.personio.de/v1/company/time-offs/{id}",
    "path": [
     "id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.get_company_attendances",
   "name": "Personio get company attendances",
   "description": "This endpoint is responsible for fetching attendance data for the company employees. It is possible to paginate results, filter by period, the date and/or time it was updated, and/or specific employees. The result will contain a list of attendance periods, structured as defined here.",
   "input": {
    "type": "object",
    "properties": {
     "start_date": {
      "type": "string",
      "description": "First day of the period to be queried. It is inclusive, so the day specified as start_date will also be considered on the results"
     },
     "end_date": {
      "type": "string",
      "description": "Last day of the period to be queried. It is inclusive, so the day specified as end_date will also be considered on the results."
     },
     "updated_from": {
      "type": "string",
      "description": "Datetime from when the queried periods have been updated. Same format as updated_at. It is inclusive, so the day specified as updated_from will also be considered on the results. Can be just the date, or the date and the time, with or without the timezone."
     },
     "updated_to": {
      "type": "string",
      "description": "Datetime until when the queried periods have been updated. Same format as updated_at. It is inclusive, so the day specified as updated_to will also be considered on the results. Can be just the date, or the date and the time, with or without the timezone."
     },
     "employees": {
      "type": "array",
      "description": "A list of Personio employee identifiers to filter the results. Only those employees specified here will be returned."
     },
     "limit": {
      "type": "integer",
      "description": "Pagination attribute to limit how many attendances will be returned per page"
     },
     "offset": {
      "type": "integer",
      "description": "Pagination attribute to identify which page you are requesting, by the form of telling an offset from the first record that would be returned."
     }
    },
    "required": [
     "end_date",
     "start_date"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.personio.de/v1/company/attendances",
    "path": [],
    "query": [
     "start_date",
     "end_date",
     "updated_from",
     "updated_to",
     "employees",
     "limit",
     "offset"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.get_company_employees",
   "name": "Personio get company employees",
   "description": "List Employees",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.personio.de/v1/company/employees",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.get_company_employees_employee_id",
   "name": "Personio get company employees employee id",
   "description": "Show employee by ID",
   "input": {
    "type": "object",
    "properties": {
     "employee_id": {
      "type": "integer",
      "description": "Numeric `id` of the employee"
     }
    },
    "required": [
     "employee_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.personio.de/v1/company/employees/{employee_id}",
    "path": [
     "employee_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.get_company_employees_employee_id_profile_picture_width",
   "name": "Personio get company employees employee id profile picture width",
   "description": "Show employee profile picture",
   "input": {
    "type": "object",
    "properties": {
     "employee_id": {
      "type": "integer",
      "description": "Numeric `id` of the employee"
     },
     "width": {
      "type": "integer",
      "description": "Width of the image. Default 75x75"
     }
    },
    "required": [
     "employee_id",
     "width"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.personio.de/v1/company/employees/{employee_id}/profile-picture/{width}",
    "path": [
     "employee_id",
     "width"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.get_company_time_off_types",
   "name": "Personio get company time off types",
   "description": "Provides a list of available time-off types, for example 'Paid vacation', 'Parental leave' or 'Home office'",
   "input": {
    "type": "object",
    "properties": {
     "limit": {
      "type": "integer",
      "description": "Pagination attribute to limit how many records will be returned per page"
     },
     "offset": {
      "type": "integer",
      "description": "Pagination attribute to identify which page you are requesting, by the form of telling an offset from the first record that would be returned."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.personio.de/v1/company/time-off-types",
    "path": [],
    "query": [
     "limit",
     "offset"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.get_company_time_offs",
   "name": "Personio get company time offs",
   "description": "This endpoint is responsible for fetching absence data for the company employees. It is possible to paginate results, filter by period and/or specific employees. The result will contain a list of absence periods, structured as defined here.",
   "input": {
    "type": "object",
    "properties": {
     "start_date": {
      "type": "string",
      "description": "First day of the period to be queried. It is inclusive, so the day specified as start_date will also be considered on the results"
     },
     "end_date": {
      "type": "string",
      "description": "Last day of the period to be queried. It is inclusive, so the day specified as end_date will also be considered on the results."
     },
     "updated_from": {
      "type": "string",
      "description": "Datetime from when the queried periods have been updated. It is inclusive, so the day specified as updated_from will also be considered on the results."
     },
     "updated_to": {
      "type": "string",
      "description": "Datetime until when the queried periods have been updated. It is inclusive, so the day specified as updated_to will also be considered on the results."
     },
     "employees": {
      "type": "array",
      "description": "A list of Personio employee identifiers to filter the results. Only those employees specified here will be returned."
     },
     "limit": {
      "type": "integer",
      "description": "Pagination attribute to limit how many attendances will be returned per page"
     },
     "offset": {
      "type": "integer",
      "description": "Pagination attribute to identify which page you are requesting, by the form of telling an offset from the first record that would be returned."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.personio.de/v1/company/time-offs",
    "path": [],
    "query": [
     "start_date",
     "end_date",
     "updated_from",
     "updated_to",
     "employees",
     "limit",
     "offset"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.get_company_time_offs_id",
   "name": "Personio get company time offs id",
   "description": "Absence Period",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": "Numeric `id` of the absence period"
     }
    },
    "required": [
     "id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.personio.de/v1/company/time-offs/{id}",
    "path": [
     "id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.patch_company_attendances_id",
   "name": "Personio patch company attendances id",
   "description": "This endpoint is responsible for updating attendance data for the company employees. Attributes are not required and if not specified, the current value will be used. It is not possible to change the employee id.",
   "input": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer",
      "description": "ID of the attendance period to update"
     },
     "break": {
      "type": "integer",
      "description": "Break in minutes"
     },
     "comment": {
      "type": "string",
      "description": "Optional comment"
     },
     "date": {
      "type": "string",
      "description": "Attendance date as YYYY-MM-DD"
     },
     "end_time": {
      "type": "string",
      "description": "End time as HH:MM"
     },
     "start_time": {
      "type": "string",
      "description": "Start time as HH:MM"
     }
    },
    "required": [
     "id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://api.personio.de/v1/company/attendances/{id}",
    "path": [
     "id"
    ],
    "query": [],
    "header": [],
    "body": [
     "break",
     "comment",
     "date",
     "end_time",
     "start_time"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.post_company_attendances",
   "name": "Personio post company attendances",
   "description": "This endpoint is responsible for adding attendance data for the company employees. It is possible to add attendances for one or many employees at the same time. The payload sent on the request should be a list of attendance periods, in the form of an array containing attendance period objects.",
   "input": {
    "type": "object",
    "properties": {
     "attendances": {
      "type": "array",
      "description": ""
     }
    },
    "required": []
   },
   "request": {
    "method": "POST",
    "url": "https://api.personio.de/v1/company/attendances",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "attendances"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.post_company_employees",
   "name": "Create an employee",
   "description": "Creates new employee. Status of the employee will be set to `active` if `hire_date` provided is in past. Otherwise status will be set to `onboarding`. This endpoint will respond with `id` of created employee in case of success.",
   "input": {
    "type": "object",
    "properties": {
     "employee[department]": {
      "type": "string",
      "description": "Employee department"
     },
     "employee[email]": {
      "type": "string",
      "description": "Employee email"
     },
     "employee[first_name]": {
      "type": "string",
      "description": "Employee first name"
     },
     "employee[gender]": {
      "type": "string",
      "description": "Employee gender",
      "enum": [
       "male",
       "female",
       "diverse"
      ]
     },
     "employee[hire_date]": {
      "type": "string",
      "description": "Employee hire date"
     },
     "employee[last_name]": {
      "type": "string",
      "description": "Employee last name"
     },
     "employee[position]": {
      "type": "string",
      "description": "Employee position"
     },
     "employee[weekly_hours]": {
      "type": "number",
      "description": "Employee weekly working hours"
     }
    },
    "required": [
     "employee[email]",
     "employee[first_name]",
     "employee[last_name]"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.personio.de/v1/company/employees",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "employee[department]",
     "employee[email]",
     "employee[first_name]",
     "employee[gender]",
     "employee[hire_date]",
     "employee[last_name]",
     "employee[position]",
     "employee[weekly_hours]"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "personio.post_company_time_offs",
   "name": "Personio post company time offs",
   "description": "This endpoint is responsible for adding absence data for the company employees.",
   "input": {
    "type": "object",
    "properties": {
     "comment": {
      "type": "string",
      "description": "Optional comment"
     },
     "employee_id": {
      "type": "integer",
      "description": "Employee identifier"
     },
     "end_date": {
      "type": "string",
      "description": "Absence end date as YYYY-MM-DD"
     },
     "half_day_end": {
      "type": "boolean",
      "description": "Weather the end date is a half-day off"
     },
     "half_day_start": {
      "type": "boolean",
      "description": "Weather the start date is a half-day off"
     },
     "start_date": {
      "type": "string",
      "description": "Absence start date as YYYY-MM-DD"
     },
     "time_off_type_id": {
      "type": "integer",
      "description": "Time-off type identifier"
     }
    },
    "required": [
     "employee_id",
     "end_date",
     "half_day_end",
     "half_day_start",
     "start_date",
     "time_off_type_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.personio.de/v1/company/time-offs",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "comment",
     "employee_id",
     "end_date",
     "half_day_end",
     "half_day_start",
     "start_date",
     "time_off_type_id"
    ],
    "body_format": "json"
   }
  }
 ]
}
