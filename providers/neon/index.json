{
 "_": "GENERATED \u2014 what an authoring tool needs to FIND an operation. The calling table is the .lua beside it.",
 "provider": "neon",
 "source": "https://neon.tech/openapi.json",
 "operations": [
  {
   "slug": "neon.accept_project_transfer_request",
   "name": "Accept a project transfer request",
   "description": "Accepts a transfer request for the specified project, transferring it to the specified organization or user. If org_id is not passed, the project will be transferred to the current user or organization account.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "request_id": {
      "type": "string",
      "description": "The Neon project transfer request ID"
     },
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID to transfer the project to. If not provided, the project will be transferred to the current user or organization account."
     }
    },
    "required": [
     "project_id",
     "request_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/transfer_requests/{request_id}",
    "path": [
     "project_id",
     "request_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "org_id"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.add_branch_neon_auth_oauth_provider",
   "name": "Add an oauth provider",
   "description": "Adds an OAuth provider configuration to the specified branch's Neon Auth integration. After adding, users can authenticate using the configured provider.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "id": {
      "type": "string",
      "description": "",
      "enum": [
       "google",
       "github",
       "microsoft",
       "vercel"
      ]
     },
     "client_id": {
      "type": "string",
      "description": "The client ID issued by the OAuth provider for your application. Used to identify the application during the OAuth flow."
     },
     "client_secret": {
      "type": "string",
      "description": "OAuth client secret for the provider."
     },
     "microsoft_tenant_id": {
      "type": "string",
      "description": "Tenant ID for the Microsoft OAuth provider. Only relevant when the OAuth provider is Microsoft; omit or leave blank for other providers."
     }
    },
    "required": [
     "branch_id",
     "id",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/oauth_providers",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "id",
     "client_id",
     "client_secret",
     "microsoft_tenant_id"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.add_branch_neon_auth_trusted_domain",
   "name": "Add domain to redirect uri whitelist",
   "description": "Adds a domain to the redirect URI whitelist for the specified branch. Only domains in this list are permitted as redirect targets after authentication.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "domain": {
      "type": "string",
      "description": "URI to add to the redirect URI allowlist for the auth provider."
     },
     "auth_provider": {
      "type": "string",
      "description": "Authentication provider integrated with this Neon Auth configuration. `better_auth` integrates with Better Auth (the current, recommended provider). `stack` integrates with Stack Auth (deprecated). `mock` is a simulated provider for local development and testing only.",
      "enum": [
       "mock",
       "stack",
       "better_auth"
      ]
     }
    },
    "required": [
     "auth_provider",
     "branch_id",
     "domain",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/domains",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "domain",
     "auth_provider"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.add_project_jwks",
   "name": "Add jwks url",
   "description": "Adds a JWKS URL to the specified project for verifying JWTs used as the authentication mechanism. The URL must be a valid HTTPS URL that returns a JSON Web Key Set. The `provider_name` field allows you to specify which authentication provider you're using (e.g., Clerk, Auth0, AWS Cognito). The `branch_id` scopes the JWKS URL to specific branches; if not specified, it applies to all branches. The `",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "jwks_url": {
      "type": "string",
      "description": "URL of the provider's JWKS endpoint used to verify JWTs."
     },
     "provider_name": {
      "type": "string",
      "description": "The name of the authentication provider (e.g., Clerk, Stytch, Auth0)"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID. Returned as `id` from `GET /projects/{project_id}/branches`."
     },
     "jwt_audience": {
      "type": "string",
      "description": "Expected `aud` claim in incoming JWTs. When set, tokens with a different audience are rejected; tokens with no audience are still accepted. Omit to skip audience validation."
     },
     "role_names": {
      "type": "array",
      "description": "Deprecated. The roles the JWKS should be mapped to. By default, the JWKS is mapped to the `authenticator`, `authenticated`, and `anonymous` roles."
     },
     "skip_role_creation": {
      "type": "boolean",
      "description": "Deprecated. Only used with Neon RLS. If true, role creation is skipped."
     }
    },
    "required": [
     "jwks_url",
     "project_id",
     "provider_name"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/jwks",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "jwks_url",
     "provider_name",
     "branch_id",
     "jwt_audience",
     "role_names",
     "skip_role_creation"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.assign_organization_vpcendpoint",
   "name": "Assign or update vpc endpoint",
   "description": "Assigns a VPC endpoint to a Neon organization or updates its existing assignment.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "region_id": {
      "type": "string",
      "description": "The Neon region ID. Azure regions are currently not supported."
     },
     "vpc_endpoint_id": {
      "type": "string",
      "description": "The VPC endpoint ID"
     },
     "label": {
      "type": "string",
      "description": "Human-readable name for the VPC endpoint assignment, used to identify it within the organization."
     }
    },
    "required": [
     "label",
     "org_id",
     "region_id",
     "vpc_endpoint_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/vpc/region/{region_id}/vpc_endpoints/{vpc_endpoint_id}",
    "path": [
     "org_id",
     "region_id",
     "vpc_endpoint_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "label"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.assign_project_vpcendpoint",
   "name": "Set vpc endpoint restriction",
   "description": "Sets or updates a VPC endpoint restriction for a Neon project. When a VPC endpoint restriction is set, the project only accepts connections from the specified VPC. A VPC endpoint can be set as a restriction only after it is assigned to the parent organization of the Neon project.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "vpc_endpoint_id": {
      "type": "string",
      "description": "The VPC endpoint ID"
     },
     "label": {
      "type": "string",
      "description": "Human-readable name for the VPC endpoint assignment, used to identify it within the organization."
     }
    },
    "required": [
     "label",
     "project_id",
     "vpc_endpoint_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/vpc_endpoints/{vpc_endpoint_id}",
    "path": [
     "project_id",
     "vpc_endpoint_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "label"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.count_project_branches",
   "name": "Retrieve number of branches",
   "description": "Retrieves the total number of branches in the specified project. Supports an optional `search` parameter to count branches matching a name filter.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "search": {
      "type": "string",
      "description": "Count branches matching the `name` in search query"
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/count",
    "path": [
     "project_id"
    ],
    "query": [
     "search"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_api_key",
   "name": "Create api key",
   "description": "Creates an API key. The `key_name` is a user-specified name for the key. Returns an `id` and `key`; the `key` is a randomly generated, 64-bit token required to access the Neon API. Store the key securely \u2014 it is only returned once. API keys can also be managed in the Neon Console. See [Manage API keys](https://neon.com/docs/manage/api-keys/).",
   "input": {
    "type": "object",
    "properties": {
     "key_name": {
      "type": "string",
      "description": "A user-specified API key name. This value is required when creating an API key."
     }
    },
    "required": [
     "key_name"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/api_keys",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "key_name"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_branch_neon_auth_new_user",
   "name": "Create new auth user",
   "description": "Creates a new user in the Neon Auth user directory for the specified branch. The user is created in the `neon_auth.users_sync` table and can immediately authenticate using the branch's configured auth providers.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "email": {
      "type": "string",
      "description": "Email address of the new Neon Auth user to create."
     },
     "name": {
      "type": "string",
      "description": "Display name for the new user. Optional. Pair with the required email field when creating a new user."
     }
    },
    "required": [
     "branch_id",
     "email",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/users",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "email",
     "name"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_credential",
   "name": "Issue a scoped credential on the branch",
   "description": "Issues a new scoped service credential anchored to the specified branch. The response carries `api_token` and `s3_secret_access_key` exactly once \u2014 they are not stored server-side. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "name": {
      "type": "string",
      "description": "Free-form customer label for the credential."
     },
     "scopes": {
      "type": "array",
      "description": ""
     },
     "principal_type": {
      "type": "string",
      "description": "Principal type for the credential. Only `user` is customer-managed and accepted here. `function` and `system` credentials are platform-internal (e.g. function-serve auto-mint, presign signer) and are never issued through the customer-facing API.",
      "enum": [
       "user"
      ]
     }
    },
    "required": [
     "branch_id",
     "principal_type",
     "project_id",
     "scopes"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/credentials",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "scopes",
     "principal_type"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_neon_auth",
   "name": "Enable neon auth for the branch",
   "description": "Enables Neon Auth for the specified branch by connecting it to an authentication provider. Creating the integration provisions the `neon_auth` schema in the branch database, which stores user identity data synchronized from the provider.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "auth_provider": {
      "type": "string",
      "description": "Authentication provider integrated with this Neon Auth configuration. `better_auth` integrates with Better Auth (the current, recommended provider). `stack` integrates with Stack Auth (deprecated). `mock` is a simulated provider for local development and testing only.",
      "enum": [
       "mock",
       "stack",
       "better_auth"
      ]
     },
     "database_name": {
      "type": "string",
      "description": "Name of the database to enable Neon Auth on. When omitted, the integration uses the project's default database."
     }
    },
    "required": [
     "auth_provider",
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "auth_provider",
     "database_name"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_neon_auth_provider_sdkkeys",
   "name": "Create auth provider sdk keys",
   "description": "Generates SDK or API Keys for the auth provider. These might be called different things depending on the auth provider you're using, but are generally used for setting up the frontend and backend SDKs.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID. Returned as `id` from `GET /projects`."
     },
     "auth_provider": {
      "type": "string",
      "description": "Authentication provider integrated with this Neon Auth configuration. `better_auth` integrates with Better Auth (the current, recommended provider). `stack` integrates with Stack Auth (deprecated). `mock` is a simulated provider for local development and testing only.",
      "enum": [
       "mock",
       "stack",
       "better_auth"
      ]
     }
    },
    "required": [
     "auth_provider",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/auth/keys",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "project_id",
     "auth_provider"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_org_api_key",
   "name": "Create organization api key",
   "description": "Creates an API key for the specified organization. The `key_name` is a user-specified name for the key. Returns an `id` and `key`; the `key` is a randomly generated, 64-bit token required to access the Neon API. Store the key securely \u2014 it is only returned once. API keys can also be managed in the Neon Console. See [Manage API keys](https://neon.com/docs/manage/api-keys/).",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "key_name": {
      "type": "string",
      "description": "A user-specified API key name. This value is required when creating an API key."
     }
    },
    "required": [
     "key_name",
     "org_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/api_keys",
    "path": [
     "org_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "key_name"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_organization_invitations",
   "name": "Create organization invitations",
   "description": "Creates invitations for a specific organization. If the invited user has an existing account, they automatically join as a member. If they don't yet have an account, they are invited to create one, after which they become a member. Each invited user receives an email notification.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "invitations": {
      "type": "array",
      "description": "Invitations to create for the organization."
     }
    },
    "required": [
     "invitations",
     "org_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/invitations",
    "path": [
     "org_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "invitations"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_project",
   "name": "Create project",
   "description": "Creates a Neon project within an organization. If using a personal API key, include the `org_id` parameter to specify which organization to create the project in. If using an org API key, `org_id` is automatically inferred from the key. Plan limits define how many projects you can create. For more information, see [Manage projects](https://neon.com/docs/manage/projects/). You can specify a region ",
   "input": {
    "type": "object",
    "properties": {
     "project": {
      "type": "object",
      "description": "Configuration for the new project, including name, region, and Postgres compute and storage settings."
     }
    },
    "required": [
     "project"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "project"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_project_branch",
   "name": "Create branch",
   "description": "Creates a branch in the specified project. No request body is required, but you can specify one to create a compute endpoint or select a non-default parent branch. By default, the branch is created from the project's default branch with no compute endpoint, and the branch name is auto-generated. To access the branch, add a `read_write` endpoint. Each branch supports one read-write endpoint and mul",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "endpoints": {
      "type": "array",
      "description": "Compute endpoints to create together with the branch. If omitted, the branch is created without any compute endpoint. Endpoints can be added to the branch separately after creation."
     },
     "branch": {
      "type": "object",
      "description": "Optional configuration for the new branch, for example `name`, `parent_id` (fork from a branch), `parent_lsn` or `parent_timestamp` (point-in-time branching), and `protected`."
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "endpoints",
     "branch"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_project_branch_anonymized",
   "name": "Create anonymized branch",
   "description": "Creates a new branch with anonymized data using PostgreSQL Anonymizer for static masking. This allows developers to work with masked production data. Optionally, provide `masking_rules` to set initial masking rules for the branch and `start_anonymization` to automatically start anonymization after creation. This combines functionality of updating masking rules and starting anonymization into the b",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "annotation_value": {
      "type": "object",
      "description": "A free-form map of string key-value pairs for attaching metadata to a resource (for example, a git commit reference). Maximum 50 entries."
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branch_anonymized",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "annotation_value"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_project_branch_bucket",
   "name": "Create a bucket on the branch",
   "description": "Creates a new branchable object storage bucket on the specified branch. Buckets are managed by the Neon Platform branchable object storage service. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "name": {
      "type": "string",
      "description": "The bucket name."
     },
     "access_level": {
      "type": "string",
      "description": "Access level for the bucket. Defaults to `private`. Set to `public_read` to allow anonymous `GetObject`/`HeadObject` on objects in this bucket.",
      "enum": [
       "private",
       "public_read"
      ]
     }
    },
    "required": [
     "branch_id",
     "name",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/buckets",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "name",
     "access_level"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_project_branch_data_api",
   "name": "Create neon data api",
   "description": "Creates a new instance of Neon Data API in the specified branch. The Data API exposes a REST interface over the branch database. The `database_name` path parameter determines which database the API serves.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "database_name": {
      "type": "string",
      "description": "The database name"
     },
     "auth_provider": {
      "type": "string",
      "description": "Authentication provider for the Neon Data API. `neon_auth`: use Neon's built-in managed authentication (no JWKS configuration required). `external`: use an external JWT provider, which requires `jwks_url`. When omitted, no auth provider is configured (existing setup is kept).",
      "enum": [
       "neon_auth",
       "external"
      ]
     },
     "jwks_url": {
      "type": "string",
      "description": "URL of the JWKS endpoint used to verify JWTs for this Data API. Required when configuring JWT-based authentication; omit when using a non-JWT auth provider."
     },
     "provider_name": {
      "type": "string",
      "description": "Display name for the authentication provider. Accepted values include \"Clerk\", \"Stytch\", and \"Auth0\", but any non-empty string is valid. Optional field."
     },
     "jwt_audience": {
      "type": "string",
      "description": "Expected `aud` claim in incoming JWTs. When set, tokens with a different audience are rejected; tokens with no audience are still accepted. Omit to skip audience validation."
     },
     "add_default_grants": {
      "type": "boolean",
      "description": "Grant all permissions to the tables in the public schema to authenticated users"
     },
     "skip_auth_schema": {
      "type": "boolean",
      "description": "Skip creating the auth schema and RLS functions"
     },
     "settings": {
      "type": "object",
      "description": "Configuration settings for the Neon Data API"
     }
    },
    "required": [
     "branch_id",
     "database_name",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/data-api/{database_name}",
    "path": [
     "project_id",
     "branch_id",
     "database_name"
    ],
    "query": [],
    "header": [],
    "body": [
     "auth_provider",
     "jwks_url",
     "provider_name",
     "jwt_audience",
     "add_default_grants",
     "skip_auth_schema",
     "settings"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_project_branch_database",
   "name": "Create database",
   "description": "Creates a database in the specified branch. A branch can have multiple databases. For related information, see [Manage databases](https://neon.com/docs/manage/databases/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "database": {
      "type": "object",
      "description": "Configuration for the new Postgres database."
     }
    },
    "required": [
     "branch_id",
     "database",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/databases",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "database"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_project_branch_function_deployment",
   "name": "Deploy code to a function",
   "description": "Creates a deployment for the function. Supply any subset of zip, environment, and runtime; omitted fields inherit the function's latest version. At least one field must be supplied. The first deployment of a function must include zip. The newest deployment becomes active. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "slug": {
      "type": "string",
      "description": "The function slug"
     },
     "zip": {
      "type": "string",
      "description": "Optional ZIP archive of the function source code. Omit to reuse the latest version's bundle (a config-only change). Required for the first deployment of a function."
     },
     "runtime": {
      "type": "string",
      "description": "",
      "enum": [
       "nodejs24"
      ]
     },
     "environment": {
      "type": "string",
      "description": "Optional JSON object (a string-to-string map) of environment variables for the deployment, e.g. {\"KEY\":\"VALUE\"}. Carried as a JSON-encoded string because multipart form data does not support typed object parts. Values are write-only: they are encrypted at rest, and responses carry only the variable names (the `environment` array), never the values."
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "slug"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/functions/{slug}/deployments",
    "path": [
     "project_id",
     "branch_id",
     "slug"
    ],
    "query": [],
    "header": [],
    "body": [
     "zip",
     "runtime",
     "environment"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_project_branch_role",
   "name": "Create role",
   "description": "Creates a Postgres role in the specified branch. For related information, see [Manage roles](https://neon.com/docs/manage/roles/). Connections established to the active compute endpoint will be dropped. If the compute endpoint is idle, the endpoint becomes active for a short period of time and is suspended afterward.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "role": {
      "type": "object",
      "description": "Properties of the role to create."
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "role"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/roles",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "role"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_project_branch_trigger",
   "name": "Create a trigger",
   "description": "Creates a trigger for a Function visible on the branch. The required `type` discriminator selects the trigger-specific configuration. The supported types are `schedule` and `storage_object_created`. A schedule trigger uses a numeric five-field cron expression interpreted in UTC. A storage-object-created trigger fires only after a successful upload to one exact bucket and may narrow matches to an o",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/triggers",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_project_endpoint",
   "name": "Create compute endpoint",
   "description": "Creates a compute endpoint for the specified branch. A compute endpoint is a Neon compute instance. There is a maximum of one read-write compute endpoint per branch. If the specified branch already has a read-write compute endpoint, the operation fails. A branch can have multiple read-only compute endpoints. For more information about compute endpoints, see [Manage computes](https://neon.com/docs/",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "endpoint": {
      "type": "object",
      "description": "Configuration for the compute endpoint to create."
     }
    },
    "required": [
     "endpoint",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/endpoints",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "endpoint"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_project_transfer_request",
   "name": "Create a project transfer request",
   "description": "Creates a transfer request for the specified project. The request expires after a set period. To accept the request, the recipient calls `PUT /projects/{project_id}/transfer_requests/{request_id}` or uses the Neon Console claim link. The optional `ru` parameter redirects the recipient after acceptance.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "ttl_seconds": {
      "type": "integer",
      "description": "Number of seconds the transfer request stays valid before it expires. Defaults to 86400 (24 hours)."
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/transfer_requests",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "ttl_seconds"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.create_snapshot",
   "name": "Create snapshot",
   "description": "Creates a snapshot from the specified branch. This operation may initiate an asynchronous process.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "lsn": {
      "type": "string",
      "description": "The target Log Sequence Number (LSN) to take the snapshot from. Must fall within the restore window. Cannot be used with `timestamp`"
     },
     "timestamp": {
      "type": "string",
      "description": "The target timestamp for the snapshot. Must fall within the restore window. RFC 3339 format. Cannot be used with `lsn`."
     },
     "name": {
      "type": "string",
      "description": "A name for the snapshot."
     },
     "expires_at": {
      "type": "string",
      "description": "The time at which the snapshot will be automatically deleted. RFC 3339 format."
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/snapshot",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [
     "lsn",
     "timestamp",
     "name",
     "expires_at"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_branch_neon_auth_oauth_provider",
   "name": "Delete oauth provider",
   "description": "Deletes an OAuth provider from the specified project.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "oauth_provider_id": {
      "type": "string",
      "description": "The OAuth provider ID",
      "enum": [
       "google",
       "github",
       "microsoft",
       "vercel"
      ]
     }
    },
    "required": [
     "branch_id",
     "oauth_provider_id",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/oauth_providers/{oauth_provider_id}",
    "path": [
     "project_id",
     "branch_id",
     "oauth_provider_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_branch_neon_auth_trusted_domain",
   "name": "Delete domain from redirect uri whitelist",
   "description": "Removes a domain from the redirect URI whitelist for the specified branch. After removal, the domain can no longer be used as a redirect target after authentication.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "auth_provider": {
      "type": "string",
      "description": "Authentication provider integrated with this Neon Auth configuration. `better_auth` integrates with Better Auth (the current, recommended provider). `stack` integrates with Stack Auth (deprecated). `mock` is a simulated provider for local development and testing only.",
      "enum": [
       "mock",
       "stack",
       "better_auth"
      ]
     },
     "domains": {
      "type": "array",
      "description": "Domain names to remove from the redirect URI whitelist for the specified auth provider."
     }
    },
    "required": [
     "auth_provider",
     "branch_id",
     "domains",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/domains",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "auth_provider",
     "domains"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_branch_neon_auth_user",
   "name": "Delete auth user",
   "description": "Deletes the specified user from the Neon Auth user directory for the specified branch. Removes the user record from `neon_auth.users_sync`. This action cannot be undone.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "auth_user_id": {
      "type": "string",
      "description": "The Neon user ID"
     }
    },
    "required": [
     "auth_user_id",
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/users/{auth_user_id}",
    "path": [
     "project_id",
     "branch_id",
     "auth_user_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_organization_spending_limit",
   "name": "Remove organization spending limit",
   "description": "Removes the configured monthly spending limit for the specified organization. Idempotent \u2014 removing an already-unset limit still succeeds. Available to organization admins on Launch and Scale plans only.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     }
    },
    "required": [
     "org_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/billing/spending_limit",
    "path": [
     "org_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_organization_vpcendpoint",
   "name": "Delete vpc endpoint",
   "description": "Deletes the VPC endpoint from the specified Neon organization. If you delete a VPC endpoint from a Neon organization, that VPC endpoint cannot be added back to the Neon organization.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "region_id": {
      "type": "string",
      "description": "The Neon region ID. Azure regions are currently not supported."
     },
     "vpc_endpoint_id": {
      "type": "string",
      "description": "The VPC endpoint ID"
     }
    },
    "required": [
     "org_id",
     "region_id",
     "vpc_endpoint_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/vpc/region/{region_id}/vpc_endpoints/{vpc_endpoint_id}",
    "path": [
     "org_id",
     "region_id",
     "vpc_endpoint_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project",
   "name": "Delete project",
   "description": "Deletes the specified project and all its endpoints, branches, databases, and users. Deleted projects can be recovered within 7 days using `POST /projects/{project_id}/recover`. To list recoverable projects, use `GET /projects?recoverable=true`.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_branch",
   "name": "Delete branch",
   "description": "Deletes the specified branch from a project and places all compute endpoints into an idle state, breaking existing client connections. The deletion completes after all operations finish. You cannot delete a project's root or default branch, or a branch that has a child branch. A project must have at least one branch. For related information, see [Manage branches](https://neon.com/docs/manage/branc",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_branch_bucket",
   "name": "Delete a bucket on the branch",
   "description": "Deletes the named bucket from the specified branch. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "bucket_name": {
      "type": "string",
      "description": "The bucket name"
     }
    },
    "required": [
     "branch_id",
     "bucket_name",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/buckets/{bucket_name}",
    "path": [
     "project_id",
     "branch_id",
     "bucket_name"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_branch_bucket_object",
   "name": "Delete an object in a bucket",
   "description": "Deletes the named object from the bucket on the specified branch. Served by the user's session (no customer S3 credentials required). **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "bucket_name": {
      "type": "string",
      "description": "The bucket name"
     },
     "object_key": {
      "type": "string",
      "description": "The object key. Keys may contain `/`; the `/` characters of nested keys must be percent-encoded (`%2F`) in the path segment."
     }
    },
    "required": [
     "branch_id",
     "bucket_name",
     "object_key",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/buckets/{bucket_name}/objects/{object_key}",
    "path": [
     "project_id",
     "branch_id",
     "bucket_name",
     "object_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_branch_bucket_objects_by_prefix",
   "name": "Delete every object under a key prefix (folder) in a bucket",
   "description": "Soft-deletes every object on the specified branch whose key starts with `prefix`, in a single call. Intended to back a \"delete folder\" action in an object browser: a `prefix` of `app/avatars/` removes every object beneath that folder. Served by the user's session (no customer S3 credentials required). `prefix` must be non-empty, end with `/`, be at most 1024 bytes, and contain no control character",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "bucket_name": {
      "type": "string",
      "description": "The bucket name"
     },
     "prefix": {
      "type": "string",
      "description": "The key prefix (folder) to delete. Must be non-empty and end with `/`. Every object on this branch whose key starts with this prefix is soft-deleted."
     }
    },
    "required": [
     "branch_id",
     "bucket_name",
     "prefix",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/buckets/{bucket_name}/objects-by-prefix",
    "path": [
     "project_id",
     "branch_id",
     "bucket_name"
    ],
    "query": [
     "prefix"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_branch_custom_domain",
   "name": "Delete a custom domain from a branch",
   "description": "Removes a custom domain registered on the branch and stops routing it. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "domain": {
      "type": "string",
      "description": "The registered custom domain (case-insensitive)."
     }
    },
    "required": [
     "branch_id",
     "domain",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/custom-domains/{domain}",
    "path": [
     "project_id",
     "branch_id",
     "domain"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_branch_data_api",
   "name": "Delete neon data api",
   "description": "Deletes the Neon Data API for the specified branch. Existing connections using the Data API endpoint will fail after deletion.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "database_name": {
      "type": "string",
      "description": "The database name"
     }
    },
    "required": [
     "branch_id",
     "database_name",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/data-api/{database_name}",
    "path": [
     "project_id",
     "branch_id",
     "database_name"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_branch_database",
   "name": "Delete database",
   "description": "Deletes the specified database from the branch. For related information, see [Manage databases](https://neon.com/docs/manage/databases/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "database_name": {
      "type": "string",
      "description": "The database name"
     }
    },
    "required": [
     "branch_id",
     "database_name",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/databases/{database_name}",
    "path": [
     "project_id",
     "branch_id",
     "database_name"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_branch_function",
   "name": "Delete a function on the branch",
   "description": "Deletes the function identified by its slug. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "slug": {
      "type": "string",
      "description": "The function slug"
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "slug"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/functions/{slug}",
    "path": [
     "project_id",
     "branch_id",
     "slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_branch_role",
   "name": "Delete role",
   "description": "Deletes the specified Postgres role from the branch. For related information, see [Manage roles](https://neon.com/docs/manage/roles/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "role_name": {
      "type": "string",
      "description": "The role name"
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "role_name"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/roles/{role_name}",
    "path": [
     "project_id",
     "branch_id",
     "role_name"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_branch_trigger",
   "name": "Delete a trigger",
   "description": "Deletes a branch-local trigger or writes a branch-local tombstone for an inherited trigger so it does not reappear. Deletion stops future scheduling or storage-event matching but does not cancel invocations already committed for delivery. The supported trigger types are `schedule` and `storage_object_created`. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "trigger_id": {
      "type": "string",
      "description": "The opaque, project-wide trigger ID"
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "trigger_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/triggers/{trigger_id}",
    "path": [
     "project_id",
     "branch_id",
     "trigger_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_endpoint",
   "name": "Delete compute endpoint",
   "description": "Deletes the specified compute endpoint. A compute endpoint is a Neon compute instance. Deleting a compute endpoint drops existing network connections to the compute endpoint. The deletion is completed when the last operation in the chain finishes successfully. An `endpoint_id` has an `ep-` prefix. For information about compute endpoints, see [Manage computes](https://neon.com/docs/manage/endpoints",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "endpoint_id": {
      "type": "string",
      "description": "The endpoint ID"
     }
    },
    "required": [
     "endpoint_id",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/endpoints/{endpoint_id}",
    "path": [
     "project_id",
     "endpoint_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_jwks",
   "name": "Delete jwks url",
   "description": "Removes the specified JWKS URL from the project. JWTs signed by keys from the removed URL can no longer authenticate to the project's endpoints.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "jwks_id": {
      "type": "string",
      "description": "The JWKS ID"
     }
    },
    "required": [
     "jwks_id",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/jwks/{jwks_id}",
    "path": [
     "project_id",
     "jwks_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_project_vpcendpoint",
   "name": "Delete vpc endpoint restriction",
   "description": "Removes the specified VPC endpoint restriction from a Neon project.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "vpc_endpoint_id": {
      "type": "string",
      "description": "The VPC endpoint ID"
     }
    },
    "required": [
     "project_id",
     "vpc_endpoint_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/vpc_endpoints/{vpc_endpoint_id}",
    "path": [
     "project_id",
     "vpc_endpoint_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.delete_snapshot",
   "name": "Delete snapshot",
   "description": "Deletes the specified snapshot.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "snapshot_id": {
      "type": "string",
      "description": "The snapshot ID"
     }
    },
    "required": [
     "project_id",
     "snapshot_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/snapshots/{snapshot_id}",
    "path": [
     "project_id",
     "snapshot_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.disable_neon_auth",
   "name": "Disable neon auth for the branch",
   "description": "Disables the Neon Auth integration for the specified branch, removing the connection to the authentication provider. If `delete_data` is `true`, also deletes the `neon_auth` schema and all associated tables from the branch database. The integration can be re-enabled by calling `POST /projects/{project_id}/branches/{branch_id}/auth`.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "delete_data": {
      "type": "boolean",
      "description": "If true, deletes the `neon_auth` schema from the database"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "delete_data"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.finalize_restore_branch",
   "name": "Finalize branch restore from snapshot",
   "description": "Finalize the restore operation for a branch created from a snapshot. This operation updates the branch so it functions as the original branch it replaced. This includes: - Reassigning any computes from the original branch to the restored branch (this will restart the computes) - Renaming the restored branch to the original branch's name - Renaming the original branch so it no longer uses the origi",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "name": {
      "type": "string",
      "description": "Name for the replaced branch. If omitted, a unique name is generated."
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/finalize_restore",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "name"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_active_regions",
   "name": "List supported regions",
   "description": "Lists supported Neon regions. **Note:** Not all regions are available to all organizations. Pass the `org_id` parameter to get an accurate list of regions available to your organization.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "Organization ID. When provided, returns only regions available to this organization. Recommended for accurate region availability."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/regions",
    "path": [],
    "query": [
     "org_id"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_anonymized_branch_status",
   "name": "Retrieve anonymized branch status",
   "description": "Retrieves the current status of an anonymized branch, including its state and progress information. This endpoint allows you to monitor the anonymization process from initialization through completion. Only anonymized branches will have status information available. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/anonymized_status",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_auth_details",
   "name": "Retrieve request authentication details",
   "description": "Returns authentication details for the credentials used in the request, including the credential type (API key, Bearer token, or OAuth session) and the associated identity.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/auth",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_available_preload_libraries",
   "name": "List available shared preload libraries",
   "description": "Returns the shared preload libraries available for the specified project's Postgres version. Shared preload libraries are Postgres extensions that require the `shared_preload_libraries` setting and a compute restart to activate. Use this list to determine which libraries can be enabled in the project's `settings.preload_libraries` configuration.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": ""
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/available_preload_libraries",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_connection_uri",
   "name": "Retrieve connection uri",
   "description": "Retrieves a connection URI for the specified database. The URI uses the standard PostgreSQL connection string format. Set `pooled=true` to include the `-pooler` suffix for a connection pooler URI.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID. Defaults to your project's default `branch_id` if not specified."
     },
     "endpoint_id": {
      "type": "string",
      "description": "The endpoint ID. Defaults to the read-write `endpoint_id` associated with the `branch_id` if not specified."
     },
     "database_name": {
      "type": "string",
      "description": "The database name"
     },
     "role_name": {
      "type": "string",
      "description": "The role name"
     },
     "pooled": {
      "type": "boolean",
      "description": "Adds the `-pooler` option to the connection URI when set to `true`, creating a pooled connection URI."
     }
    },
    "required": [
     "database_name",
     "project_id",
     "role_name"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/connection_uri",
    "path": [
     "project_id"
    ],
    "query": [
     "branch_id",
     "endpoint_id",
     "database_name",
     "role_name",
     "pooled"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_consumption_history_per_branch_v2",
   "name": "Retrieve branch consumption metrics",
   "description": "Returns consumption metrics for each branch across one or more projects listed in `project_ids` (1 to 100 projects). Available for accounts on paid usage-based Launch, Scale, Agent, and Enterprise plans. History starts when the account first ingests branch-level consumption data. The `metrics` query parameter is required. Only these six values are supported on this endpoint: `compute_unit_seconds`",
   "input": {
    "type": "object",
    "properties": {
     "cursor": {
      "type": "string",
      "description": "Cursor from the previous response (`pagination.cursor`). Pass it to fetch the next page of branches. Pages are ordered by project ID, then branch ID."
     },
     "limit": {
      "type": "integer",
      "description": "Maximum number of branches per page. Allowed range: 1 to 1000. Default: 100."
     },
     "project_ids": {
      "type": "array",
      "description": "Project IDs to include (required, 1 to 100). Returns metrics for branches in these projects. Pass multiple IDs as repeated query parameters or a comma-separated list: - `project_ids=cold-poetry-09157238&project_ids=quiet-snow-71788278` - `project_ids=cold-poetry-09157238,quiet-snow-71788278`"
     },
     "branch_ids": {
      "type": "array",
      "description": "Optional branch IDs to filter the response (up to 100). If omitted, all branches in the listed projects are included. Pass multiple IDs as repeated query parameters or a comma-separated list: - `branch_ids=br-aged-salad-637688&branch_ids=br-sweet-breeze-497520` - `branch_ids=br-aged-salad-637688,br-sweet-breeze-497520`"
     },
     "from": {
      "type": "string",
      "description": "Specify the start `date-time` for the consumption period. The `date-time` value is rounded according to the specified `granularity`. For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`. The specified `date-time` value must respect the specified `granularity`: - For `hourly`, consumption metrics are limited to the last 168 hours. - For `daily`, cons"
     },
     "to": {
      "type": "string",
      "description": "Specify the end `date-time` for the consumption period. The `date-time` value is rounded according to the specified `granularity`. For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`. The specified `date-time` value must respect the specified `granularity`: - For `hourly`, consumption metrics are limited to the last 168 hours. - For `daily`, consum"
     },
     "granularity": {
      "type": "string",
      "description": "Specify the granularity of consumption metrics. Hourly, daily, and monthly metrics are available for the last 168 hours, 60 days, and 1 year, respectively.",
      "enum": [
       "hourly",
       "daily",
       "monthly"
      ]
     },
     "org_id": {
      "type": "string",
      "description": "Organization ID. Metrics are returned for projects in this organization."
     },
     "metrics": {
      "type": "array",
      "description": "Required. List the metrics to return. Only these values are supported: - `compute_unit_seconds` - `root_branch_bytes_month` - `child_branch_bytes_month` - `instant_restore_bytes_month` - `public_network_transfer_bytes` - `private_network_transfer_bytes` Not supported on this endpoint: `extra_branches_month`, `snapshot_storage_bytes_month`. Use `GET /consumption_history/v2/projects` for those. Pass"
     }
    },
    "required": [
     "from",
     "granularity",
     "metrics",
     "org_id",
     "project_ids",
     "to"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/consumption_history/v2/branches",
    "path": [],
    "query": [
     "cursor",
     "limit",
     "project_ids",
     "branch_ids",
     "from",
     "to",
     "granularity",
     "org_id",
     "metrics"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_consumption_history_per_project",
   "name": "Retrieve project consumption metrics (legacy plans)",
   "description": "Retrieves consumption metrics for Scale, Business, and Enterprise plan projects. History begins at the time of upgrade. Results are ordered by time in ascending order (oldest to newest). Issuing a call to this API does not wake a project's compute endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "cursor": {
      "type": "string",
      "description": "Specify the cursor value from the previous response to get the next batch of projects."
     },
     "limit": {
      "type": "integer",
      "description": "Specify a value from 1 to 100 to limit number of projects in the response."
     },
     "project_ids": {
      "type": "array",
      "description": "Specify a list of project IDs to filter the response. If omitted, the response will contain all projects. A list of project IDs can be specified as an array of parameter values or as a comma-separated list in a single parameter value. - As an array of parameter values: `project_ids=cold-poetry-09157238%20&project_ids=quiet-snow-71788278` - As a comma-separated list in a single parameter value: `pr"
     },
     "from": {
      "type": "string",
      "description": "Specify the start `date-time` for the consumption period. The `date-time` value is rounded according to the specified `granularity`. For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`. The specified `date-time` value must respect the specified `granularity`: - For `hourly`, consumption metrics are limited to the last 168 hours. - For `daily`, cons"
     },
     "to": {
      "type": "string",
      "description": "Specify the end `date-time` for the consumption period. The `date-time` value is rounded according to the specified granularity. For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`. The specified `date-time` value must respect the specified `granularity`: - For `hourly`, consumption metrics are limited to the last 168 hours. - For `daily`, consumpt"
     },
     "granularity": {
      "type": "string",
      "description": "Specify the granularity of consumption metrics. Hourly, daily, and monthly metrics are available for the last 168 hours, 60 days, and 1 year, respectively.",
      "enum": [
       "hourly",
       "daily",
       "monthly"
      ]
     },
     "org_id": {
      "type": "string",
      "description": "Specify the organization for which the project consumption metrics should be returned. If this parameter is not provided, the endpoint will return the metrics for the authenticated user's projects."
     },
     "include_v1_metrics": {
      "type": "boolean",
      "description": "The field is deprecated. Please use `metrics` instead. If `metrics` is specified, this field is ignored. Include metrics utilized in previous pricing models. - **data_storage_bytes_hour**: The sum of the maximum observed storage values for each hour, which never decreases."
     },
     "metrics": {
      "type": "array",
      "description": "Specify a list of metrics to include in the response. If omitted, active_time, compute_time, written_data, synthetic_storage_size are returned. Possible values: - `active_time_seconds` - `compute_time_seconds` - `written_data_bytes` - `synthetic_storage_size_bytes` - `data_storage_bytes_hour` - `logical_size_bytes` - `logical_size_bytes_hour` A list of metrics can be specified as an array of param"
     }
    },
    "required": [
     "from",
     "granularity",
     "to"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/consumption_history/projects",
    "path": [],
    "query": [
     "cursor",
     "limit",
     "project_ids",
     "from",
     "to",
     "granularity",
     "org_id",
     "include_v1_metrics",
     "metrics"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_consumption_history_per_project_v2",
   "name": "Retrieve project consumption metrics",
   "description": "Returns consumption metrics for up to `limit` projects per page. If `project_ids` is omitted, projects in the organization are included across pages (use `cursor`). If `project_ids` is provided, the response is limited to those projects (up to 100). Available for accounts on Launch, Scale, Agent, Business, and Enterprise plans. History starts when the account upgrades to an eligible plan. The `met",
   "input": {
    "type": "object",
    "properties": {
     "cursor": {
      "type": "string",
      "description": "Cursor from the previous response (`pagination.cursor`). Pass it to fetch the next page of projects. Pages are ordered by project creation order (newest first)."
     },
     "limit": {
      "type": "integer",
      "description": "Maximum number of projects per page. Allowed range: 1 to 100. Default: 10."
     },
     "project_ids": {
      "type": "array",
      "description": "Optional project IDs to filter the response (up to 100). If omitted, projects in the organization are included across pages (use `cursor` and `limit`). Pass multiple IDs as repeated query parameters or a comma-separated list: - `project_ids=cold-poetry-09157238&project_ids=quiet-snow-71788278` - `project_ids=cold-poetry-09157238,quiet-snow-71788278`"
     },
     "from": {
      "type": "string",
      "description": "Specify the start `date-time` for the consumption period. The `date-time` value is rounded according to the specified `granularity`. For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`. The specified `date-time` value must respect the specified `granularity`: - For `hourly`, consumption metrics are limited to the last 168 hours. - For `daily`, cons"
     },
     "to": {
      "type": "string",
      "description": "Specify the end `date-time` for the consumption period. The `date-time` value is rounded according to the specified `granularity`. For example, `2024-03-15T15:30:00Z` for `daily` granularity will be rounded to `2024-03-15T00:00:00Z`. The specified `date-time` value must respect the specified `granularity`: - For `hourly`, consumption metrics are limited to the last 168 hours. - For `daily`, consum"
     },
     "granularity": {
      "type": "string",
      "description": "Specify the granularity of consumption metrics. Hourly, daily, and monthly metrics are available for the last 168 hours, 60 days, and 1 year, respectively.",
      "enum": [
       "hourly",
       "daily",
       "monthly"
      ]
     },
     "org_id": {
      "type": "string",
      "description": "Organization ID. Metrics are returned for projects in this organization."
     },
     "metrics": {
      "type": "array",
      "description": "Required. List the metrics to return. Supported values: - `compute_unit_seconds` - `root_branch_bytes_month` - `child_branch_bytes_month` - `instant_restore_bytes_month` - `public_network_transfer_bytes` - `private_network_transfer_bytes` - `extra_branches_month` - `snapshot_storage_bytes_month` Pass multiple values as repeated query parameters or a comma-separated list: - `metrics=compute_unit_se"
     }
    },
    "required": [
     "from",
     "granularity",
     "metrics",
     "org_id",
     "to"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/consumption_history/v2/projects",
    "path": [],
    "query": [
     "cursor",
     "limit",
     "project_ids",
     "from",
     "to",
     "granularity",
     "org_id",
     "metrics"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_current_user_info",
   "name": "Retrieve current user details",
   "description": "Retrieves information about the currently authenticated Neon user, including account identifiers, plan details, and linked auth accounts.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/users/me",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_current_user_organizations",
   "name": "List organizations for the current user",
   "description": "Retrieves the organizations that the currently authenticated user belongs to. When called with an organization- or project-scoped API key (which is not tied to a user), this returns the single organization that owns the key.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/users/me/organizations",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_masking_rules",
   "name": "Retrieve masking rules",
   "description": "Retrieves the masking rules for the specified anonymized branch. Masking rules define how sensitive data should be anonymized using PostgreSQL Anonymizer. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/masking_rules",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_neon_auth",
   "name": "Retrieve neon auth details for the branch",
   "description": "Retrieves the Neon Auth integration details for the specified branch, including the auth provider type and integration status.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_neon_auth_allow_localhost",
   "name": "Retrieve localhost allow setting",
   "description": "Retrieves the localhost allow setting for the specified branch's Neon Auth integration. When enabled, authentication flows work from `localhost` without adding it to the redirect URI whitelist.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/allow_localhost",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_neon_auth_email_and_password_config",
   "name": "Retrieve email and password configuration",
   "description": "Retrieves the email and password authentication configuration for the specified branch's Neon Auth integration, including whether it is enabled and the email verification method.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/email_and_password",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_neon_auth_email_provider",
   "name": "Retrieve email provider configuration",
   "description": "Retrieves the email provider configuration for the specified branch's Neon Auth integration, including the provider type and server settings.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/email_provider",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_neon_auth_phone_number_plugin",
   "name": "Retrieve phone number plugin configuration",
   "description": "Returns the phone number plugin configuration for Neon Auth. The phone number plugin enables phone-based OTP authentication.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/plugins/phone-number",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_neon_auth_plugin_configs",
   "name": "Retrieve neon auth plugin configurations",
   "description": "Returns all plugin configurations for Neon Auth in a single response. This endpoint aggregates organization, email provider, email and password, OAuth providers, and localhost settings.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/plugins",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_neon_auth_webhook_config",
   "name": "Retrieve neon auth webhook configuration",
   "description": "Returns the webhook configuration for the specified branch's Neon Auth integration, including the endpoint URL and the events that trigger it.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/webhooks",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_organization",
   "name": "Retrieve organization details",
   "description": "Retrieves details for the specified organization, including its name, plan, and configuration.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     }
    },
    "required": [
     "org_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}",
    "path": [
     "org_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_organization_invitations",
   "name": "List organization invitations",
   "description": "Retrieves pending and accepted invitations for the specified organization.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     }
    },
    "required": [
     "org_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/invitations",
    "path": [
     "org_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_organization_member",
   "name": "Retrieve organization member details",
   "description": "Retrieves information about the specified organization member.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "member_id": {
      "type": "string",
      "description": "The Neon organization member ID"
     }
    },
    "required": [
     "member_id",
     "org_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/members/{member_id}",
    "path": [
     "org_id",
     "member_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_organization_members",
   "name": "List organization members",
   "description": "Retrieves a paginated list of members for the specified organization.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "sort_by": {
      "type": "string",
      "description": "Sort the members by the specified field. Defaults to `joined_at`.",
      "enum": [
       "email",
       "role",
       "joined_at"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "A cursor to use in pagination. A cursor defines your place in the data list. Include `response.pagination.next` in subsequent API calls to fetch next page of the list."
     },
     "sort_order": {
      "type": "string",
      "description": "Defines the sorting order of entities.",
      "enum": [
       "asc",
       "desc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of members to return in the response"
     }
    },
    "required": [
     "org_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/members",
    "path": [
     "org_id"
    ],
    "query": [
     "sort_by",
     "cursor",
     "sort_order",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_organization_spending_limit",
   "name": "Retrieve organization spending limit",
   "description": "Returns the configured monthly spending limit for the specified organization. `spending_limit_cents: null` indicates that no limit is currently set. Available to organization members with read access on Launch and Scale plans only.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     }
    },
    "required": [
     "org_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/billing/spending_limit",
    "path": [
     "org_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_organization_vpcendpoint_details",
   "name": "Retrieve vpc endpoint details",
   "description": "Retrieves the current state and configuration details of a specified VPC endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "region_id": {
      "type": "string",
      "description": "The Neon region ID. Azure regions are currently not supported."
     },
     "vpc_endpoint_id": {
      "type": "string",
      "description": "The VPC endpoint ID"
     }
    },
    "required": [
     "org_id",
     "region_id",
     "vpc_endpoint_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/vpc/region/{region_id}/vpc_endpoints/{vpc_endpoint_id}",
    "path": [
     "org_id",
     "region_id",
     "vpc_endpoint_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project",
   "name": "Retrieve project details",
   "description": "Retrieves information about the specified project. Returned details include the project settings, compute configuration, history retention, owner information, and current usage metrics.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_advisor_security_issues",
   "name": "Get advisor issues",
   "description": "Analyzes the database for security and performance issues. Returns a list of issues categorized by severity (ERROR, WARN, INFO). Requires read access to the project and Data API enabled.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "Branch ID to analyze. If not specified, the project's default branch is used."
     },
     "database_name": {
      "type": "string",
      "description": "Database name to analyze. Required if branch has multiple databases."
     },
     "category": {
      "type": "string",
      "description": "Filter issues by category",
      "enum": [
       "SECURITY",
       "PERFORMANCE"
      ]
     },
     "min_severity": {
      "type": "string",
      "description": "Minimum severity level to include. For example, WARN returns WARN and ERROR issues, excluding INFO.",
      "enum": [
       "INFO",
       "WARN",
       "ERROR"
      ]
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/advisors",
    "path": [
     "project_id"
    ],
    "query": [
     "branch_id",
     "database_name",
     "category",
     "min_severity"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch",
   "name": "Retrieve branch details",
   "description": "Retrieves information about the specified branch. A `branch_id` value has a `br-` prefix. Each Neon project is initially created with a root and default branch named `main`. A project can contain one or more branches. A parent branch is identified by a `parent_id` value, which is the `id` of the parent branch. For related information, see [Manage branches](https://neon.com/docs/manage/branches/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch_ai_gateway",
   "name": "Get branch ai gateway endpoint",
   "description": "Returns the AI Gateway endpoint host for the specified branch, used to render code-snippet base URLs. A 200 response means the branch is registered and this region serves the AI gateway. A 404 response includes a `reason` field indicating why the gateway is unavailable. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/ai_gateway",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch_bucket_object",
   "name": "Download an object's bytes",
   "description": "Streams the raw bytes of the named object from the bucket on the specified branch, including objects inherited from ancestor branches. Served by the user's session (no customer S3 credentials required). The body is returned as `application/octet-stream` so a browser treats it as a download; the `Content-Length` and `ETag` response headers echo the stored object metadata. BINARY-STREAM EXCEPTION TO",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "bucket_name": {
      "type": "string",
      "description": "The bucket name"
     },
     "object_key": {
      "type": "string",
      "description": "The object key. Keys may contain `/`; the `/` characters of nested keys must be percent-encoded (`%2F`) in the path segment."
     }
    },
    "required": [
     "branch_id",
     "bucket_name",
     "object_key",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/buckets/{bucket_name}/objects/{object_key}/download",
    "path": [
     "project_id",
     "branch_id",
     "bucket_name",
     "object_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch_data_api",
   "name": "Retrieve neon data api configuration",
   "description": "Retrieves the Neon Data API configuration for the specified branch, including endpoint URL, enabled state, and database settings.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "database_name": {
      "type": "string",
      "description": "The database name"
     }
    },
    "required": [
     "branch_id",
     "database_name",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/data-api/{database_name}",
    "path": [
     "project_id",
     "branch_id",
     "database_name"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch_database",
   "name": "Retrieve database details",
   "description": "Retrieves information about the specified database. For related information, see [Manage databases](https://neon.com/docs/manage/databases/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "database_name": {
      "type": "string",
      "description": "The database name"
     }
    },
    "required": [
     "branch_id",
     "database_name",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/databases/{database_name}",
    "path": [
     "project_id",
     "branch_id",
     "database_name"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch_function",
   "name": "Get function details",
   "description": "Returns the function identified by its slug. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "slug": {
      "type": "string",
      "description": "The function slug"
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "slug"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/functions/{slug}",
    "path": [
     "project_id",
     "branch_id",
     "slug"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch_role",
   "name": "Retrieve role details",
   "description": "Retrieves details about the specified role. In Neon, the terms \"role\" and \"user\" are synonymous. For related information, see [Manage roles](https://neon.com/docs/manage/roles/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "role_name": {
      "type": "string",
      "description": "The role name"
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "role_name"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/roles/{role_name}",
    "path": [
     "project_id",
     "branch_id",
     "role_name"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch_role_password",
   "name": "Retrieve role password",
   "description": "Retrieves the password for the specified Postgres role, if possible. For related information, see [Manage roles](https://neon.com/docs/manage/roles/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "role_name": {
      "type": "string",
      "description": "The role name"
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "role_name"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/roles/{role_name}/reveal_password",
    "path": [
     "project_id",
     "branch_id",
     "role_name"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch_schema",
   "name": "Retrieve database schema",
   "description": "Retrieves the database schema. Specify `lsn` or `timestamp` (not both) to read at a point in time; omit both to read from the database's head.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "db_name": {
      "type": "string",
      "description": "Name of the database for which the schema is retrieved"
     },
     "lsn": {
      "type": "string",
      "description": "The Log Sequence Number (LSN) for which the schema is retrieved"
     },
     "timestamp": {
      "type": "string",
      "description": "The point in time for which the schema is retrieved"
     },
     "format": {
      "type": "string",
      "description": "The format of the schema to retrieve. Possible values: - `sql` (default) - `json`"
     }
    },
    "required": [
     "branch_id",
     "db_name",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/schema",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [
     "db_name",
     "lsn",
     "timestamp",
     "format"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch_schema_comparison",
   "name": "Compare database schema",
   "description": "Compares the schema from the specified database with another branch's schema.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "base_branch_id": {
      "type": "string",
      "description": "The branch ID to compare the schema with"
     },
     "db_name": {
      "type": "string",
      "description": "Name of the database for which the schema is retrieved"
     },
     "lsn": {
      "type": "string",
      "description": "The Log Sequence Number (LSN) for which the schema is retrieved"
     },
     "timestamp": {
      "type": "string",
      "description": "The point in time for which the schema is retrieved"
     },
     "base_lsn": {
      "type": "string",
      "description": "The Log Sequence Number (LSN) for the base branch schema"
     },
     "base_timestamp": {
      "type": "string",
      "description": "The point in time for the base branch schema"
     }
    },
    "required": [
     "branch_id",
     "db_name",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/compare_schema",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [
     "base_branch_id",
     "db_name",
     "lsn",
     "timestamp",
     "base_lsn",
     "base_timestamp"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch_storage",
   "name": "Get branch object storage state",
   "description": "Returns whether branchable object storage is usable for the specified branch. A 200 response means the branch is registered in the object storage service and the S3 data plane will accept requests for it. A 404 response includes a `reason` field indicating why object storage is unavailable. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/storage",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_branch_trigger",
   "name": "Get a trigger",
   "description": "Returns the trigger visible on the branch. The supported trigger types are `schedule` and `storage_object_created`. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "trigger_id": {
      "type": "string",
      "description": "The opaque, project-wide trigger ID"
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "trigger_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/triggers/{trigger_id}",
    "path": [
     "project_id",
     "branch_id",
     "trigger_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_endpoint",
   "name": "Retrieve compute endpoint details",
   "description": "Retrieves information about the specified compute endpoint. A compute endpoint is a Neon compute instance. An `endpoint_id` has an `ep-` prefix. For information about compute endpoints, see [Manage computes](https://neon.com/docs/manage/endpoints/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "endpoint_id": {
      "type": "string",
      "description": "The endpoint ID"
     }
    },
    "required": [
     "endpoint_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/endpoints/{endpoint_id}",
    "path": [
     "project_id",
     "endpoint_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_jwks",
   "name": "List jwks urls",
   "description": "Returns the JWKS URLs available for verifying JWTs used as the authentication mechanism for the specified project.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/jwks",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_project_operation",
   "name": "Retrieve operation details",
   "description": "Retrieves details for the specified operation. An operation is an action performed on a Neon project resource.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "operation_id": {
      "type": "string",
      "description": "The operation ID"
     }
    },
    "required": [
     "operation_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/operations/{operation_id}",
    "path": [
     "project_id",
     "operation_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.get_snapshot_schedule",
   "name": "Retrieve backup schedule",
   "description": "Returns the backup schedule for the specified branch, including the configured snapshot frequencies.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/backup_schedule",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.grant_permission_to_project",
   "name": "Grant project access",
   "description": "Grants project access to the account associated with the specified email address.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": ""
     },
     "email": {
      "type": "string",
      "description": "Email address of the user to grant project access to."
     }
    },
    "required": [
     "email",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/permissions",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "email"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_api_keys",
   "name": "List api keys",
   "description": "Retrieves the API keys for your Neon account. The response does not include API key tokens. A token is only provided when creating an API key. API keys can also be managed in the Neon Console. For more information, see [Manage API keys](https://neon.com/docs/manage/api-keys/).",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/api_keys",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_branch_neon_auth_oauth_providers",
   "name": "List oauth providers for the branch",
   "description": "Lists the OAuth providers configured for the specified branch's Neon Auth integration.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/oauth_providers",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_branch_neon_auth_trusted_domains",
   "name": "List domains in redirect uri whitelist",
   "description": "Lists the trusted domains in the redirect URI whitelist for the specified branch. Only domains in this list are permitted as redirect targets after authentication.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/domains",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_credentials",
   "name": "List credentials on the branch",
   "description": "Returns metadata for customer-issued credentials on the branch. Secrets are never included. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/credentials",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_org_api_keys",
   "name": "List organization api keys",
   "description": "Retrieves the API keys for the specified organization. The response does not include API key tokens. A token is only provided when creating an API key. API keys can also be managed in the Neon Console. For more information, see [Manage API keys](https://neon.com/docs/manage/api-keys/).",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     }
    },
    "required": [
     "org_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/api_keys",
    "path": [
     "org_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_organization_vpcendpoints",
   "name": "List vpc endpoints",
   "description": "Retrieves the list of VPC endpoints for the specified Neon organization.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "region_id": {
      "type": "string",
      "description": "The Neon region ID"
     }
    },
    "required": [
     "org_id",
     "region_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/vpc/region/{region_id}/vpc_endpoints",
    "path": [
     "org_id",
     "region_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_organization_vpcendpoints_all_regions",
   "name": "List vpc endpoints across all regions",
   "description": "Retrieves the list of VPC endpoints for the specified Neon organization across all regions.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     }
    },
    "required": [
     "org_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/vpc/vpc_endpoints",
    "path": [
     "org_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_branch_bucket_objects",
   "name": "List objects in a bucket",
   "description": "Lists objects visible in the named bucket on the specified branch, including those inherited from ancestor branches. Listing is served by the user's session (no customer S3 credentials required). When `delimiter` is supplied (typically `/`), keys are collapsed into common prefixes (`folders`) so callers can render a folder-style browser; keys that do not contain the delimiter after `prefix` are re",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "bucket_name": {
      "type": "string",
      "description": "The bucket name"
     },
     "prefix": {
      "type": "string",
      "description": "Only list objects whose key starts with this prefix."
     },
     "delimiter": {
      "type": "string",
      "description": "Collapse keys sharing a common prefix up to the first occurrence of this delimiter (typically `/`) into the `folders` array."
     },
     "cursor": {
      "type": "string",
      "description": "Opaque pagination cursor returned as `next_cursor` by a previous call. Resume listing after the last item of the previous page."
     },
     "limit": {
      "type": "integer",
      "description": "Maximum number of items (objects + folders) to return."
     }
    },
    "required": [
     "branch_id",
     "bucket_name",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/buckets/{bucket_name}/objects",
    "path": [
     "project_id",
     "branch_id",
     "bucket_name"
    ],
    "query": [
     "prefix",
     "delimiter",
     "cursor",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_branch_buckets",
   "name": "List buckets on the branch",
   "description": "Lists branchable object storage buckets visible on the specified branch, including those inherited from ancestor branches. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/buckets",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_branch_custom_domains",
   "name": "List the custom domains on a branch",
   "description": "Lists all custom domains registered on the branch, across every target entity. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "cursor": {
      "type": "string",
      "description": "A cursor to use in pagination. A cursor defines your place in the data list. Include `response.pagination.next` in subsequent API calls to fetch next page of the list."
     },
     "limit": {
      "type": "integer",
      "description": "Specify a value from 1 to 1000 to limit number of domains in the response"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/custom-domains",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [
     "cursor",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_branch_databases",
   "name": "List databases",
   "description": "Retrieves a list of databases for the specified branch. A branch can have multiple databases. For related information, see [Manage databases](https://neon.com/docs/manage/databases/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/databases",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_branch_endpoints",
   "name": "List branch endpoints",
   "description": "Retrieves a list of compute endpoints for the specified branch. Neon permits only one read-write compute endpoint per branch. A branch can have multiple read-only compute endpoints.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/endpoints",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_branch_functions",
   "name": "List functions on the branch",
   "description": "Lists functions on the specified branch. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "cursor": {
      "type": "string",
      "description": "A cursor to use in pagination. A cursor defines your place in the data list. Include `response.pagination.next` in subsequent API calls to fetch next page of the list."
     },
     "limit": {
      "type": "integer",
      "description": "Specify a value from 1 to 1000 to limit number of functions in the response"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/functions",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [
     "cursor",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_branch_log_field_values",
   "name": "List branch log field values",
   "description": "Lists the distinct values observed for a low-cardinality log field in the requested time range. Call the log fields endpoint first to learn which `field_name` values this branch supports; a field that branch has never emitted is rejected with `unknown_field`. Give the window either as `since` or as an explicit `start_time`; supplying both is rejected. If neither is given, the previous six hours ar",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "field_name": {
      "type": "string",
      "description": "The log field whose distinct values should be returned. Must be one of the names returned by the log fields endpoint for this branch."
     },
     "since": {
      "type": "string",
      "description": "Length of the lookup window, ending at `end_time` or at the current time when `end_time` is omitted. Mutually exclusive with `start_time`. Defaults to six hours."
     },
     "start_time": {
      "type": "string",
      "description": "Inclusive beginning of the lookup window. Mutually exclusive with `since`."
     },
     "end_time": {
      "type": "string",
      "description": "Exclusive end of the lookup window. Defaults to the current time."
     },
     "source": {
      "type": "string",
      "description": "Only consider records emitted by this Neon service.",
      "enum": [
       "function",
       "storage",
       "pg_endpoint"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "Maximum number of distinct values to return. The response sets `is_truncated` when this bound, or the server's own scan cap, cut the list short."
     }
    },
    "required": [
     "branch_id",
     "field_name",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/logs/fields/{field_name}/values",
    "path": [
     "project_id",
     "branch_id",
     "field_name"
    ],
    "query": [
     "since",
     "start_time",
     "end_time",
     "source",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_branch_log_fields",
   "name": "List branch log fields",
   "description": "Lists the low-cardinality log fields observed on this branch whose distinct values can be discovered with the log field-values endpoint. The set is computed per branch and grows as new fields are observed, so treat it as data rather than a fixed list: discover a field here, then pass it as `field_name` to the field-values endpoint. **Note**: This endpoint is currently in Private Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/logs/fields",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_branch_roles",
   "name": "List roles",
   "description": "Retrieves a list of Postgres roles from the specified branch. For related information, see [Manage roles](https://neon.com/docs/manage/roles/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/roles",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_branch_triggers",
   "name": "List triggers on the branch",
   "description": "Lists the complete project-bounded set of triggers visible on the branch, ordered by `trigger_id`. An inherited trigger keeps its project-wide ID and configuration, and is disabled on the child until explicitly enabled there. The supported trigger types are `schedule` and `storage_object_created`. A storage-object-created trigger watches one exact bucket and fires only after an object upload succe",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/triggers",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_branches",
   "name": "List branches",
   "description": "Retrieves a list of branches for the specified project. Each Neon project has a root branch named `main`. A `branch_id` value has a `br-` prefix. A project may contain child branches that were branched from `main` or from another branch. A parent branch is identified by the `parent_id` value, which is the `id` of the parent branch. For related information, see [Manage branches](https://neon.com/do",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "search": {
      "type": "string",
      "description": "Search by branch `name` or `id`. You can specify partial `name` or `id` values to filter results."
     },
     "sort_by": {
      "type": "string",
      "description": "Sort the branches by sort_field. If not provided, branches will be sorted by updated_at descending order",
      "enum": [
       "name",
       "created_at",
       "updated_at"
      ]
     },
     "cursor": {
      "type": "string",
      "description": "A cursor to use in pagination. A cursor defines your place in the data list. Include `response.pagination.next` in subsequent API calls to fetch next page of the list."
     },
     "sort_order": {
      "type": "string",
      "description": "Defines the sorting order of entities.",
      "enum": [
       "asc",
       "desc"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of records to be returned in the response"
     },
     "include_deleted": {
      "type": "boolean",
      "description": "If true, return recoverable deleted branches too (soft-deleted within the recovery window). If false or not provided, return only active (non-deleted) branches. This parameter is part of the Branch Recovery feature, which is in preview and not available to all users."
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches",
    "path": [
     "project_id"
    ],
    "query": [
     "search",
     "sort_by",
     "cursor",
     "sort_order",
     "limit",
     "include_deleted"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_endpoints",
   "name": "List compute endpoints",
   "description": "Retrieves a list of compute endpoints for the specified project. A compute endpoint is a Neon compute instance. For information about compute endpoints, see [Manage computes](https://neon.com/docs/manage/endpoints/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/endpoints",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_members",
   "name": "List org members and their project roles",
   "description": "Lists organization members and their per-project roles for an org-owned project. Returns 404 when the project is not org-owned, per-project role management is disabled, or the caller has no access. Callers with VIEWER or EDITOR see members with effective project access. Callers with ADMIN also see unassigned org members.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": ""
     },
     "cursor": {
      "type": "string",
      "description": "A cursor to use in pagination. A cursor defines your place in the data list. Include `response.pagination.next` in subsequent API calls to fetch next page of the list."
     },
     "limit": {
      "type": "integer",
      "description": "The maximum number of members to return in the response"
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/members",
    "path": [
     "project_id"
    ],
    "query": [
     "cursor",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_operations",
   "name": "List operations",
   "description": "Retrieves a list of operations for the specified Neon project. The number of operations returned can be large. To paginate the response, issue an initial request with a `limit` value. Then, add the `cursor` value that was returned in the response to the next request. Operations older than 6 months may be deleted from our systems. If you need more history than that, you should store your own histor",
   "input": {
    "type": "object",
    "properties": {
     "cursor": {
      "type": "string",
      "description": "Specify the cursor value from the previous response to get the next batch of operations"
     },
     "limit": {
      "type": "integer",
      "description": "Specify a value from 1 to 1000 to limit number of operations in the response"
     },
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/operations",
    "path": [
     "project_id"
    ],
    "query": [
     "cursor",
     "limit"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_permissions",
   "name": "List project access",
   "description": "Retrieves details about users who have access to the project, including the permission `id`, the granted-to email address, and the date project access was granted.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": ""
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/permissions",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_project_vpcendpoints",
   "name": "List vpc endpoint restrictions",
   "description": "Lists VPC endpoint restrictions for the specified Neon project.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/vpc_endpoints",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_projects",
   "name": "List projects",
   "description": "Retrieves a list of projects for the specified organization. If using a personal API key, include the `org_id` parameter to specify which organization to work with. If using an org API key, `org_id` is automatically inferred from the key. For more information, see [Manage organizations using the Neon API](https://neon.com/docs/manage/orgs-api) and [Manage projects](https://neon.com/docs/manage/pro",
   "input": {
    "type": "object",
    "properties": {
     "cursor": {
      "type": "string",
      "description": "Specify the cursor value from the previous response to retrieve the next batch of projects."
     },
     "limit": {
      "type": "integer",
      "description": "Specify a value from 1 to 400 to limit number of projects in the response."
     },
     "search": {
      "type": "string",
      "description": "Search by project `name` or `id`. You can specify partial `name` or `id` values to filter results."
     },
     "org_id": {
      "type": "string",
      "description": "Search for projects by `org_id`."
     },
     "timeout": {
      "type": "integer",
      "description": "Specify an explicit timeout in milliseconds to limit response delay. After timing out, the incomplete list of project data fetched so far will be returned. Projects still being fetched when the timeout occurred are listed in the \"unavailable\" attribute of the response. If not specified, an implicit implementation defined timeout is chosen with the same behaviour as above"
     },
     "recoverable": {
      "type": "boolean",
      "description": "Show only deleted projects within the recovery window."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects",
    "path": [],
    "query": [
     "cursor",
     "limit",
     "search",
     "org_id",
     "timeout",
     "recoverable"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_shared_projects",
   "name": "List shared projects",
   "description": "Retrieves a list of projects shared with your Neon account. For more information, see [Manage projects](https://neon.com/docs/manage/projects/).",
   "input": {
    "type": "object",
    "properties": {
     "cursor": {
      "type": "string",
      "description": "Specify the cursor value from the previous response to get the next batch of projects."
     },
     "limit": {
      "type": "integer",
      "description": "Specify a value from 1 to 400 to limit number of projects in the response."
     },
     "search": {
      "type": "string",
      "description": "Search query by name or id."
     },
     "timeout": {
      "type": "integer",
      "description": "Specify an explicit timeout in milliseconds to limit response delay. After timing out, the incomplete list of project data fetched so far will be returned. Projects still being fetched when the timeout occurred are listed in the \"unavailable\" attribute of the response. If not specified, an implicit implementation defined timeout is chosen with the same behaviour as above"
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/shared",
    "path": [],
    "query": [
     "cursor",
     "limit",
     "search",
     "timeout"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.list_snapshots",
   "name": "List project snapshots",
   "description": "Lists the snapshots for the specified project. Each snapshot represents a point-in-time backup of the project data.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/snapshots",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.presign_project_branch_bucket_object",
   "name": "Presign an upload or download for an object in a bucket",
   "description": "Returns a presigned URL that transfers bytes directly to or from the object's bucket on the specified branch, without the caller ever handling S3 credentials. The `operation` field selects the direction: - `upload` returns a presigned `PUT` URL (the caller `PUT`s the file bytes straight to `url` with the returned `headers`). Authorized with project write access. - `download` returns a presigned `G",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "bucket_name": {
      "type": "string",
      "description": "The bucket name"
     },
     "object_key": {
      "type": "string",
      "description": "The object key. Keys may contain `/`; the `/` characters of nested keys must be percent-encoded (`%2F`) in the path segment."
     },
     "operation": {
      "type": "string",
      "description": "The transfer direction. `upload` returns a presigned `PUT` URL; `download` returns a presigned `GET` URL.",
      "enum": [
       "upload",
       "download"
      ]
     },
     "content_type": {
      "type": "string",
      "description": "The `Content-Type` to bind into the signed request. Only meaningful for `upload`: when set, the caller MUST send the same `Content-Type` header on the `PUT`, and the value is echoed back in the response `headers`. Ignored for `download`."
     },
     "expires_in_seconds": {
      "type": "integer",
      "description": "How long the presigned URL stays valid, in seconds. Defaults to 900 (15 minutes); capped at 604800 (7 days)."
     }
    },
    "required": [
     "branch_id",
     "bucket_name",
     "object_key",
     "operation",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/buckets/{bucket_name}/objects/{object_key}/presign",
    "path": [
     "project_id",
     "branch_id",
     "bucket_name",
     "object_key"
    ],
    "query": [],
    "header": [],
    "body": [
     "operation",
     "content_type",
     "expires_in_seconds"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.query_project_branch_logs",
   "name": "Query branch logs",
   "description": "Returns logs emitted by services running on the specified branch, ordered by timestamp according to `sort_order`. All supplied filters are combined with `AND`: a record is returned only when it matches every filter. `minimum_severity` and `severity_text` are independent filters, so setting both requires a record to clear the severity floor *and* match the exact severity text. Supply `logql` instea",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "since": {
      "type": "string",
      "description": "Length of the query window, ending at `end_time` or at the current time when `end_time` is omitted. Mutually exclusive with `start_time`. Prefer this over computing absolute bounds when the caller only means \"the last hour\"."
     },
     "start_time": {
      "type": "string",
      "description": "Inclusive beginning of the query window. Mutually exclusive with `since`. Defaults to one hour before `end_time`, or one hour before the current time when both bounds are omitted."
     },
     "end_time": {
      "type": "string",
      "description": "Exclusive end of the query window. Defaults to the current time."
     },
     "limit": {
      "type": "integer",
      "description": "Maximum number of log records to return per page."
     },
     "cursor": {
      "type": "string",
      "description": "Opaque pagination cursor returned as `next_cursor` by a previous call. Resume the query after the last record of the previous page, repeating the time range and every filter unchanged."
     },
     "sort_order": {
      "type": "string",
      "description": "Order matching records by timestamp. `desc`, the default, returns the newest records first.",
      "enum": [
       "asc",
       "desc"
      ]
     },
     "source": {
      "type": "string",
      "description": "The Neon service that emitted the log record.",
      "enum": [
       "function",
       "storage",
       "pg_endpoint"
      ]
     },
     "service_name": {
      "type": "string",
      "description": "Match the OpenTelemetry `service.name` resource attribute exactly."
     },
     "scope_name": {
      "type": "string",
      "description": "Match the OpenTelemetry instrumentation scope name exactly."
     },
     "minimum_severity": {
      "type": "string",
      "description": "An OpenTelemetry severity level. A minimum severity includes every higher level in this order: `trace`, `debug`, `info`, `warn`, `error`, `fatal`.",
      "enum": [
       "trace",
       "debug",
       "info",
       "warn",
       "error",
       "fatal"
      ]
     },
     "severity_text": {
      "type": "string",
      "description": "Match the OpenTelemetry severity text exactly."
     },
     "body_contains": {
      "type": "string",
      "description": "Match records whose rendered `message` contains this case-sensitive substring. Records with a structured body are matched against their JSON rendering, so the substring meets JSON syntax rather than prose: a bare key name such as `operation` matches every record carrying that key, and `http_status: 200` matches none, because the rendering contains `\"http_status\":200` with no space."
     },
     "trace_id": {
      "type": "string",
      "description": "Match records associated with this OpenTelemetry trace ID. W3C Trace Context defines a trace ID as 32 lowercase hex digits, and that is what is stored, so an uppercase value is rejected rather than silently matching nothing."
     },
     "logql": {
      "type": "string",
      "description": "Escape hatch for selections the structured filters cannot express: a raw LogQL expression, evaluated against this branch's log stream. Only stream selectors and line filters are accepted \u2014 no aggregations and no parser stages. Supplying this alongside any structured filter is rejected with `conflicting_filters` rather than silently ignoring one of them. `limit`, `sort_order`, and the time window s"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/logs/query",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "since",
     "start_time",
     "end_time",
     "limit",
     "cursor",
     "sort_order",
     "source",
     "service_name",
     "scope_name",
     "minimum_severity",
     "severity_text",
     "body_contains",
     "trace_id",
     "logql"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.recover_project",
   "name": "Recover a deleted project",
   "description": "Recovers a deleted project within the 7-day deletion recovery period. Restores branches, endpoints, settings, and connection strings. Some integrations require manual reconfiguration after recovery. To list recoverable projects, use `GET /projects?recoverable=true`.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     }
    },
    "required": [
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/recover",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.register_project_branch_custom_domain",
   "name": "Register a custom domain on a branch",
   "description": "Registers a customer-owned domain (for example `dashboard.acme.com`) on the branch and points it at a target entity, chosen by `entity_type` + `entity_id`. In v1 only `entity_type: function` is supported (an unsupported type is rejected with `400`), where `entity_id` is the function slug and the function must already exist on the branch (else `404`). The response includes the `cname_target` the cu",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "domain": {
      "type": "string",
      "description": "The custom domain to register (for example `dashboard.acme.com`). Case-insensitive; normalized to lowercase (a trailing root dot is stripped, so the 254-char bound admits a fully-qualified name whose normalized form is 253 chars). Neon-managed and internal hostnames are rejected."
     },
     "entity_type": {
      "type": "string",
      "description": "The kind of branch entity to point the domain at. v1 supports only `function`; any other value is rejected with `invalid_entity_type`."
     },
     "entity_id": {
      "type": "string",
      "description": "The target entity's identifier within the branch. For `function` this is the function slug (which must already exist on the branch)."
     }
    },
    "required": [
     "branch_id",
     "domain",
     "entity_id",
     "entity_type",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/custom-domains",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "domain",
     "entity_type",
     "entity_id"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.remove_organization_member",
   "name": "Remove organization member",
   "description": "Removes the specified member from the organization. Only organization admins can perform this action. The last admin in an organization cannot be removed.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "member_id": {
      "type": "string",
      "description": "The Neon organization member ID"
     }
    },
    "required": [
     "member_id",
     "org_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/members/{member_id}",
    "path": [
     "org_id",
     "member_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.remove_project_member_role",
   "name": "Remove an org member's role on a project",
   "description": "Idempotently removes the explicit project grant. The member's organization-role default project permission still applies. Self-DELETE requires `confirm_self_lockout=true` when effective manage access would be lost.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": ""
     },
     "member_id": {
      "type": "string",
      "description": ""
     },
     "confirm_self_lockout": {
      "type": "boolean",
      "description": ""
     }
    },
    "required": [
     "member_id",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/members/{member_id}/role",
    "path": [
     "project_id",
     "member_id"
    ],
    "query": [
     "confirm_self_lockout"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.reset_project_branch_role_password",
   "name": "Reset role password",
   "description": "Resets the password for the specified Postgres role. Returns a new password and operations. The new password is ready to use when the last operation finishes. The old password remains valid until last operation finishes. Connections to the compute endpoint are dropped. If idle, the compute endpoint becomes active for a short period of time. For related information, see [Manage roles](https://neon.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "role_name": {
      "type": "string",
      "description": "The role name"
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "role_name"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/roles/{role_name}/reset_password",
    "path": [
     "project_id",
     "branch_id",
     "role_name"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.restart_project_endpoint",
   "name": "Restart compute endpoint",
   "description": "Restarts the specified compute endpoint by immediately suspending it and then starting it again. An `endpoint_id` has an `ep-` prefix. For information about compute endpoints, see [Manage computes](https://neon.com/docs/manage/endpoints/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "endpoint_id": {
      "type": "string",
      "description": "The endpoint ID"
     }
    },
    "required": [
     "endpoint_id",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/endpoints/{endpoint_id}/restart",
    "path": [
     "project_id",
     "endpoint_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.restore_project_branch",
   "name": "Restore branch to a historical state",
   "description": "Restores a branch to an earlier state in its own or another branch's history by specifying an LSN or timestamp. Creates a new branch from the historical state.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "source_branch_id": {
      "type": "string",
      "description": "The `branch_id` of the restore source branch. If `source_timestamp` and `source_lsn` are omitted, the branch will be restored to head. If `source_branch_id` is equal to the branch's id, `source_timestamp` or `source_lsn` is required."
     },
     "source_lsn": {
      "type": "string",
      "description": "A Postgres LSN (for example, `0/1A2B3C4`) on the source branch to restore from. Mutually exclusive with `source_timestamp`. Omit both to restore to head."
     },
     "source_timestamp": {
      "type": "string",
      "description": "A point in time on the source branch to restore from, in RFC 3339 format. When omitted alongside `source_lsn`, the branch is restored to the latest available state of the source branch."
     },
     "preserve_under_name": {
      "type": "string",
      "description": "Name under which to save the current branch state before restoring. Required when the branch has children or when `source_branch_id` equals the branch being restored; in those cases all existing child branches are moved to the newly created branch. If omitted and not required, the previous state is not preserved."
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "source_branch_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/restore",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "source_branch_id",
     "source_lsn",
     "source_timestamp",
     "preserve_under_name"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.restore_snapshot",
   "name": "Restore snapshot",
   "description": "Restores the specified snapshot to a new branch, and optionally finalizes the restore operation to replace the original branch.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "snapshot_id": {
      "type": "string",
      "description": "The snapshot ID"
     },
     "name": {
      "type": "string",
      "description": "A name for the newly restored branch. If not provided, the server generates a unique name for the branch automatically."
     },
     "target_branch_id": {
      "type": "string",
      "description": "ID of the branch to restore the snapshot into. Defaults to the snapshot's source branch (`snapshot.source_branch_id`); fails if that cannot be determined."
     },
     "finalize_restore": {
      "type": "boolean",
      "description": "Set to `true` to finalize the restore operation immediately. This will complete the restore and move any associated computes to the new branch, similar to the `finalizeRestoreBranch` operation. Defaults to `false` to allow previewing the restored snapshot data first."
     }
    },
    "required": [
     "project_id",
     "snapshot_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/snapshots/{snapshot_id}/restore",
    "path": [
     "project_id",
     "snapshot_id"
    ],
    "query": [
     "name"
    ],
    "header": [],
    "body": [
     "name",
     "target_branch_id",
     "finalize_restore"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.reveal_credential",
   "name": "Reveal a credential's secrets",
   "description": "Returns the live `api_token` and `s3_secret_access_key` of an existing credential, so a credential whose issuance response was lost can be recovered without minting a new one. This is a POST with an explicit `/reveal` verb so the secrets never ride a GET, where they would land in access logs, browser history and proxy caches. Revoked and expired credentials return 404, as does a `token_id` that do",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "token_id": {
      "type": "string",
      "description": "The opaque credential id (e.g. nak_live_)."
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "token_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/credentials/{token_id}/reveal",
    "path": [
     "project_id",
     "branch_id",
     "token_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.revoke_api_key",
   "name": "Revoke api key",
   "description": "Revokes the specified API key. An API key that is no longer needed can be revoked. This action cannot be reversed. API keys can also be managed in the Neon Console. See [Manage API keys](https://neon.com/docs/manage/api-keys/).",
   "input": {
    "type": "object",
    "properties": {
     "key_id": {
      "type": "integer",
      "description": "The API key ID"
     }
    },
    "required": [
     "key_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/api_keys/{key_id}",
    "path": [
     "key_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.revoke_credential",
   "name": "Revoke a credential",
   "description": "Soft-deletes the credential. Idempotent. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "token_id": {
      "type": "string",
      "description": "The opaque credential id (e.g. nak_live_)."
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "token_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/credentials/{token_id}",
    "path": [
     "project_id",
     "branch_id",
     "token_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.revoke_org_api_key",
   "name": "Revoke organization api key",
   "description": "Revokes the specified organization API key. An API key that is no longer needed can be revoked. This action cannot be reversed. API keys can also be managed in the Neon Console. See [Manage API keys](https://neon.com/docs/manage/api-keys/).",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "key_id": {
      "type": "integer",
      "description": "The API key ID"
     }
    },
    "required": [
     "key_id",
     "org_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/api_keys/{key_id}",
    "path": [
     "org_id",
     "key_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.revoke_permission_from_project",
   "name": "Revoke project access",
   "description": "Revokes project access from the user associated with the specified permission `id`. You can retrieve a user's permission `id` by listing project access.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": ""
     },
     "permission_id": {
      "type": "string",
      "description": ""
     }
    },
    "required": [
     "permission_id",
     "project_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/permissions/{permission_id}",
    "path": [
     "project_id",
     "permission_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.rotate_credential",
   "name": "Rotate a credential's secrets",
   "description": "Replaces the secret material on an existing scoped credential in place. `token_id` is preserved \u2014 it is the `AWS_ACCESS_KEY_ID` for S3-compatible clients, so the access key id your application already holds keeps working and only the secret changes. This is the analog of resetting a Postgres password, not of issuing a second credential. The response carries the new `api_token` and `s3_secret_acces",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "token_id": {
      "type": "string",
      "description": "The opaque credential id (e.g. nak_live_)."
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "token_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/credentials/{token_id}/rotate",
    "path": [
     "project_id",
     "branch_id",
     "token_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.send_neon_auth_email_provider_test",
   "name": "Send test email using the saved email provider",
   "description": "Sends a test email using the branch's already-saved custom SMTP configuration. Only the `recipient_email` is provided \u2014 the stored SMTP settings and password are used server-side, so the caller does not need to re-supply (or be able to read) the password. This avoids the GET response's masked password being sent back, which would fail SMTP authentication. Requires a configured custom SMTP provider",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "recipient_email": {
      "type": "string",
      "description": "The email address to send the test email to."
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "recipient_email"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/email_provider/test",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "recipient_email"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.set_default_project_branch",
   "name": "Set branch as default",
   "description": "Sets the specified branch as the project's default branch. The default designation is automatically removed from the previous default branch. For more information, see [Manage branches](https://neon.com/docs/manage/branches/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/set_as_default",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.set_organization_spending_limit",
   "name": "Set organization spending limit",
   "description": "Sets the monthly spending limit for the specified organization. To remove a previously configured limit, send a DELETE request to this endpoint. When a limit is configured, email notifications are sent at 80% and 100% of the limit. Computes are not suspended when the limit is reached. Available to organization admins on Launch and Scale plans only.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "spending_limit_cents": {
      "type": "integer",
      "description": "Monthly spending cap in cents. Must be positive. To remove a previously configured limit, send a DELETE request to the spending_limit endpoint \u2014 `0` and `null` are rejected here. The cap is alert-only: notifications fire at 80% and 100%, but computes are not suspended. Setting a cap below the period's already-accrued spend is permitted and will trigger the over-limit notification on the next worke"
     }
    },
    "required": [
     "org_id",
     "spending_limit_cents"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/billing/spending_limit",
    "path": [
     "org_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "spending_limit_cents"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.set_project_member_role",
   "name": "Set an org member's role on a project",
   "description": "Idempotently sets or updates the explicit project grant of the specified org member. Self-demotion requires `confirm_self_demotion=true`.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": ""
     },
     "member_id": {
      "type": "string",
      "description": ""
     },
     "confirm_self_demotion": {
      "type": "boolean",
      "description": ""
     },
     "role": {
      "type": "string",
      "description": "Per-project role. `viewer` maps to `VIEWER`, `editor` maps to `EDITOR`, and `admin` maps to `ADMIN`.",
      "enum": [
       "viewer",
       "editor",
       "admin"
      ]
     }
    },
    "required": [
     "member_id",
     "project_id",
     "role"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/members/{member_id}/role",
    "path": [
     "project_id",
     "member_id"
    ],
    "query": [
     "confirm_self_demotion"
    ],
    "header": [],
    "body": [
     "role"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.set_snapshot_schedule",
   "name": "Update backup schedule",
   "description": "Updates the backup schedule for the specified branch. The schedule defines how often automatic snapshots are created (for example, `daily` or `weekly`). Requires a paid plan.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "schedule": {
      "type": "array",
      "description": "List of schedule entries defining the backup frequency. At least one entry is required."
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "schedule"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/backup_schedule",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "schedule"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.start_anonymization",
   "name": "Start anonymization",
   "description": "Starts the anonymization process for an anonymized branch that is in the initialized, error, or anonymized state. This will apply all defined masking rules to anonymize sensitive data in the branch databases. The branch must be an anonymized branch to start anonymization. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/anonymize",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.start_project_endpoint",
   "name": "Start compute endpoint",
   "description": "Starts a compute endpoint. The compute endpoint is ready to use after the last operation in the chain finishes successfully. An `endpoint_id` has an `ep-` prefix. For information about compute endpoints, see [Manage computes](https://neon.com/docs/manage/endpoints/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "endpoint_id": {
      "type": "string",
      "description": "The endpoint ID"
     }
    },
    "required": [
     "endpoint_id",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/endpoints/{endpoint_id}/start",
    "path": [
     "project_id",
     "endpoint_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.suspend_project_endpoint",
   "name": "Suspend compute endpoint",
   "description": "Suspends the specified compute endpoint. An `endpoint_id` has an `ep-` prefix. For information about compute endpoints, see [Manage computes](https://neon.com/docs/manage/endpoints/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "endpoint_id": {
      "type": "string",
      "description": "The endpoint ID"
     }
    },
    "required": [
     "endpoint_id",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/endpoints/{endpoint_id}/suspend",
    "path": [
     "project_id",
     "endpoint_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.transfer_neon_auth_provider_project",
   "name": "Transfer neon managed auth project to your own account",
   "description": "Transfers ownership of your Neon-managed auth project to your own auth provider account.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID. Returned as `id` from `GET /projects`."
     },
     "auth_provider": {
      "type": "string",
      "description": "Authentication provider integrated with this Neon Auth configuration. `better_auth` integrates with Better Auth (the current, recommended provider). `stack` integrates with Stack Auth (deprecated). `mock` is a simulated provider for local development and testing only.",
      "enum": [
       "mock",
       "stack",
       "better_auth"
      ]
     }
    },
    "required": [
     "auth_provider",
     "project_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/projects/auth/transfer_ownership",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "project_id",
     "auth_provider"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.transfer_projects_from_org_to_org",
   "name": "Transfer projects between organizations",
   "description": "Transfers selected projects, identified by their IDs, from your organization to another specified organization.",
   "input": {
    "type": "object",
    "properties": {
     "source_org_id": {
      "type": "string",
      "description": "The Neon organization ID (source org, which currently owns the project)"
     },
     "destination_org_id": {
      "type": "string",
      "description": "The destination organization identifier"
     },
     "project_ids": {
      "type": "array",
      "description": "The list of projects ids to transfer. Maximum of 400 project ids"
     }
    },
    "required": [
     "destination_org_id",
     "project_ids",
     "source_org_id"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://console.neon.tech/api/v2/organizations/{source_org_id}/projects/transfer",
    "path": [
     "source_org_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "destination_org_id",
     "project_ids"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_branch_neon_auth_oauth_provider",
   "name": "Update oauth provider",
   "description": "Updates an OAuth provider for the specified project.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "oauth_provider_id": {
      "type": "string",
      "description": "The OAuth provider ID",
      "enum": [
       "google",
       "github",
       "microsoft",
       "vercel"
      ]
     },
     "client_id": {
      "type": "string",
      "description": "The OAuth client ID registered with the provider. Omit to keep the currently configured value."
     },
     "client_secret": {
      "type": "string",
      "description": "OAuth client secret for the provider. Omit to leave the existing secret unchanged."
     },
     "microsoft_tenant_id": {
      "type": "string",
      "description": "The tenant ID scoping the Microsoft OAuth provider. Supply this field when the provider type is microsoft; it has no effect for other provider types."
     }
    },
    "required": [
     "branch_id",
     "oauth_provider_id",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/oauth_providers/{oauth_provider_id}",
    "path": [
     "project_id",
     "branch_id",
     "oauth_provider_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "client_id",
     "client_secret",
     "microsoft_tenant_id"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_masking_rules",
   "name": "Update masking rules",
   "description": "Updates the masking rules for the specified anonymized branch. Masking rules define how sensitive data should be anonymized using PostgreSQL Anonymizer. **Note**: This endpoint is currently in Beta.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "masking_rules": {
      "type": "array",
      "description": "List of masking rules to apply to the branch. This will replace all existing masking rules for the branch."
     }
    },
    "required": [
     "branch_id",
     "masking_rules",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/masking_rules",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "masking_rules"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_neon_auth_allow_localhost",
   "name": "Update localhost allow setting",
   "description": "Updates the localhost allow setting for the specified branch's Neon Auth integration. When enabled, authentication flows work from `localhost` without adding it to the redirect URI whitelist.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "allow_localhost": {
      "type": "boolean",
      "description": "Whether to allow localhost connections"
     }
    },
    "required": [
     "allow_localhost",
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/allow_localhost",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "allow_localhost"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_neon_auth_config",
   "name": "Update auth configuration",
   "description": "Updates the auth configuration for the branch. Currently supports updating the application name used in auth emails.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "name": {
      "type": "string",
      "description": "The application name used in auth emails and communications."
     }
    },
    "required": [
     "branch_id",
     "name",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/config",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "name"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_neon_auth_email_and_password_config",
   "name": "Update email and password configuration",
   "description": "Updates the email and password authentication configuration for the specified branch's Neon Auth integration. Only the fields provided in the request body are updated.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "enabled": {
      "type": "boolean",
      "description": "Controls whether email and password authentication is enabled for this project. When omitted from an update request, the current value is unchanged."
     },
     "email_verification_method": {
      "type": "string",
      "description": "The email verification method to use. - `link`: Sends a verification link via email - `otp`: Sends a one-time password (OTP) via email",
      "enum": [
       "link",
       "otp"
      ]
     },
     "require_email_verification": {
      "type": "boolean",
      "description": "When true, users must verify their email address before they can sign in. Omitting this field from an update request leaves the current value unchanged."
     },
     "auto_sign_in_after_verification": {
      "type": "boolean",
      "description": "Whether users are automatically signed in after verifying their email"
     },
     "send_verification_email_on_sign_up": {
      "type": "boolean",
      "description": "Whether to send a verification email when users sign up."
     },
     "send_verification_email_on_sign_in": {
      "type": "boolean",
      "description": "Whether to send a verification email when a user with an unverified email signs in."
     },
     "disable_sign_up": {
      "type": "boolean",
      "description": "Whether to disable new user sign ups. When omitted, the current setting is not changed."
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/email_and_password",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "enabled",
     "email_verification_method",
     "require_email_verification",
     "auto_sign_in_after_verification",
     "send_verification_email_on_sign_up",
     "send_verification_email_on_sign_in",
     "disable_sign_up"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_neon_auth_email_provider",
   "name": "Update email provider configuration",
   "description": "Updates the email provider configuration for the specified branch's Neon Auth integration. The email provider handles transactional messages such as verification emails and password reset links. Partial `standard` updates \u2014 omitting fields to keep their stored values \u2014 are supported only for Better Auth integrations, which merge omitted fields server-side. Legacy Stack Auth integrations do not mer",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/email_provider",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_neon_auth_magic_link_plugin",
   "name": "Update magic link plugin configuration",
   "description": "Updates the magic link plugin configuration for Neon Auth. The magic link plugin enables passwordless authentication via email magic links.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "enabled": {
      "type": "boolean",
      "description": "Whether to enable the magic link plugin."
     },
     "expires_in": {
      "type": "integer",
      "description": "Minutes until the magic link expires."
     },
     "disable_sign_up": {
      "type": "boolean",
      "description": "When true, sign-up via magic link is disabled."
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/plugins/magic-link",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "enabled",
     "expires_in",
     "disable_sign_up"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_neon_auth_organization_plugin",
   "name": "Update organization plugin configuration",
   "description": "Updates the organization plugin configuration for Neon Auth. The organization plugin enables multi-tenant organization support.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "enabled": {
      "type": "boolean",
      "description": "Controls whether the organization plugin is active for the organization."
     },
     "organization_limit": {
      "type": "integer",
      "description": "Maximum organizations a user can belong to (created or joined). At the limit, the user cannot create or join more."
     },
     "membership_limit": {
      "type": "integer",
      "description": "Maximum members per organization."
     },
     "creator_role": {
      "type": "string",
      "description": "Role of the organization's creator. `owner`: full control, including deleting the org and transferring ownership. `admin`: manage members and settings only.",
      "enum": [
       "admin",
       "owner"
      ]
     },
     "send_invitation_email": {
      "type": "boolean",
      "description": "When true, invited users receive an email containing an accept link. Requires that the invited user has a verified email address."
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/plugins/organization",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "enabled",
     "organization_limit",
     "membership_limit",
     "creator_role",
     "send_invitation_email"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_neon_auth_phone_number_plugin",
   "name": "Update phone number plugin configuration",
   "description": "Updates the phone number plugin configuration for Neon Auth. Only the fields provided in the request body are updated; omitted fields retain their current values. The phone number plugin enables phone-based OTP authentication. OTP codes are delivered via the `send.otp` webhook event with `delivery_preference: \"sms\"`. A webhook must be configured with the `send.otp` event enabled for SMS delivery t",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "enabled": {
      "type": "boolean",
      "description": "Whether the phone number plugin is enabled."
     },
     "otp_expires_in": {
      "type": "integer",
      "description": "Time in seconds before the OTP expires"
     }
    },
    "required": [
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/plugins/phone-number",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "enabled",
     "otp_expires_in"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_neon_auth_user_role",
   "name": "Update auth user role",
   "description": "Updates the role of a user in the Neon Auth user directory for the specified branch. The role controls the user's level of access within the Neon Auth integration.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "auth_user_id": {
      "type": "string",
      "description": "The Neon user ID"
     },
     "roles": {
      "type": "array",
      "description": "Roles to assign to the user in the Neon Auth (Better Auth) directory. `user` and `admin` are the built-in roles; custom role strings are also supported."
     }
    },
    "required": [
     "auth_user_id",
     "branch_id",
     "project_id",
     "roles"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/users/{auth_user_id}/role",
    "path": [
     "project_id",
     "branch_id",
     "auth_user_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "roles"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_neon_auth_webhook_config",
   "name": "Update neon auth webhook configuration",
   "description": "Updates the webhook configuration for the specified branch's Neon Auth integration. Webhooks notify an external endpoint when auth events occur, such as user creation or sign-in.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "enabled": {
      "type": "boolean",
      "description": "Whether the webhook is active."
     },
     "webhook_url": {
      "type": "string",
      "description": "Destination URL that receives webhook event payloads."
     },
     "enabled_events": {
      "type": "array",
      "description": "Event types that trigger this webhook. Covers user lifecycle, email/OTP delivery, organization invitations, and phone verification events; see the enum for exact values."
     },
     "timeout_seconds": {
      "type": "integer",
      "description": "Maximum time, in seconds, to wait for a response from the webhook endpoint."
     }
    },
    "required": [
     "branch_id",
     "enabled",
     "project_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/auth/webhooks",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "enabled",
     "webhook_url",
     "enabled_events",
     "timeout_seconds"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_organization_member",
   "name": "Update role for organization member",
   "description": "Updates the role of an existing member in the specified organization. The requested role must be valid for the organization. Only organization admins can call this endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "org_id": {
      "type": "string",
      "description": "The Neon organization ID"
     },
     "member_id": {
      "type": "string",
      "description": "The Neon organization member ID"
     },
     "role": {
      "type": "string",
      "description": "Organization member's role. `admin`: full administrative access. `editor` (and its legacy alias `member`): standard access governed by project permissions. `viewer` and `collaborator`: additional scoped project roles. Some values may not be available for all organizations.",
      "enum": [
       "admin",
       "member",
       "editor",
       "viewer",
       "collaborator"
      ]
     }
    },
    "required": [
     "member_id",
     "org_id",
     "role"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/organizations/{org_id}/members/{member_id}",
    "path": [
     "org_id",
     "member_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "role"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_project",
   "name": "Update project",
   "description": "Updates the specified project. Configurable properties include the project name, default compute settings, history retention period, and IP allowlist.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "project": {
      "type": "object",
      "description": ""
     }
    },
    "required": [
     "project",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}",
    "path": [
     "project_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "project"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_project_branch",
   "name": "Update branch",
   "description": "Updates the specified branch. For more information, see [Manage branches](https://neon.com/docs/manage/branches/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "branch": {
      "type": "object",
      "description": "Branch attributes to update. Supply only the fields you want to change, for example `name` or `protected`."
     }
    },
    "required": [
     "branch",
     "branch_id",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}",
    "path": [
     "project_id",
     "branch_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "branch"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_project_branch_data_api",
   "name": "Update neon data api",
   "description": "Updates the Neon Data API configuration for the specified branch. You can optionally provide settings to update the Data API configuration. The schema cache is always refreshed as part of this operation.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "database_name": {
      "type": "string",
      "description": "The database name"
     },
     "settings": {
      "type": "object",
      "description": "Configuration settings for the Neon Data API"
     }
    },
    "required": [
     "branch_id",
     "database_name",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/data-api/{database_name}",
    "path": [
     "project_id",
     "branch_id",
     "database_name"
    ],
    "query": [],
    "header": [],
    "body": [
     "settings"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_project_branch_database",
   "name": "Update database",
   "description": "Updates the specified database in the branch. For related information, see [Manage databases](https://neon.com/docs/manage/databases/).",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The branch ID"
     },
     "database_name": {
      "type": "string",
      "description": "The database name"
     },
     "database": {
      "type": "object",
      "description": "Properties to update on the database."
     }
    },
    "required": [
     "branch_id",
     "database",
     "database_name",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/databases/{database_name}",
    "path": [
     "project_id",
     "branch_id",
     "database_name"
    ],
    "query": [],
    "header": [],
    "body": [
     "database"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_project_branch_function",
   "name": "Update a function",
   "description": "Updates the function's mutable metadata \u2014 currently only the display `name`. A string sets the display name; `null` clears it, after which the function's `name` falls back to its slug. Leading and trailing whitespace is trimmed; a whitespace-only name is rejected. Acts only on a function owned by the branch: a slug that is only inherited from an ancestor branch returns 404 \u2014 rename it on the branc",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "slug": {
      "type": "string",
      "description": "The function slug"
     },
     "name": {
      "type": "string",
      "description": "New display name for the function. `null` clears the display name; the function's `name` then falls back to its slug. Leading and trailing whitespace is trimmed; a whitespace-only name is rejected."
     }
    },
    "required": [
     "branch_id",
     "name",
     "project_id",
     "slug"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/functions/{slug}",
    "path": [
     "project_id",
     "branch_id",
     "slug"
    ],
    "query": [],
    "header": [],
    "body": [
     "name"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_project_branch_trigger",
   "name": "Update a trigger",
   "description": "Applies a partial update. The required `type` discriminator must identify the existing trigger kind. The supported types are `schedule` and `storage_object_created`. Editing an inherited trigger creates a child-local shadow with the same `trigger_id`; it remains disabled unless this request explicitly enables it. For a schedule trigger, updating the schedule or enabled state increments `version` a",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "branch_id": {
      "type": "string",
      "description": "The Neon branch ID"
     },
     "trigger_id": {
      "type": "string",
      "description": "The opaque, project-wide trigger ID"
     }
    },
    "required": [
     "branch_id",
     "project_id",
     "trigger_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/branches/{branch_id}/triggers/{trigger_id}",
    "path": [
     "project_id",
     "branch_id",
     "trigger_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_project_endpoint",
   "name": "Update compute endpoint",
   "description": "Updates the specified compute endpoint. An `endpoint_id` has an `ep-` prefix. A `branch_id` has a `br-` prefix. For more information about compute endpoints, see [Manage computes](https://neon.com/docs/manage/endpoints/). If the returned list of operations is not empty, the compute endpoint is not ready to use. The client must wait for the last operation to finish before using the compute endpoint",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "endpoint_id": {
      "type": "string",
      "description": "The endpoint ID"
     },
     "endpoint": {
      "type": "object",
      "description": "Parameters for the compute endpoint update."
     }
    },
    "required": [
     "endpoint",
     "endpoint_id",
     "project_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/endpoints/{endpoint_id}",
    "path": [
     "project_id",
     "endpoint_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "endpoint"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "neon.update_snapshot",
   "name": "Update snapshot",
   "description": "Updates the specified snapshot.",
   "input": {
    "type": "object",
    "properties": {
     "project_id": {
      "type": "string",
      "description": "The Neon project ID"
     },
     "snapshot_id": {
      "type": "string",
      "description": "The snapshot ID"
     },
     "snapshot": {
      "type": "object",
      "description": "Fields to update on the snapshot. Updatable fields include `name` and `expires_at`."
     }
    },
    "required": [
     "project_id",
     "snapshot",
     "snapshot_id"
    ]
   },
   "request": {
    "method": "PATCH",
    "url": "https://console.neon.tech/api/v2/projects/{project_id}/snapshots/{snapshot_id}",
    "path": [
     "project_id",
     "snapshot_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "snapshot"
    ],
    "body_format": "json"
   }
  }
 ]
}
