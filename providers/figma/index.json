{
 "_": "GENERATED \u2014 what an authoring tool needs to FIND an operation. The calling table is the .lua beside it.",
 "provider": "figma",
 "source": "https://raw.githubusercontent.com/figma/rest-api-spec/main/openapi/openapi.yaml",
 "operations": [
  {
   "slug": "figma.delete_comment",
   "name": "Delete a comment",
   "description": "Deletes a specific comment. Only the person who made the comment is allowed to delete it.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to delete comment from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     },
     "comment_id": {
      "type": "string",
      "description": "Comment id of comment to delete"
     }
    },
    "required": [
     "comment_id",
     "file_key"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.figma.com/v1/files/{file_key}/comments/{comment_id}",
    "path": [
     "file_key",
     "comment_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.delete_comment_reaction",
   "name": "Delete a reaction",
   "description": "Deletes a specific comment reaction. Only the person who made the reaction is allowed to delete it.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to delete comment reaction from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     },
     "comment_id": {
      "type": "string",
      "description": "ID of comment to delete reaction from."
     },
     "emoji": {
      "type": "string",
      "description": "The emoji type of reaction as shortcode (e.g. `:heart:`, `:+1::skin-tone-2:`). The list of accepted emoji shortcodes can be found in [this file](https://raw.githubusercontent.com/missive/emoji-mart/main/packages/emoji-mart-data/sets/14/native.json) under the top-level emojis and aliases fields, with optional skin tone modifiers when applicable."
     }
    },
    "required": [
     "comment_id",
     "emoji",
     "file_key"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.figma.com/v1/files/{file_key}/comments/{comment_id}/reactions",
    "path": [
     "file_key",
     "comment_id"
    ],
    "query": [
     "emoji"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.delete_dev_resource",
   "name": "Delete dev resource",
   "description": "Delete a dev resource from a file",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "The file to delete the dev resource from. This must be a main file key, not a branch key."
     },
     "dev_resource_id": {
      "type": "string",
      "description": "The id of the dev resource to delete."
     }
    },
    "required": [
     "dev_resource_id",
     "file_key"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.figma.com/v1/files/{file_key}/dev_resources/{dev_resource_id}",
    "path": [
     "file_key",
     "dev_resource_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.delete_webhook",
   "name": "Delete a webhook",
   "description": "Deletes the specified webhook. This operation cannot be reversed.",
   "input": {
    "type": "object",
    "properties": {
     "webhook_id": {
      "type": "string",
      "description": "ID of webhook to delete"
     }
    },
    "required": [
     "webhook_id"
    ]
   },
   "request": {
    "method": "DELETE",
    "url": "https://api.figma.com/v2/webhooks/{webhook_id}",
    "path": [
     "webhook_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_activity_logs",
   "name": "Get activity logs",
   "description": "Returns a list of activity log events",
   "input": {
    "type": "object",
    "properties": {
     "events": {
      "type": "string",
      "description": "Event type(s) to include in the response. Can have multiple values separated by comma. All events are returned by default."
     },
     "start_time": {
      "type": "number",
      "description": "Unix timestamp of the least recent event to include. This param defaults to one year ago if unspecified."
     },
     "end_time": {
      "type": "number",
      "description": "Unix timestamp of the most recent event to include. This param defaults to the current timestamp if unspecified."
     },
     "limit": {
      "type": "number",
      "description": "Maximum number of events to return. This param defaults to 1000 if unspecified."
     },
     "order": {
      "type": "string",
      "description": "Event order by timestamp. This param can be either \"asc\" (default) or \"desc\".",
      "enum": [
       "asc",
       "desc"
      ]
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/activity_logs",
    "path": [],
    "query": [
     "events",
     "start_time",
     "end_time",
     "limit",
     "order"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_ai_usage_daily",
   "name": "Get daily ai credit usage",
   "description": "Returns per-user, per-day AI credit usage for the plan associated with the calling token. This endpoint requires a plan access token with the `org:ai_metering_usage_read` scope.",
   "input": {
    "type": "object",
    "properties": {
     "start_date": {
      "type": "string",
      "description": "The first day to include, inclusive, as a `YYYY-MM-DD` calendar date (UTC). Required. Must be on or after `2025-12-01` and no more than 366 days before the current UTC day."
     },
     "end_date": {
      "type": "string",
      "description": "The last day to include, inclusive, as a `YYYY-MM-DD` calendar date (UTC). Required. Must be on or after `start_date` and the current UTC day or earlier."
     },
     "user_email": {
      "type": "string",
      "description": "Restrict the results to a single Figma user, identified by email. When omitted, rows for every user in the plan with usage in the range are returned. An email that matches no Figma user returns a 400."
     },
     "limit": {
      "type": "integer",
      "description": "Maximum number of rows to return. This param defaults to 1000 if unspecified, and may not exceed 1000."
     },
     "cursor": {
      "type": "string",
      "description": "An opaque cursor returned from a previous request, used for pagination."
     }
    },
    "required": [
     "end_date",
     "start_date"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/ai_usage/daily",
    "path": [],
    "query": [
     "start_date",
     "end_date",
     "user_email",
     "limit",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_comment_reactions",
   "name": "Get reactions for a comment",
   "description": "Gets a paginated list of reactions left on the comment.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to get comment containing reactions from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     },
     "comment_id": {
      "type": "string",
      "description": "ID of comment to get reactions from."
     },
     "cursor": {
      "type": "string",
      "description": "Cursor for pagination, retrieved from the response of the previous call."
     }
    },
    "required": [
     "comment_id",
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/comments/{comment_id}/reactions",
    "path": [
     "file_key",
     "comment_id"
    ],
    "query": [
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_comments",
   "name": "Get comments in a file",
   "description": "Gets a list of comments left on the file.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to get comments from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     },
     "as_md": {
      "type": "boolean",
      "description": "If enabled, will return comments as their markdown equivalents when applicable."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/comments",
    "path": [
     "file_key"
    ],
    "query": [
     "as_md"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_component",
   "name": "Get component",
   "description": "Get metadata on a component by key.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/components/{key}",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_component_set",
   "name": "Get component set",
   "description": "Get metadata on a published component set by key.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/component_sets/{key}",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_dev_resources",
   "name": "Get dev resources",
   "description": "Get dev resources in a file",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "The file to get the dev resources from. This must be a main file key, not a branch key."
     },
     "node_ids": {
      "type": "string",
      "description": "Comma separated list of nodes that you care about in the document. If specified, only dev resources attached to these nodes will be returned. If not specified, all dev resources in the file will be returned."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/dev_resources",
    "path": [
     "file_key"
    ],
    "query": [
     "node_ids"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_developer_logs",
   "name": "Get developer logs",
   "description": "Returns a list of developer log entries for REST API and MCP server requests made within the organization. This endpoint requires a plan access token with the `org:developer_log_read` scope.",
   "input": {
    "type": "object",
    "properties": {
     "token_type": {
      "type": "string",
      "description": "Filter by the type of token used for authentication.",
      "enum": [
       "plan_access_token",
       "developer_token",
       "oauth_token"
      ]
     },
     "token_name": {
      "type": "string",
      "description": "Filter by token name prefix(es). Multiple values can be separated by commas."
     },
     "user_email": {
      "type": "string",
      "description": "Filter by user email prefix(es). Multiple values can be separated by commas."
     },
     "ip_address": {
      "type": "string",
      "description": "Filter by IP address prefix(es). Multiple values can be separated by commas."
     },
     "event_source": {
      "type": "string",
      "description": "Filter by event source.",
      "enum": [
       "rest_api",
       "mcp_server"
      ]
     },
     "date_range": {
      "type": "string",
      "description": "Filter by date range.",
      "enum": [
       "last_24h",
       "last_7d",
       "last_30d"
      ]
     },
     "limit": {
      "type": "integer",
      "description": "Maximum number of entries to return."
     },
     "cursor": {
      "type": "string",
      "description": "A cursor returned from a previous request, used for pagination."
     }
    },
    "required": []
   },
   "request": {
    "method": "POST",
    "url": "https://api.figma.com/v1/developer_logs",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "token_type",
     "token_name",
     "user_email",
     "ip_address",
     "event_source",
     "date_range",
     "limit",
     "cursor"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_file",
   "name": "Get file json",
   "description": "Returns the document identified by `file_key` as a JSON object. The file key can be parsed from any Figma file url: `https://www.figma.com/file/{file_key}/{title}`. The `document` property contains a node of type `DOCUMENT`. The `components` property contains a mapping from node IDs to component metadata. This is to help you determine which components each instance comes from.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to export JSON from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     },
     "version": {
      "type": "string",
      "description": "A specific version ID to get. Omitting this will get the current version of the file."
     },
     "ids": {
      "type": "string",
      "description": "Comma separated list of nodes that you care about in the document. If specified, only a subset of the document will be returned corresponding to the nodes listed, their children, and everything between the root node and the listed nodes. Note: There may be other nodes included in the returned JSON that are outside the ancestor chains of the desired nodes. The response may also include dependencies"
     },
     "depth": {
      "type": "number",
      "description": "Positive integer representing how deep into the document tree to traverse. For example, setting this to 1 returns only Pages, setting it to 2 returns Pages and all top level objects on each page. Not setting this parameter returns all nodes."
     },
     "geometry": {
      "type": "string",
      "description": "Set to \"paths\" to export vector data."
     },
     "plugin_data": {
      "type": "string",
      "description": "A comma separated list of plugin IDs and/or the string \"shared\". Any data present in the document written by those plugins will be included in the result in the `pluginData` and `sharedPluginData` properties."
     },
     "branch_data": {
      "type": "boolean",
      "description": "Returns branch metadata for the requested file. If the file is a branch, the main file's key will be included in the returned response. If the file has branches, their metadata will be included in the returned response. Default: false."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}",
    "path": [
     "file_key"
    ],
    "query": [
     "version",
     "ids",
     "depth",
     "geometry",
     "plugin_data",
     "branch_data"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_file_component_sets",
   "name": "Get file component sets",
   "description": "Get a list of published component sets within a file library.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to list component sets from. This must be a main file key, not a branch key, as it is not possible to publish from branches."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/component_sets",
    "path": [
     "file_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_file_components",
   "name": "Get file components",
   "description": "Get a list of published components within a file library.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to list components from. This must be a main file key, not a branch key, as it is not possible to publish from branches."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/components",
    "path": [
     "file_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_file_meta",
   "name": "Get file metadata",
   "description": "Get file metadata",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to get metadata for. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/meta",
    "path": [
     "file_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_file_nodes",
   "name": "Get file json for specific nodes",
   "description": "Returns the nodes referenced to by `ids` as a JSON object. The nodes are retrieved from the Figma file referenced to by `file_key`. The node ID and file key can be parsed from any Figma node url: `https://www.figma.com/file/{file_key}/{title}?node-id={id}` The `name`, `lastModified`, `thumbnailUrl`, `editorType`, and `version` attributes are all metadata of the specified file. The `linkAccess` fie",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to export JSON from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     },
     "ids": {
      "type": "string",
      "description": "A comma separated list of node IDs to retrieve and convert."
     },
     "version": {
      "type": "string",
      "description": "A specific version ID to get. Omitting this will get the current version of the file."
     },
     "depth": {
      "type": "number",
      "description": "Positive integer representing how deep into the node tree to traverse. For example, setting this to 1 will return only the children directly underneath the desired nodes. Not setting this parameter returns all nodes. Note: this parameter behaves differently from the same parameter in the `GET /v1/files/:key` endpoint. In this endpoint, the depth will be counted starting from the desired node rathe"
     },
     "geometry": {
      "type": "string",
      "description": "Set to \"paths\" to export vector data."
     },
     "plugin_data": {
      "type": "string",
      "description": "A comma separated list of plugin IDs and/or the string \"shared\". Any data present in the document written by those plugins will be included in the result in the `pluginData` and `sharedPluginData` properties."
     }
    },
    "required": [
     "file_key",
     "ids"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/nodes",
    "path": [
     "file_key"
    ],
    "query": [
     "ids",
     "version",
     "depth",
     "geometry",
     "plugin_data"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_file_styles",
   "name": "Get file styles",
   "description": "Get a list of published styles within a file library.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to list styles from. This must be a main file key, not a branch key, as it is not possible to publish from branches."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/styles",
    "path": [
     "file_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_file_versions",
   "name": "Get versions of a file",
   "description": "This endpoint fetches the version history of a file, allowing you to see the progression of a file over time. You can then use this information to render a specific version of the file, via another endpoint.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to get version history from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     },
     "page_size": {
      "type": "number",
      "description": "The number of items returned in a page of the response. If not included, `page_size` is `30`."
     },
     "before": {
      "type": "number",
      "description": "A version ID for one of the versions in the history. Gets versions before this ID. Used for paginating. If the response is not paginated, this link returns the same data in the current response."
     },
     "after": {
      "type": "number",
      "description": "A version ID for one of the versions in the history. Gets versions after this ID. Used for paginating. If the response is not paginated, this property is not included."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/versions",
    "path": [
     "file_key"
    ],
    "query": [
     "page_size",
     "before",
     "after"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_folder_files",
   "name": "Get files in a folder",
   "description": "Get a list of the files directly within the specified folder.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "ID of the folder to list files from"
     },
     "branch_data": {
      "type": "boolean",
      "description": "Returns branch metadata in the response for each main file with a branch inside the folder."
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v2/folders/{folder_id}/files",
    "path": [
     "folder_id"
    ],
    "query": [
     "branch_data"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_folder_folders",
   "name": "Get subfolders in a folder",
   "description": "Get a list of the direct subfolders within the specified folder.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "ID of the parent folder to list subfolders from"
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v2/folders/{folder_id}/folders",
    "path": [
     "folder_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_folder_meta",
   "name": "Get folder metadata",
   "description": "Get metadata for a folder (name, thumbnail, file count, timestamps) without enumerating its files.",
   "input": {
    "type": "object",
    "properties": {
     "folder_id": {
      "type": "string",
      "description": "ID of the folder to get metadata for"
     }
    },
    "required": [
     "folder_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v2/folders/{folder_id}/meta",
    "path": [
     "folder_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_image_fills",
   "name": "Get image fills",
   "description": "Returns download links for all images present in image fills in a document. Image fills are how Figma represents any user supplied images. When you drag an image into Figma, we create a rectangle with a single fill that represents the image, and the user is able to transform the rectangle (and properties on the fill) as they wish. This endpoint returns a mapping from image references to the URLs a",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to get image URLs from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/images",
    "path": [
     "file_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_images",
   "name": "Render images of file nodes",
   "description": "Renders images from a file. If no error occurs, `\"images\"` will be populated with a map from node IDs to URLs of the rendered images, and `\"status\"` will be omitted. The image assets will expire after 30 days. Images up to 32 megapixels can be exported. Any images that are larger will be scaled down. Important: the image map may contain values that are `null`. This indicates that rendering of that",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to export images from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     },
     "ids": {
      "type": "string",
      "description": "A comma separated list of node IDs to render."
     },
     "version": {
      "type": "string",
      "description": "A specific version ID to get. Omitting this will get the current version of the file."
     },
     "scale": {
      "type": "number",
      "description": "A number between 0.01 and 4, the image scaling factor."
     },
     "format": {
      "type": "string",
      "description": "A string enum for the image output format.",
      "enum": [
       "jpg",
       "png",
       "svg",
       "pdf"
      ]
     },
     "svg_outline_text": {
      "type": "boolean",
      "description": "Whether text elements are rendered as outlines (vector paths) or as `` elements in SVGs. Rendering text elements as outlines guarantees that the text looks exactly the same in the SVG as it does in the browser/inside Figma. Exporting as `` allows text to be selectable inside SVGs and generally makes the SVG easier to read. However, this relies on the browser's rendering engine which can vary betwe"
     },
     "svg_include_id": {
      "type": "boolean",
      "description": "Whether to include id attributes for all SVG elements. Adds the layer name to the `id` attribute of an svg element."
     },
     "svg_include_node_id": {
      "type": "boolean",
      "description": "Whether to include node id attributes for all SVG elements. Adds the node id to a `data-node-id` attribute of an svg element."
     },
     "svg_simplify_stroke": {
      "type": "boolean",
      "description": "Whether to simplify inside/outside strokes and use stroke attribute if possible instead of ``."
     },
     "contents_only": {
      "type": "boolean",
      "description": "Whether content that overlaps the node should be excluded from rendering. Passing false (i.e., rendering overlaps) may increase processing time, since more of the document must be included in rendering."
     },
     "use_absolute_bounds": {
      "type": "boolean",
      "description": "Use the full dimensions of the node regardless of whether or not it is cropped or the space around it is empty. Use this to export text nodes without cropping."
     }
    },
    "required": [
     "file_key",
     "ids"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/images/{file_key}",
    "path": [
     "file_key"
    ],
    "query": [
     "ids",
     "version",
     "scale",
     "format",
     "svg_outline_text",
     "svg_include_id",
     "svg_include_node_id",
     "svg_simplify_stroke",
     "contents_only",
     "use_absolute_bounds"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_library_analytics_component_actions",
   "name": "Get library analytics component action data",
   "description": "Returns a list of library analytics component actions data broken down by the requested dimension.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File key of the library to fetch analytics data for."
     },
     "cursor": {
      "type": "string",
      "description": "Cursor indicating what page of data to fetch. Obtained from prior API call."
     },
     "group_by": {
      "type": "string",
      "description": "A dimension to group returned analytics data by.",
      "enum": [
       "component",
       "team"
      ]
     },
     "start_date": {
      "type": "string",
      "description": "ISO 8601 date string (YYYY-MM-DD) of the earliest week to include. Dates are rounded back to the nearest start of a week. Defaults to one year prior."
     },
     "end_date": {
      "type": "string",
      "description": "ISO 8601 date string (YYYY-MM-DD) of the latest week to include. Dates are rounded forward to the nearest end of a week. Defaults to the latest computed week."
     }
    },
    "required": [
     "file_key",
     "group_by"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/analytics/libraries/{file_key}/component/actions",
    "path": [
     "file_key"
    ],
    "query": [
     "cursor",
     "group_by",
     "start_date",
     "end_date"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_library_analytics_component_usages",
   "name": "Get library analytics component usage data",
   "description": "Returns a list of library analytics component usage data broken down by the requested dimension.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File key of the library to fetch analytics data for."
     },
     "cursor": {
      "type": "string",
      "description": "Cursor indicating what page of data to fetch. Obtained from prior API call."
     },
     "group_by": {
      "type": "string",
      "description": "A dimension to group returned analytics data by.",
      "enum": [
       "component",
       "file"
      ]
     }
    },
    "required": [
     "file_key",
     "group_by"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/analytics/libraries/{file_key}/component/usages",
    "path": [
     "file_key"
    ],
    "query": [
     "cursor",
     "group_by"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_library_analytics_style_actions",
   "name": "Get library analytics style action data",
   "description": "Returns a list of library analytics style actions data broken down by the requested dimension.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File key of the library to fetch analytics data for."
     },
     "cursor": {
      "type": "string",
      "description": "Cursor indicating what page of data to fetch. Obtained from prior API call."
     },
     "group_by": {
      "type": "string",
      "description": "A dimension to group returned analytics data by.",
      "enum": [
       "style",
       "team"
      ]
     },
     "start_date": {
      "type": "string",
      "description": "ISO 8601 date string (YYYY-MM-DD) of the earliest week to include. Dates are rounded back to the nearest start of a week. Defaults to one year prior."
     },
     "end_date": {
      "type": "string",
      "description": "ISO 8601 date string (YYYY-MM-DD) of the latest week to include. Dates are rounded forward to the nearest end of a week. Defaults to the latest computed week."
     }
    },
    "required": [
     "file_key",
     "group_by"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/analytics/libraries/{file_key}/style/actions",
    "path": [
     "file_key"
    ],
    "query": [
     "cursor",
     "group_by",
     "start_date",
     "end_date"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_library_analytics_style_usages",
   "name": "Get library analytics style usage data",
   "description": "Returns a list of library analytics style usage data broken down by the requested dimension.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File key of the library to fetch analytics data for."
     },
     "cursor": {
      "type": "string",
      "description": "Cursor indicating what page of data to fetch. Obtained from prior API call."
     },
     "group_by": {
      "type": "string",
      "description": "A dimension to group returned analytics data by.",
      "enum": [
       "style",
       "file"
      ]
     }
    },
    "required": [
     "file_key",
     "group_by"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/analytics/libraries/{file_key}/style/usages",
    "path": [
     "file_key"
    ],
    "query": [
     "cursor",
     "group_by"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_library_analytics_variable_actions",
   "name": "Get library analytics variable action data",
   "description": "Returns a list of library analytics variable actions data broken down by the requested dimension.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File key of the library to fetch analytics data for."
     },
     "cursor": {
      "type": "string",
      "description": "Cursor indicating what page of data to fetch. Obtained from prior API call."
     },
     "group_by": {
      "type": "string",
      "description": "A dimension to group returned analytics data by.",
      "enum": [
       "variable",
       "team"
      ]
     },
     "start_date": {
      "type": "string",
      "description": "ISO 8601 date string (YYYY-MM-DD) of the earliest week to include. Dates are rounded back to the nearest start of a week. Defaults to one year prior."
     },
     "end_date": {
      "type": "string",
      "description": "ISO 8601 date string (YYYY-MM-DD) of the latest week to include. Dates are rounded forward to the nearest end of a week. Defaults to the latest computed week."
     }
    },
    "required": [
     "file_key",
     "group_by"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/analytics/libraries/{file_key}/variable/actions",
    "path": [
     "file_key"
    ],
    "query": [
     "cursor",
     "group_by",
     "start_date",
     "end_date"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_library_analytics_variable_usages",
   "name": "Get library analytics variable usage data",
   "description": "Returns a list of library analytics variable usage data broken down by the requested dimension.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File key of the library to fetch analytics data for."
     },
     "cursor": {
      "type": "string",
      "description": "Cursor indicating what page of data to fetch. Obtained from prior API call."
     },
     "group_by": {
      "type": "string",
      "description": "A dimension to group returned analytics data by.",
      "enum": [
       "variable",
       "file"
      ]
     }
    },
    "required": [
     "file_key",
     "group_by"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/analytics/libraries/{file_key}/variable/usages",
    "path": [
     "file_key"
    ],
    "query": [
     "cursor",
     "group_by"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_local_variables",
   "name": "Get local variables",
   "description": "**This API is available to full members of Enterprise orgs.** The `GET /v1/files/:file_key/variables/local` endpoint lets you enumerate local variables created in the file and remote variables used in the file. Remote variables are referenced by their `subscribed_id`. As a part of the Variables related API additions, the `GET /v1/files/:file_key` endpoint now returns a `boundVariables` property, c",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to get variables from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/variables/local",
    "path": [
     "file_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_me",
   "name": "Get current user",
   "description": "Returns the user information for the currently authenticated user.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/me",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_oembed",
   "name": "Get o embed data",
   "description": "Returns oEmbed data for a Figma file or published Make site URL, following the [oEmbed specification](https://oembed.com/).",
   "input": {
    "type": "object",
    "properties": {
     "url": {
      "type": "string",
      "description": "The URL of the Figma file or published Make site to retrieve oEmbed data for."
     },
     "maxwidth": {
      "type": "integer",
      "description": "Maximum width of the embed in pixels. Defaults to 800. The response width will be adjusted to maintain a 16:9 aspect ratio with maxheight."
     },
     "maxheight": {
      "type": "integer",
      "description": "Maximum height of the embed in pixels. Defaults to 450. The response height will be adjusted to maintain a 16:9 aspect ratio with maxwidth."
     }
    },
    "required": [
     "url"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/oembed",
    "path": [],
    "query": [
     "url",
     "maxwidth",
     "maxheight"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_payments",
   "name": "Get payments",
   "description": "There are two methods to query for a user's payment information on a plugin, widget, or Community file. The first method, using plugin payment tokens, is typically used when making queries from a plugin's or widget's code. The second method, providing a user ID and resource ID, is typically used when making queries from anywhere else. Note that you can only query for resources that you own. In mos",
   "input": {
    "type": "object",
    "properties": {
     "plugin_payment_token": {
      "type": "string",
      "description": "Short-lived token returned from \"getPluginPaymentTokenAsync\" in the plugin payments API and used to authenticate to this endpoint. Read more about generating this token through \"Calling the Payments REST API from a plugin or widget\" below."
     },
     "user_id": {
      "type": "string",
      "description": "The ID of the user to query payment information about. You can get the user ID by having the user OAuth2 to the Figma REST API."
     },
     "community_file_id": {
      "type": "string",
      "description": "The ID of the Community file to query a user's payment information on. You can get the Community file ID from the file's Community page (look for the number after \"file/\" in the URL). Provide exactly one of \"community_file_id\", \"plugin_id\", or \"widget_id\"."
     },
     "plugin_id": {
      "type": "string",
      "description": "The ID of the plugin to query a user's payment information on. You can get the plugin ID from the plugin's manifest, or from the plugin's Community page (look for the number after \"plugin/\" in the URL). Provide exactly one of \"community_file_id\", \"plugin_id\", or \"widget_id\"."
     },
     "widget_id": {
      "type": "string",
      "description": "The ID of the widget to query a user's payment information on. You can get the widget ID from the widget's manifest, or from the widget's Community page (look for the number after \"widget/\" in the URL). Provide exactly one of \"community_file_id\", \"plugin_id\", or \"widget_id\"."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/payments",
    "path": [],
    "query": [
     "plugin_payment_token",
     "user_id",
     "community_file_id",
     "plugin_id",
     "widget_id"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_published_variables",
   "name": "Get published variables",
   "description": "**This API is available to full members of Enterprise orgs.** The `GET /v1/files/:file_key/variables/published` endpoint returns the variables that are published from the given file. The response for this endpoint contains some key differences compared to the `GET /v1/files/:file_key/variables/local` endpoint: - Each variable and variable collection contains a `subscribed_id`. - Modes are omitted ",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to get variables from. This must be a main file key, not a branch key, as it is not possible to publish from branches."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/files/{file_key}/variables/published",
    "path": [
     "file_key"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_style",
   "name": "Get style",
   "description": "Get metadata on a style by key.",
   "input": {
    "type": "object",
    "properties": {},
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/styles/{key}",
    "path": [],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_team_component_sets",
   "name": "Get team component sets",
   "description": "Get a paginated list of published component sets within a team library.",
   "input": {
    "type": "object",
    "properties": {
     "team_id": {
      "type": "string",
      "description": "Id of the team to list component sets from."
     },
     "page_size": {
      "type": "number",
      "description": "Number of items to return in a paged list of results. Defaults to 30."
     },
     "after": {
      "type": "number",
      "description": "Cursor indicating which id after which to start retrieving component sets for. Exclusive with before. The cursor value is an internally tracked integer that doesn't correspond to any Ids."
     },
     "before": {
      "type": "number",
      "description": "Cursor indicating which id before which to start retrieving component sets for. Exclusive with after. The cursor value is an internally tracked integer that doesn't correspond to any Ids."
     }
    },
    "required": [
     "team_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/teams/{team_id}/component_sets",
    "path": [
     "team_id"
    ],
    "query": [
     "page_size",
     "after",
     "before"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_team_components",
   "name": "Get team components",
   "description": "Get a paginated list of published components within a team library.",
   "input": {
    "type": "object",
    "properties": {
     "team_id": {
      "type": "string",
      "description": "Id of the team to list components from."
     },
     "page_size": {
      "type": "number",
      "description": "Number of items to return in a paged list of results. Defaults to 30. Maximum of 1000."
     },
     "after": {
      "type": "number",
      "description": "Cursor indicating which id after which to start retrieving components for. Exclusive with before. The cursor value is an internally tracked integer that doesn't correspond to any Ids."
     },
     "before": {
      "type": "number",
      "description": "Cursor indicating which id before which to start retrieving components for. Exclusive with after. The cursor value is an internally tracked integer that doesn't correspond to any Ids."
     }
    },
    "required": [
     "team_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/teams/{team_id}/components",
    "path": [
     "team_id"
    ],
    "query": [
     "page_size",
     "after",
     "before"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_team_folders",
   "name": "Get top level folders in a team",
   "description": "Get a list of the top-level folders (previously called \"projects\") within the specified team. Subfolders can be traversed with the GET /v2/folders/{folder_id}/folders endpoint. It is not possible to programmatically obtain team IDs. To obtain a team ID, navigate to the team page in the Figma file browser. The team ID is present in the URL after the word team. For example, in `https://www.figma.com",
   "input": {
    "type": "object",
    "properties": {
     "team_id": {
      "type": "string",
      "description": "ID of the team to list folders from"
     }
    },
    "required": [
     "team_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v2/teams/{team_id}/folders",
    "path": [
     "team_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_team_styles",
   "name": "Get team styles",
   "description": "Get a paginated list of published styles within a team library.",
   "input": {
    "type": "object",
    "properties": {
     "team_id": {
      "type": "string",
      "description": "Id of the team to list styles from."
     },
     "page_size": {
      "type": "number",
      "description": "Number of items to return in a paged list of results. Defaults to 30."
     },
     "after": {
      "type": "number",
      "description": "Cursor indicating which id after which to start retrieving styles for. Exclusive with before. The cursor value is an internally tracked integer that doesn't correspond to any Ids."
     },
     "before": {
      "type": "number",
      "description": "Cursor indicating which id before which to start retrieving styles for. Exclusive with after. The cursor value is an internally tracked integer that doesn't correspond to any Ids."
     }
    },
    "required": [
     "team_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v1/teams/{team_id}/styles",
    "path": [
     "team_id"
    ],
    "query": [
     "page_size",
     "after",
     "before"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_webhook",
   "name": "Get a webhook",
   "description": "Get a webhook by ID.",
   "input": {
    "type": "object",
    "properties": {
     "webhook_id": {
      "type": "string",
      "description": "ID of webhook to get"
     }
    },
    "required": [
     "webhook_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v2/webhooks/{webhook_id}",
    "path": [
     "webhook_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_webhook_requests",
   "name": "Get webhook requests",
   "description": "Returns all webhook requests sent within the last week. Useful for debugging.",
   "input": {
    "type": "object",
    "properties": {
     "webhook_id": {
      "type": "string",
      "description": "The id of the webhook subscription you want to see events from"
     }
    },
    "required": [
     "webhook_id"
    ]
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v2/webhooks/{webhook_id}/requests",
    "path": [
     "webhook_id"
    ],
    "query": [],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.get_webhooks",
   "name": "Get webhooks by context or plan",
   "description": "Returns a list of webhooks corresponding to the context or plan provided, if they exist. For plan, the webhooks for all contexts that you have access to will be returned, and theresponse is paginated",
   "input": {
    "type": "object",
    "properties": {
     "context": {
      "type": "string",
      "description": "Context to create the resource on. Should be \"team\", \"project\", or \"file\"."
     },
     "context_id": {
      "type": "string",
      "description": "The id of the context that you want to get attached webhooks for. If you're using context_id, you cannot use plan_api_id."
     },
     "plan_api_id": {
      "type": "string",
      "description": "The id of your plan. Use this to get all webhooks for all contexts you have access to. If you're using plan_api_id, you cannot use context or context_id. When you use plan_api_id, the response is paginated."
     },
     "cursor": {
      "type": "string",
      "description": "If you're using plan_api_id, this is the cursor to use for pagination. If you're using context or context_id, this parameter is ignored. Provide the next_page or prev_page value from the previous response to get the next or previous page of results."
     }
    },
    "required": []
   },
   "request": {
    "method": "GET",
    "url": "https://api.figma.com/v2/webhooks",
    "path": [],
    "query": [
     "context",
     "context_id",
     "plan_api_id",
     "cursor"
    ],
    "header": [],
    "body": [],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.post_comment",
   "name": "Add a comment to a file",
   "description": "Posts a new comment on the file.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to add comments in. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     },
     "message": {
      "type": "string",
      "description": "The text contents of the comment to post."
     },
     "comment_id": {
      "type": "string",
      "description": "The ID of the comment to reply to, if any. This must be a root comment. You cannot reply to other replies (a comment that has a parent_id)."
     },
     "client_meta": {
      "type": "string",
      "description": "The position where to place the comment."
     }
    },
    "required": [
     "file_key",
     "message"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.figma.com/v1/files/{file_key}/comments",
    "path": [
     "file_key"
    ],
    "query": [],
    "header": [],
    "body": [
     "message",
     "comment_id",
     "client_meta"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.post_comment_reaction",
   "name": "Add a reaction to a comment",
   "description": "Posts a new comment reaction on a file comment.",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to post comment reactions to. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     },
     "comment_id": {
      "type": "string",
      "description": "ID of comment to react to."
     },
     "emoji": {
      "type": "string",
      "description": "The emoji type of reaction as shortcode (e.g. `:heart:`, `:+1::skin-tone-2:`). The list of accepted emoji shortcodes can be found in [this file](https://raw.githubusercontent.com/missive/emoji-mart/main/packages/emoji-mart-data/sets/14/native.json) under the top-level emojis and aliases fields, with optional skin tone modifiers when applicable."
     }
    },
    "required": [
     "comment_id",
     "emoji",
     "file_key"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.figma.com/v1/files/{file_key}/comments/{comment_id}/reactions",
    "path": [
     "file_key",
     "comment_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "emoji"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.post_dev_resources",
   "name": "Create dev resources",
   "description": "Bulk create dev resources across multiple files. Dev resources that are successfully created will show up in the links_created array in the response. If there are any dev resources that cannot be created, you may still get a 200 response. These resources will show up in the errors array. Some reasons a dev resource cannot be created include: - Resource points to a `file_key` that cannot be found. ",
   "input": {
    "type": "object",
    "properties": {
     "dev_resources": {
      "type": "array",
      "description": "An array of dev resources."
     }
    },
    "required": [
     "dev_resources"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.figma.com/v1/dev_resources",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "dev_resources"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.post_variables",
   "name": "Create modify delete variables",
   "description": "**This API is available to full members of Enterprise orgs with Editor seats.** The `POST /v1/files/:file_key/variables` endpoint lets you bulk create, update, and delete variables and variable collections. The request body supports the following 4 top-level arrays. Changes from these arrays will be applied in the below order, and within each array, by array order. - **variableCollections**: For c",
   "input": {
    "type": "object",
    "properties": {
     "file_key": {
      "type": "string",
      "description": "File to modify variables in. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key."
     },
     "variableCollections": {
      "type": "array",
      "description": "For creating, updating, and deleting variable collections."
     },
     "variableModes": {
      "type": "array",
      "description": "For creating, updating, and deleting modes within variable collections."
     },
     "variables": {
      "type": "array",
      "description": "For creating, updating, and deleting variables."
     },
     "variableModeValues": {
      "type": "array",
      "description": "For setting a specific value, given a variable and a mode."
     }
    },
    "required": [
     "file_key"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.figma.com/v1/files/{file_key}/variables",
    "path": [
     "file_key"
    ],
    "query": [],
    "header": [],
    "body": [
     "variableCollections",
     "variableModes",
     "variables",
     "variableModeValues"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.post_webhook",
   "name": "Create a webhook",
   "description": "Create a new webhook which will call the specified endpoint when the event triggers. By default, this webhook will automatically send a PING event to the endpoint when it is created. If this behavior is not desired, you can create the webhook and set the status to PAUSED and reactivate it later.",
   "input": {
    "type": "object",
    "properties": {
     "event_type": {
      "type": "string",
      "description": "An enum representing the possible events that a webhook can subscribe to",
      "enum": [
       "PING",
       "FILE_UPDATE",
       "FILE_VERSION_UPDATE",
       "FILE_DELETE",
       "LIBRARY_PUBLISH",
       "FILE_COMMENT",
       "DEV_MODE_STATUS_UPDATE"
      ]
     },
     "team_id": {
      "type": "string",
      "description": "Team id to receive updates about. This is deprecated, use 'context' and 'context_id' instead."
     },
     "context": {
      "type": "string",
      "description": "Context to create the webhook for. Must be \"team\", \"project\", or \"file\"."
     },
     "context_id": {
      "type": "string",
      "description": "The id of the context you want to receive updates about."
     },
     "endpoint": {
      "type": "string",
      "description": "The HTTP endpoint that will receive a POST request when the event triggers. Max length 2048 characters."
     },
     "passcode": {
      "type": "string",
      "description": "String that will be passed back to your webhook endpoint to verify that it is being called by Figma. Max length 100 characters."
     },
     "status": {
      "type": "string",
      "description": "An enum representing the possible statuses you can set a webhook to: - `ACTIVE`: The webhook is healthy and receive all events - `PAUSED`: The webhook is paused and will not receive any events",
      "enum": [
       "ACTIVE",
       "PAUSED"
      ]
     },
     "description": {
      "type": "string",
      "description": "User provided description or name for the webhook. Max length 150 characters."
     }
    },
    "required": [
     "context",
     "context_id",
     "endpoint",
     "event_type",
     "passcode"
    ]
   },
   "request": {
    "method": "POST",
    "url": "https://api.figma.com/v2/webhooks",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "event_type",
     "team_id",
     "context",
     "context_id",
     "endpoint",
     "passcode",
     "status",
     "description"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.put_dev_resources",
   "name": "Update dev resources",
   "description": "Bulk update dev resources across multiple files. Ids for dev resources that are successfully updated will show up in the `links_updated` array in the response. If there are any dev resources that cannot be updated, you may still get a 200 response. These resources will show up in the `errors` array.",
   "input": {
    "type": "object",
    "properties": {
     "dev_resources": {
      "type": "array",
      "description": "An array of dev resources."
     }
    },
    "required": [
     "dev_resources"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.figma.com/v1/dev_resources",
    "path": [],
    "query": [],
    "header": [],
    "body": [
     "dev_resources"
    ],
    "body_format": "json"
   }
  },
  {
   "slug": "figma.put_webhook",
   "name": "Update a webhook",
   "description": "Update a webhook by ID.",
   "input": {
    "type": "object",
    "properties": {
     "webhook_id": {
      "type": "string",
      "description": "ID of webhook to update"
     },
     "event_type": {
      "type": "string",
      "description": "An enum representing the possible events that a webhook can subscribe to",
      "enum": [
       "PING",
       "FILE_UPDATE",
       "FILE_VERSION_UPDATE",
       "FILE_DELETE",
       "LIBRARY_PUBLISH",
       "FILE_COMMENT",
       "DEV_MODE_STATUS_UPDATE"
      ]
     },
     "endpoint": {
      "type": "string",
      "description": "The HTTP endpoint that will receive a POST request when the event triggers. Max length 2048 characters."
     },
     "passcode": {
      "type": "string",
      "description": "String that will be passed back to your webhook endpoint to verify that it is being called by Figma. Max length 100 characters."
     },
     "status": {
      "type": "string",
      "description": "An enum representing the possible statuses you can set a webhook to: - `ACTIVE`: The webhook is healthy and receive all events - `PAUSED`: The webhook is paused and will not receive any events",
      "enum": [
       "ACTIVE",
       "PAUSED"
      ]
     },
     "description": {
      "type": "string",
      "description": "User provided description or name for the webhook. Max length 150 characters."
     }
    },
    "required": [
     "endpoint",
     "event_type",
     "passcode",
     "team_id",
     "webhook_id"
    ]
   },
   "request": {
    "method": "PUT",
    "url": "https://api.figma.com/v2/webhooks/{webhook_id}",
    "path": [
     "webhook_id"
    ],
    "query": [],
    "header": [],
    "body": [
     "event_type",
     "endpoint",
     "passcode",
     "status",
     "description"
    ],
    "body_format": "json"
   }
  }
 ]
}
